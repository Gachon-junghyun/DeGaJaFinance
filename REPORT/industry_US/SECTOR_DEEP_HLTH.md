# SECTOR_DEEP_HLTH — Health Care · industry_US · 2026-09-08 (Tue) · Stage 8 / L1·DEEP

> **CONTINUOUS TRACK** (deep-dived 09-07, 09-06, 09-05 …) ⇒ **leads with the DELTA.** Unchanged
> structure carried by reference to `llm_outputs/2026-09-07/industry_US/SECTOR_DEEP_HLTH.md`.
> Price numbers filtered to `index <= 2026-09-04` (`D577`). Benchmark `SPY` named inline (`C1`).

## 0 · Mandate from ROTATION — one question

*"HLTH is positive on every cut with median ≥ mean on both windows and 20 of 32 positive on `exc5`.
**Is that breadth, or is it the absence of a driver? Name the driver or say there is none.**"*

## ★ 1 · The answer: there is no driver, and this run says so rather than inventing one

**Searched, not assumed** (`--scope foreign`, direct path, made after an idle gap so `G1`'s
sweep-outage does not apply):

| candidate driver | 7-day count | `theme-age` |
|---|--:|---|
| `Medicare` | 160 | — |
| `FDA approval` | 94 | — |
| `drug pricing` | **28** | 🔴 **FADING 0.45×** (7d avg 2.0 on a 210 base) |
| `obesity drug` | **19** | — |

For scale, the same instrument returns **`rate hike` 1,382**, **`inflation` 2,980**, **`tariff`
1,757**, **`refinery` 324** in the same window. ⇒ **Health Care's four candidate drivers together
total 301 articles — under a fifth of the single term `rate hike`** — and the one with a
`theme-age` reading is **FADING**.

⚠ **This is an admissible "no driver" claim and it is worth stating why**, because `G1` forbids most
of them: the sweep's velocity axis is dead (17.39%) and **Utilities/Materials/Real Estate may not be
called quiet at all**. Health Care is different — it has **5 of 32 names velocity-measured** and,
more importantly, this claim is made from **direct `fts` counts with their denominators**, not from
the sweep. The denominator is stated: the 09-07 foreign pool held **1,720 articles / 279 events**.

⇒ **Verdict on the mandate: BREADTH WITHOUT A DRIVER — Health Care is a residual, not a thesis.**

## 2 · The mechanism that follows, stated as a testable claim

If there is no sector-specific driver, the positive readings must come from **somewhere else**, and
the week supplies exactly one candidate: **a global bond selloff with `bond selloff` `theme-age` at
4.18×, `DGS10` 4.77 (highest since Nov-2023 per the tape), and two-thirds of the five-session move
REAL** (MACRO §A-2). In a real-yield shock, low-beta defensive cash-flow is where money parks when it
leaves duration-sensitive and cyclical buckets.

**The board is consistent with that reading:** the only two sectors with positive `eqflow` are
**Energy (+0.562, a supply story)** and **Health Care (+0.190, no story)**, while `RE` (1/12 positive),
`STPL` (7/19) and `DISC` (7/28) — the three most duration- and consumer-sensitive buckets — are the
worst. ⇒ **HLTH's breadth and RE's collapse are plausibly the same trade seen from two ends.**

⚠ **Labelled `[inferred]`, not `[measured]`.** No row on this board tests it, and this stage does not
register one (`D343`: `P148`, registered today, measures the *curve*, and if the duration story is the
driver then HLTH should move with `P148`'s branches — that is the free cross-check, **stated now**).

## 3 · Flow — node decomposition (sector row by reference to `SECTOR_FLOW_US.json §sector_rotation`)

| sub-node | n | eqflow | exc5 median | 🟢 |
|---|--:|--:|--:|--:|
| **Biotechnology** | 6 | ★ **+0.519** | **+2.27** | 0 |
| Pharmaceuticals | 5 | +0.302 | +1.23 | 0 |
| Health Care Distributors | 3 | +0.257 | **+2.66** | 0 |
| Life Sciences Tools & Services | 4 | +0.226 | 🔻 **−1.75** | 0 |
| Managed Health Care | 3 | +0.128 | **+3.20** | 0 |
| Health Care Services | 2 | +0.110 | +2.52 | 0 |
| **Health Care Equipment** | **8** | 🔻 **−0.044** | 🔻 **−2.16** | 1 (`MDT`) |
| Health Care Facilities | 1 | −0.472 | −3.18 | 0 |

★ **The sector's single 🟢 sits in its ONLY negative-eqflow sub-node.** `MDT` (+0.928, surge 1.47,
OBV 매집, `exc5` **+3.11**) is green inside Health Care Equipment, whose other seven names average
**−0.044 eqflow** and a **−2.16** `exc5` median, with `SYK` **−0.706 / exc5 −8.44** and `IDXX`
**−0.692 / −3.64** both 🔴. **RULE D6 exempt** — the OBV term appears only as one column of a multi-axis description (`flow_score`, `vol_surge`, `exc5`); the claim rests on `eqflow` and `exc5`, and no verdict is issued. ⇒ **the shortlist's one Health Care name is the exception to its own
sub-node**, and BET must not read it as sub-node confirmation.

★ **Dispersion / sector-move = 3.618 / 0.632 = 5.72×** — worse than IT's 4.6× (`D566`). ⇒ **by the
desk's own unit-of-analysis test there is no sector-level fact here either.** The `N+` verdict is a
statement about a *basket*, not about an industry, and this file says so.

## 4 · The DELTA vs the 09-07 file

| | |
|---|---|
| **NEW ①** | The **driver search was actually run** (four terms, counts and `theme-age` printed) instead of being asserted. The 09-07 file described the flow; this one answers whether anything is behind it |
| **NEW ②** | The **`5.72×` dispersion ratio**, which puts HLTH *worse* than IT on the one test that decides whether a sector verdict is admissible at all |
| **NEW ③** | The **`MDT`-inside-a-negative-sub-node** observation, which changes what the shortlist name means |
| **CARRIED** | flow numbers unchanged (`asof` 2026-09-04, 4th run); `LLY` at 19.4% of sector cap is a **non-flipper** (+0.098 → ex-top1 **+0.232**, i.e. the sector is *more* positive without it) — and `LLY` is itself the sector's only 🔴 among the top ten by cap, at **−0.462, OBV 분산, exc5 −2.26** (**RULE D6 exempt** — descriptive column set; the line's point is the non-flipper test) |

## 5 · Track KPIs and anti-signals

| | observable | current |
|---|---|---|
| **KPI 1** | `drug pricing` `theme-age` | 🔴 FADING **0.45×** — under the residual reading it stays below 1× |
| **KPI 2** | HLTH `eqflow` sign vs `RE`+`STPL` `eqflow` sign | **+0.190 vs −0.124 / −0.098** — the residual reading requires them to stay opposite |
| **KPI 3 (the free cross-check)** | **`P148` branch B** (dovish dots, curve steepens, settles 09-17) | if B fires and HLTH's `eqflow` does **not** deteriorate, the duration-residual mechanism is wrong |
| **ANTI-SIGNAL 1** | any single HLTH term clearing **500 articles / 7d** with `theme-age` ≥ 2× ⇒ a real driver arrived and this file's verdict is void | none of four exceeds 160 |
| **ANTI-SIGNAL 2** | HLTH `exc5` breadth falling below **16 of 32** while `eqflow` stays positive ⇒ the basket is narrowing into a few names | currently **20 of 32** |

## 6 · What this file does NOT claim

- **No verdict change.** ROTATION holds `HLTH N+`; the mandate asked for a mechanism, not a re-rank.
- **The residual mechanism is `[inferred]` and is not passed downstream as evidence** for any other
  proposition (HANDOVER's carry rule).
- **No sizing** (`P4`). `MDT` is named as an instrument observation, not a candidate.
