# SECTOR_DEEP_RE — industry_US · 2026-08-26 · **ROTATING TRACK (R1) — FULL FRESH MAP**

> Last deep-dived **2026-08-22 = 4 runs ago**, the board's worst recency (three-way tie).
> Selected over `INDU` and `FIN`-alone on evidence density: **a registered row of this desk's own
> (`P78`) is about to falsify the framing this sector's underweight rests on.**
> **Zero sizing, zero buy/sell (P4).** Price: equal-weight `us_top300`, excess vs **`SPY`**,
> **settled 2026-08-25**. Flow: 2026-08-26 sweep ⚠ **pre-market bar (`D355`)**.

---

## §1 · Why this sector, in one number

**`P78`'s pre-settle range is 4.889pp — ABOVE its own branch-A line of 4.71.**
Legs, `exc5` vs `SPY`: **`XLV` +3.476 · `XLRE` +1.836 · `XLP` +1.299 · `XLU` −1.412.**
`S108` settled the same way one session earlier: **three legs 2.46pp apart with one negative**, so its
registered premise — *"the three underweights rip together"* — **was not observed even in the branch
that fired.**
⇒ **Two independent registered observables now say the "duration complex" disperses** (`M950`).
**Real Estate's `N−` is inherited from `XLU`'s evidence, and `XLU` is the leg it least resembles.**

---

## §2 · Value chain / node map — 12 names, **and the sector is a 38-point spread wearing one label**

`exc60` spans **+20.11 (`CBRE`) to −17.87 (`CCI`) = 37.98pp inside a 12-name sector** (`W5`).

| # | Node | Names | flow | OBV | **`delta`** | `exc5` | `exc20` | `exc60` |
|---|---|---|---:|---|---:|---:|---:|---:|
| 1 | **Real Estate Services** *(not a REIT — the transaction layer)* | `CBRE` | −0.148 | 매집 | −0.23 | **+3.21** | −0.19 | **+20.11 — sector best** |
| 2 | **Data-centre REITs** | `EQIX` · `DLR` | −0.053 / −0.159 | 중립 / 매집 | **+0.28 / +0.31 — the sector's two largest POSITIVE** | −0.87 / −1.30 | +0.35 / −3.83 | −0.74 / −0.03 ≈ **flat** |
| 3 | **Health-care REITs** | `WELL` · `VTR` | −0.360 / −0.250 | 중립 / 매집 | +0.11 / +0.08 | +2.63 / +3.08 | −4.37 / −7.77 | **+16.20 / +9.90** |
| 4 | **Telecom towers** | `AMT` · `CCI` | −0.326 / −0.475 | 매집 / 중립 | **−0.39 / −0.41 — the sector's two largest NEGATIVE** | +3.82 / +3.25 | +0.74 / −2.67 | **−5.73 / −17.87** |
| 5 | **Industrial / logistics** | `PLD` | −0.601 | 중립 | +0.01 | +2.56 | −5.96 | −1.34 |
| 6 | **Retail + self-storage** | `SPG` · `O` · `PSA` | −0.972 / −0.894 / −0.835 | **분산 ×3 — all 🔴** | +0.00 / −0.12 / +0.03 | −0.53 / +1.20 / +0.07 | **−10.86 / −7.79 / −5.74** | +5.62 / +1.28 / +5.00 |
| 7 | **Other specialised** | `IRM` | −0.092 | 매집 | +0.07 | **−4.35** | **−7.20** | **−6.98** |

### 🚨 The chain finding: the desk's carried data-centre grouping is REFUTED on this frame
The standing registry row (carried since 07-25) reads: *"the real data-centre node is
`{DLR, EQIX, IRM}` — `CCI` joins `AMT` in duration; `IRM` trades with `DLR`/`EQIX`."*

**On the 2026-08-25 settled frame:**
- `DLR` and `EQIX` carry the sector's **two largest positive `delta`s (+0.31, +0.28)** and are the
  **only two names in the sector flat-to-positive on `exc60`** (−0.03, −0.74).
- **`IRM` is negative on ALL THREE windows** (`exc5 −4.35` · `exc20 −7.20` · `exc60 −6.98`) — the only
  name in the sector that is, and the **opposite** sign to `DLR`/`EQIX` on every one.
- `AMT` and `CCI` carry the **two largest negative `delta`s (−0.39, −0.41)**.

⇒ **`M953`: on this frame the data-centre node is `{DLR, EQIX}` only, and the towers are its mirror
image — a `delta` spread of 0.72 across four names in a 12-name sector.** The `{DLR, EQIX, IRM}`
grouping is **not observed today**. ⚠ **This is one frame, `n=1` (`S5`)** — it is written as a
refutation of the carried grouping's *currency*, not as a new permanent grouping.

### The binding constraint — and it is NOT rates
- The **measured rate beta of `XLRE` is −0.0072 pp/bp ≈ zero** (carried `[measured]`, `S102`'s own
  regression). **The sector's underweight does not rest on rates and never did.**
- What actually separates the winners from the losers here is **tenant-cashflow duration, not
  interest-rate duration**: `CBRE` (transaction fees, +20.11), `WELL`/`VTR` (senior-housing operating
  recovery, +16.20/+9.90) and `SPG`/`PSA` (retail/storage operating, +5.62/+5.00) all carry positive
  `exc60`, while `CCI` (**contractual tenant churn**, −17.87) and `IRM` carry the negatives.
- ⇒ **The bottleneck is the tower node's contracted revenue roll-off**, which is a *dated, contractual,
  expiring* headwind — not a macro one. **Strong demand elsewhere in the sector is not the constraint.**

---

## §3 · Valuation and the denominator's direction (description, **not** a leading indicator)

> ⚠ The `RESEARCH` carry is explicit: revisions are **a description of where the denominator has
> been going** — arithmetic and valid — and are **not** a leading indicator (`measure_ic.py`, two
> non-overlapping windows disagreed in sign). Read as description only.

| Name | Forward P/E | Upside to mean target | CY revision, 90d | Near-term breadth (30d) |
|---|---:|---:|---:|---|
| **`EQIX`** | **57.83** | +14.2% | **+0.8%** | **1↑ / 3↓** both periods |
| **`DLR`** | **66.99** | +15.3% | **+12.9%** | 0↑ / 1↓ both periods |
| **`CCI`** | **26.48** | **+24.3% — the sector's widest** | **−3.6%** | **1↑ / 3↓** both periods |

★ **The set-up that makes this sector interesting is a mismatch, and it runs the same way on both
data-centre names**: `EQIX` and `DLR` carry the sector's **two largest positive flow deltas** while
their **near-term revision breadth is 1↑/3↓ and 0↑/1↓** — money in, estimates out.
⚠ **And `DLR`'s and `EQIX`'s forward P/E of 67.0 and 57.8 are GAAP and are a category error for a
REIT** — the desk logged this exact trap on `DLR` (GAAP 67.87 vs **24.4× on AFFO**) and on `VTR`
(a "−33.3% consensus cut" that was **~1.6% of FFO**). **The multiples above are printed for continuity
with the tool's output and are NOT used to call anything cheap or expensive.**
★ **`CCI` is the one name where the multiple is comparable** (16.3× AFFO on the carried figure) and it
carries the sector's widest consensus upside **against its worst `exc60`** — a genuine disagreement.

---

## §4 · Track KPIs and anti-signals (dated observables)

| KPI | Now | Confirms `N−` if | Refutes `N−` if | Date |
|---|---|---|---|---|
| **`P78` range** across `{XLU,XLRE,XLP,XLV}` | **4.889pp (above branch A 4.71)** | ≤ 1.69 (one object) | ≥ 4.71 at the settle ⇒ **the complex is four bets and the sector-level UW is a label** | **tonight, 08-26** |
| `EQIX`+`DLR` `delta` | **+0.28 / +0.31** | turns negative | stays positive 3 consecutive settled sweeps ⇒ a real data-centre bid the sector verdict is missing | **09-08** |
| `CCI` `exc60` | **−17.87** | stays ≤ −15 | > −5 ⇒ the tower roll-off is being discounted through | **09-08** |
| Retail/storage 🔴 count | **3 of 3 (`SPG`,`O`,`PSA`)** | stays 3 | ≤ 1 | **09-08** |
| `IRM` sign agreement with `DLR`/`EQIX` | **opposite on all 3 windows** | stays opposite ⇒ `M953` holds | re-converges ⇒ the carried grouping was right and this frame was noise | **09-08** |

**Anti-signals**: a **Fed cut priced in** (the sector's rate beta is ≈0, so a rate move that *does*
lift it would itself falsify the measured beta); a **large data-centre REIT M&A** (the desk has one on
file — `DLR`/Blackstone $3.5bn); a **DISH-class tenant resolution at `CCI`** ending the roll-off early.

---

## §5 · What this deep-dive could NOT do
- ⚠ **No primary filing opened.** `CCI`'s tenant-churn figure (**≈$49m/quarter**), `DLR`'s AFFO
  multiple (24.4×) and `XLRE`'s rate beta (−0.0072 pp/bp) are all **carried `[measured]` from prior
  runs and were NOT re-verified today.** The chain map above rests on **tape + consensus**, and the
  *"binding constraint is the tower roll-off"* claim is therefore **`[inferred]` on inherited
  `[measured]` inputs** — it may be carried, **not** cited as evidence for a new proposition.
- ⚠ **No chain-hop pass.** No un-named beneficiary layer was searched for this sector.
- ⚠ **`vol_surge` unusable on all 12 names** (0.41–0.68 on a 2.48%-volume bar, `D355`) ⇒ **no 🟢 is
  possible in this sector today by construction**, so the sector's `0 🟢 / 3 🔴` is not evidence of
  narrowness. It is diagnosed, not cited (`SWEEP_READ §7`).
- ⚠ `n = 12` names. Every node above except retail/storage has **n ≤ 2**, so **no node-level mean is a
  statistic** (`C4`) — they are read as named pairs, not as estimates.
