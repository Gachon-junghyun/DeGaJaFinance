# 하네스 업그레이드 인수인계 — 2026-07-31

> **이 문서를 읽는 세션은 그날의 대화를 모른다.** 그래서 결론이 아니라 **측정값과 파일:줄**을 남긴다.
> 목적: *"예측해서 수익률을 만든다"* 는 이 리포의 목표에 대해, **이번 주에 무엇이 되고 무엇이 안 됐는지**를
> 숫자로 넘기고, **어디부터 고쳐야 하는지의 순서**를 근거와 함께 남긴다.
> ⚠ 여기 적힌 것은 전부 2026-07-31에 실측된 값이다. 추정에는 `[추정]` 을 달았다.

---

## 0. 한 줄 요약

**예측은 됐다. 예측이 비중으로 바뀌는 경로가 없었다.**
데스크는 폭락을 현금 58%로 통과해 **KOSPI 대비 +16.77pp** 를 만들었고, 그다음 날 **같은 현금 58%로
반등을 놓쳐 하루에 −12.5pp** 를 반납했다. **같은 변수, 반대 방향.**

---

## 1. 이번 주에 실제로 일어난 일 (전부 실측)

### 1-a. 시장
`069500.KS`(KODEX 200) 종가:
`07-22 108,315 · 07-23 113,230 · 07-24 106,365 · 07-27 107,730 · 07-28 95,675 · 07-29 89,605 ·
07-30 87,635 · 07-31 종가 102,715` — **07-10 고점 120,605 대비 5세션 −27.3%, 그리고 07-31 하루 +17.2%.**
07-28·07-29는 **사상 첫 이틀 연속 서킷브레이커**, 사이드카 2026년 누적 41회(2008년 26회).

### 1-b. 콘테스트 계좌 (타임폴리오 RFM 12회, 2,049명)
| 시점 | NAV | 당일 | 순위 | 투자비중 |
|---|---|---|---|---|
| 07-31 09:5x (주문 전) | 950.86 | +1.79% | **286위** | **41.8%** |
| 07-31 10:2x (주문 후) | 954.18 | +2.15% | **302위** | **53.3%** |

같은 시각 **KOSPI 당일 +16.61% · 기간 −21.44% · 대비 +16.86pp**. 금주 회전율 26.9%, 누적 위반 1.

### 1-c. ★ 수익 분해 — **이 리포가 한 번도 해본 적 없는 계산**
진입 평단(콘테스트 화면 실측) 대비, 창 = 07-24 → 07-31 10:2x:

| | 값 |
|---|---|
| 투자 슬리브 가중수익률 | **+0.03%** |
| 벤치 `069500` 같은 창 | **−2.51%** |
| **종목선택 초과** | **+2.54pp** |

기여도: **삼성물산 +0.89pp 단독**, 그 뒤 삼바 +0.07 · HMM +0.05 · 한미반도체 +0.05 · 삼성전자 +0.05
vs SK이노 **−0.37** · 하이닉스 −0.35 · 삼성E&A −0.28 · KB −0.10.

⇒ **선택 알파는 n=11 · 창 1개 · 한 종목 의존 = 0과 구분 불가(C4).**
⇒ **콘테스트의 대비 +16.86pp 중 약 14pp는 현금비중이다. 이 구조가 실제로 하는 일은 베타 줄이기다.**

---

## 2. 결함 — 전부 파일:줄 또는 실측값

| # | 결함 | 증거 |
|---|---|---|
| **F1** | **비대칭 게이트.** "위험 줄이기" 트리거만 있고 "되돌리기" 트리거가 없다 | 🟢LIVE 태그 **8런 연속 0건**. 🟢은 `theme_age` FRESH(≤14일 ∧ ≥2× 가속)를 요구하는데 보드 테마가 전부 90일 이상 ⇒ **구조상 발화 불가** |
| **F2** | **점수판 비대칭.** 거부는 채점하고 **미진입은 채점하지 않는다** | `scripts/reject_ledger.py` 는 거부 45건을 채점. **"안 산 종목"을 재는 장치가 리포 전체에 없다.** 한미반도체 +27.4%(07-31)를 놓친 것이 어느 숫자에도 안 남는다 |
| **F3** | **자기 수익을 분해한 적이 없다** | §1-c 가 첫 계산. 그전까지 "현금 덕인가 선택 덕인가"를 아무도 묻지 않았다 |
| **F4** | **미러 반쪽 이식 — 자동 미러가 구조적으로 불가능** | `module_timefolio/_sync.py:44` → `from alert_bot import alert_db` · `alert_bot` 은 이 리포에 없다(옛 mvp 전용) ⇒ `plan`·`sync` **ModuleNotFoundError 크래시**. `account`/`holdings`/`order` 만 동작 |
| **F5** | **현금비중이 어느 층에서도 결정 변수가 아니다** | `_sync.py` `book_targets(seed=10_000_000)` 는 **투자 슬리브 비중만** 타깃으로 낸다. 책이 42% 투자면 콘테스트도 영원히 42% |
| **F6** | **KR 슬리브에 담당 데스크가 없다** | `pipeline/protocols/미러링.md:39` — *"US-knowledge-limited; a KR holding is **out of scope** this desk"*. 콘테스트는 KR로 굴린다 |
| **F7** | **9거래일 침묵을 아무도 몰랐다** | 콘테스트 holdings 스크린샷이 **07-22에서 정지**, 07-31까지 주문 0건. 책 트랜잭션은 **07-15 이후 0건(16일)**. **경보 장치 부재** |
| **F8** | **arming 미결정** | `.env` 에 `TIMEFOLIO_EXECUTE` 키가 **아예 없다.** `__init__.py:192` 가 이 값을 보고, 없으면 `--execute` 를 붙여도 **조용히 드라이런으로 떨어진다**(229행) |
| **F9** | **주력 계기가 이미 음성 판정** | **M224**: 스윕 4축(`flow_score`·`obv_norm`·`vol_surge`·`rs20/60`) vs 다음세션 잔차 스피어만 **ρ −0.21~+0.19, n=22**, p=0.05 임계 **\|ρ\|=0.43** ⇒ **전부 노이즈 안.** 알고도 계속 쓰고 있다 |
| **F10** | **창립 가설 미검증** | `Finance_PLAYGROUND/PLAY23_multiple_vs_flow_duel/out/` **비어 있음**(dig D15). 자체 README가 리밸런스 **27~31회**라고 적어 둠 ⇒ 돌려도 "구분 불가"가 정직한 결과일 가능성 높음 |
| **F11** | **라이브 판정을 후행 데이터로 함** | 07-31 09:05 선물 인쇄(+8.00%)를 "현물 +15%는 일어나지 않았다"며 아티팩트로 오판. **실제로 벤치는 +14.05%** 였다. 교차확인에 쓴 **yfinance는 KRX 당일 장중 봉이 없다**(dig D103) |

---

## 3. 고치는 순서 — 의존성 있음, 이 순서대로

### ① 대칭 노출 규칙 ← **여기서 시작**
**이유**: 측정된 알파의 **약 14/16.86pp가 이 변수**이고, 07-31 손실의 **100%도 이 변수**다.
그리고 **매일 관측이 1개씩 쌓여 한 달이면 n=20** — 이 리포에서 **검증 가능한 유일한 영역**이다.
(대조: 종목선택은 종목당 1포지션 × 월 회전 ⇒ **연간 독립 관측 10~20개** ⇒ 유의성이 산술적으로 안 나온다.)

**핵심은 "복귀" 규칙이다.** 바닥을 예측하지 않는다 — *"돌아섰다는 증거가 나오면 되돌린다"* 만 한다.
그러면 07-31 같은 날이 **−12.5pp 전손이 아니라 수 pp 지각**이 된다.

⚠ **밴드 숫자(정상/방어/복귀 %)는 사람이 정한다.** 그건 위험선호이지 분석이 아니다.

### ② 점수판 두 개
- **기회비용 원장** — 거부 원장과 대칭. "검토했으나 안 산 것"을 같은 방식으로 채점.
- **일별 수익 분해 적립** — NAV · 벤치 · 투자비중 · 선택초과. §1-c를 매일 자동화.

### ③ 배선
- **F4 SSOT 컷오버** ← **사람 결정**(P5). 옛 mvp `alert_bot` 책 vs 이 리포 `module_paper_book`/`data/paper_book.db`
- **F6** — `미러링.md` 를 KR로 확장할지 `미러링_kr.md` 를 신설할지 ← **사람 결정**
- **F8 arming** ← **사람 결정**
- **09:10 개장직후 LIVENESS 태스크 추가** (15:00짜리는 이미 있음, §4 참조)

### ④ 예측층은 **줄인다**
지금 8스테이지 · DEEP 4에이전트 · 서브에이전트 **약 71만 토큰**이 **검증 불가 영역(종목 테제)** 에 들어간다.
**DEEP 4 → 1~2개**로 줄이고 예산을 ①②로 옮긴다.
**예측을 더 잘하려 하지 말고, 이미 맞힌 예측을 비중으로 바꾸는 데 쓴다.**

---

## 4. 오늘 이미 해둔 것 (중복 작업 금지)

- ✅ **스케줄 태스크 `timefolio-close-check` 생성** — 평일 **15:0x KST**, `C:\Users\fivep\.claude\scheduled-tasks\timefolio-close-check\SKILL.md`.
  LIVENESS(마지막 주문 이후 거래일 · 투자비중 밴드 · `plan` import 생존 · 스크린샷 대조 · 조건형 브래킷 재검사)를 돌리고 **주문은 제출하지 않는다.**
  ⚠ **밴드가 미설정이라 지금은 "밴드 미설정 🚨"만 반복 출력한다** — ①이 끝나야 쓸모가 생긴다.
  ⚠ 앱이 켜져 있어야 돈다. 브라우저 권한은 "Run now" 한 번으로 선승인 권장.
- ✅ **07-31 B안 집행 완료**(사람이 발사): 매도 096770 −3.9 · 068270 −1.6 · 207940 −1.4 /
  매수 042700 +6.0(신규) · 005930 +5.0 · 028260 +2.8 · 000660 +2.6 · 028050 +1.8 ⇒ **투자 41.8% → 53.3%**
- ✅ `industry_kr` 2026-07-31 런 완주(8스테이지) — 산출 `llm_outputs/2026-07-31/industry_KR/`, `REPORT/industry_KR/` 복사, 원장 갱신
- ✅ 거부 원장 **레거시(부활조건 없는 행) 1 → 0** — 사상 처음 전 행이 조건부

---

## 5. 하지 말 것

- **주문 실제 제출 금지.** `--execute` 를 붙이지 말고 `TIMEFOLIO_EXECUTE` 를 세팅하지 마라. 발사는 사람이 한다.
- **F4·F6·F8 및 밴드 숫자를 임의로 결정하지 마라** — 전부 사람 결정(CLAUDE.md P5).
- **`_sync.py` 를 SSOT 결정 전에 수정하지 마라.**
- 시크릿(.env 값) 출력 · git push · 텔레그램/이메일 발송 금지.
- 조용한 폴백 금지 — **F4를 9일간 숨긴 것이 조용한 실패였다.**

---

## 6. 2차 세션 (2026-07-31 10:2x~) — 만든 것과 **정정한 것**

### 6-a. 만든 것
- `scripts/exposure_rule.py` — ①. 4상태 판정 + `out/exposure/ledger.csv` 일별 적립 + 수익분해.
  밴드는 `data/exposure_bands.json`(사람 결정, **미생성**). `propose` 가 임계값 실측표를 낸다.
- `scripts/missed_ledger.py` — ②. 기회비용 원장. `reject_ledger` 를 import 재사용(P1).
- 스케줄 태스크 `timefolio-open-check` (평일 09:10, 지터로 실효 09:19) 신설.
  `timefolio-close-check` 에 STEP 0.5(노출 적립) 추가 — **적립이 매일 두 번 돌고 종가값이 장중값을 덮는다.**
- `module_timefolio.Holding.day_pct` 추가 — 표에 이미 있던 당일등락을 버리고 있었다(수익분해 재료).

### 6-b. ★ §2 의 정정 — 실측으로 뒤집힌 것 두 개
| # | 문서가 적은 것 | 실측 |
|---|---|---|
| **F8** | *".env 에 `TIMEFOLIO_EXECUTE` 키가 **아예 없다**. --execute 를 붙여도 조용히 드라이런"* | **틀렸다. 키가 있고 값이 `1` 이다 — ARMED.** `Timefolio.attach()` 가 로그인 중 `.env` 를 주입하고 `place_order` 는 `os.environ` 을 직접 보므로 **`--execute` 하나만 붙으면 실제 제출된다.** 이중게이트가 아니라 **단일 게이트**. 07-31 B안 집행 때 켠 뒤 미해제로 추정. **끄지 않았다(사람 결정).** |
| **F2** | *"한미반도체 +27.4%를 놓친 게 어느 숫자에도 안 남는다"* | **예시가 틀렸다.** 042700 은 `reject_ledger` 에 **2건** 있다(07-14 C.차트붕괴 · 07-16 E.상관가드) — 채점되고 있었다. **F2 의 본체(미진입 자체를 재는 장치 부재)는 그대로 유효**하고, 그건 `missed_ledger` 로 닫았다. |

### 6-c. F4 — 양쪽 SSOT 후보의 **현재 실측** (사람 결정용)
| | 옛 mvp `alert_bot` 책 id=6 '한국모의' | 이 리포 `data/paper_book.db` |
|---|---|---|
| 보유 | **KR 8종목** | US 8 + KR 2 (009150·096770) |
| 투자비중 | **41.09%** (현금 58.91%) | KR 슬리브만 존재, 콘테스트와 무관 |
| 마지막 쓰기 | **2026-07-24** (콘테스트 미러 재기준) | 트랜잭션 2026-07-15 · 미러 2026-07-23 |
| 콘테스트와의 정합 | 종목은 8/11 일치, **비중이 07-24 에 멈춤** | 불일치(다른 시장) |
| 스키마 | `positions`(user_id·portfolio_id·shares·avg_price·thesis·active) | `positions`+`cash`+`transactions`+`mandate` … |

**같은 시각 콘테스트 = 투자 52.9% / 현금 47.1% (11종목).**
⇒ **콘테스트는 07-31 에 움직였고 책은 07-24 에 멈춰 있다.** 그래서 지금 `book_targets()` 가 살아나면
콘테스트를 **41% 로 되돌린다** — F5 가 말한 *"책이 42% 면 콘테스트도 영원히 42%"* 가 실측으로 확인됐다.
⚠ `_sync.py` 는 **손대지 않았다**(P5).

### 6-d. 실측으로 드러난 새 사실
- **`069500.KS` 장중 봉은 존재하지만 지연된다.** 10:3x 동시각 — yfinance `103,625 / 10.49M주`
  vs KIS `102,545 / 12.09M주` (가격 **1.05%** 괴리). 장중 판정은 KIS 를 1차로 쓴다.
  ⇒ D103 의 *"yfinance 는 KRX 당일 장중 봉이 없다"* 는 **09:05 시점의 관측**이지 일반 명제가 아니다.
- **미정착 봉의 거래량 배수는 아래로 편향된다**(D74 의 산술): 경과 24% 에서 원시 **0.46×**.
  그대로 쓰면 **가격 조건은 충족인데 거래량 조건이 구조적으로 거짓** ⇒ 방어는 발화하고 복귀는 못 하는
  **F1 의 비대칭이 도구 안에서 되살아난다.** 그래서 경과비율 투영 + 장 초반은 `판정불가`(거짓 아님, C3).
- **일별 수익분해는 매매일에 항등식이 깨진다.** 07-31 실측: 보유 비중가중 슬리브 **+11.82%** vs
  NAV 역산 **+2.53%**, 차이 **+4.91pp**. 원인은 오류가 아니라 **그날 10:2x 매매** — 신규 매수 종목의
  "당일%" 는 보유하지 않은 시간대의 상승분까지 담는다. ⇒ 분해는 NAV 기준으로 **닫고**, 그 차이를
  `timing_gap_pp` 로 따로 잰다. 무거래일에 0 으로 수렴하지 않으면 그건 파서 결함 신호다.
- **F10 의 진짜 원인**: PLAY23 코드는 멀쩡하다. `fetch_data.py` 가 **155/200 에서 끊겼고**
  DONE sentinel·`fetch_summary.json` 이 없어 `analyze.py` 가 돌 재료가 없었던 것이다. 재개했다.

### 6-e. 사람 결정 4건 (2026-07-31 세션) + 그에 따른 집행
| 항목 | 결정 | 집행한 것 |
|---|---|---|
| **F8 arming** | **켠 채로 둔다** | 끄지 않았다. 대신 `exposure_rule` 이 매 실행 `🚨🚨ARMED` 를 올리고, 09:10·15:00 두 SKILL.md 에 "이중게이트가 아니라 **단일 게이트**" 경고를 박았다 |
| **F4 SSOT** | **콘테스트 자체가 SSOT — 책을 없앤다** | `_sync.py` 재작성: `book_targets()` 은퇴(호출 시 이유를 말하고 죽는다), `targets_from_intents()`+`apply_exposure_rule()` 신설. `plan` 이 **크래시 대신 경고**를 낸다 |
| **① 밴드** | **공격** 정상95/방어55/복귀90/과열75 | `data/exposure_bands.json` 생성. 복귀=20일저점 +3% ∧ 거래량 1.0× ∧ 상승마감 |
| **④ DEEP** | **연속1 + 회전1 = 2개** | `rotation.md` 의 N 을 **프로토콜 소유**로 바꿈(us=4 유지, kr=2). `industry_kr.md` 에 DEEP budget 절 + 근거. SKILL description 갱신. 컴파일 검증 완료 |

### 6-f. 밴드 적용 후 즉시 드러난 것
- **냉시동 함정**: 원장이 비면 첫 실행이 `prev_state=정상` 으로 시작해 오늘이 `방어`(괴리 −1.9pp)로 찍혔다.
  정착 시계열(…07-30)로 `backfill` 한 뒤 다시 돌리니 **`복귀` · 목표 90% · 현재 52.8% · 괴리 −37.2pp 🚨밴드이탈.**
  ⇒ **경로 의존 규칙은 원장이 비어 있으면 경보를 통째로 삼킨다.** `backfill` 서브커맨드로 막았다.
- **공격 프로필은 휩소했다 — 측정하고 고쳤다.** `simulate` 로 120세션 회전 비용을 재니
  **전이 36회 · 누적 1040pp · 주당 43.3pp** — 노출 규칙 하나가 데스크 **전체 주간 회전율(26.9%)의 1.6배**를
  종목 매매 전에 쓴다(누적 위반 이미 1건). 5일 내 왕복 11회. **과열 상태 제거로는 3.3pp/주밖에 못 줄인다**
  (범인은 방어↔복귀 진동). ⇒ `min_hold_sessions` 게이트 신설:

  | 최소유지 | 전이 | 주당 회전 | 복귀 발화 |
  |---|---|---|---|
  | 0 | 36회 | 43.3pp | 7회 |
  | **3 (채택)** | **18회** | **19.6pp** | 3회 |
  | 5 | 15회 | 16.7pp | **0회 — 규칙이 죽는다** |

  ★ **그리고 이 게이트는 07-31 에 비용이 0이다**: 07-30 상태가 오래된 `방어` 라 최소유지 0·2·3·5
  **전부** 07-31 을 `복귀`(목표 90%)로 판정한다. 잃는 것은 휩소뿐.
- **종목 상한 가드가 없었다.** 재정규화는 상한을 모른다 — **투자 90% 에 종목 7개 미만이면 균등 배분만으로도
  15% 상한을 깬다.** 실측 가드 테스트(5종목 90%): 4개가 18% 로 나던 것을 15% 로 깎고 초과분을 005930
  (상한 40%)에 재배분해 30% 로 안착, **깎은 내역 전부 출력**. `_sync._apply_caps` 신설.
- **`day_pct` 파싱 검증**: 보유 11종목을 KIS 당일등락과 대조 — 10/11 이 0.35pp 이내 일치(잔차는 호출 간
  실시간 변동). ⇒ **타이밍갭 +4.8pp 는 파서 결함이 아니라 실제 매매 흔적**이다.
- **두 원장 합산 가능성 검증**: `missed_ledger` 가 `reject_ledger` 를 그대로 import(동일 객체 확인),
  벤치 유니버스 218종목·노이즈밴드 5.0pp 동일. 부호만 반대.
- **F10 결과 — 창립 가설은 "구분 불가"가 아니었다.** PLAY23 완주(196종목·리밸런스 **29회**):
  **FM t: flow 2.68 (계수 +0.0043) vs value 0.56 (+0.0018)** ⇒ 판정 **수급 승**.
  quintile Q5−Q1: flow **+1.20%/20일, t=2.05, 승률 76%** vs value +0.05%, t=0.06, 승률 41%.
  ⚠ 이중정렬 마진은 **flow t=1.95 로 임계 바로 아래**(밸류 마진 t=−0.30). 4개 검정 중 3개가 수급 편,
  1개가 경계선. ⚠ 생존편향(오늘의 KOSPI200 소급)은 **양쪽에 걸리므로 상대비교만** 읽는다(README 자체 경고).
- **F10 견고성 — 판정이 몇 기간에 얹혀 있나** (`PLAY23/out/ROBUSTNESS.md`, `analyze.py` 추정기 재사용):
  · **부호검정 flow 21/29, 단측 p=0.0121** (value 12/29, p=0.87) — 분포 가정 없이도 남는다. 가장 견고한 한 줄.
  · **Leave-one-out t 최저 +1.997**, 1.0 미만 0회, 음수 0회. ★ **영향력 최대 기간(2026-02-10, coef −0.0298)이
    수급에 *불리*하다** — 빼면 t 가 2.68→4.58. "운 좋은 한 기간이 업고 있다"는 흔한 실패 모드가 **없다**.
  · **블록 잭나이프(연속 5기간) +1.91~+3.89**, 부호 반전 0. 단 2.0 을 네 번 스침 ⇒ "견고한 2.68" 이 아니라
    **"섭동 주면 2 언저리를 오가는 효과"**.
  · ⚠ **핵심 단서 — 최근 창에서 약해진다**: 최근20 t=+2.08 · 최근15 **+1.56** · 최근10 +0.98 · 최근8 **+0.51**,
    점추정도 **+0.0043 → +0.0019 단조 감소**. 검정력 소실과 효과크기 감소가 **겹쳐 있어 분리 불가**(n 부족).
    ⇒ 정직한 문장은 **"최근 창에서는 구분 불가이고, 추정치는 내려가는 방향"** — "죽었다"(검정력 없음)도
    "여전히 유효"(하락은 실재)도 아니다. **데스크가 매매하는 창이 정확히 그 창이다.**
  ⇒ **dig D15 를 CLOSED 로 갱신**(`handoff/RESEARCH.md`). 다음 할 일은 창립 가설 재주장이 아니라
    **다가올 리밸런스 시점을 관측점으로 사전등록**하는 것.
