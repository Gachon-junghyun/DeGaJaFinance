# HANDOVER — industry_US · 2026-08-20 (Thu) · Stage 0 / L1·HANDOVER

> Inheritance packet. **This stage transports analysis; it makes no market call, names no size and
> issues no buy/sell language (P4).** Run clock **KST 22:10–23:0x = ET 09:10–10:0x, US cash
> pre-market** (bell 22:30 KST). Terminal settled bar throughout: **2026-08-19**.
> Read: `handoff/STANDING_VIEW.md` (spine, §1–§6 + §5 retracted ledger through `R85`) ·
> `STANDING_VIEW_US.md` (§2 through `M737`, §3a rows overwritten 2026-08-19) ·
> `SCENARIOS.md` (spine: scoring log through the 2026-08-20 KR entry + MASTER INDEX) ·
> `SCENARIOS_US.md` (S1–S103, opened in full for past-dated rows) ·
> `SCENARIOS_KR.md` (**opened** — the other market's file is this run's to score if past-dated) ·
> `RESEARCH.md` (Part A triggers, Part B lenses, Part C dig list through `D294-KR`).

---

## §0 · Instrument health inherited — what this run may NOT claim

`llm_outputs/2026-08-20/preflight/PREFLIGHT_US.md` **was run** (22:10–22:30 KST, this run's own
stage −1). **PASS 3 / FAIL 5.** The rights table binds every stage below and is not re-litigated here.

**Revoked for this run:** the sweep's news-velocity axis · theme freshness · any "it went quiet"
sentence · `breadth` as a news-independent reading · the **Materials `wflow` sign** (`LIN` at 24.7%
owns it) · any concentration number without its `--days` · `wflow` described as *current*
market-cap weighting (weights are **36 days** old) · any flow/RS/OBV/short call on **`EA`** ·
`--ic`-derived sizes as evidence-backed · any 08-20 price.

**Granted:** settled prices and every price-derived statistic through the **2026-08-19** close · a
**one-session Δ (08-18 → 08-19)** — the first clean single-session Δ since 08-12 · `eqflow` and the
ten non-flipper sector signs · FINRA short pressure · CFTC COT · FRED · primary filings ·
`module_chart --read` (with the "`--help` dead, output probed" stamp) · hand-probed per-name news
velocity (40/40 valid at 22:14 KST).

⚠ **Two carried rules interact with the granted Δ and must travel with it into SWEEP and ROTATION:**
- **`D293`** — `sector_flow` publishes `delta` but not `rs20_earned` / `rs20_rolloff`. A rolling
  20-session excess advances by dropping sessions off the back as well as adding them at the front.
  On 08-19 that defect **cost one promotion of two** (`R82`, Staples). Today's Δ spans **one**
  session, so the roll-off share is one session in twenty rather than two — smaller, **not zero**.
- **`R81`/`D290`** — `flow_tag` does **not** take the run-level axis set while `flow_score` does. With
  `vel_axis=false`, a 🟢 may still be velocity-derived. **No stage may promote on a tag today.**
  ⚠ The 08-19 run wrote that same sentence in its own HANDOVER and then used the tag in three
  stages. Writing it is not complying with it.

---

## §1 · Inherited regime call and standing view

**Regime (spine §1, `[inferred]`, unchanged):** *memory is a price-cycle industry in rate-of-change
deceleration while its level stays tight* — the equity tracks the **second derivative** of price, not
the level, which is why "shortage persists" and "stocks struggle" are both true.

**Carried per-name theses (§3a, all overwritten 2026-08-19, `asof` 08-18/08-19 settle).** Tags
preserved; no `[inferred]` row below is passed downstream as evidence.

| Node | Carried state | Tag | Live re-check |
|---|---|---|---|
| **`MPC` · `PSX` · `VLO`** | Refining-**capacity** destruction, attributed: the crack owns **70%** of the fitted 5-session and **100%** of the 20-session excess vs `SPY` while `BZ=F` returned **+0.011%**. Constraint is now **`L2` peak-margin**, not the barrel — `MPC` quarterly GM above its own peak year, **no take-or-pay floor** | `[measured]` price · `[filing]` structure | 🟡 PARTIAL. Kill = a **second consecutive** crack-rate decline (one has fired). `P70`/`P75`/`S88` → **08-21** |
| **`RTX`** | Re-filed at lower conviction. The "new-🟢" did not survive DEEP (`vol_surge` 0.83, tag velocity-derived). What survives: RS20 **+13.9** / RS60 **+24.8** vs `SPY`, revisions 22↑/0↓. **The defense group's beta, not its leader**; negative tech beta (−0.51, t −4.96) makes it the book's diversifier | `[measured]` | 🟡 PARTIAL, C-grade disagreement (D6). **Customer unmeasured 203 days.** → **08-21** |
| **`LMT` · `NOC` · `GD`** | The defense accumulation is real and it is **not `RTX`'s** — replicates on both OBV instruments, 9/10 of A&D accumulate; `M719` strengthened to **+5.11pp on 60d**. Revision breadth **18↑/0↓ · 14↑/3↓ · 17↑/1↓** but **shallow** (90d level +1.8% / +0.8% / +2.4%) | `[measured]` | 🟡 PARTIAL. Kill = two-instrument replication breaking. `S97` → **08-21** |
| **`NUE`** | Post-earnings re-rate **predating** the tariff thread by 12 sessions ⇒ EVENT_ALPHA Card 4's driver refuted by dating. Survives: **+19.9%** 90d estimate change, 10↑/0↓, against PEG 5.21, β 1.89 | `[measured]` | 🟡 PARTIAL + 🚨 the sheet's **only 🔴 FINRA axis** (short-vol z +1.99). Kill = z > +1.5 for 5 sessions with OBV still 매집. `S77` scored **FIRED-C** by the KR desk (see §2) |
| **`T`** | 🚨 **An orphan position** — **9.74%** of invested (`M737` corrected `9.59%`, which is `RTX`), telecom `eqflow` **+0.404**, 3/3 OBV-accumulating, **no cycle label, no card, no thread, no bracket, no KPI** | `[measured]` | 🟡 PARTIAL — **the residual is a LABEL, not a price.** → **08-26** |
| **`WMT` · `TGT`** | The Staples carriers, and they disagree: `WMT` new-🟢 with the sheet's **only negative estimate axis** (−0.4%, 1↑/7↓); `TGT` the **under-computed** leg (1.8% sector cap weight, best settled `flow_score`) | `[measured]` | ⚠ **`R82` reverted Staples UW− → the promotion is withdrawn**; `S100` settles **08-21**. Both prints are now past (`TGT` 08-19, `WMT` 08-20) |
| **`GOOGL`** | **Half the COMM underweight is already wrong** — β-adjusted excess **+1.227** vs `SPY` while `META` carries **−6.704**; FINRA z **−2.00**, 5v5 **−10.1▼** | `[measured]` | 🟡 PARTIAL. Trigger = close above **344.48** with OBV 누적 and RS60 > −10. → **08-21** |
| **`KKR` · `LITE` · `ORCL`** | The cross-sector orphans, carried **because their sectors hold no DEEP slot** — the `006360` shape | `[measured]` | 🟡 PARTIAL ×3 → **09-02** / **09-11** |
| **`XOM`** | 🔴 RESOLVED — flat on every axis; **that flatness is what makes it the crack-attribution control**, and a control is not a bet | `[measured]` | Ledger `A.flow미도착`. Revives on breadth ≥2:1 up **and** RS60 > +10. → **09-16** |
| **`FCX` / the `Copper` COT** | 🔴 RESOLVED — COT at the **100th percentile** and `FCX` β **+1.177pp per 1% `HG=F`**, but ex-`FCX` moves the `S77` observable **0.007pp**. Survives as a risk, not a thesis | `[measured]` | Ledger `I.테제반증` → **09-16** |
| **The optical/interconnect cycle** | 🚨 `cycle_registry.json` has **no row** for it ⇒ exposure is **unmeasurable, not zero**. `M731`: 5 of the 12 real-book names are absent from the registry; the three AI-chain names alone are **14.75% of total assets** | `[measured]` | Registry maintenance is a **human** item (P5). `S94` → **08-31** |

**Settled 08-19 read on the carried names** (three-axis, tag NOT used as evidence per `R81`):

```
MPC  +0.667 rs20 +11.3 rs60 +38.5 OBV매집 surge 1.00   PSX  +0.683 rs20 +11.7 rs60 +33.2 OBV매집 surge 1.03
RTX  +0.617 rs20 +10.2 rs60 +21.3 OBV매집 surge 0.91   HPE  +0.557 rs20  +7.5 rs60 +38.2 OBV매집 surge 0.84
ANET +0.321 rs20  +3.7 rs60 +17.9 OBV매집 surge 0.82   T    +0.287 rs20  +6.1 rs60  −3.7 OBV매집 surge 0.46
NUE  +0.465 rs20  +2.5 rs60  +4.1 OBV매집 surge 1.05   MET  −0.164 rs20  −0.9 rs60 +10.0 OBV중립 surge 0.60
NVDA −0.253 rs20  −0.3 rs60  −2.1 OBV중립 surge 0.71   AVGO −0.625 rs20 −11.5 rs60 −15.6 OBV분산 surge 1.00
ETN (INDU, held)                                        NDAQ −0.372 rs20  +3.1 rs60  +2.7 OBV분산 surge 0.51
LITE +0.182 rs20  −3.1 rs60 −15.7 OBV매집 surge 0.96   COHR +0.058 rs20 −10.8 rs60 −27.0 OBV중립 surge 1.51
```

★ **`AVGO` is the only book name tagged 🔴 on the settled board** (flow −0.625, OBV distribution,
RS20 −11.5), and it sits in the same measured risk unit as `NVDA` at 500d/750d and **not** at 250d
(`--days` stated per G4). Handed to ROTATION/DEEP as an observation, not a verdict.

### Retracted ledger read BEFORE forming today's view (spine §5, through `R85`)

The four that bind this run:
- **`R78` · `R81`** — *a 🟢 tag is not evidence while `vel_axis=false`*. Both were written by runs that
  then used the tag anyway. **Binding constraint, not a caution.**
- **`R82`** — the **Consumer Staples UW− → UW promotion is withdrawn** (Δ +0.325 was **77.7%
  roll-off**; earned improvement ranks 6th of 11, not 1st). ⚠ **Health Care N → N+ is explicitly NOT
  retracted** (40.2% roll-off but **+1.55pp earned, 2nd of 11**, and three legs that use no Δ).
  ⚠ **Energy was the only sector whose improvement was fully earned (+3.75 earned vs −0.56 drag)** —
  the desk over-rated the sector it promoted and under-rated the one it held.
- **`R80`** — the KR desk's *"survivor set is deterministic and market-cap aligned"* does **not**
  transfer to US. Today's US shape reproduces the US shape (40 runs, 33 singletons, indices 0–297),
  not KR's. `W1`.
- **`R83`** — `theme_age` does **not** cap its age readout; 🟢LIVE reads zero because the
  **conjunction** (age ≤14d ∧ accel ≥2×) is unmet on a desk that trades multi-quarter cycles.
  **This is arithmetic, not an instrument story** — do not re-file it as a tool defect.

**Nothing in this run's packet re-argues a retracted claim.** Any stage that wants `TGT` 🟢, a Staples
promotion, a survivor-set selection reading, or a `theme_age` cap story must bring a *new*
measurement and name the retraction it is overturning.

---

## §2 · Scenarios — scored, deferred, and the one flagged before it is scored

### (a) Scored this run

| ID | Settle | Observable | Bands | **Measured** | Verdict |
|---|---|---|---|---|---|
| **`S80`** | 2026-08-19 | EW{`XLU`,`XLRE`,`XLP`} **4-session** cum excess vs `SPY`, 08-13 → 08-19 close | A ≥ **+1.85** (against us) · B ≤ **−2.29** | **+1.232 pp** | **FIRED-C** — no conclusion changes |

Full arithmetic, settled closes (`yfinance auto_adjust=False`, `SPY` last bar **2026-08-19 769.06**
verified): 08-13 → 08-19 returns `XLU` **−0.045%** · `XLRE` **−0.288%** · `XLP` **+0.628%** ⇒ EW
**+0.098%**; `SPY` **−1.134%** ⇒ excess **+1.232pp**.

★ **The number that matters is the direction of travel, and it is against the desk.** The 08-18
pre-settle reading recorded by the 08-19 run was **+0.801 raw / −0.387 β-adjusted**. One session later
it is **+1.232**, i.e. it moved **+0.43pp toward branch A** and finished **0.62pp short of falsifying
three underweights at once**. **C is not "nothing happened"** — the duration complex beat `SPY` by
1.23pp over four sessions while the desk is short three legs of it. Recorded, not smoothed.

### (b) 🚨 Flagged BEFORE scoring — `S98`'s anti-signal appears to have fired

`S98` (settles at the **08-20** close) carries this VOID clause verbatim: *"An **FOMC-dated
communication** or a CPI/PPI print inside the window drives all four together for a reason that is not
the duration structure ⇒ **VOID**."* Its window is **08-13 → 08-20**.

**The FOMC minutes were released 2026-08-19 — inside that window.** This desk's own `S102`, registered
the same day, opens with the sentence *"The FOMC minutes released 2026-08-19 are a dated ≤48h binary."*

⇒ **`S98` is on track to be VOID, not scored**, and the scoring desk must decide that on the clause
rather than on the outcome. This is written **today, before the observable settles**, precisely so it
cannot be decided after seeing which branch would have fired (`§4c` / `D48`).

⚠ And the pre-settle reading makes the timing consequential: at the 08-19 close, 4 of 5 sessions in,
**all four legs are POSITIVE** — `XLU` **+1.089** · `XLRE` **+0.846** · `XLP` **+1.762** · `XLV`
**+5.469** (excess vs `SPY`, 08-13 → 08-19). That is **branch A's shape** — "one bet wearing four GICS
labels" — which is the branch that would retire a four-sector tilt. **A VOID would discard the run's
highest-information row on the day it was about to pay.** The clause still governs; that is the point
of pre-registration.

### (c) Pre-settle readings recorded for the D+1 scorer (not scores)

| ID | Settles | Observable | Bands | Reading at the **08-19** close (4 of 5 sessions) |
|---|---|---|---|---|
| **`S82`** | 2026-08-20 | [`XLV` exc5] − [EW{`XLU`,`XLRE`,`XLP`} exc5], 08-13 → 08-20 | A ≥ **+2.45** · B ≤ **−1.62** | **+4.237 pp** — already **1.79pp above branch A**, and above its own 252d p85 (+2.299). ⚠ `XLV` alone returned **+3.51% on 08-19**, the single session carrying most of it |
| **`S83`** | 2026-08-20 | [`ANET` exc5] − [`HPE` exc5], 08-13 → 08-20 | A ≥ **+7.16** · B ≤ **−9.68** | not computed at HANDOVER (a pair spread with sd **8.417pp**, the board's widest estimator; C is the disclosed heavy favourite). ⚠ **Check the VOID clause first: an `HPE` earnings print inside the window voids the row** — `module_fundamentals_us` gives `HPE` next print **2026-09-04**, outside, so the clause does not fire on that leg |
| **`S98`** | 2026-08-20 | sign of exc5 for `XLU`/`XLRE`/`XLP`/`XLV` | A all same sign · B split · C 3–1 | **all four positive** (above) ⇒ **branch A shape** — but see (b): **VOID is the live question, not the branch** |

**Why these are deferred and not `EXPIRED`:** all three settle on the **2026-08-20 settled close**,
which arrives at **05:00 KST 2026-08-21**, after this pre-market run ends. This is the mechanical
D→D+1 rule the 08-19 run wrote down explicitly and the KR desk applied this morning to `S63-KR`.
**They are past-due on arrival for the next run.**

### (d) Already scored by the sibling desk — verified, not assumed

The `industry_kr` run of this morning (09:2x KST) scored **four US-owned rows** whose deferral reason
had expired when the 08-19 US close landed at 05:00 KST: **`S75` FIRED-B** (`XLE` exc5 **+4.622** ≥
+3.94 — the ENRG promotion verified on its own axis) · **`S76` FIRED-B on both legs** (`XLV` exc5
**+4.742**; accumulation count **22/32**) · **`S77` FIRED-C** (non-`NEM` EW **−1.589**) · **`S78`
FIRED-C** (median RS20 **−0.594**). Ownership confers no exclusivity (README §3); this desk **verified
the entries exist** rather than re-scoring them.

★ **One observation the KR desk explicitly handed back to this desk**: `S77`'s −1.589 is made by two
names — **`STLD` −11.78** and **`NUE` −8.04**, with 6 of the remaining 9 inside ±1.4pp. *"We removed
`NEM` and now two steel names dominate."* **`NUE` is held.** Handed to ROTATION/DEEP-MATR.

### (e) Still armed, with dates

**08-21** (the next dense settle): `S84` `S85` `S86` `S87` `S88` `S89` `S90` `S91` `S93` `S95` `S96`
`S97` `S99` `S100` `S102` · **08-24**: `S74` · **08-26/27**: `S79` `S81` · **08-29**: `S103` ·
**08-31**: `S92` `S94` · **09-30**: `S48`.

⚠ **`S99` carries a construction defect recorded ~90 minutes after its own registration** (its premise
— `ETN`–`ANET` +0.722 — was cited without the machinery control; `ETN`–`CMI` **+0.811** and
`ETN`–`CAT` **+0.807** are both higher, and a joint regression gives β`XLI` **1.396** vs β`XLK`
**0.619**). **The row settles 08-21 as registered and is not withdrawn** — but its branches do not
contain the answer, and the successor test is a **joint-loading** test.
⚠ **`S103`** (`NVDA` 08-26) has **hand-set ±5.0pp bands** because the readable chain expired 08-21.
**`D295` obliges a re-derivation from the 08-26-or-later straddle before the print.** Not yet done.

### (f) 🚨 `S8` — undated and unscoreable for the **18th** consecutive run

Still on `CATALYST_WATCH` as its **undated** 🔀binary. **A human must `VOID` it or re-register it with
a date (P5).** `S95` brackets the expiry `S8` could not. ★ Eighteen runs of writing the same line is
itself the cost, and this run declines to write more than one line about it.

**Score this run: 1 scored (`S80` FIRED-C) · 3 deferred with the settle clock stated · 1 VOID-risk
flagged pre-settle · 0 `EXPIRED` · 0 silent skips.**

---

## §3 · Both ledgers — audited symmetrically

`reject_ledger.py due` **and** `missed_ledger.py due` were both run (not `score` alone).

**At entry:** rejections **200 rows · 104 resolved · 15 past-due · legacy (no `revives_if`) 0** ·
misses **178 rows · 70 resolved · 29 past-due · legacy 0**.
**At exit:** rejections **111 resolved · 8 past-due** · misses **82 resolved · 16 past-due**.
★ **The legacy count is 0 on both ledgers** — the practice has taken hold; it is no longer a quiet
pass. **19 rows resolved this run.**

**Every remaining past-due row on both ledgers is a KR ticker** (`002960 007070 010130 028050 035720
069620 073240 161890 192820 267260 373220 375500 377300` on the miss side; `009830 010060 010950
017670 003650 007070 082640` on the rejection side). **Zero US rows are carried unexamined.** They are
named here rather than dropped: their revival/entry conditions are written against KIS investor
actuals and domestic-scope news, which this desk cannot measure under `--scope foreign` (`W1`), so
they belong to `industry_kr` and are handed back explicitly.

### Rejections resolved (7)

| Ticker | Rejected | Outcome | The measurement |
|---|---|---|---|
| `AMAT` | 07-31 | **reaffirmed** | rs20 **−13.3**, OBV **분산** (−0.15) — both legs of "RS20>0 AND OBV→매집" fail |
| `LRCX` | 07-31 | **reaffirmed** | rs20 −6.7, OBV 분산; **delta −0.259**, the largest single-session deterioration of the three |
| `KLAC` | 07-31 | **reaffirmed** | rs20 **−15.7** (worst), OBV 중립 (−0.04) |
| `EMR` | 08-06 | **reaffirmed** | ★ **Leg 1 body-read on the PRIMARY text**: the 2026-08-04 10-Q (118,475 chars after tag-strip) contains **"data center" 0 · "datacenter" 0 · "hyperscale" 0**; the one "AI" hit is segment boilerplate. Leg 2 (OR): CY 90d **+0.7%**, below the 1% bar (30d CY breadth 17↑/1↓ *is* net-up) |
| **`AMGN`** | 08-06 | ★ **REVIVED** | **Its own pre-registered number fired on its own date**: CY consensus **23.04** clears the registered **22.90** bar by **2026-08-20** (22.35 at registration; +2.8%/90d). Returns to the candidate pool **with its original evidence, not laundered**. Corroboration only: rs20 +18.0, rs60 +27.2, OBV 매집 |
| `AMZN` | 08-06 | **reaffirmed** | Leg 1 **MET** (next-year 30d breadth **39↑/5↓**). **Leg 2 NOT MEASURED** — no primary text was searched for a named Treasury/CBP refund instrument. Recorded as *unmeasured*, not as failed |
| `VST` | 08-08 | **reaffirmed** | FY/CY breadth 30d **2↑/5↓** and next-year **0↑/6↓** = net-negative; CY level −2.4%, next-year −6.0%. Weakest flow of the due set (−0.778, rs20 −17.3) |

### Misses resolved (12)

| Ticker | Missed | Outcome | The measurement |
|---|---|---|---|
| **`VLO`** | 08-04 | **entered** | rs60 **+37.1** vs a **+15** bar, and no named windfall instrument has appeared. ⚠ The desk does **not** hold `VLO`; this closes as a measured coverage fact |
| **`CVNA`** | 08-06 | **entered**, margin disclosed | rs60 computed to full precision is **+0.0225 pp** — positive by **2 basis points**, i.e. inside rounding (the JSON prints 0.0). OBV 매집, rs20 +9.4 ⇒ the AND holds **as registered**. 🚩 **A threshold set at exactly zero has no buffer** — new dig |
| **`NUE`** | 08-06 · 08-08 | **entered** ×2 (book state) | `NUE` is **HELD in both books** — paper (1 sh, `steel/MATR`) and real (1 sh, −9.04%). The registered `vol_surge ≥1.2` leg still fails (**1.05**). A miss row on a name the desk owns is a bookkeeping error, not an open miss (the `316140` precedent) |
| **`LITE`** | 08-08 | **entered** (book state) | **Held in the REAL book** (1 sh, −10.48%) and **absent from the paper book** — `T22` applies, the unit is named. Registered leg still fails (tag neutral, `vol_surge` 0.96) ⇒ closes on **ownership**, not on the condition |
| `EQIX` | 08-04 | reaffirmed | delta **−0.228**; not one positive session, let alone two |
| `SPCX` | 08-06 | reaffirmed | 🚨 **Structural, not market-dependent**: `us_top300.csv` unchanged (300 rows, 36 days stale), no SPCX row, no ad-hoc path registered. **This row cannot resolve on price and will keep re-surfacing until a human acts (P5)** |
| `STLD` | 08-06 | reaffirmed | `vol_surge` 0.90; Materials green count still **0** (0🟢 / 4🔴, breadth 0.00). ⚠ Its sector's `wflow` sign is flipper-owned (G3), so no sector route existed either |
| `GOOGL` | 08-06 | reaffirmed | OBV **중립** (+0.05) not 매집, rs20 **−2.1**; the AND inside that OR-leg needs both. Next print 2026-10-30 |
| `PRU` | 08-08 | reaffirmed | rs20 **−0.1** ⇒ cannot enter a top-15 cut with rs20>0. OR-leg 1 ("FIN returns to OW") is **not determinable at HANDOVER** — carried to ROTATION; FIN reads `wflow` −0.009 / `eqflow` −0.127, 2🟢/8🔴 |
| `NDAQ` | 08-13 | reaffirmed | Both legs measured: **no bracket exists** for `NDAQ` in `SCENARIOS_US.md`, and exc20 **+3.099** against its **own** 252d **p85 +6.439** (p50 −1.831, pctile 73.4). **The row's own point stands: a held name with no bracket** |
| `XLV` | 08-13 | reaffirmed (**not yet observable**) | Keyed to `S82` branch A at the **08-20** settle, which has not arrived. Pre-settle **+4.237** recorded for the scorer |

⚠ **Sign hygiene (`3c`)**: the two ledgers' `excess` columns are **inverted** relative to each other
and are **not summed** anywhere in this packet.
⚠ **`score` is carried as accumulation, not as an edge** — the miss ledger's first 6 rows are
outcome-selected (harvested from `leak_scan --top`), and the script says so itself.
★ Carried from 08-19 and still binding (`T25`): the rejection ledger's loss asymmetry replicates at
**n=96 — loss sum +423.1pp vs gain sum −196.5pp = 2.15×** (against 2.2× at n=24), and the class this
desk reached for most recently, `G.섹터중립`, is the ledger's **worst at +13.9pp (n=2)**, where
positive means **the rejection cost us**. Any structural rejection this run makes must re-print that
number beside itself.

---

## §4 · Exposure state — size context for BET/ALPHA (read-only; this stage never runs `log`)

`exposure_rule.py state` · `show --tail 8`.

- **Rule state `정상`** (previous state also 정상; no firing condition). Bench `069500.KS` closed
  **108,190**, **+6.319%** on the day, 20d-high **−4.45%**, volume **1.539×**.
- **Target invested 95% · current invested 81.1% · band gap −13.9pp** 🚨 (the `state` call itself
  could not fetch the account and printed *투자비중 미상*; the **81.1%** figure is the 08-20 **live**
  row in `out/exposure/ledger.csv`, which is an **intraday, unsettled** mark — stated, not laundered).
- **Ledger n = 10** (not a cold start — `show` returns 37 rows). Cumulative decomposition:
  **total excess −14.94pp = cash −6.25pp + selection −8.69pp**.
- ⚠ **The band gap is closing**: −39.4pp (08-13) → −27.1pp (08-14) → −14.0pp (08-19) → **−13.9pp**
  (08-20), i.e. invested has been rising 55.5% → 67.9% → 81.0% → 81.1%.
- ⚠ **`n=10` is a sample, not a sign (C4).** Neither the −6.25 nor the −8.69 may be quoted as an
  established contribution; the report's own line asks for n≈20 before the question is even posed.
- 🚨 **Standing alarm on every row: `ARMED (TIMEFOLIO_EXECUTE=1)`.** Analytical note only — this desk
  issues no orders and never passes `--execute`.

---

## §5 · Reconciliation — belief vs coverage

`module_report_tags show` was cross-queried with `DEGAJA_REPORT_DIR=llm_outputs`.

🚨 **The mechanical ledger cannot perform this reconciliation today: its index (`REPORT/_tags.json`)
is stale.** The newest `industry_US` rows it returns are dated **2026-07-21** — **30 days behind** the
carry, which runs through 08-19. Every belief in §1 would read as "no ledger coverage" and every
recent DEEP as "no standing thesis", both spuriously.

⇒ **No demotion, no dig and no coverage-gap claim in this run may cite the ledger.** The
reconciliation is deferred to the ledger refresh at run end (the scheduled task's step 5 — copy the
finalized reports into `REPORT/industry_US/` and re-scan), and **whether it then reconciles is the
next run's check, not a claim this one may make.**

★ Stated positively for the writeback stage: **this is the first run in which the ledger's staleness
has been measured rather than assumed**, and it is the specific reason the "beliefs without coverage
/ coverage without beliefs" table is **absent** from this packet rather than empty.

---

## §6 · Catalyst injection and the ≤48h binary rule

`catalyst_calendar.py --days 10` (saved to `llm_outputs/2026-08-20/CATALYST_WATCH.json`):

| Window | Event | Axis | Status |
|---|---|---|---|
| **D-6** | **`NVDA` earnings 2026-08-26** | earnings | 🔀binary — bracketed by **`S79`** (name), **`S81`** (readthrough), **`S103`** (cycle) |
| **D-8** | **July PCE 2026-08-28** | inflation | 🔀binary `[bea~est]` — ⚠ **no bracket on the board is keyed to this date.** Flagged for PREMORTEM |
| undated | Iran "Strait of Hormuz open" statement | oil | 🔀binary — **still undated for the 19th run** |

⇒ **No binary lands ≤48h from this run.** The protocol's mandatory both-sides bracket rule
**does not trigger today** — and this line exists because an empty `ACTION_TICKETS.md` cannot
distinguish "nothing was due" from "the tool could not emit one" (**`D294`**: `action_bracket.py`
named a binary in its header and reported none in its body; its `earnings` axis holds placeholder
prose and can never produce a ticket).

⚠ **`D288-KR` reproduces on the US calendar.** The calendar (a) did not carry the **FOMC minutes of
2026-08-19** — the single dated macro binary of the window, and the event that now threatens `S98`
with VOID — and (b) still carries the **undated** Hormuz row whose KR-side bracket (`S57-KR`) closed
`EXPIRED-미도래` this morning. **It can neither add a known date nor remove an expired row.**
PREMORTEM should treat **July PCE (08-28)** as the un-bracketed dated binary of this window.

---

## §7 · ★ The cross-market check this run owes the next one — `vol_surge` does NOT transfer

`ic_ledger.py score --market kr` and `--market us` were both run after `axis_inflection.py` +
`ic_ledger.py log` (58 new rows accrued; ledger now **699 rows = 479 KR + 220 US**).

**This morning the KR desk registered `D290-KR`**: *"`vol_surge` IC has cleared Bonferroni and
`sector_flow`'s 🟢 gate weights that axis positively"* — h=1 **t(NW) −3.30, n_eff 27.0, mean IC
−0.0479**; h=5 **−3.14, n_eff 4.4**. Both beyond |t| > 2.8. That measurement is not disputed.

**On the US ledger the same axis reads the other way and is not significant:**

| Axis · horizon | market | n_eff | mean IC | t(NW) | verdict |
|---|---|---:|---:|---:|---|
| `vol_surge` h=1 | **KR** | 27.0 | **−0.0479** | **−3.30** | ★ clears Bonferroni |
| `vol_surge` h=5 | **KR** | 4.4 | **−0.0438** | **−3.14** | ★ clears Bonferroni |
| **`vol_surge` h=1** | **US** | 20.0 | **+0.0253** | **+1.49** | **구분 불가 — and the sign is POSITIVE** |
| `vol_surge` h=5 | US | 3.0 | +0.0209 | +0.99 | n_eff<4, unquotable |
| **`rs60` h=1** | **US** | 20.0 | **−0.0843** | **−3.00** | ★ **the only US cell clearing Bonferroni — and it is negative** |

⇒ **Two things, stated positively.**
1. **`D290-KR` must not be imported into this desk's gate reasoning.** The US sample (n_eff 20, 15
   simultaneous tests) puts `vol_surge` at **+0.025 with t +1.49** — indistinguishable from zero, and
   pointing the *opposite* way. Importing the KR conclusion is exactly the `W1` violation the
   protocol's own DEEP-budget note logs. **The US gate question is open, not answered.**
2. **The one US axis that has cleared multiple comparison is `rs60` at h=1, with a NEGATIVE IC
   (−0.084).** High 60-day relative strength **measured against `SPY`** — `rs60` is defined in
   `SECTOR_FLOW_US.json` as the 60-session excess return vs `SPY`, and the benchmark is named here
   because the sign of any excess statistic flips with it (`C1`) — predicts *lower* next-day return
   in this window. ⚠ The
   window is the **August drawdown-and-rebound tape** and a regime label is mandatory (the ledger
   says so itself); one significant cell out of 15 with `rs60` h=5/h=10 both at n_eff<4 is **a clock,
   not a verdict**. But it is the desk's own ranking axis, it sits inside `flow_score`, and **`RTX`,
   `MPC`, `PSX`, `HPE` are carried on RS60 arguments.** Handed to DEEP as a question.

★ **This is the first run to measure the two markets' IC ledgers against each other on the same day.**
No axis is flipped here — the fix is a human item (P5) — and every citation of `vol_surge` downstream
must carry its market.

---

## §8 · RESEARCH rules loaded as binding constraints — which group binds which stage

Loaded from `handoff/RESEARCH.md` (Part A triggers · Part B lenses · Part C dig list). **Grouped by
the moment they fire, not summarized.**

| Group | Fires when | IDs | Binds, this run |
|---|---|---|---|
| **C** | you cite a number | C1 baseline · C2 both halves · C3 unknown column · C4 "indistinguishable" · C5 arbitrary choice | **MACRO** and every stage. ★ **C4 is live on three objects today**: exposure `n=10`, `G.섹터중립` `n=2`, US `vol_surge` `t +1.49` |
| **S** | you make a statistical claim | S1 date-fold · S2 diagnose the null · S3 power first · S4 in-sample≠done · S5 short samples · S6 future labels | **any stage citing a test** — §7's IC table, every `D93` band |
| **D** | you read data | D1 second venue · D2 proxy sign · D3 signed vs unsigned · D4 regime contamination · D5 cross-provider · **D6 signal grade (OBV is C)** | **SWEEP · ALPHA · L2 indicators · L2 money_trail**. ★ **D6 is stamped on `NUE`** (OBV 매집 against a 🔴 FINRA axis) and on `RTX` |
| **W** | you write a conclusion | W1 cross-market transfer · W2 inherited lead/lag · W3 real≠profitable · W4 name the customers · W5 sub-sector dispersion · W6 reader's-market spine | **DEEP · BET · ROTATION**. ★ **W1 is the run's headline constraint** (§7, and `R80`). ★ **W4 is unmet on `RTX` for 203 days** |
| **L** | lenses, not triggers | L1 second derivative · L2 peak-margin trap · L3 branch information content | **DEEP · PREMORTEM**. ★ **L2 is the live constraint on the refiners** — the crack sits at the 95.2nd %ile of 250d and **is not yet in the margin series** |

**New rules added 08-19 that bind from this run on:** **`T22`** (name the book unit in the same
sentence — paper 11 vs real 12; this packet does so throughout) · **`T23`** (a term burst must be
body-read **and** sat next to `HY OAS`/`NFCI`) · **`T24`** (print the denominator next to any
"quiet/fading" tag — under n≈15 the tag is not a reading) · **`T25`** (re-print a rejection class's
own ledger score beside the rejection).

---

## §9 · Dig list ranked for today — candidate DEEP assignments

| Rank | Dig | Why it is today's | Owner |
|---|---|---|---|
| **1** | **`D290` / `R81`** — `flow_tag` must take the run-level axis set | `vel_axis=false` again today. Until fixed, **every 🟢 on the board is unusable as evidence** and the shortlist is not a quality signal | code fix = human (P5); this run works around it |
| **2** | **`D293`** — publish `rs20_earned` / `rs20_rolloff` beside `delta` | It cost a promotion on 08-19 (`R82`) and **six of eleven sectors printed an improving Δ while losing to `SPY`**. Today's Δ is one session, so the roll-off share is smaller, **not zero** | ROTATION must not carry a verdict on Δ alone |
| **3** | **`D250` / `M731`** — `cycle_registry.json` misses 5 of the 12 real-book names (`LITE` `COHR` `HPE` `T` `NUE`) | Reported AI any-layer exposure **23.64%** of total would read **≈38.39%**. The GAP check **cannot fire** on the optical cycle at all | registry maintenance = human (P5) |
| **4** | **`D294` / `D295`** — `action_bracket.py` emits no ticket for a single-name earnings binary; a beyond-window bracket has no straddle | **`NVDA` 08-26 is D-6 and `S103`'s bands are hand-set.** `D295` obliges re-derivation **before** the print | ALPHA |
| **5** | **`T`'s missing label** | 9.74% of invested, best `eqflow` on the board, **zero artifacts**. *The desk cannot claim credit for a position it has no thesis for, and cannot manage one either* | DEEP-COMM candidate |
| **6** | **`S77`'s steel residual, handed over by the KR desk** | non-`NEM` EW −1.589 is **`STLD` −11.78 + `NUE` −8.04**; `NUE` is held and carries the sheet's only 🔴 FINRA axis | DEEP-MATR candidate |
| **7** | **NEW — a threshold set at exactly zero has no buffer** | `CVNA` entered on rs60 **+0.0225pp**, 2bp above a zero bar, inside the sweep's own rounding. Registered conditions should carry a band, not a point | condition-writing rule candidate |
| **8** | **NEW — `us_top300.csv` staleness is now blocking a ledger row, not just weighting** | `SPCX`'s entry condition is *"SPCX is added to `us_top300`"*; the file has not moved in **36 days**, so the row **cannot resolve on any market outcome** and will re-surface indefinitely | human (P5) |

⚠ **`D10` carried forward, not re-discovered**: the news-body boilerplate defect is an open code
defect requiring human approval **and a server console** (FTS writes are server-only, P6).
⚠ **`D9` remains half-closed**: measured units are surfaced (`risk_units` · `RISK_UNITS.json`), but
whether a label/unit mismatch **blocks** or only **warns** is a human call.

---

## §10 · Staleness flags

| Object | `asof` | Flag |
|---|---|---|
| `data/us_universe/us_top300.csv` | **2026-07-15** | 🚨 **36 days** (limit ≤8). Every `wflow` in this run is weighted on 36-day-old caps; and it now **blocks a ledger row** (§9 rank 8) |
| `REPORT/_tags.json` (handoff ledger index) | **~2026-07-21** | 🚨 **30 days** — §5 reconciliation is unavailable |
| §3a per-name rows | 2026-08-18/19 settle | ✅ fresh (overwritten by the 08-19 run) |
| §3b KR rows | 2026-07-25 ~ 07-29 | ⚠ **~3 weeks** — read-only for this desk, **not cited as evidence anywhere in this packet** |
| `data/estimates` snapshot history | 12 files / 30 days | 🚨 G6 — accrual **2.5× slower** than ideal; **thin history cannot be back-filled** |
| `cycle_registry.json` | — | 🚨 no row for optical/interconnect or custom AI silicon ⇒ **unmeasurable, not zero** |

**Cleared suspensions converted into dig instructions:** none matured this run (the refiners' `L2`
re-check and `S88`/`P70`/`P75` all fall on **08-21**; `RTX`'s customer measurement has no date and is
carried as an open exposure, §9 is where it lands).

---

## §11 · §4c — what this run asserted and then refuted (D48)

**One, and it is procedural rather than analytical.** This stage first read the `due` listings'
leading date column as the **recheck** date and drafted `--date` arguments from it; the ledger's
`resolve` matches on the **original rejection/miss date**, which is a different field. Caught by
reading `out/reject_ledger.jsonl` directly before writing, so **no resolution was filed against a
wrong row**. Recorded rather than edited away.

⚠ **Zero *analytical* self-refutations in a run that ran controls is worth a line, and this run's
controls were real**: the `EMR` leg was decided by a **body read of the primary text** rather than by
the revision proxy, `CVNA`'s knife-edge was found by recomputing to full precision instead of trusting
the JSON's printed `0.0`, and `NDAQ`'s second leg was measured against **its own** 252-day
distribution rather than a round number. Each of those could have gone the other way and two of them
changed an outcome.

★ **And the run's largest finding is a refutation aimed at itself in advance**: §2(b) records
`S98`'s VOID clause **before** the observable settles, on a board whose pre-settle reading points at
the branch the desk would most like to fire.

---

## §12 · Handoff to MACRO (stage 1)

1. **Frame ends 2026-08-19.** No 08-20 price. Nothing re-pulled after 22:30 KST may be mixed with
   sweep statistics.
2. **The Δ is one session.** Say "one session"; do not say "recent".
3. **No tag is evidence** (`R81`). **No Δ-only verdict** (`D293`). **No Materials `wflow` sign** (G3).
   **No news velocity, no theme freshness, no "quiet"** (G1).
4. **`S80` fired C but moved toward A** — the duration complex beat `SPY` by **+1.23pp** over four
   sessions while the desk is short three legs of it. MACRO's `P51-A` transmission claim owns this.
5. **`S98` is at VOID risk on the FOMC-minutes clause** — MACRO must state whether the 08-19 minutes
   are an "FOMC-dated communication" for the purposes of that clause. That is a MACRO judgment, and
   it must be made on the clause, not on the branch.
6. **July PCE (08-28) is the window's un-bracketed dated binary.** PREMORTEM should own it.
7. **`vol_surge` carries opposite signs in the two markets' IC ledgers.** Every citation names its
   market (§7).
8. **Exposure: `정상`, invested 81.1% (live mark), band gap −13.9pp, n=10 and therefore unquotable as
   a contribution.** BET/ALPHA inherit this as size *context* — not as a size.

> ⚠ No position sizing, no buy/sell language appears anywhere above (P4). This stage transports
> analysis; the carry writeback to `handoff/*.md` happens at run end, append-only for retractions.
