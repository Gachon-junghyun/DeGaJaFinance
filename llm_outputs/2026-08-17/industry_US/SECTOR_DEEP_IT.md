# SECTOR_DEEP_IT — Information Technology (N+) / the optical-interconnect layer · industry_US · 2026-08-17 · **5th slot, PREMORTEM-PROMOTED**

> Promoted by `BLINDSPOT_PREMORTEM` Lens 1 over ROTATION's recency rule (IT was covered 08-16 = 1 run).
> Flow numbers are the **settled 3-axis sweep** (`asof 2026-08-14`). P4 throughout.

## 0 · ⛔ Mandate rebuttal first — the promotion's own counter-argument, stated before its case

Three independent instruments selected the optical/interconnect node today. **That is exactly the shape
a tunnel takes**, so the refutation goes first:

| Objection | Number |
|---|---|
| **The two promoted names have NEGATIVE 60-day excess** | `COHR` rs60 **−13.7** · `LITE` rs60 **−1.8** vs `SPY` |
| `COHR` round-tripped and is still below its own trigger | 379.13 (08-07) → 325.83 (08-14) = **−14.06% in 5 sessions**, **−14.46pp** vs `SPY`; ignition trigger **332.48** not reclaimed |
| The 08-16 run already dated their flow to their **own earnings prints** (08-11/08-12) | ⇒ **reaction volume, not accumulation** — the desk's own finding, one run old |
| The node's third name is unmeasurable | **`FN` (Fabrinet) is NOT in `us_top300`** — no flow, RS, OBV or short reading exists (`D274`) |
| The sector's `wflow` sign is a name that lags the benchmark | **`NVDA` rs60 −3.7 vs `SPY`** while owning **19.4%** of the bucket |

⇒ **This is a 20-day phenomenon promoted on a 5-day narrative.** The DEEP's job is to decide whether
that is *early* or *late*, and `S96` (08-21) freezes the answer.

## 1 · Why it was promoted anyway — four instruments, four independent mechanisms

| Instrument | What it measures | Reading |
|---|---|---|
| `theme_age` | fixed-vocabulary term velocity | `optical` **1.74× — highest of 13 terms probed** (22:30 KST) |
| `burst` | **no fixed vocabulary**, title-field z vs 30d baseline | **`OPTOELECTRONICS` z = 5.7** · `GPU` z = 5.2 |
| `thread` | event trajectory, per-day outlet curve | *"Lightmatter sets optical interconnect blueprint as **AI hits copper wall**"* — **BUILDING 2→2→2**, precursor form |
| `sector_flow` | price/volume only, **no news input** | **`COHR` +0.99 and `LITE` +0.82 are #1 and #2 `flow_score` board-wide**, and **both are 3-axis admissible** |

★ **The fourth is the one that matters**: `sector_flow`'s admissible score uses **no news at all**, so
the convergence is not four readings of one corpus. Sizing from the corpus: *"AI data center optical
interconnect market to hit **$144 billion by 2030**, an over ten-fold increase."*

## 2 · Value chain, left → right, with the binding constraint marked

`compute die (NVDA · AMD · custom ASIC) → HBM / memory (MU · SK hynix · CXMT) → advanced package &
substrate → ★ INTERCONNECT — the copper wall ★ → optical engine / transceiver (COHR · LITE · FN) →
optical components & connectors (APH · GLW · CIEN) → switch / fabric (ANET · CSCO) → data centre`

**Bottleneck = the interconnect node, and the claim is physical, not commercial.** `digitimes` 08-17
states it directly: AI has *"hit the copper wall"* — electrical reach collapses at 200G+/lane, so the
link layer must go optical regardless of demand elsewhere. **Strong demand upstream (GPUs) is not a
bottleneck; the reach limit is.** ⚠ This is a `[news]`-grade mechanism from **2 outlets**; no primary
(filing/spec) was obtained this run. **Graded, not upgraded.**

⚠ **Cross-sector chain note**: this is the same AI→power→electricals chain that made `EMR`/`ETN` the
best Industrials leg (`SECTOR_DEEP_INDU §3`). **The desk holds `ETN` and `ANET` — both are on this
chain, one node apart.** That is a concentration observation, not a sizing one (P4).

## 3 · Node map with flow — and the layer splits in two

| Node | Name | flow | OBV | rs20 | rs60 | surge | admissible 🟢? |
|---|---|---|---|---|---|---|---|
| transceiver | **`COHR`** | **+0.990** | 매집 +0.155 | +12.9 | **−13.7** | **1.67** | ✅ |
| transceiver | **`LITE`** | +0.817 | 매집 **+0.275** | **+21.9** | −1.8 | **1.27** | ✅ |
| transceiver | `FN` | — | — | — | — | — | 🚫 **not in universe** |
| components | `CIEN` | +0.342 | flat +0.031 | +10.1 | **−26.7** | 0.90 | — |
| connectors | `APH` | +0.130 | 매집 +0.047 | +6.1 | **+34.4** | 0.60 | — |
| glass/optics | `GLW` | −0.173 | 분산 −0.021 | +2.9 | −11.4 | 0.55 | — |
| switch/fabric | `ANET` *(held)* | +0.583 | 매집 **+0.290** | +13.5 | **+34.6** | 0.85 | — |
| compute | `NVDA` | +0.458 | 매집 +0.224 | +6.6 | **−3.7** | 0.73 | — |
| compute | `MRVL` | +0.378 | 매집 +0.168 | +13.2 | +20.1 | 0.48 | — |
| compute | `AVGO` *(held)* | +0.207 | 매집 +0.198 | +1.5 | **−10.2** | 0.66 | — |
| memory | `MU` | +0.213 | flat +0.038 | +10.0 | **+33.3** | 0.64 | — |

★★ **The layer is two different trades wearing one theme label.**

| Group | rs20 | rs60 | Reading |
|---|---|---|---|
| **`COHR` · `LITE` · `CIEN`** | **+12.9 / +21.9 / +10.1** | **−13.7 / −1.8 / −26.7** | **20-day strength on a 60-day drawdown** = a bounce off a broken trend |
| **`APH` · `ANET` · `MRVL`** | +6.1 / +13.5 / +13.2 | **+34.4 / +34.6 / +20.1** | **sustained 60-day leadership** — but none clears the volume gate |

⇒ **The two names the flow score promotes (`COHR`, `LITE`) are the two with the worst 60-day records,
and the three names with the best 60-day records are all blocked by `vol_surge`** (0.60 / 0.85 / 0.48).
**The `vol_surge` gate is selecting the bounce and rejecting the trend.** That is `D270` and the
KR-measured negative IC on `vol_surge` (**t −3.30 / −3.14, both clearing Bonferroni**) pointing at the
same defect from two directions — ⚠ **and W1 forbids importing that IC result**; it is named as a
**dig**, not used as a gate (`D243`: the US desk has no IC column of its own).

## 4 · Chain-hop — one hop past the headline

`chain-hop "optical interconnect" --days 7` (26 articles scanned):

| Layer | Names | Note |
|---|---|---|
| **Headline-named** (crowded by construction) | `NVDA` (16 body) · `AVGO` (8) · `MRVL` (5) · `INTC` (5) | already printed by every outlet |
| **★ Body-proximate, title-0** | **`APH`** (prox 3 / body 3) · `GOOGL`/`GOOG` (3/7) · `MSFT` (2/6) · `META` (2/7) · `AAPL` (2/3) | `APH` is cited in *"The Next Big AI Market Bottleneck Is Here: 8 Stocks to Play the Boom in Optics"* |

**`APH` flow cross-check** (the tool's own required next step): OBV **accumulating**, RS60 **+34.4**,
FINRA short-z **−2.14 = the strongest short-exit on the board**, `vol_surge` **0.60**.
⚠ **Honest correction to the tool's suggestion**: `chain_hop` says the alpha is *"OBV accumulating with
RS not yet run."* **`APH` fails the second half — RS60 is already +34.4.** What it actually is: a
**confirmed accumulation whose price has already moved**, with shorts leaving. Weaker claim, stated.
⇒ **Filed to `missed_ledger` (`Q.확신부족`, enters-if pullback with OBV accumulating and surge ≥1.2,
recheck 08-25)** rather than promoted. The hyperscalers (`GOOGL`/`MSFT`/`META`) are **buyers**, not
exposure — chain position **customer**, not bottleneck, and they are not carried forward.

## 5 · Lens L2 and the contract question — asked, and the answer is "no filing was opened"

The memory lesson was that **take-or-pay contracting invalidates the peak-margin collapse mechanism**,
and the desk found it only by opening the 10-Q. **The transferable rule is: when a frame is load-bearing
in one sector, point it at the others.**
⇒ **Pointed here, and the honest answer is `unknown` (C3).** Transceiver economics turn on whether
hyperscaler volume is committed or spot, and **no `module_disclosure_us` pull was made for `COHR` or
`LITE` this run.** ⚠ **This is a named gap, not a finding**: the desk is promoting a node while unable
to say what share of its volume is contracted. **First task for the next IT DEEP**, and it is exactly
the failure class the memory case documented (21 files applied take-or-pay elsewhere, zero applied it
where it mattered).

## 6 · The N+ verdict's own foundation, re-examined

IT's **N+** rests on `eqflow +0.044 > wflow +0.023` — a **0.021 flow-point gap** after a 95% Δ decay.
🚨 **And `wflow`'s sign is decided by `NVDA` (19.4%), which carries rs60 −3.7 vs `SPY`.**
⇒ **The sector's only positive aggregate is produced by a name that has underperformed the benchmark
over 60 days.** ROTATION correctly refuses to move a flipper on `wflow`; this file records that the
number ROTATION is *holding* is built the same way. `S85` settles **08-21** and already reads
**+1.343 = branch C**.

## 7 · Track KPIs and anti-signals

| # | KPI | Threshold | Date |
|---|---|---|---|
| 1 | `EW{COHR,LITE}` 5-session excess vs `SPY` **and** the Lightmatter thread's daily outlet count | ≥ +5.0pp **and** ≥4 outlets = early · ≤ −5.0pp = late | **08-21** (`S96`) |
| 2 | `COHR` close vs its **332.48** ignition trigger | reclaimed with `vol_surge` ≥ 1.0 | 08-21 |
| 3 | `S85` (IT N+ falsifier, on the axis the promotion used) | frozen thresholds; **state it sits at branch C now** | **08-21** |
| 4 | `S86` (the optical layer, max flow score / zero exposure) | frozen thresholds | **08-21** |
| 5 | `APH` `vol_surge` | ≥1.2 with OBV still accumulating ⇒ the trend leg opens | 08-25 |
| 6 | Contracted-volume share for `COHR`/`LITE` from a **primary filing** | presence of a disclosed commitment structure | next IT DEEP |
| 🚨 | **Anti-signal** | An **`NVDA` 08-26 announcement naming optical** ⇒ the layer reprices on a headline event, the opposite of the chain-hop premise ⇒ `S96` **VOID** |
| 🚨 | **Anti-signal 2** | A **`COHR` or `LITE` guidance/issuer event** inside the window ⇒ KPI 1 is idiosyncratic ⇒ **VOID for that leg** |

## 8 · 🚨 The structural item this DEEP hands upward

**`cycle_registry.json` contains no row for optical/interconnect** (nor for custom AI silicon).
`CYCLE_EXPOSURE`'s GAP check is a function of the registry, so **the desk's exposure to the cycle owning
its #1 and #2 `flow_score` names is not zero — it is unmeasurable.** A check that cannot fail is not a
check; rank-3 missile-defense has the same problem from the other side (**no floor set ⇒ ⚪n/a**).
`D250`, and `S94` (08-31) already brackets it. **Registry maintenance is a human item (P5).**

## ✅ Verdict for BET

**N+ held — this file does not move the sector, and says why.** The optical node is real enough to have
been selected by four instruments including one that reads no news at all, and **too young to survive
its own 60-day record**: the two names the score promotes are the two with the worst 60-day excess,
while the three with the best are blocked by the volume gate.
⇒ **`COHR` and `LITE` travel to BET §B as CONFIRMED-EARLY** (both 3-axis admissible, both OBV
accumulating) **with their rs60 attached to the same line**, per PREFLIGHT's rule that a number travels
with its counter-evidence. **`APH`, `CIEN`, `GLW`, `MRVL` do not travel.**
