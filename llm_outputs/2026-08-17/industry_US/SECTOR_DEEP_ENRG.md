# SECTOR_DEEP_ENRG — Energy (OW) · industry_US · 2026-08-17 · **CONTINUOUS track, 3rd consecutive slot ⇒ DELTA-led**

> Structure carried by reference to `SECTOR_DEEP_ENRG.md` of 2026-08-16. This file writes the delta.
> All flow numbers are the **settled 3-axis sweep** (`asof 2026-08-14`). No sizing, no buy/sell (P4).
> ⚠ **Execution note**: the stage specifies a parallel agent fan-out; this session's harness forbids
> subagents, so the five DEEPs were written inline. Cost stated in `BLINDSPOT_PREMORTEM §0`.

## 0 · ⛔ Mandate rebuttal first — the number that gave this sector its slot did not move

ENRG holds the slot on `eqflow +0.102` / `breadth 0.12`, promoted 08-15. **Both are byte-identical to
the 08-15 and 08-16 runs** — 299/299 `flow_score` unchanged, zero sessions settled. **Nothing about the
flow justifies a third look.** What justifies it is that the sector's *binary* fired today and the
*tape* handed the desk a control it has never had.

## 1 · ★★★ The delta: a D-0 binary expired, and the barrel did not pay

**The 60-day US–Iran MoU expired 2026-08-17** with Hormuz traffic near zero (5 outlets,
`MACRO_REPORT §D-1`). Settled prices across the four sessions in which that escalation happened:

| | 08-11 | 08-14 | Δ | vs `SPY` |
|---|---|---|---|---|
| Brent `BZ=F` | 88.91 | **88.52** | **−0.44%** | — |
| WTI `CL=F` | 83.20 | **82.40** | **−0.96%** | — |
| `XLE` | 60.93 | 61.91 | +1.61% | **+0.86pp** |
| `SPY` | 770.56 | 776.34 | +0.75% | — |

`fortune` 08-16 names the mechanism: *"Covert mideast oil flows are keeping global prices in check."*
**WTI spec positioning is 18th-percentile SHORT** (COT, Tue 08-11) and has not been punished.

⇒ **The headline "crude approaches $90 again" is arithmetically true and informationally empty** —
crude has sat in an **87.07–88.98** band, a 2.2% range, through the entire escalation.

## 2 · ★★★ What the control actually settles — refiners vs integrateds, one GICS code

The desk's standing thesis (`STANDING_VIEW_US §3a`) is **"refining-CAPACITY destruction, not a crude
trade."** Until this week crude and cracks moved together, so the claim had no discriminating test.
**Now it does**, and the settled 60-day excess vs **`SPY`** splits the sector cleanly:

| Node | Names | rs60 vs `SPY` | EW |
|---|---|---|---|
| **Refiners** | `MPC` **+29.3** · `VLO` **+24.3** · `PSX` **+22.3** | all ≥ +22 | **+25.30** |
| **Integrateds** | `XOM` **−7.3** · `COP` **−4.5** · `CVX` **−4.4** | all negative | **−5.40** |
| **Services** | `SLB` −11.1 · `OXY` −9.7 | negative | — |
| **Midstream** | `KMI` −10.2 · `WMB` −11.1 | negative | — |

**A 30.70pp spread inside one GICS sector**, and the sign flips exactly at the refining node.
⇒ **The capacity thesis is doing the work; the barrel is not.** If this were a crude trade the
integrateds — which own the barrel — would lead, and they are the sector's worst names.

⚠ **W5 binds and is satisfied**: "Energy" is at least four businesses here, and only one of them works.
⚠ **C4 scope**: this **narrows the alternative**; it does not confirm the thesis. `S88` (08-21) and
`P70` (08-21) are the rows that can.

## 3 · Flow cross-check — and the sector's green count is one, not two

| Name | flow | OBV | rs20 | rs60 | `vol_surge` | tag | 3-axis admissible? |
|---|---|---|---|---|---|---|---|
| `MPC` | **+0.806** | 매집 +0.22 | +9.3 | **+29.3** | **1.25** | 🟢가속 | ✅ **YES** |
| `PSX` | +0.628 | 매집 +0.139 | +8.5 | +22.3 | 1.01 | 🟡중립 | — |
| `COP` | +0.532 | 매집 **+0.334** | +6.1 | −4.5 | 0.90 | 🟡중립 | — |
| `VLO` | +0.490 | 매집 +0.188 | +5.9 | +24.3 | 0.84 | 🟡중립 | — |
| `CVX` | +0.401 | 매집 **+0.283** | +2.3 | −4.4 | **0.95** | 🟢가속 | 🚫 **NO** |
| `XOM` | +0.244 | 매집 +0.073 | +4.2 | −7.3 | 0.85 | 🟡중립 | — |

🚫 **`CVX`'s 🟢 is produced by `velocity 1.21` alone** — its `vol_surge` is 0.95, so it could never be
green on the axes the score is built from (`D261`). **The sector has ONE admissible green: `MPC`.**
⇒ Any statement of the form "two Energy names are accelerating" is instrument, not tape.

★ **And the `D270` correction applies here too**: `PSX` and `VLO` are the sector's #2 and #3 on RS60
and are excluded from their own shortlist **on `vol_surge` 1.01 / 0.84 alone** — `D251`, third run.
Their OBV is accumulating and their RS is top-of-sector; the volume gate is what stops them.

**Short pressure (FINRA, 08-14):** `MPC` z **−0.71 ✅ clean rise** — the only ✅ in the sector.

## 4 · Lens L2 (peak-margin trap) — does **not** fire, and the reason is unchanged

`MPC` gross margin **10.0%** against its own **8-year median 10.5%** (peak FY2022 14.5%) — the name is
**at its median, not its peak**, so the low-multiple-because-the-denominator-is-peaking mechanism does
not apply (`M670`, carried).
⚠ **But the trap's entrance is named, not waved away**: the crack sits at the **95.2nd percentile of
250 days** and is **not yet in the margin series**. That is precisely how the trap gets built one to
three quarters out. **Re-check at the FQ3 print.**

🚨 **Contract-terms check (the L2 escape hatch, run rather than assumed).** The memory lesson was that
take-or-pay contracting invalidates the collapse mechanism, and the desk found it only by opening the
10-Q. **Applied here: US refining has no take-or-pay analogue** — crack spreads are spot-priced,
merchant, and un-floored. ⇒ **The escape hatch does NOT exist for this sector**, which means the
downside mechanism is live in a way it is not for `MU`. **Stated as a difference, not inherited as a
similarity.**

## 5 · `M707`'s three refutations — re-tested, not restated

| Refutation (08-16) | Status on today's settled data |
|---|---|
| No OBV confirmation on a +19% 5-session move | ⚠ **Partially refuted**: `MPC` OBV is **accumulating (+0.22)**, as are `PSX` (+0.139) and `VLO` (+0.188). The correct 08-16 claim was about the *slope* not confirming the price move, not about the level. **Restated precisely rather than dropped** |
| Bearish RSI divergence at the upper band | Unchanged — same 08-14 close, same chart. **One observation, read twice** |
| No issuer event until **2026-11-03** | Unchanged. ⇒ nothing company-specific can move these names inside the `S88`/`P70` window; whatever happens is the **macro/crack leg**, which is exactly what makes the window clean |

★ **Lens 3's finding binds this section**: `MPC` +3.10z, `PSX` +3.15z and `VLO` +2.96z are all
**date-clustered in the same five sessions on the same driver** ⇒ **n≈1, not n=3.**
🚫 **"Three names confirm it" is not available as an argument.**

## 6 · Track KPIs and anti-signals (observables, dated)

| # | KPI | Threshold | Date |
|---|---|---|---|
| 1 | `BZ=F` settle | ≥93.00 = A · ≤86.50 = B · between = **no-information** | **08-21** (`S95`) |
| 2 | `EW{MPC,PSX,VLO}` 5-session excess vs `SPY` **while** `BZ=F` 5-session ≤ +2.0% | ≥ +2.0pp ⇒ capacity thesis | **08-21** (`P70`) |
| 3 | `EW{MPC,VLO,PSX}` excess vs `SPY` | ≤ +0.061 (2y median) falsifies the OW | **08-21** (`S88`) — state **+15.698 = the 100th percentile of two years** |
| 4 | Further **dated** strike on Russian refining capacity, ≥1 in window | presence/absence | **08-21** (`P66` A) |
| 🚨 | **Anti-signal** | **US refinery outage · PADD3 hurricane · OPEC+ quota decision** ⇒ the move is domestic-supply or cartel, not Hormuz ⇒ **VOID** | — |
| 🚨 | **Anti-signal 2** | A US-brokered **halt to strikes on Russian energy infrastructure** reported by ≥3 outlets ⇒ `P66` branch B ⇒ the capacity leg dies | — |

## 7 · The cycle-exposure line, carried with its counter-argument

`CYCLE_EXPOSURE` flags rank-2 Energy epicenter at **7.13% vs an 8.0% floor = −0.868pp GAP**, on the day
the binary expired. ⚠ **PREMORTEM Lens 4a's objection travels with it**: Lens 3 tagged the same three
names as a **single date-clustered ~3z event**, and Lens 2 argued the equity complex has priced a
premium the barrel has not. **Closing a 0.87pp gap into that is the "buy the confirmation" shape
`M707` warns about.** Reported as a GAP, **not** as an action (P4).

## ✅ Verdict for BET

**OW held.** The delta is **evidence quality, not direction**: for the first time the desk has a clean
control separating "refining capacity" from "crude premium", and it points at capacity (30.70pp).
**Three things constrain any use of it**: the sector has **one** admissible green (`MPC`), the three
refiners are **one observation**, and the entire thesis settles on **08-21** across three registered
rows (`S88` · `S95` · `P70`).
