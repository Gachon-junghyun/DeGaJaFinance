# D9 수정 — 지주 혼입 · 「이 숫자가 한 이름인가」 · 2026-08-09~10

> D9 는 *"`MAX_THEME_PCT` 에 지주/자회사 로직이 없어 모회사와 자회사가 **다른** 위험단위로 세진다"*
> 로 등록돼 **10런째 대기**했다. 고치면서 알게 된 것: **지주 매핑표는 필요 없다.**

## 핵심 판단 — 표를 만들지 않는다

`risk_units.py` 는 벤치 잔차상관으로 군집한다. **잔차상관이 임계(0.35)를 넘으면 지주든 자회사든
자동으로 한 단위로 병합된다.** PLAY15 가 실측한 「상위 20 상관쌍 중 6개가 지주–자회사」는
*이미 그 도구가 잡아내는 것*이다. 빠져 있던 것은 **매핑표가 아니라 배선**이었다.

⚠ 다만 실측 단위 자체가 창 선택에 민감하다 — 2026-08-09 실측: `--days` 250 → **5단위**,
500/750 → **7단위**(AI-compute 가 1년 창에서 통째로 해체). ⇒ **라벨을 대체하지 않고 나란히 놓고,
갈리는 지점만 낸다.** 어느 쪽이 옳은지는 말하지 않는다(P4).

---

## (a) 위험단위 — `module_paper_book._risk`

**추가**
- `load_measured_units()` — 최신 `RISK_UNITS.json` 을 읽는다(재계산 0, P1). 없으면 `({}, {note})`
  로 **사유를 실어** 돌려준다(조용한 폴백 금지).
- `measured_unit_exposure()` — 라벨이 아니라 **실측 군집**으로 노출을 합산.
- `label_unit_mismatch()` — 두 세는 법이 갈리는 지점만 반환:
  - `unit_split_across_labels` — 시장은 한 단위인데 라벨이 쪼개짐 ⇒ **상한이 헐거워진다**(D9 본체)
  - `label_split_across_units` — 라벨은 하나인데 시장은 여러 단위 ⇒ **상한이 과하게 조인다**
- `concentration_check(..., units=None)` — `units` 를 주면 같은 상한으로 실측 기준도 재고
  미스매치를 함께 낸다. **안 주면 이전과 완전히 동일**(하위호환).
- `__main__ mark` 가 이제 이 진단을 출력한다.

**⚠ 오늘 책에서는 미스매치가 0건이다** — 라벨 7개와 실측단위 7개가 1:1 로 일치한다.
즉 **D9 가 겨냥한 분기가 이 책에서는 한 번도 실행되지 않는다.** 안 돌아본 분기는 없는 것과 같으므로
합성 케이스로 발화를 증명했다:

| 케이스 | 라벨 기준 | 실측 기준 | 결과 |
|---|---|---|---|
| 지주+자회사가 다른 라벨 (각 33.3%) | 33.3% < 40% ⇒ **통과** | 한 단위 **66.7% > 40%** ⇒ 위반 | ✅ `measured_unit` + `unit_split_across_labels` 발화 |
| 한 라벨이 무관한 두 단위에 걸침 | — | — | ✅ `label_split_across_units` 발화 |
| `units` 미전달 | 기존과 동일 | — | ✅ 회귀 없음 |

⇒ **라벨만 보면 상한을 지키는 책이 실측 기준으로는 위반일 수 있다**는 D9 의 주장이 코드에서 재현된다.

---

## (b) 섹터 집계 — `scripts/sector_flow.aggregate_sectors`

D9 의 두 번째 얼굴: 시총가중 섹터 flow 를 **한 이름이 지배**하는데 그 사실이 출력에 없다.
(지주가 KRX 분류상 *금융* 으로 섞이는 것도 같은 가족.)

**추가(숫자를 바꾸지 않는다 — 정보만 더한다)**: 섹터마다 `top1` · `top1_w`(시총비중) ·
`wflow_ex_top1` · `top1_flips_sign`. 부호가 뒤집히는 버킷은 텍스트 표에 **🚨1名** 마커와 전용 블록.

### 실측 — KR 26개 섹터(n≥2)

| | 개수 |
|---|---|
| **최대 종목 1개로 부호가 뒤집히는 버킷** | **10 / 26 (38%)** |
| 최대 종목이 섹터 시총의 30% 이상 | 18 / 26 |
| 50% 이상(한 이름이 과반) | 6 / 26 |

| 섹터 | wflow | ex-top1 | 최대 종목 | 시총% | n |
|---|---|---|---|---|---|
| **전기·가스** | −0.046 | **+0.468** | 한국전력 | **79.9%** | 10 |
| **미분류** | +0.468 | **−0.094** | 신라교역 | 84.4% | 2 |
| **보험** | −0.049 | **+0.384** | 삼성생명 | 53.4% | 12 |
| **의료·정밀기기** | −0.235 | **+0.144** | 케이씨텍 | 41.4% | 8 |
| **통신** | +0.198 | **−0.180** | SK텔레콤 | 48.2% | 5 |
| **금융** | −0.046 | **+0.236** | SK스퀘어 | 26.5% | 75 |
| **유통** | −0.046 | **+0.154** | 삼성물산 | 52.7% | 59 |
| **오락·문화** | −0.060 | **+0.038** | 하이브 | 58.7% | 13 |
| **제조** | −0.042 | **+0.109** | 퍼시스 | 34.1% | 8 |

★ **2026-08-09 KR 런은 이 중 2개(금융·유통)를 손으로 찾아 적었다. 열 개다.**
그리고 가장 극단적인 **전기·가스(한국전력이 10종목 버킷의 79.9%)** 는 아무도 짚은 적이 없다.

★ **집중도 ≠ 부호반전을 제대로 가른다**: 삼성전자는 전기·전자의 **45.6%** 지만 빼도 여전히 음
(−0.433 → −0.547) ⇒ **flip 아님.** 크다고 다 사고가 아니라는 걸 구분해서 잰다.

⇒ **`ROTATION` 이 섹터를 승격·강등할 때 「이 버킷의 부호가 한 이름인가」를 이제 볼 수 있다.**
D225-KR 수정 후 flip 이 12개 → 10개로 줄었다(채점 수정이 허위 반전 2개를 제거).

---

## 남는 것 (사람 판단)

1. **`--days` 를 확정해야 한다.** 실측단위가 250일/500일에서 갈린다. 지금은 기본 500 이고
   **아무도 고른 값이 아니다.** 상한을 실측 기준으로 걸 거라면 이 값이 정책이 된다.
2. **미스매치를 「경고」로 둘지 「차단」으로 둘지**는 정하지 않았다. 지금은 **출력만** 한다(P4).
3. **섹터 진단을 ROTATION 의 EXIT CHECK 에 넣을지** — 넣으면 *"flip 버킷으로 승격/강등하지 않는다"*
   가 기계 검사가 된다. 프로토콜 변경이라 사람 결정.

**변경 파일**: `module_paper_book/_risk.py` · `module_paper_book/__main__.py` ·
`module_paper_book/__init__.py` · `scripts/sector_flow.py`
