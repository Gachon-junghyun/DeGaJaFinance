# FINDINGS — idle_probe 첫 런 · 2026-08-09 · `--market us`

> ⚠ **이 파일은 캐리가 아니다.** R/M/D/S 번호는 산업 데스크 런이 소유하고 카운터 충돌이 미해결
> dig(**D137·D211**)로 열려 있다. 아래는 **후보**이고, 번호 부여와 `handoff/` 반영은 다음 산업 런
> 또는 사람이 한다. 이 런은 `handoff/` 에 한 글자도 쓰지 않았다.

## 0. 이 런이 실제로 한 것

| # | 가설 | 유휴 능력 | 등록된 질문 | 결과 |
|---|---|---|---|---|
| H1 | LTA 가격 하한은 실재하는가 | `module_disclosure_us` (CLI 호출 **0**) | dig **D1** + 열린 모순 **C1**(3주 미측정) | ★★★ **닫힘 — 하한 쪽으로** |
| H2 | 추정치 리비전 IC 는 축인가 섹터인가 | `scripts/measure_ic.py` (**0**) | **M19** *"구분 불가(단일테마 집중)"* | ★★★ **섹터 로딩으로 판정** |
| H3 | 책은 몇 개의 베팅인가 | `scripts/risk_units.py` (pipeline **0**) | **R10** · `MAX_THEME_PCT` | ★★ **라벨은 맞고, 단위 수가 기본값에 달림** |
| H4 | 거부 69건은 하나의 사각인가 | `risk_units` × `reject_ledger` 교차(전례 **0**) | — (상상 태그) | ★ **아님. 대신 H3 과 교차하는 게 나옴** |

**처음 불린 모듈/스크립트 5개**: `module_disclosure_us` · `scripts/measure_ic.py` · `scripts/risk_units.py`(pipeline 기준) · `module_epistemics adjudicate` · `module_paper_book stage`.

---

## 1. ★★★ 철회 후보 A — `RESEARCH.md` D6 부록 IC 눈금표의 한 줄

**캐리된 문장**: *"추정치 리비전(US) — 창별 **+0.36 / +0.28** → 접은 값 **+0.32** · ⚠ 구분 불가(Q5 의 72%가 IT) · **부호는 두 창에서 일치**"*

**측정 (2026-08-09, `scripts/measure_ic.py`, 사상 첫 호출)**

| 표본 | W1 (신호 90→60, 수익 60→30) | W2 (신호 60→30, 수익 30→오늘) | Q5−Q1 |
|---|---|---|---|
| 전체 90종목 (Q5 의 **79%** IT) | **+0.403** | **−0.299** | **+9.4pp** |
| **ex-IT 120종목 (통제군)** | **+0.074** | **−0.064** | **−1.1pp** |

**두 가지가 깨진다.**
1. **"부호는 두 창에서 일치"가 거짓이다.** 두 표본 모두에서 부호가 갈렸다. 그리고 전체 표본에서는
   **양쪽 창 모두 검정력 문턱(|IC|<0.21)을 넘으면서 서로 반대 방향**이다 — 접은 값 +0.052 가
   무의미하다는 가장 깨끗한 시연.
2. **판정 근거가 「추론」에서 「통제 실험」으로 승격되며 내용이 강해진다.** M19 는 *구성*을 보고
   "구분 불가"라 했다. IT 를 빼면 크기가 **5.5배** 줄고 Q5−Q1 부호가 뒤집힌다 ⇒
   **"표본이 나빠 못 잰다"가 아니라 "리비전 축이 아니라 IT 섹터 로딩이다".**

**⇒ 조치**: `kelly_size --ic 0.32` 는 근거 없음. 눈금표 행을 *"[측정됨, 통제군 대조] IT 로딩 — 축으로 사용 불가"* 로 교체 권고.

## 2. ★★★ 캐리 정정 후보 B — 열린 모순 **C1** 이 닫힌다 (그리고 M1 의 의미가 바뀐다)

**`module_disclosure_us` → SEC 원문 `[1차, 직접 읽음]`** (MU 10-Q FY26Q3, 2026-06-25 제출):
- 전략고객계약은 **take-or-pay** 구조, 다년 계약기간, **법적 강제력 있는 물량 약정**
- 최대 계약들은 **기존 제품에 대해 「2026년 2분기 시장가에 근접한」 상한가 + 계약기간 내내 유지되는 하한가**
- 회사 진술: 가격밴드가 있는 계약의 총이익률은 **하한가 수준에서조차 과거 어느 사이클의 분기 최고 마진보다 훨씬 높을 것**으로 예상
- RPO 약 **$5bn**(계약부채 $422m) — 단, **최소물량 × 최소가격** 기준이고 밴드 없는 계약·5/28 이후 체결분 제외 ⇒ **하한값**

**대조 — FY2025 10-K (2025-10-03) `[1차]`**: 고객이 장기 고정가 계약을 꺼리며 가격·수량은 시장 상황을 반영해 **주기적으로 재협상**된다고 기술. ⇒ **두 공시 사이에 계약 레짐이 바뀌었다.**

**콜 보도 `[2차 · 미검증]`** — 16건 SCA · **DRAM 물량 20% / NAND 1/3** · 하한가 기준 **누적 최소매출 $100bn** · 고객예치 **$22bn** · 통상 5년 · **매출 절반 이상까지 확대 전망**.
1차 트랜스크립트: `https://investors.micron.com/static-files/631b1a32-5537-46ae-8f40-82e42fc79dfe` — **다음 런이 검증할 몫**.

**두 개의 캐리 정정이 따라온다.**
1. **레지스트리 `MU·SNDK·WDC` 행의 등록된 kill 이 발화했다.** 그 행은 *"LTA price floors remain the
   best counterargument on file (C1)"* 을 kill 로 적어뒀고, C1 이 하한 쪽으로 해소됐다.
   렌즈 **L2**(피크마진 = 무너질 분모)의 기전이 **계약된 물량에는 발행사 본인에게 반박당한다.**
2. 🚨 **더 큰 것 — `M1` 의 의미가 바뀐다.** 계약가 QoQ 감속(+90~95% → +58~63% → **+13~18%**)은
   레짐 콜의 증거 1번이다. 그런데 가격이 **floor/ceiling 밴드 안에서 분기 협상**되고 천장이
   **2026Q2 시장가**에 계약으로 박혀 있다면, 그 감속은 **수요 신호가 아니라 천장에 닿은 산술**이다.
   ⇒ 렌즈 **L1** 이 읽으라는 2차미분이 **조항 하나로 눌려 있다.** `R46`(등록된 킬 조건이 해협을
   지목했는데 기전은 시설이었다)과 **같은 범주 오류 클래스**.

**★ 사각의 모양**: 데스크는 `take-or-pay` 프레임을 **에너지에서만** 21개 파일에 걸쳐 정교하게 굴린다
(KMI $35.67B RPO XBRL 검증 · LNG 톨링 · VST 20년 PPA floor). **메모리 맥락 0건.**
없는 능력이 아니라 **잘 쓰는 능력을 정작 가장 중요한 자리에 겨눈 적이 없는 것.**

---

## 3. dig 후보 (사람 승인 필요 표시)

| # | dig | 근거 | 사람? |
|---|---|---|---|
| **A** | 🚨 **D16 일일 스냅샷이 조용히 죽어가고, 로그가 그 사실을 은폐한다.** 19일에 5개, 간격 **2→3→4→6 단조 증가**. 로그는 매번 *"누적 N일치 — 대략 (40−N)일 더"* 로 **파일 개수**를 세고 **캘린더 일**을 안 센다 ⇒ 실측 0.26개/일이면 남은 건 35일이 아니라 **~134일**. **근본 원인은 버그가 아니라 P6** — 데몬이 *"필요할 때만 켜는"* 클라이언트 PC 의 일일 작업으로 걸려 있다(24시간 도는 건 서버 PC). 간격 증가는 사실상 **PC 를 켜는 빈도의 측정치**. | 오늘 것 회수 완료(`eps_2026-08-09.json`, 6개째) | ✅ **스케줄 이관 = 사람** |
| **B** | ✅ **`scripts/risk_units.py --help` 가 죽었다 — 고쳤다.** `:260` 의 `help=` 문자열 맨 `%` 를 argparse 가 포맷 지정자로 읽어 `TypeError: * wants int`. 모르는 스크립트에 사람이 제일 먼저 치는 게 `--help` 다. **한 글자가 R10 을 죽인 도구를 3주간 접근 불가로 만들었다.** | 수정 `%` → `%%`, 검증 완료 | ❌ 완료 |
| **C** | ★★ **`risk_units --days` 기본 500 이 답을 정하고, 아무도 고른 적이 없다.** 250/500/750 스윕: **250일에서 AI-compute 단위가 통째로 해체**(AVGO·NVDA·TSM 각각 단독). 책의 최대 노출이 **한 단위 11.58%** 냐 **세 단위 4.62/2.42/4.54%** 냐가 그 기본값으로 갈린다. | 규칙 **C5** | ❌ 긍정형: 결론과 같은 줄에 `--days` 를 적는다 |
| **D** | ★★ **`risk_units` 의 ARI 는 반정보적이다.** 그룹핑이 **완전히 동일한** 두 창에 **0.239 와 1.0**, 혼자 다른 그룹핑을 낸 창에 **0.65**. 도구는 자기 출력에 *"ARI 를 먼저 봐라"* 라고 적어놨는데 그대로 하면 **틀린 창을 믿는다.** 별도로 **무상관 단독 이름을 넣으면 ARI 가 오른다**(8→12종목에서 +0.097, 단조 아님). | 6회 교란 실행 | ❌ 캡션 수정 후보 |
| **E** | ★★ **데스크 성적 1위 거부 클래스가 아무도 고른 적 없는 파라미터 위에 서 있다.** `E.상관가드` 평균초과 **−10.2pp** = 69건 중 최고. 그 판단의 입력이 `risk_units` 이고 그게 dig **C**. ⚠ **n=4, 검정력 없음** — 관측으로만. | H3 × H4 교차 | ❌ C 승인에 종속 |
| **F** | ★ **`paper_book fill` 드라이런이 조용한 거짓양성이다.** USD 슬리브 **0** 인데 `[DRY-RUN] BUY MU 1.0 @ 877.57` 을 통과시킨다. `_allocate` 경로는 막힌 레그를 슬리브 잔액과 함께 드러내는데 `fill` 은 안 그런다. **`D155`(action_bracket 조용한 거짓음성)와 같은 클래스.** | 실제 시도 | ✅ 거동 변경 = 사람 |
| **G** | ★ **`S31` 이 없는 포지션을 서술하고 있고, 그 정정도 틀렸다.** S31 은 08-06 에 *"XOM 은 책에 없다 — `cycle_exposure` 는 MPC·PSX 로 읽는다"* 로 정정됐는데, **오늘 `paper_book status` 상 MPC·PSX 도 없다**(에너지는 KMI·LNG·096770). | 책 실측 | ❌ 다음 US 런 확인 |

---

## 4. 매매 레그 — 착지: **조건부 카드, 진입 없음**

`out/order_desk/kis_stack.json` 에 **1건 적재(총 8건, 기존 카드 보존)**. `--clear` 안 씀. **사람이 [체결]로 발사.**

**왜 진입이 아니라 조건부인가 — 세 축이 각각 막는다:**
1. **A급 모멘텀이 반대한다.** MU `🔴분산` · RS20 vs SPY **−12.8%** · OBV 분산 · `vol_surge` 0.73.
   규칙 **D6**: C급(OBV)은 단독 명제를 못 들지만, **A급이 반대할 때 1차 서사가 A급을 이기지 못한다.**
2. **`module_epistemics adjudicate` 사후확률 0.526** (사전 0.5) · 지배축 = 10-Q 계약조항 ·
   **"잔여충돌 = 방향 상충 신호 존재 → 실측으로 해소 필요"**. 모듈이 결론을 안 준 것이 결론.
3. 🚨 **정당한 크기가 존재하지 않는다.** `kelly_size` 는 `--ic` 를 0.02 든 0.32 든 **전부 `IC하한 0.0%`**
   를 반환한다. 구속조건은 IC 값이 아니라 **관측치 2개** — 95% CI 하한 IC = **−1.336**,
   *"이 표본으로는 엣지의 부호조차 확립 못 한다"*. 하한이 IC 의 13%라도 남으려면 **2,030개** 필요.

**★ 그리고 그 사유가 dig A 로 되돌아간다:**
```
오늘 US 포지션을 정당하게 사이징할 수 없다
  ↑ 관측치 2개 (엣지 부호 확립 불가)
  ↑ eps_trend 가 히스토리가 아니라 스냅샷
  ↑ 해법 = D16 (매일 저장), 2026-07-22 자동화 등록
  ↑ 그 자동화가 조용히 죽어가는 중 (19일에 5개)
  ↑ 로그가 진행률을 「파일 개수」로 세서 은폐
```
**오늘 못 사는 이유의 뿌리가 3주 전에 죽기 시작한 스케줄 작업이다.**

**추가 차단**: **USD 슬리브 잔액 0** — 원화 현금 356만원으로 US 종목 매수 불가(FX 전환 프리미티브 없음).
카드를 발사하려면 **환전이 선행**돼야 한다.

---

## 5. 이 런의 정직한 약점

1. **`--market us` 단독.** KR 은 안 봤다. 유휴 집합 중 `module_fundamentals_kr`(수익의 질 — 발생액·**미청구공사**)는 **호출 0 인데 데스크가 건설 3종목(000720·047040·006360)을 캐리 중**이고, MODULE_MAP 은 미청구공사를 *"수주산업 1번 지표"* 라고 적어놨다. **가장 큰 미착수 가설.**
2. **`module_inflection`·`module_watchlist`·`module_publish` 는 여전히 호출 0.** 이번 런이 안 건드렸다.
3. **H1 의 커버리지 숫자(20%/33%·$100bn·$22bn)는 2차다.** 1차 트랜스크립트를 안 읽었다. 링크는 §2 에.
4. **저자가 런 중간에 한 번 틀렸다.** *"그룹핑은 모든 교란에 불변"* 이라 적고 **한 스텝 뒤 `--days` 스윕에 잡혔다** — **D48 「검증이 주장 뒤에 온다」 패턴.** 본문을 고치지 않고 깨진 사실을 남겼다.
5. **일요일 휴장이라 08-07 정착바 하나**뿐이다. 모든 흐름 숫자가 관측 1개(**S1**).
6. **`ic_ledger` 축 등록 0건.** 이번 런의 착지는 카드 1장 + dig 7건이고, **누적되는 축은 안 만들었다** — 종결 규칙 4형태 중 1형태를 못 채웠다.
