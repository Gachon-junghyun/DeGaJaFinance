# US 유니버스 빌더 — 신설 + 그 과정에서 잡은 설계 오류 · 2026-08-10

## 무엇이 없었나
`data/us_universe/us_top300.csv` 는 **빌더 없는 스냅샷 복사본**이었다(MODULE_MAP 「다음 후보(미착수)」).
그래서 US 커버리지가 300에 고정됐고, **유니버스 밖 종목은 「안 보이는」 게 아니라 「존재하지 않았다」** —
데스크는 `LNG` 에 대해 *"us_top300 밖 = flow·RS·OBV·숏 어느 축도 데스크 계기로는 존재하지 않는다"* 를
**9런 연속** 기록했다.

## 🚨 1차 시도가 틀렸다 — 지수 구성종목 목록은 유니버스가 아니다

S&P 500+400+600(1,506종목)으로 지으니 **추가 1,216 · 이탈 11** 이 나왔다.
이탈: **ALNY · ARM · ASML · CCEP · EA · FER · MELI · MSTR · PDD · SHOP · TRI**
⇒ **S&P 지수는 외국 국적을 배제한다.** ASML(네덜란드)·ARM(영국)·PDD(중국)·SHOP(캐나다)가 그래서 빠진다.
그리고 **크기와 무관하게 비편입 종목도 배제한다** — `LNG`(Cheniere)는 목록에 **아예 없고**(위키 원본에서
티커·회사명 양쪽으로 확인), 탱커(STNG·FRO·DHT·TNK)도 없다.

★ **즉 지수 기반 확장은 고치려던 문제를 못 고친다.** 1,505종목으로 늘려도 `LNG` 는 여전히 안 보인다.

## 🚨 그리고 더 강한 불변식이 깨져 있었다

**데스크는 자기가 보유한 것을 태그할 수 있어야 한다.** 실측 — 책의 US 보유 8종 중
**`TSM` 과 `LNG` 두 종목이 현행 유니버스에 없다.** 확장 이전에도 없었고, 1차 후보에도 없었다.
`risk_units`·`module_flow` 는 `--tickers` 로 직접 부르면 동작하므로 이 구멍이 지금까지 안 보였다.

## 고침 — 합집합

```
유니버스 = S&P 1500  ∪  현행 us_top300(ADR·외국국적 보존)  ∪  보유 종목  ∪  --include
```
지수 밖 종목의 GICS 는 `yfinance .info` 로 채우고(`YF_TO_GICS` 매핑), 실패하면 `(unmapped)` 로
남긴다(드롭하지 않는다). 시총 실패도 **0 으로 채우지 않고 제외** — 0 은 랭킹 바닥에 가짜 종목을 만든다(P4).

| | 1차 | **합집합(최종)** |
|---|---|---|
| 종목 | 1,505 | **1,522** |
| 이탈 | **11** | **0** ✅ |
| LNG·탱커 5종 | INSW 만 | **6종 전부** ✅ |
| 보유 US 커버 | TSM·LNG 누락 | **8/8** ✅ |
| 시총 실패 | CWEN-A 1종(상장폐지) — 빈칸 처리 | 동일 |

산출은 **후보 파일** `us_all_v2_candidate.csv`. 라이브 승격은 사람.

## ⚠ 승격 전에 막아둔 것 — 티커 오탐

유니버스가 5배가 되면 **자유 텍스트에서 티커를 알아보는 경로**가 무너진다. 실측:
신규 1,217종 중 4자 이하 **1,129개**, 그중 **최소 26개가 일반 영어 단어와 철자 동일** —
`CASH · FORM · POST · TECH · UNIT · WAY · PLUS · RUN · ROAD · ROCK · SAFE · STEP · PATH · POOL · BOX · CAR …`
(손으로 센 값이라 **하한**). 기존 방어는 `AMBIGUOUS_TICKERS` 하드코딩 ~50개 + 2자 이하 제외뿐이라 못 막는다.
그리고 `CASH`·`FORM`·`POST`·`TECH` 는 **금융 기사 매 문단에 나온다.**

**구조로 막았다 — 파일을 쪼개지 않고 소비자가 슬라이스한다.**
`module_news_data._config.US_TEXT_TOP_N = 300` 이 단일 원본이고
(`_chain_hop.AMBIGUOUS_TICKERS` 를 재사용하는 것과 같은 이유: 「텍스트에서 티커를 알아보는 법」은
한 곳이 정한다, P1), **텍스트 탐지 경로만** 시총 상위 N 으로 자른다. 숫자 스윕은 전체를 쓴다.

**회귀 검증**: 현행 파일에서 `chain_hop.load_universe` 300 · `report_tags.load_us_tickers` 300 ·
최상위 NVDA / 최하위 KMB — **확장 이전과 완전히 동일.** 넓히려면 사람이 `US_TEXT_TOP_N` 을 올린다.

## 남는 것 (사람 판단)

1. **승격 여부.** 유니버스가 바뀌면 스윕·섹터집계·**거부원장 벤치의 분모가 전부 바뀐다.**
   특히 `reject_ledger`/`missed_ledger` 의 벤치는 「시총 1조+ 동일가중」이라 구성이 달라진다.
2. **스윕 비용.** 1,522종 전수는 KR 827종(뉴스축 포함 3.5분)의 약 2배. `sector_flow --limit` 으로
   상위 N 만 도는 운용이 현실적일 수 있다.
3. **`US_TEXT_TOP_N` 을 올릴지.** 올리려면 `AMBIGUOUS_TICKERS` 를 먼저 확장해야 한다(현재 ~50개).
4. **`us_aliases.json`** 은 여전히 스냅샷 복사본 — 신규 1,222종에 별칭이 없다.

**변경 파일**: `data/us_universe/build_us_universe.py`(신설) · `module_news_data/_config.py` ·
`module_news_data/_chain_hop.py` · `module_report_tags/_config.py` · `MODULE_MAP.md`
