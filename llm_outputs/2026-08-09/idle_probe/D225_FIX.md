# D225-KR 수정 + before/after 실측 — 2026-08-09

> dig 하나를 고치러 들어갔다가 **세 겹**이 나왔다. 세 번째가 가장 크고, 그건 채점 버그가 아니라
> **인프라 장애가 조용히 점수를 부풀리는 경로**다.

## 결함 1 — 축 개수가 종목마다 달랐다 (D225-KR (a))

`flow_score` 는 `if vel is not None:` 으로 4번째 축을 **종목별로** 드롭했다. 드롭한 종목은 3축 평균,
넣은 종목은 4축 평균 — **같은 척도가 아닌데 그대로 섹터 wflow/eqflow 로 평균됐다.**

**일반형 유도**: 속도축이 하한(−1.0)일 때
`3축평균 − 4축평균 = s₃/3 − (s₃−1)/4 = (s₃+3)/12 ∈ [0, +0.5]` (s₃ = 가격 3축 합 ∈ [−3,3])
⇒ **드롭은 어떤 s₃ 에서도 손해가 아니고 최대 +0.5 이득.** 실측 평균 보너스 **+0.305** 와 일치.

**고침**: 속도축은 **런 단위로 전원 포함이거나 전원 제외**(`VEL_COVERAGE_MIN = 0.80`).

## 결함 2 — `clip(nan)` 이 +1.0 이었다 (D225-KR (b))

`clip(x) = max(-1.0, min(1.0, x))` 인데 파이썬에서 `min(1.0, nan) → 1.0`.
⇒ **결측 축이 최대 양수 점수**가 됐다. 실측: `vol_surge` NaN 인 20종이 **전부 `flow_score +0.667`**
(= `[0, 1.0(rs20 클립), 1.0(nan)] / 3`, 소수 3자리 일치).

**고침**: `_finite()` 게이트 — 가격 3축 중 하나라도 결측이면 **점수를 주지 않고 집계에서 뺀다**(P4).

## 🚨 결함 3 — 뉴스축이 「엉뚱한 풀」을 세다가, 아예 「못 세는」 상태였다

**3-a. 스코프**: `news_velocity(query, recent, base, kr=False)` 의 docstring 이 계약을 적어놨다 —
*"`kr=True` 면 국내 스코프(+국문 트라이그램 색인)로 센다. **호출부는 `kr_code(ticker)` 로 자동판별해 넘긴다**"*.

| 호출부 | `kr=` | 결과 |
|---|---|---|
| `module_flow/__main__.py:56` | `kr=bool(kr_code(t))` ✅ | 국내 스코프 |
| **`scripts/sector_flow.py`** | **누락** 🚨 | **기본값 False → 한국 기업명을 해외(영문) 풀에** |

실측(터널 생존 중): 삼성바이오로직스 base **5건** · LG에너지솔루션 **2건** · 기아 **0건**
⇒ velocity **0.00** ⇒ 옛 `flow_score` 에서 `clip(-2.5) = −1.0` **최대 페널티** ⇒ **시총가중**으로 섹터 순위에.
**그 축은 보상으로 발화한 적이 한 번도 없다.** 페널티를 맞은 건 삼바(64.7조)·LG엔솔(77.1조)·기아(55.9조)·
현대모비스(44.3조)·하나금융(36.4조)·POSCO(24.4조) — **한국에서 가장 큰 회사들.**

**3-b. 그리고 그 뉴스 검색은 ngrok 무료 터널을 탄다.**
`DEGAJA_NEWS_API = https://impolite-coherent-props.ngrok-free.dev` · 로컬 폴백 `data/news_fts.db` **부재**(서버 소유, P6).
**측정 중 터널이 죽었고**(15분 간격으로 성공 → 5/5 실패 → 다시 성공, **간헐**),
죽으면 전 종목 `velocity: None` 이 되는데 그게 **「기사 0건」과 구분되지 않는다.**

⇒ **옛 코드에서 「터널 다운」 = 「전 종목 점수 평균 +0.305 인플레」** 였고, 출력 어디에도 그 사실이 없었다.
⇒ 오늘 아침 런의 *"96.6%가 결측"* 은 **「한국 기업에 뉴스가 없다」가 아니라 「스윕의 96.6% 동안 터널이 죽어 있었다」**이고,
velocity 를 받은 3.4% 는 **터널이 살아 있던 순간에 걸린 무작위 부분집합**이다.

**고침**: `kr=bool(kr_code(tk))` 전달 · 커버리지 미달 시 **🚨 로그로 시끄럽게** · 원인 후보와 확인 명령을 같이 출력.

## 결함 4 — 기존 가드가 실패 케이스에서만 우회됐다

`save_snapshot` 은 이미 `_mode` 를 각인하고 주석까지 달아뒀다: *"3축(nonews) vs 4축(news) flow_score는
크기가 달라 Δ를 섞으면 안 됨"*. **그런데 `mode = "news" if use_news else "nonews"` — 실제 축 수가 아니라
CLI 플래그에서 나왔다.** ⇒ 터널이 죽어 3축으로 매긴 런도 `"news"` 로 각인돼, **다음 날 Δ가
3축 − 4축을 뺐다.** 저자가 문제를 정확히 예상했는데 키를 의도에 걸었다.

**고침**: `mode = "news" if axis_meta["vel_axis"] else "nonews"` — **실현된 축 수**에서 파생.

---

## before / after — 같은 정착바(2026-08-07), 같은 가격 캐시

| | BEFORE | AFTER |
|---|---|---|
| 채점 종목 | 827 | **807** (NaN 축으로 점수 받던 20종 제외) |
| 유니버스 wflow | −0.286 | −0.216 |
| 🟢 / 🔴 | 67 / 32 | **67 / 32** — 태그 로직 미변경, 예상대로 |
| **순위가 바뀐 섹터** | — | **25 / 28** |
| **부호가 반전된 섹터** | — | **2** |

**순위 이동(상위)**: 음식료·담배 **1→4** · 건설 **2→1** · 금속 **5→2** · 제약 **6→3** · 운송·창고 9→7
**부호 반전**: 운송장비·부품 **−0.098 → +0.057** · 기계·장비 **−0.001 → +0.117**
**종목 점수 변화**: n=807 · 평균 +0.007 · 중앙 **+0.000** · 범위 −0.127 ~ **+0.383**
(대부분 불변 — 변화는 **velocity 를 받아 벌 받던 27종의 메가캡**에 집중)

⇒ **`ROTATION` 이 이 순위로 섹터를 승격·강등한다.** 상위 6칸 중 4칸이 움직였다.

## ⚠ 남는 것 (사람 판단)

1. **히스토리 불연속.** 측정 런의 Δ 오염을 막으려 `history_kr.json` 을 **백업본으로 원복**했다.
   다음 정식 KR 런은 새 채점으로 스냅샷을 쓰게 되고, 그 첫 Δ 는 옛 스냅샷과 비교된다 —
   결함 4 수정이 `mode` 로 막아주지만, **`--no-news` 런과 「뉴스축 살아있는 런」의 이력이 이제 갈린다.**
2. **터널.** ngrok 무료 터널이 간헐적으로 끊기고 **로컬 폴백 색인이 없다.** 뉴스축을 신뢰하려면
   (a) 고정 주소/터널 안정화 또는 (b) 클라이언트 로컬 FTS 폴백 색인 중 하나가 필요하다.
   지금은 **끊긴 걸 알 수 있게만** 해뒀다(P4: 빈칸이 거짓보다 낫다).
3. **뉴스축의 진짜 커버리지는 아직 모른다.** 터널이 안정된 상태에서 전수 스윕을 한 번 돌려야
   *"국내 스코프에서 827종 중 몇 %가 실제로 기사를 갖는가"* 를 말할 수 있다. 오늘 값(3.4% → 6.0%)은
   **터널 가용성에 오염된 값**이라 인용 금지.

**변경 파일**: `scripts/sector_flow.py` (+123/−18) · `scripts/risk_units.py` (`%` → `%%`, `--help` 복구)
