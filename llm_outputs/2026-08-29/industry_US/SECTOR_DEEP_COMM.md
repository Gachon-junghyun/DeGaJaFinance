# SECTOR_DEEP_COMM — Communication Services · industry_US · 2026-08-29 (Stage 8 / L1·DEEP)

> **ROTATING TRACK — FULL FRESH MAP.** Last actual `SECTOR_DEEP_COMM.md` was **2026-08-24 = 3 runs
> ago**, the least-recent coverage of any OW sector (`SECTOR_ROTATION §3`, recency read from **files
> on disk**, not from DEEP_LOG lines).
> ⚠ Fan-out executed **in-line, not as parallel subagents** (`BLINDSPOT_PREMORTEM §0`).
> P4 — analytical only, no sizing.

## §0 · The mandate ROTATION handed this file, answered in one line

> *"The board's best settled tape on BOTH windows produces ZERO shortlist names. Is there a name
> here, or is the sector's `exc1` +1.645pp one index-level print?"*

**Answer: there are several names, the move was broad, and the zero is a filter artifact.**
**11 of 12 constituents were positive on 08-28**, and the leaders were **telecom and cable, not
Alphabet.** The sector produced no shortlist entry because **every candidate has `vol_surge < 1.00`**,
and the 🟢가속 tag requires a volume surge.

## §1 · Flow — and the shape that makes this sector unusual

`asof 2026-08-27` settled, n=12 (`SECTOR_FLOW_US.json`). **Sector `wflow` −0.260 · `eqflow` −0.004 ·
breadth 0.00 · `top1` `GOOGL`, NOT a flipper.**

| ticker | industry | mcap | flow | OBV | `rs20` | `rs60` | `vol_surge` |
|---|---|---|---|---|---|---|---|
| `WBD` | Broadcasting | 66B | **+0.550** | **매집** | **+9.4** | +4.7 | 0.79 |
| `DIS` | Movies & Ent. | 180B | +0.513 | **매집** | +7.1 | +3.8 | 0.79 |
| `CMCSA` | Cable & Satellite | 80B | +0.400 | **매집** | **+7.6** | +4.8 | 0.55 |
| `META` | Interactive Media | **1,465B** | +0.367 | **매집** | +2.0 | −6.0 | 0.91 |
| `VZ` | Integrated Telecom | 189B | +0.283 | **매집** | +3.2 | +1.7 | 0.67 |
| `T` | Integrated Telecom | 153B | +0.244 | **매집** | +5.6 | +1.7 | 0.42 |
| `NFLX` | Movies & Ent. | 326B | +0.138 | 중립 | +5.2 | −5.7 | 0.57 |
| `GOOGL` | Interactive Media | **4,491B** | −0.322 | 중립 | −1.9 | −7.4 | 0.72 |
| `LYV` | Movies & Ent. | 40B | −0.492 | 중립 | −5.6 | +6.2 | 0.74 |
| `GOOG` | Interactive Media | **4,484B** | **−0.509** | **분산** | −2.8 | −7.3 | 0.77 |
| `TMUS` | Wireless Telecom | 197B | −0.547 | **분산** | −1.4 | −7.4 | 0.72 |
| `TTWO` | Interactive Home Ent. | 44B | **−0.667** | **분산** | −9.8 | +3.3 | 1.00 |

★★ **`M1075`** `[measured]` **This sector's `wflow` is Alphabet twice.** `GOOGL` + `GOOG` are
**8,975B of the bucket** — two share classes of one company, both negative on flow (**−0.322 / −0.509**) and on `rs60`
(**−7.4 / −7.3**), with `GOOG` also `OBV 분산` (**RULE D6** — the C-grade OBV read is paired with two
other axes here, never quoted alone) —
and `META` adds 1,465B. **Three listings of two companies are ~87% of the cap weight.**
⇒ `wflow` **−0.260** is a statement about Alphabet; `eqflow` **−0.004** is the average of a positive
half and a negative half. **Neither is a statement about the sector's median name.**
⚠ **`top1_flips_sign` is `False` here** — the flipper guard does **not** fire, because removing
`GOOGL` still leaves `GOOG`. **The guard is blind to a dual-share-class listing**, and this is the
board's clearest instance. Registered as **`D407`** (`D1`'s second-listing-venue rule has a
same-venue twin).

★ **The positive half is coherent and it is not the megacaps**: `WBD` `DIS` `CMCSA` `VZ` `T` — **five
consecutive `OBV 매집` names with positive `rs20`**, spanning Broadcasting, Movies, Cable and
Integrated Telecom. **The negative half is Alphabet, `TMUS`, `TTWO` and `LYV`.**

## §2 · ★★ The 08-28 session — the mandate's actual answer

Settled 08-28 vs `SPY` (**benchmark named inline**; `SPY` **−0.227%** on the day):

| name | `exc1` | | name | `exc1` |
|---|---|---|---|---|
| **`CMCSA`** | **+2.688pp** | | `VZ` | +1.582pp |
| **`NFLX`** | **+2.582pp** | | `META` | +1.439pp |
| **`T`** | **+2.508pp** | | `DIS` | +1.425pp |
| **`TMUS`** | **+2.264pp** | | `TTWO` | +1.253pp |
| `GOOGL` | +1.971pp | | `LYV` | +0.786pp |
| `GOOG` | +1.758pp | | **`WBD`** | **−0.154pp** |

★★★ **`M1076`** `[measured]` **11 of 12 names were positive and Alphabet was mid-pack.** The sector's
`exc1` **+1.645pp** — the best of 11 sectors — was carried by **`CMCSA` +2.69, `NFLX` +2.58, `T` +2.51,
`TMUS` +2.26**, i.e. **cable, streaming and telecom**, with `GOOGL` at +1.97 sitting *below* the
top four. **This was not an index-level print and it was not one name.** The mandate is answered.

🚨 **`M1077`** `[measured]` **The two integrated telecoms rallied on a hawkish-rates session, and that
is counterintuitive enough to state rather than absorb.** On the day Warsh signalled rate hikes and
`XLU` fell 0.815pp and `XLRE` fell 0.176pp, **`T` +2.508pp and `VZ` +1.582pp** — the two highest-yield,
most duration-like names in the sector — were among its best. ⇒ **telecom is not trading as duration
right now**, which is a third independent instance of `M1052`'s finding that the desk's
"duration complex" grouping does not describe the tape.
⚠ **`TMUS` is the counter-example inside the same industry**: `exc1` **+2.264pp** on the day but
`OBV 분산`, `rs60` **−7.4** and the sector's 2nd-worst flow. **One good session inside a distribution
pattern** — recorded so the day is not read as a turn.

## §3 · Value chain — 6 nodes, left → right, binding constraint marked

`content creation → aggregation/studio → distribution pipe → ad or subscription monetisation → device/last mile → audience`

| # | node | who | state (`asof 08-27` settled) |
|---|---|---|---|
| 1 | content creation | `WBD` `DIS` `LYV` `TTWO` | `WBD`/`DIS` **매집**; `TTWO` **분산, `rs20` −9.8**; `LYV` 중립 |
| 2 | aggregation / streaming | `NFLX` `DIS` | `NFLX` 중립, `rs60` −5.7 — **the best 1-day (+2.58pp) on the worst 60-day of the positive half** |
| 3 | 🚨 **distribution pipe — the binding constraint** | `CMCSA` `T` `VZ` `TMUS` | **`CMCSA`/`T`/`VZ` all 매집 with positive `rs20`; `TMUS` 분산** |
| 4 | monetisation (ads) | `GOOGL`/`GOOG` `META` | **Alphabet 중립/분산 and the sector's worst `rs60` (−7.4/−7.3)**; `META` 매집 but `rs60` −6.0 |
| 5 | device / last mile | (outside this sector) | cross-sector chain → `Information Technology` |
| 6 | audience | — | not a node with a ticker |

**Binding constraint = node 3, the distribution pipe.** Not content (content is abundant and
`TTWO` is distributing) and not advertising (node 4 is the sector's weakest). **The scarce asset is
the pipe**, and it is exactly where the accumulation sits: `CMCSA` `T` `VZ` are three of the five
`OBV 매집` names with positive `rs20`.
★ **Cross-sector chain marked**: node 3's capex is **fiber and wireless backhaul**, which lands in
**Industrials/`PWR`** — and `PWR` is `🟡중립` flow **−0.321, `rs20` −9.5**. ⇒ **the pipe is
accumulating and its build-out contractor is not.** Named, not resolved.

## §4 · The Alphabet question — because it is 87% of the weight and it is the negative

**Dated, in-window, from the 08-28 head/body layers:**
- *"Why Alphabet (GOOGL) Is Proving **AI Can Strengthen Its Sear[ch]**"* [2 outlets]
- *"A Union Just Delayed Alphabet (GOOGL) Google's **$10 Million** [—]"* [single-source tier]
- *"**Prediction Markets Now Expect AI Start-Up Anthropic to Unsea[t]**"* [7 outlets] — and separately
  *"US judge rules Pentagon's blacklisting of **Anthropic** unlawful"* [**21 outlets**, the day's
  2nd-largest head item] and *"**Salesforce Stock Just Soared. Thank Anthropic.**"* [4 outlets]

★ **`M1078`** `[measured]` **The competitive threat to node 4 is being priced in a different sector.**
The `Anthropic` cluster ran **21 + 7 + 4 outlets across three separate head/body items on 08-28**,
and the equity that moved on it was **`CRM` (Information Technology, `exc1` +1.794pp, `flow_score`
+0.856, the run's #4 green)** — not Alphabet, which was mid-pack. ⇒ **the sector's `rs60` −7.4 on both
Alphabet lines has a named, dated competitive narrative attached, and the money is expressing it one
sector over.**
🚫 **No Alphabet view is issued.** The evidence is a narrative cluster plus a 60-day relative-strength
number; the desk has **no** disclosed-financial read on Alphabet in this run (`C3` — `unknown`).

## §5 · Customers and their disclosed spend (rule A6)

**Named**: node 4's customers are **advertisers**; node 3's are **households and enterprises**.
⚠ **Advertiser spend is `unknown` from this run's instruments (`C3`)** — no ad-spend series was
pulled and none is available in `module_macro_us`. **No conclusion is drawn around it.**
✅ **One disclosed, dated consumer data point in-window**, and it cuts the other way:
***"US Consumer Sentiment Drops, Year-Ahead Inflation Views Impr[ove]"*** (5 outlets, 08-28) and
***"U.S. Consumer Confidence Shows Modest Deterioration"*** (REIGNITED thread 2→6→5). **Softening
consumer sentiment is a headwind to node 3's subscription pricing power**, and it is stated as the
demand-side risk to this file's own conclusion rather than omitted.

## §6 · Chain-hop

🚫 **Not run for this sector.** The tool's own scope note is `universe = us_top300`, and Comm Svcs
has **n=12 in that universe** — the sector's genuine chain-hop candidates (regional fiber, tower
REITs, content licensors) are **below the universe cut or classified into other sectors**.
⇒ **Stated as a coverage limit, not reported as "no candidates found."** `CCI` (towers) carries a
standing §3a row and sits in **Real Estate**, not here — the desk's own label problem.

## §7 · Track KPIs and anti-signals — stated as observables

| # | KPI / anti-signal | observable | kills what |
|---|---|---|---|
| 1 | **breadth persistence** | the count of COMM names `OBV 매집` with `rs20` > 0 on the next settled sweep. **Today: 5 of 12** (`WBD` `DIS` `CMCSA` `VZ` `T`). **Falls to ≤2** ⇒ the 08-28 breadth was one session | `M1076`, and the `OW` |
| 2 | **the pipe** | `CMCSA` `OBV` turning 분산, **or** `T`/`VZ` `rs20` going negative on a settled sweep | §3's bottleneck, and with it the sector thesis |
| 3 | **the filter, restated as a test** | any COMM name printing `vol_surge` ≥ 1.20 on a settled sweep ⇒ the sector produces a shortlist name and the artifact clears. **Today the sector's maximum is `TTWO` at 1.00 — and `TTWO` is `OBV 분산`** | `SWEEP_READ §5`'s artifact diagnosis |
| 4 | **Alphabet** | `GOOGL` `rs60` crossing above 0 **with** `OBV` turning 매집 ⇒ 87% of the cap weight stops dragging and `wflow` converges to `eqflow` | `M1075` |
| 5 | 🚨 **anti-signal / VOID** | a **US antitrust or regulatory action against Alphabet with a named date** in the next 10 sessions. ⚠ Base rate **not remote** — the 08-28 head carried a **federal court ruling against the Pentagon on Anthropic** (21 outlets) and a *"Union Just Delayed … $10 Million"* item; the regulatory temperature around large-cap tech is live. **A ruling would move 87% of this bucket for a reason unrelated to node 3** | the whole sector read |
| 6 | ⚠ `TMUS` specifically | it rose **+2.264pp** on 08-28 while `OBV 분산` and `rs60` −7.4. **If `rs20` stays negative on the next settled sweep, the session was noise** | any read of 08-28 as a sector turn |

## §8 · Non-redundancy and what this file does NOT claim

- 🚫 **No `module_chart` OBV verdict is used** — `CMCSA` is one of the three names where it disagrees
  with the sweep (`매집` vs `중립 −7%`), because the two tools read different terminal bars
  (`M1067`, `SECTOR_DEEP_HLTH §9`). `module_chart` also reports a **bearish divergence** on `CMCSA`;
  **it is recorded and not acted on**, for the same reason.
- 🚫 **No freshness or velocity claim** (G1; this run's sweep measured `vel_coverage` **0.0%**).
- 🚫 **No commodity node in this sector**, so no QoQ rate series is required here — stated rather than
  silently skipped (the price-cycle series for this run is in `SECTOR_DEEP_ENRG §2`).
- ⚠ **No lead/lag claim is made** between content, pipe and monetisation (rule A5). Any future
  "content leads distribution" assertion must be tested first.

## ✅ EXIT CHECK (this file)
- [x] flow → players (all 12, full fresh map) → dated events → chain map (6 nodes, bottleneck = **the distribution pipe**, cross-sector chain to `PWR` marked) → chain-hop **(scope limit stated, not reported as an empty result)** → KPIs + anti-signals.
- [x] **Rotating track: FULL FRESH MAP**, no reference-by-carry.
- [x] **ROTATION's flagged divergence resolved explicitly** (§0/§2): the zero shortlist is a `vol_surge` filter artifact, the 08-28 move was **11 of 12 names broad**, and the leaders were cable/telecom, not Alphabet.
- [x] **Sub-sector dispersion stated (B5)**: `WBD` +0.550 flow to `TTWO` −0.667, and `rs60` +6.2 (`LYV`) to −7.4 (`GOOGL`/`TMUS`).
- [x] **The evidence against this file's own conclusion is written down** (§5 consumer sentiment; §2's `TMUS` counter-example; §7 KPI 6).
- [x] A new instrument defect found and registered rather than absorbed: **`D407`** — the `top1_flips_sign` guard is blind to a dual-share-class listing (`GOOGL` + `GOOG`).
