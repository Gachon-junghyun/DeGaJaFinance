# SECTOR_DEEP_IT — Information Technology · industry_US · 2026-08-29 (Stage 8 / L1·DEEP)

> **PREMORTEM-PROMOTED 4th SLOT.** ROTATION **declined** to promote IT and deliberately left slot 4
> unfilled; PREMORTEM Lens 1 filled it for a reason ROTATION could not use: **two held names print
> inside five sessions (`AVGO` 09-02, `HPE` 09-03) and no stage in this run had looked at either.**
> ⚠ Fan-out executed **in-line, not as parallel subagents** (`BLINDSPOT_PREMORTEM §0`).
> P4 — analytical only, no sizing.

## §0 · The mandate, answered in one line — and the answer is that the sector label is broken

> *"Two held names print inside five sessions and nothing has looked at them. And ROTATION declined
> IT because three admissible numbers gave three different signs."*

**Answer: the three signs are not a measurement failure — they are three different objects.**
On the **60-day** frame IT is a clean software-versus-semiconductor split of **~80pp**. On the
**1-day** frame that split **disappears**, and the dispersion *inside* each sub-node (**8.0–8.7pp**)
**exceeds** the dispersion *between* them. ⇒ **`eqflow` +0.060, `wflow_ex_top1` −0.121 and
"worst-1-day / best-5-day" are all correct, about different horizons.** The unit of analysis has to
be named with a horizon attached, and this file names it.

## §1 · Flow — the 60-day frame, and it is a clean split

`asof 2026-08-27` settled, n=56. **Sector `wflow` +0.008 · `wflow_ex_top1` −0.121 · `eqflow` +0.060 ·
breadth 0.07 · `top1` `NVDA`, 🚨 `top1_flips_sign` = True.**

| SOFTWARE / SECURITY — top by `rs60` | `rs60` | `rs20` | OBV | | SEMIS / HARDWARE — bottom by `rs60` | `rs60` | `rs20` | OBV |
|---|---|---|---|---|---|---|---|---|
| `SHOP` | **+30.4** | **+22.1** | 매집 | | `APP` | **−49.9** | −26.6 | **분산** |
| `PANW` | +27.3 | +13.6 | 중립 | | `ON` | −43.4 | −14.6 | 중립 |
| `CRM` 🟢 | +24.0 | **+35.5** | 매집 | | `ARM` | −38.1 | +1.7 | 중립 |
| `PLTR` | +20.7 | **+48.1** | 매집 | | `CIEN` | −37.7 | +3.5 | 중립 |
| `CRWD` | +17.1 | +19.1 | 중립 | | `QCOM` | −33.1 | +4.7 | 매집 |
| `MSI` | +15.6 | +9.0 | 매집 | | `COHR` | −32.3 | **+14.6** | **매집** |
| `FTNT` | +14.6 | +8.0 | 매집 | | `AVGO` (held) | **−24.4** | −8.2 | 중립 |
| `ANET` (held) | +13.2 | +13.6 | 매집 | | `NVDA` (held) | **+0.8** | +12.9 | 중립 |
| `MSFT` | +12.9 | +8.0 | 매집 | | `MU` | −13.6 | +3.0 | 중립 |
| `ADSK` | +12.8 | +11.2 | 매집 | | `ORCL` | −39.4 | **+15.1** | **매집** |

★★★ **`M1079`** `[measured]` **On the 60-day frame the split is ~80pp wide and it is not marginal**:
`SHOP` **+30.4** to `APP` **−49.9**. **Nine of the ten highest `rs60` names in a 56-name sector are
software or security; every one of the ten lowest is semiconductor, hardware or optical.** This is
`M1016`'s `SAASPOCALYPSE` token and `M935`'s 10.36pp software−hardware gap, **now at 80pp**.
⇒ **Lens B5 fires at maximum strength: the spread inside the label is an order of magnitude larger
than the label's own move (`eqflow` +0.060). "Information Technology" is the wrong unit on this frame.**

★ **And it explains ROTATION's three signs arithmetically**: `eqflow` +0.060 is the equal-weighted
average of a large positive software cohort and a large negative semis cohort; `wflow_ex_top1` −0.121
is negative because the **cap weight** sits in semis; `wflow` +0.008 is positive **only because
`NVDA` is 5,103B**. **All three are the same fact seen through three weighting schemes.**

## §2 · 🚨 The 1-day frame destroys the split — and this is the file's central finding

Settled 08-28 vs `SPY` (**benchmark named inline**; `SPY` −0.227%):

| SOFTWARE | `exc1` | | SEMIS / HARDWARE | `exc1` |
|---|---|---|---|---|
| **`NOW`** | **+4.764pp** | | `APP` | **+1.868pp** |
| **`INTU`** | **+3.118pp** | | `MU` | −0.044pp |
| `MSFT` | +1.904pp | | `QCOM` | −0.131pp |
| `CRM` | +1.794pp | | `AVGO` (held) | −0.513pp |
| `PLTR` | +0.421pp | | `AMD` | −2.100pp |
| `SHOP` | −0.700pp | | `ANET` (held) | −2.613pp |
| `PANW` | −2.714pp | | `ON` | −2.701pp |
| `ADSK` | −3.439pp | | `HPE` (held) | −3.633pp |
| `FTNT` | −3.697pp | | **`NVDA`** (held) | **−4.348pp** |
| **`CRWD`** | **−3.967pp** | | **`ARM`** | **−6.105pp** |

★★★ **`M1080`** `[measured]` **The intra-node dispersion on one session exceeds the inter-node
dispersion.** Software spans **`NOW` +4.764 to `CRWD` −3.967 = 8.73pp**; semis span **`APP` +1.868 to
`ARM` −6.105 = 7.97pp**. **The gap between the two cohorts' means is a fraction of either.**
⇒ **On a 1-day frame, "software vs semis" is ALSO the wrong unit.** What actually moved together was
**security — `CRWD` −3.967, `FTNT` −3.697, `PANW` −2.714, three names, one direction** — against
**workflow/ERP — `NOW` +4.764, `INTU` +3.118.** ⚠ Per lens **B3**, `CRWD`/`FTNT`/`PANW` moving
together on one day is **n≈1, not n=3.**

⇒ **`M1081`** the honest generalisation: **the correct sub-unit for IT is horizon-dependent** —
60 days: software vs semis; 1 day: security vs workflow. **A file that picks one and calls it "the
sector" is choosing a window.** This is why `P113` (\|`XLK` 5d − `XLU` 5d\|, state **1.39pp**,
branch B ≤1.00pp) exists, and it is **0.39pp from its informative branch**.

## §3 · The two held names that print inside five sessions

### `AVGO` — prints **2026-09-02 (D-4)**, the worst `rs60` on the book

| | |
|---|---|
| flow (`asof 08-27`) | **−0.517**, `OBV 중립`, `rs20` **−8.2**, `rs60` **−24.4**, `vol_surge` 0.86 |
| 08-28 session | `exc1` **−0.513pp** — mild, on a day `NVDA` fell 4.35pp |
| positioning | short **1.2% float, covering**, DTC 3.0; **option P/C 2.05 = put-heavy** |
| **implied move** | **±8.11%** — ATM straddle K=370.00 = 29.91 on the **09-04** expiry, the first that **spans** the print. Cross-checked on the 09-02 expiry (K=380) → **±8.12%** |
| 🚨 instrument | **`module_flow --positioning` returned `예상변동 ±nan%` and named the 08-31 expiry, D2 — which does NOT span a 09-02 print** (`D315`, 6th run; `M1065`). **The number above came from the chain directly** |
| brackets | **`S132`** (registered today, ±9.00pp, **outside** the implied move) and **`S127`** (inherited, A ≥ +5.928 / B ≤ −4.922 — **inside** ±8.11% ⇒ **pre-declared NO-INFORMATION**). `S127` is **not** re-frozen (`D242`) |

★ **`W4` (name the customers) — paid here for `AVGO`, and it is the crux.** `AVGO`'s custom-ASIC
customer is **Alphabet**, and Alphabet **dual-sourced to `MRVL` on 2026-08-19** with a *"$12.2bn /
59m-share warrant that does not fully vest until Google buys $120bn of chips"* (carried §3a row).
**`S116` measured the market's verdict on that hand-off and it landed `FIRED-C` at −9.54pp — 79% of
the way to branch B**, i.e. *"the share shift was over-priced."* ⇒ **`AVGO`'s −24.4 `rs60` and
`S116`'s B-lean are the same claim from opposite sides, and `S132` is the row that settles it.**

### `HPE` — prints **2026-09-03 (D-5)**, and the desk's calendar cannot see it

| | |
|---|---|
| 🚨 provenance | **`catalyst_calendar --days 14` does NOT carry it.** Recovered from the `brief` **single-source tier**: *"HPE to Report Q3 Earnings: What's in the Cards for the Sto[ck]"* (`yahoo_finance`, 08-28). **This is the third consecutive run in which a held name's print was recovered from a news body rather than from the calendar** (`M872`'s class) |
| flow (`asof 08-27`) | `OBV 매집` **paired with `vol_surge` 0.67 and a modest sector-relative flow score** (**RULE D6** — OBV is C-grade and never carries a line alone here) |
| 08-28 session | `exc1` **−3.633pp** — the 2nd-worst hardware name on the day |
| positioning | short **5.1% float, covering**, DTC 3.5; **option P/C 0.24 = call-heavy ⇒ complacent** |
| **implied move** | **±11.39%** — ATM straddle K=52.00 = 5.96 on the 09-04 expiry (spans the print) |
| brackets | **none.** PREMORTEM **declined** to register one, with the reason: bracketing a name **the same run is about to deep-dive** pre-commits the DEEP's conclusion. ⇒ **registered here as an open item for the next run's PREMORTEM, not as a gap** |

⚠ **The two held names' option markets disagree about fear**: `AVGO` P/C **2.05** (put-heavy) vs
`HPE` P/C **0.24** (call-heavy) — on prints one day apart, in the same value chain, with `HPE`
carrying **4.2× the short interest** and **the larger implied move (±11.39% vs ±8.11%)**.
**Stated as a two-sided read, not resolved.**

## §4 · Value chain — 7 nodes, left → right, binding constraint marked

`EDA/IP → foundry → merchant silicon → custom ASIC → networking/optics → system OEM → software/workload`

| # | node | who | state (`asof 08-27`) |
|---|---|---|---|
| 1 | EDA / IP | `SNPS` 🟢 · `ARM` | `SNPS` **매집, `rs20` +20.9 on `rs60` −10.1**; `ARM` **`rs60` −38.1** |
| 2 | foundry | (`TSM` — **outside `us_top300`**) | ⚠ **not taggable by this desk's universe** |
| 3 | merchant silicon | `NVDA` `AMD` `QCOM` `ON` `MU` | `NVDA` **중립** despite the print; `QCOM` 매집 on `rs60` −33.1 |
| 4 | **custom ASIC** | `AVGO` `MRVL` | **the contested node — see §3** |
| 5 | 🚨 **networking / optics — the binding constraint** | `ANET` `COHR` `CIEN` | **`ANET` 매집 `rs20` +13.6 / `rs60` +13.2 = the only held epicenter name positive on both** |
| 6 | system OEM | `HPE` `DELL` | both **매집**; `DELL` `rs20` +12.7 |
| 7 | software / workload | `CRM` `NOW` `PLTR` `MSFT` `SHOP` … | **the sector's entire 60-day leadership** |

**Binding constraint = node 5, networking/optics.** Not GPUs (node 3 is where the money already is
and `NVDA` is `OBV 중립`), not software (node 7 has no supply constraint). **The scarce thing in a
buildout is the interconnect**, and node 5 is where the one held name with **both** `rs` positive
sits. 🚨 **And the desk cannot measure the optical half of it**: `cycle_registry.json` has **no
optical row for a 13th consecutive run** (`D250`), so `COHR` — `OBV 매집` with **`rs20` +14.6 on
`rs60` −32.3**, a textbook turn candidate — is **unmeasurable, not absent**.

★ **Cross-sector chain marked**: node 5/6's constraint runs into **power and thermal**, i.e.
`VRT` (`OBV 매집`, `rs20` +14.4 on `rs60` −21.0 — `EVENT_ALPHA` Card 6) and `ETN` (held, 매집).
**Both are the same shape as `COHR`: accumulation under a broken 60-day.**

## §5 · The memory node, and what the largest disclosure of the week did NOT do

★★ **`M1056` carried forward and settled against the tape**: `NVDA` disclosed **$279bn of supplier
commitments, ~$160bn of it memory** (5 outlets: `wsj` `tomshardware` `investing_en` `yahoo_finance`
`fool`, 08-27→08-29). **The producers did not take it**: `MU` `exc1` −0.044pp, `WDC` **🔴분산
`rs20` −17.3**, `SNDK` `rs60` −15.0, and the tape's own summary was *"**Micron Lags Despite NVIDIA's
$279B Memory Commitment; Western Digital Drops 4%**."*

⚠ **The `RESEARCH.md` rule that governs this node was NOT executed and the omission is stated
(`C3`)**: *"before calling a cyclical's margin unsustainable, read its CONTRACT terms from the
filing."* No `module_disclosure_us` 10-Q pass was run on `MU`/`WDC`/`SNDK` this run.
⇒ **The share of memory volume under take-or-pay is `unknown`, and therefore no margin-collapse
assertion is made here.** ★ And the rule's own second consequence bites directly: **a contractual
ceiling flattens the very QoQ series the regime call reads**, so *"deceleration"* and *"arithmetic
hitting a cap"* are **not currently distinguishable** on this desk's evidence. Registered as `D408`.
✅ **The bracket that carries this is `P111`** (`EW{MU,SNDK,WDC}` exc10 vs `SPY`, 08-28 → 09-14,
A ≤ −3.00pp / B ≥ +5.00pp).

## §6 · Estimate momentum — used as a description, never as a leading indicator

🚫 **The rule is explicit and is honoured**: *"do NOT write 'revisions turn before price does' — that
was tested and it did not hold"* (`measure_ic.py`: W1 **+0.403** vs W2 **−0.299**, both clearing the
power floor while pointing opposite ways; the ex-IT control gives +0.074/−0.064 and a Q5−Q1 spread of
+9.4pp → −1.1pp ⇒ **the effect is an IT loading, not a revision axis**).
⚠⚠ **And this is an IT file — precisely where the confound lives.** ⇒ **no revision-momentum number is
used as evidence anywhere above**, and none is quoted. The carried denominators that *are* cited
(`COIN` CY +1.01 → −2.06; `MSTR` 43.82 → −6.69) appear only in `HANDOVER §1f` as attached caveats,
never as signals.

## §7 · Track KPIs and anti-signals — stated as observables

| # | KPI / anti-signal | observable | kills what |
|---|---|---|---|
| 1 | **`S132`** | `AVGO` 1-session excess vs `SPY` on **2026-09-03**; A ≥ +9.00pp / B ≤ −9.00pp, **outside the ±8.11% implied move** | `AVGO`'s −24.4 `rs60` as a discount-vs-franchise-loss question, and `S116`'s B-lean |
| 2 | **`P113`** | \|`XLK` 5d − `XLU` 5d\| through **09-08**; **state 1.39pp, branch B at ≤1.00pp = 0.39pp away** | whether an IT view is a **concentration** call or a **macro** call |
| 3 | **`P111`** | `EW{MU,SNDK,WDC}` exc10 vs `SPY`, 08-28 → **09-14** | the standing `[inferred]` regime call, both ways |
| 4 | **the horizon test** (`M1081`) | on the next settled sweep, measure **intra-node vs inter-node** dispersion again. If intra > inter for a **second** session, the sub-node labels are not a unit at all and the sector must be read name-by-name | this file's own §1 framing |
| 5 | **node 5** | `ANET` `OBV` turning 분산, **or** `COHR` `rs20` falling below 0 | the binding-constraint reading in §4 |
| 6 | 🚨 **VOID for `S132`** | `AVGO` postpones, **or** an announced acquisition of/by `AVGO` inside 08-31 → 09-03 | `S132`'s scoreability |
| 7 | 🚨 **open item, not a gap** | **`HPE` prints 09-03 with NO bracket** (declined by design, §3). **The next run's PREMORTEM must register one or state why not** — implied move **±11.39%**, P/C **0.24 = complacent**, short **5.1% float** | the desk's coverage of its own book |

## §8 · What this file does NOT claim

- 🚫 **No sector verdict change.** ROTATION left IT at `N` and this file **does not overturn it** —
  §2 is the reason: the direction is horizon-dependent, and `P113` is the registered decider.
  **A DEEP promoted by PREMORTEM does not inherit the right to move the sector.**
- 🚫 **No `module_chart` OBV verdict** — `ANET` is one of the three names where it disagrees with the
  sweep (**매집** vs **분산 −19%**), because the two read different terminal bars (`M1067`).
- 🚫 **No freshness/velocity claim** (G1; this run's sweep `vel_coverage` **0.0%**).
- 🚫 **No margin-collapse claim on memory** — contract terms unread, share under take-or-pay `unknown`
  (§5, `C3`).
- ⚠ **`TSM` (node 2, foundry) is outside `us_top300`** and therefore carries **no flow, RS, OBV or
  short tag** in this run. **It is not absent; it is unmeasurable** — and PREFLIGHT G5's coverage leg
  passes only because `TSM` is not currently held.

## ✅ EXIT CHECK (this file)
- [x] flow → players (56-name sector, both cohorts tabulated) → **IR/dated events for both held names, from primary or body sources** → chain map (7 nodes, bottleneck = **networking/optics**, cross-sector chain to `VRT`/`ETN` marked) → KPIs + anti-signals.
- [x] **Promoted-slot file: full fresh map**, and it states why it exists (two held prints, uncovered).
- [x] **ROTATION's declined promotion is addressed, not overturned** — §2 explains the three-signs problem arithmetically and hands the decision to `P113`.
- [x] **Sub-sector dispersion stated (B5) at maximum strength** — 80pp on 60 days, and the finding that **intra-node dispersion exceeds inter-node on 1 day** (`M1080`).
- [x] **Date-clustered moves normalised as n≈1** (B3) — `CRWD`/`FTNT`/`PANW` named as one observation.
- [x] **Every magnitude threshold stated against the implied move**, re-derived from the option chain because `module_flow --positioning` returned `±nan%` and a **non-spanning** expiry (§3).
- [x] **`W4` paid on `AVGO`** — the customer (Alphabet) is named, dated, and tied to the `MRVL` dual-source and to `S116`'s measured verdict.
- [x] Price-cycle node (memory) handled per the rule: **contract terms unread ⇒ `unknown` ⇒ no collapse assertion**, and the flattening problem registered as `D408`. The run's QoQ rate series is in `SECTOR_DEEP_ENRG §2`.
