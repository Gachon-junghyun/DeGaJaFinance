# BET_SHEET — industry_US · 2026-08-29 (Stage 9 / L1·BET)

> **ONE file, per-sector sections** — downstream desks read this exact filename; never split it.
> P4 — **zero buy/sell recommendation.** Any sizing language below is influence illustration only.

## §0 · Gates and rights that bind every number below

| gate | state | what it forbids here |
|---|---|---|
| **Thesis-confirmation gate** | 🚫 **No rows apply.** `REPORT/COMPANY_SCOREBOARD.md` is dated **2026-08-21** and holds **5 names, all KR** (`036460` `011200` `316140` `028050` `000660`). **Zero US candidates have a row** | Every US name below is **derived**, not confirmed. ⚠ `module_report_tags ticker <T>` shows no US company report newer than `company_AVGO` — **"no row ≠ no coverage" was checked, and here it genuinely is no coverage** |
| **G1** (news axis, `vel_coverage` **0.0%** this run) | FAIL | §B freshness tags read **`UNMEASURED (G1 FAIL)`** — never `stale`/`cold`/`quiet` |
| **G3 / `R108`** (flippers IT·HLTH·ENRG·DISC) | PASS-with-restriction | No name's sector context cites `wflow`; the flipper list **withholds**, it does not certify |
| **G5** (universe **45 days** old) | FAIL | `wflow` is not a current weighting; `eqflow` is the citable sector number |
| **G6** (accrual 2.0× slow) | FAIL | **No IC-backed sizing.** Any fraction is *"mechanical 1/4 — IC not yet estimable"* |
| **`D401`** | — | No `SECTOR_FLOW_US.json` figure is described as **settled** beyond its `asof 2026-08-27` |
| **`D402`** | — | 08-28 prices are `fast_info.last_price`, never `repair=True` |
| **`C21`** | open contradiction | 🚨 **The RSI convention in `us_setup_screener` is an unresolved contradiction (US, 08-27) and `module_chart` returns `nan` RSI.** Every screener RSI below is quoted **with this tag attached** and **no verdict rests on one** |
| **Revision-momentum rule** | binding | Estimate tables below are used **only as a description of the denominator's direction** (arithmetic). 🚫 **Never as a leading indicator, and never as independent evidence on an IT name** — that is exactly where the measured confound lives |

---

# §1 · ENERGY — `OW` (promoted on `eqflow` +0.092)

## §1A · Numbers — and the headline number disqualifies the headline thesis

| | `MPC` |
|---|---|
| price / 52w | **$368.83** / high **$369.12** · low $161.93 ⇒ **0.08% from its 52-week high** |
| mcap · beta · yield | $104B · **0.51** · 1.10% |
| Trailing / Forward P/E | 12.61 / **11.45** · PEG 1.76 · P/B 5.47 |
| Trailing / Forward EPS | $29.25 / $32.20 |
| mean target | **$324.56** ⇒ **upside −12.0%** (it trades **12.0% ABOVE** the mean target) |
| recommendations | SB 4 · B 6 · **H 8** · S 1 |
| **estimate direction (denominator only)** | current qtr **8.59 → 19.49 in 90 days = +127.0%** · next qtr **5.90 → 11.27 = +91.0%** |
| XBRL cross-check | ✅ **4/4 quarters within 5%** (yfinance ↔ SEC XBRL) |

🚨 **`M1082`** `[measured]` **`MPC` is lens `L2`'s peak-margin trap in textbook form, and the forward
multiple is the trap's symptom, not a valuation.** An 11.45× forward P/E sits on a current-quarter
consensus that **more than doubled in 90 days (+127.0%)** — the denominator is being revised up
steeply into the print, which the rule names as *"consensus chasing, not cheapness."*
🚫 **Per the rule, no cheapness claim is made about any refiner in this file**: a forward multiple
without its **margin percentile** is not a valuation, and **the margin percentile was not measured
this run** (`C3` — and `SECTOR_DEEP_ENRG §10` records that the contract-terms read was not run either,
so the share of volume under term contract is `unknown`).
⚠ **And the same shape holds across the node**: `MPC` trades **12.0% above** its mean target, and the
carried §3a row has **`VLO` 8.4% above** its. **Two of the three refiners are above target.**

## §1B · Thesis · freshness

**Thesis (carried, and its instrument is dead):** *refining margin, not crude beta.*
`MPC`/`PSX`/`VLO` `rs60` **+36.7 / +29.8 / +32.7**, all `OBV 매집`, `asof 2026-08-27` settled.
🚫 **The separation claim is UNAVAILABLE this run** — `R89` retracted 08-21, its registered successor
`P83` **`FIRED-B`** (the HO−RB spread collapsed **13.562 points in five sessions**), and
`SECTOR_DEEP_ENRG §10` declined to invent a third spread (`M47`).
★ **What replaces it is a property of the margin itself** (`SECTOR_DEEP_ENRG §2`): the 3-2-1 crack's
QoQ rate has decelerated **once** — **+58.92% → +27.34% like-for-like** — while the **level is at its
cycle high (71.129 on 08-27, above the Q3 mean of 65.313)**. The desk's own rule says **two**
consecutive declines is the signal.
**Freshness: `UNMEASURED (G1 FAIL)`.**

## §1C · Flow / positioning

`asof 2026-08-27` settled: `MPC` flow +0.661 `OBV 매집` `rs20` +11.8 · `PSX` +0.455 매집 +9.9 ·
`VLO` +0.338 매집 +7.2 · `SLB` +0.572 매집 +8.5 (`rs60` **−4.3** = accumulation before the trend) ·
`XOM` **−0.551 `OBV 분산`** (in the reject ledger, recheck **09-09**).
⚠ **All are `🟡중립`, and the reason is the filter, not the flow**: every one has `vol_surge < 1.00`,
and `vol_surge` is the axis whose **measured IC sign is negative at Bonferroni significance**
(`M1062`; KR-labelled ⇒ `W1`, the US measurement is `D395`, unmet).
🚨 **The screener returned ZERO setups in Energy across all three buckets** — no leader pullback, no
de-rate snapback, no washout. **Diagnosed, not cited**: the refiners are at/near 52-week highs with
RSI above the pullback bar, so the absence means *"extended,"* not *"nothing here."*

## §1D · Competition / peers

Intra-sector spread is **41pp** (`MPC` `rs60` +36.7 vs `SLB` −4.3) ⇒ **refining and oilfield services
are not one trade** (lens B5, `SECTOR_DEEP_ENRG §3`). Node map and the binding constraint (refining
**capacity**, not crude, not demand) are in `SECTOR_DEEP_ENRG §4` — **cited, not reprinted**.

## §1E · Refutation + dated catalyst

| kills it | date |
|---|---|
| **`P112`** Brent settled ≤ **84.00** (A) — the war premium is out and the sector must stand on a margin the desk **cannot prove is separable** | through **2026-09-11** (today **88.10**) |
| **The second rate decline** — 3-2-1 crack QoQ at the Q3 close | **2026-09-30** |
| `MPC`/`PSX`/`VLO` `OBV` turning **분산** on any settled sweep | rolling |
| 🚨 **VOID**: a **Venezuela supply event** (14 outlets on 08-28) moves Brent for a non-Hormuz reason | live |

---

# §2 · HEALTH CARE — `OW` (promoted on `eqflow` +0.102, the board's highest)

## §2A · Numbers

| | `AMGN` (basket representative) |
|---|---|
| price / 52w | $432.42 / high $447.03 · low $269.77 |
| mcap · beta · yield | $234B · **0.41** · **2.31%** |
| Trailing / Forward P/E | 26.89 / **17.72** · PEG 2.32 · P/B 20.00 |
| mean target | **$388.48** ⇒ **upside −10.2%** (trades **above** target) |
| recommendations | SB 4 · B 10 · **H 16** · S 2 · SS 2 |
| **estimate direction (denominator only)** | current qtr **+0.4%** / next qtr **−1.3%** over 90 days — **flat to slightly down** |
| XBRL cross-check | ✅ 4/4 quarters within 5% |

⚠ **`M1083`** `[measured]` **The Health Care leadership is a price move without an estimate move.**
`AMGN`'s `rs60` is **+31.6** while its 90-day consensus is **+0.4% / −1.3%**, and it trades **10.2%
above** its mean target — the same shape the carried row records for **`MRK` (upside −4.4%)**.
⇒ **Three of the sector's leaders are at or above their targets on flat estimates.** `L2` applies to
the sector's leadership, not just to its one prior 🟢, and **no cheapness claim is made here either.**

## §2B · Thesis · freshness

**Thesis (new this run):** *the sector's money-flow breadth is broad and the cap-weighted ETF is
reporting on `LLY`.* **11 of the top 16 by `rs60` are `OBV 매집` across FIVE GICS industries**
(`M1068`), and **`LLY` — the `top1` flipper — is not in the top 16 at all** (`M1069`).
🚨 **Counter-evidence, stated not omitted**: **five of the six basket names were negative on 08-28**
(`REGN` −1.447pp, `TMO` −1.124, `VRTX` −0.843, `AMGN` −0.819, `MRK` −0.569; only `BDX` +0.966) ⇒
EW6 `exc1` ≈ **−0.64pp** (`M1070`). **The promotion rests on 60-day accumulation and both the 1-day
and 5-day tapes are against it.**
**Freshness: `UNMEASURED (G1 FAIL)`.**

## §2C · Flow / positioning

Constituent table in `SECTOR_DEEP_HLTH §2` — **cited, not reprinted**. The one coherent negative is
**drug distribution**: `MCK` `OBV 분산`, `COR` 중립, `CAH` marginal, **all three with negative `rs20`**
despite `rs60` ≈ +19 — and that node is the sector's binding constraint (policy/payer), not demand.

## §2D · Competition / peers

**Chain-hop survivors, flow-crossed** (`SECTOR_DEEP_HLTH §5`): **`ABT`** — `OBV 매집`, `rs60` **+26.8**
with **`rs20` only +1.7**, i.e. accumulating with the 20-day *not yet caught up*, which is the tool's
own definition of chain-hop alpha. **`BMY`** — same shape, weaker (`rs20` −0.7).
🚫 Six other `chain-hop` candidates (`WMB` `SPGI` `KO` `WFC` `BLK` `CCEP`) **rejected as tool noise** —
cross-topic co-mentions, and a news co-mention alone is not a candidate.
**Screener**: 1 name only — **`ISRG`** (de-rate snapback, RSI **42.7** ⚠`C21`, px/200 −22%). **Filed
to the missed ledger** (§6), not carried.

## §2E · Refutation + dated catalyst

| kills it | date |
|---|---|
| **`S133`** branch B — `EW{REGN,AMGN,TMO,BDX,MRK,VRTX}` exc10 vs **`XLV`** ≤ **−3.00pp** ⇒ **the promotion made today was wrong** | **2026-09-14** |
| breadth collapse — the count of HLTH top-16 names carrying **both** `OBV 매집` **and** `rs20` > 0 (today **9 of 16**) falling below 6. **RULE D6 honoured: OBV is a C-grade signal and is paired with `rs20` here, never quoted alone** | rolling |
| distribution node — `COR`/`CAH` joining `MCK` in **분산** ⇒ the policy leg tightens | rolling |
| 🚨 **VOID**: an **FDA action/readout with a named date at ≥2 of the six** in-window. **Base rate NOT remote** — `LLY` took one on **08-28** (7 outlets) and `chain-hop` surfaced *"Biotech Stocks Facing FDA Decision In September 2026"* | live |

---

# §3 · COMMUNICATION SERVICES — `OW` (held; `eqflow` −0.004 could not carry a demotion)

## §3A · Numbers

| | `CMCSA` |
|---|---|
| price / 52w | $27.06 / high $32.86 · low $21.28 |
| mcap · beta · yield | $96B · **0.65** · **5.00%** |
| Trailing / Forward P/E | 8.46 / **7.47** · P/B **1.07** · P/S 0.77 |
| mean target | **$30.08** ⇒ **upside +11.2%** — 🚨 **the only finalist in this file trading BELOW its mean target** |
| recommendations | SB 2 · B 7 · **H 16** · S 2 · SS 1 |
| **estimate direction (denominator only)** | current qtr **−3.5%** · next qtr **−4.9%** over 90 days — **declining** |
| XBRL cross-check | ✅ 4/4 quarters within 5% |
| 🚫 not used | **PEG 142.98 is an artifact** of a near-zero growth denominator and is **not cited as a valuation** |

★ **`M1084`** `[measured]` **`CMCSA` is the file's only below-target name and its denominator is
falling.** 7.47× forward, 1.07× book, 5.00% yield, **+11.2% upside** — against consensus revisions of
**−3.5% / −4.9%**. **Stated as the two-sided read it is**, not resolved: cheap-with-a-shrinking-
denominator is the value trap's own signature, and `L2`'s discipline (multiple beside margin
percentile) is **not satisfied here either** — the margin percentile was not measured (`C3`).

## §3B · Thesis · freshness

**Thesis (new this run):** *the sector's strength is the distribution pipe, and its weakness is
Alphabet.* **`GOOGL` + `GOOG` are 8,975B of a 12-name bucket** — two share classes of one company,
both negative on flow (**−0.322 / −0.509**) and on `rs60` (**−7.4 / −7.3**), with `GOOG` also `OBV 분산`
(`M1075`). **RULE D6: the OBV read is carried alongside flow and `rs60`, never alone.** **`top1_flips_sign` is `False` here only because removing
`GOOGL` still leaves `GOOG`** ⇒ **the flipper guard is blind to a dual-share-class listing**,
registered as **`D407`**.
★ **The mandate's answer**: **11 of 12 names were positive on 08-28** and the leaders were
**`CMCSA` +2.688pp · `NFLX` +2.582 · `T` +2.508 · `TMUS` +2.264**, with `GOOGL` mid-pack at +1.971
(`M1076`). **Not an index print, not one name.**
**Freshness: `UNMEASURED (G1 FAIL)`.**

## §3C · Flow / positioning

Five `OBV 매집` names with positive `rs20`: **`WBD` `DIS` `CMCSA` `VZ` `T`** (`SECTOR_DEEP_COMM §1`).
🚨 **`T` +2.508pp and `VZ` +1.582pp on a hawkish-rates session** — the two most duration-like names in
the sector outperformed while `XLU` fell 0.815pp (`M1077`). **Third independent instance of `M1052`**:
the desk's "duration complex" grouping does not describe the tape.
⚠ **`TMUS` is the counter-example**: `exc1` +2.264pp on the day but `OBV 분산`, `rs60` −7.4 —
**one good session inside a distribution pattern.** Not carried.

## §3D · Competition / peers

**Screener leader-pullbacks: `GOOG` RSI 28.9 · `GOOGL` RSI 33.7 · `TTWO` RSI 35.5** (⚠ all `C21`).
🚨 **The screener says Alphabet is an oversold leader; the flow says it is the sector's drag.**
**Both are recorded; neither is adopted** — `GOOGL` goes to the **missed ledger** with an entry
condition (§6), `TTWO` to the **reject ledger** (`OBV 분산`, `rs20` −9.8).
🚫 **`chain-hop` was not run for this sector** and the reason is a **scope limit, not an empty result**:
the tool's universe is `us_top300` and COMM has n=12 in it; the sector's real chain (fiber, towers,
licensors) sits below the cut or in other sectors — `CCI` (towers) is classified **Real Estate**.

## §3E · Refutation + dated catalyst

| kills it | date |
|---|---|
| breadth — COMM `OBV 매집 ∧ rs20 > 0` count falling from **5 of 12** to ≤2 | rolling |
| the pipe — `CMCSA` `OBV` → 분산, or `T`/`VZ` `rs20` → negative | rolling |
| 🚨 **VOID**: a **US antitrust/regulatory action against Alphabet with a named date**. Base rate **not remote** — the 08-28 head carried a federal ruling against the Pentagon on **Anthropic** (21 outlets); **87% of this bucket would move for a reason unrelated to the pipe** | live |
| demand-side risk, stated against this section's own conclusion: ***"US Consumer Sentiment Drops"*** (5 outlets) and *"Consumer Confidence Shows Modest Deterioration"* (REIGNITED 2→6→5) | live |

---

# §4 · INFORMATION TECHNOLOGY — `N` (PREMORTEM-promoted DEEP; **the sector verdict was NOT moved**)

## §4A · Numbers — the two held names that print inside five sessions

| | `AVGO` (**held**, prints **2026-09-02**) | `ANET` (**held**) |
|---|---|---|
| price / 52w | **$368.79** / high **$495.00** ⇒ **25.5% below its 52-week high** | $195.38 / high $214.89 |
| mcap · beta · yield | $1.75T · 1.47 · 0.70% | $246B · **1.61** · — |
| Trailing / Forward P/E | 61.77 / **18.91** · PEG 0.40 | 63.64 / **37.87** · PEG 1.81 |
| mean target | **$525.97** ⇒ **upside +42.6%** | **$241.82** ⇒ **upside +23.8%** |
| recommendations | SB 8 · B 37 · H 4 · **S 0** | SB 8 · B 22 · **H 0 · S 0** |
| **estimate direction (denominator only)** | CQ **+1.7%** · CY **+2.7%** · **NY +6.2%**; revision breadth **25↑ / 9↓** (30d) | CQ **+17.9%** · NQ **+18.8%** |
| XBRL cross-check | ✅ 4/4 within 5% | ✅ 4/4 within 5% |

★★★ **`M1085`** `[measured]` **`AVGO`'s −24.4 `rs60` is a price move that its denominator did not
make.** The stock is **25.5% below its 52-week high** while the 90-day consensus rose **+2.7% CY and
+6.2% NY** on **25 upgrades to 9 downgrades**, and **45 of 49 analysts rate it Buy or better with
zero sells.** ⇒ **the "discount, not franchise loss" case is arithmetic, not narrative.**
🚫 **And it is explicitly NOT used as evidence**: the revision-momentum rule bars that on an IT name,
because the measured confound is an IT loading (`W1`-adjacent, `measure_ic.py`: W1 +0.403 vs
W2 −0.299, ex-IT control +0.074/−0.064). **The number describes the denominator's direction. `S132`
decides the question.**
⚠ **`ANET`'s consensus is extreme in the other direction — 30 analysts, ZERO holds, ZERO sells** —
and its estimate momentum (+17.9%/+18.8%) is **the exact cell the rule forbids using on an IT name.**
Recorded as denominator direction only; **the 63.64× trailing / 37.87× forward pair carries no margin
percentile** (`C3`) ⇒ **no cheapness or dearness claim.**

## §4B · Thesis · freshness

**Thesis (new this run):** *the "IT" label is horizon-dependent and therefore not a unit.*
60-day: an **80pp** software-vs-semis split (`SHOP` +30.4 → `APP` −49.9, `M1079`).
1-day: **the split disappears** — intra-node dispersion (software **8.73pp**, semis **7.97pp**)
**exceeds** inter-node (`M1080`), and what actually moved together was **security**
(`CRWD` −3.967 · `FTNT` −3.697 · `PANW` −2.714 — **n≈1, not n=3**, lens B3).
🚫 **The sector verdict was NOT moved.** A PREMORTEM-promoted DEEP does not inherit the right to
change the sector; **`P113`** (\|`XLK` 5d − `XLU` 5d\|, state **1.39pp**, branch B ≤1.00pp = **0.39pp
away**) is the registered decider.
**Freshness: `UNMEASURED (G1 FAIL)`.**

## §4C · Flow / positioning — finalists only

| name | flow `asof 08-27` | positioning |
|---|---|---|
| `AVGO` | −0.517, `OBV 중립`, `rs20` −8.2 | short **1.2% float, covering**, DTC 3.0 · **P/C 2.05 = put-heavy** · **implied ±8.11%** (chain-derived, 09-04 expiry spanning the print) · screener **leader pullback, RSI 22.9** ⚠`C21` |
| `HPE` (held, prints **09-03**) | `OBV 매집`, `vol_surge` 0.67 | short **5.1% float, covering**, DTC 3.5 · **P/C 0.24 = call-heavy, complacent** · **implied ±11.39%** · 08-28 `exc1` **−3.633pp** |
| `ANET` | +0.533, `OBV 매집`, `rs20` +13.6 / `rs60` +13.2 | the only held epicenter name positive on both `rs` — PREMORTEM's named **epicenter starter** |
| `VRT` | +0.472, `OBV 매집`, **`rs20` +14.4 on `rs60` −21.0** | `EVENT_ALPHA` Card 6 CONFIRMED-EARLY (**thin**) |

🚨 **The two held prints' option markets disagree about fear** — `AVGO` P/C **2.05** vs `HPE` P/C
**0.24**, one day apart in one value chain, with `HPE` carrying **4.2× the short interest** and the
**larger implied move**. **Two-sided read, not resolved.**
🚨 **`HPE` has NO bracket** — PREMORTEM declined by design (bracketing a name the same run deep-dives
pre-commits the DEEP). **Open item for the next run's PREMORTEM, recorded as such, not as a gap.**
🚫 **`module_flow --positioning` returned `예상변동 ±nan%` on every ticker** and named a **non-spanning**
expiry for `AVGO` (`M1065`, `D315` 6th run). **Every implied move above came from the chain directly.**

## §4D · Competition / peers

**Screener leader-pullbacks (⚠ all `C21`)**: `AVGO` RSI **22.9** (existing), and new: `CSCO` 33.5 ·
`ADI` 35.3 · `MPWR` 37.8 · `APH` 40.9 · `KEYS` 42.3. **De-rate snapback: `APP` RSI 32.5, px/200 −37%.**
★ **All five new names are semiconductor/electronic-component** — i.e. **the screener is finding
pullbacks in exactly the cohort the 60-day frame says is broken.** Consistent with `M1079`, and it is
why none is promoted here: a pullback setup inside an 80pp-losing cohort is a shape, not a thesis.
🚨 **`COHR` is the case the desk cannot measure**: `OBV 매집` with **`rs20` +14.6 on `rs60` −32.3** —
a textbook turn — and `cycle_registry.json` has **no optical row for a 13th run** (`D250`).
**Unmeasurable, not absent.**
⚠ **`TSM` (foundry, node 2) is outside `us_top300`** ⇒ **no flow/RS/OBV/short tag exists for it** in
this run. Not absent — **unmeasurable**.

## §4E · Refutation + dated catalyst

| kills it | date |
|---|---|
| **`S132`** — `AVGO` 1-session excess vs `SPY`, A ≥ **+9.00pp** / B ≤ **−9.00pp**, **outside** ±8.11% | **2026-09-03** |
| **`S127`** (inherited) — bands **inside** the implied move ⇒ **pre-declared NO-INFORMATION**; **not re-frozen** (`D242`), runs beside `S132`; **if they disagree, that is the finding** | 2026-09-08 |
| **`P111`** — `EW{MU,SNDK,WDC}` exc10 vs `SPY`; A ≤ −3.00pp (regime call survives) / B ≥ +5.00pp | **2026-09-14** |
| **`P113`** — \|`XLK` 5d − `XLU` 5d\|; B ≤ 1.00pp ⇒ IT and the duration `UW`s are **one bet** | 2026-09-08 |
| node 5 — `ANET` `OBV` → 분산, or `COHR` `rs20` < 0 | rolling |
| 🚨 **VOID for `S132`** — `AVGO` postpones, or an announced acquisition of/by `AVGO` inside 08-31→09-03 | live |

---

# §5 · CROSS-SECTOR — `US_LIVE_SHORTLIST` names outside the DEEP sectors

The shortlist's 8 names (mcap ≥$10B ∧ `🟢가속` ∧ flow-desc top-15), `asof 2026-08-27` settled:

| name | sector | flow | OBV | `rs20` | `rs60` | FINRA short-z | verdict |
|---|---|---|---|---|---|---|---|
| `MSTR` | IT | +0.967 | 매집 | +36.6 | −0.5 | **−1.41** | ✅ low-short / covering — **but already REJECTED 2026-08-26** (`H.밸류소진`, recheck **09-16**). **Not re-filed** |
| `COIN` | Financials | +0.922 | 매집 | +12.6 | +8.1 | −0.24 | △ normal. 🚨 **carried ONLY with its denominator: CY consensus +1.01 → −2.06 = −303.5%.** Recheck **09-08** |
| **`A`** (Agilent) | Health Care | +0.900 | 매집 | +9.7 | +15.2 | **−0.92** | ✅ **clean rise, and it is inside this run's promoted sector** — `new_green` today |
| **`CRM`** | IT | +0.856 | 매집 | **+35.5** | +24.0 | −0.76 | ✅ clean rise · `new_green` · 08-28 `exc1` **+1.794pp** on the **Anthropic** cluster (`M1078`) |
| `FCX` | Materials | +0.844 | 매집 | +19.6 | +7.8 | +1.09 | △ normal — ⚠ inside a `UW` sector, and **Copper spec is at the 100th percentile of one year** |
| `TGT` | Cons Staples | +0.833 | 매집 | +10.9 | **+33.2** | **+1.74** | ⚡ **crowded-short = turn-conditional squeeze fuel, explicitly NOT a buy** — and it sits in a `UW` sector |
| `SNPS` | IT | +0.822 | 매집 | +20.9 | −10.1 | +1.29 | △ normal · EDA, chain node 1 · turn shape (`rs20` +20.9 on `rs60` −10.1) |
| `INTU` | IT | +0.752 | 매집 | +6.3 | +6.5 | +0.20 | △ normal · 08-28 `exc1` **+3.118pp** = the day's 2nd-best software name |

★ **`M1086`** `[measured]` **Three of the eight shortlist names sit in sectors this run rates `UW`
(`FCX` Materials, `TGT` Staples) or has explicitly demoted, and the shortlist's own gate cannot see
sector context.** ⇒ **The shortlist is a name-level instrument and is read as one here** — it hands
candidates, not verdicts, and `TGT`'s ⚡ tag is carried with the tool's own words: *crowded-short is
squeeze fuel on a confirmed turn, not a standalone entry.*

**Epicenter-starter module**: 🚫 **not triggered — `cycle_exposure` reports NO GAP** (AI-compute
epicenter 16.93% ≥ 12.0%; Energy/refining 9.97% ≥ 8.0%). ⚠ **The ✅ covers two of three registered
cycles**: missile-defense (`RTX`) carries **⚪ no threshold set**, and optical has **no registry row at
all** (`D250`, 13th run). **PREMORTEM's named cleanest expressions are `ANET` (held) and `SHOP`** —
handed as candidates, **not sized** (P4).

---

# §6 · Ledger writes — both directions, machine-enforced boundary

**Rejections** (a reason was stated and the name was set aside) — each with `--revives-if` **and**
`--recheck-date`, without which `add` refuses to run:

| ticker | class | why (one line) | revives if | recheck |
|---|---|---|---|---|
| `APP` | `C.차트붕괴` | The universe's worst name on every axis simultaneously — `rs60` **−49.9**, `rs20` −26.6, `OBV 분산`, flow **−0.833** — and the screener's de-rate-snapback tag is a **shape**, not a thesis, inside an 80pp-losing cohort | `OBV` turns **매집 AND `rs20` > 0 on two consecutive settled sweeps** | 2026-09-30 |
| `TTWO` | `D.약한손` | COMM's only `🔴분산` (flow −0.667, `rs20` −9.8) **while the screener tags it a leader pullback** — the two instruments disagree and the money axis is the one that decides here | `OBV` turns **매집 with `rs20` > 0** on a settled sweep | 2026-09-30 |

**Misses** (surfaced and simply did not make this sheet) — each with `--enters-if` and a recheck date.
⚠ Sign is **inverted** vs the rejection ledger; the two are never summed:

| ticker | class | why | enters if | recheck |
|---|---|---|---|---|
| `ISRG` | `M.숏리스트탈락` | **The only screener setup in the whole promoted Health Care sector** (de-rate snapback, RSI 42.7 ⚠`C21`, px/200 −22%) and it is **not** one of the six `OBV 매집` leaders the sector thesis rests on — so it did not fit either the basket or the bottleneck | `ISRG` prints **`OBV 매집` with `rs20` > 0** on a settled sweep **while `S133` branch A is live** | 2026-09-14 |
| `GOOGL` | `Q.확신부족` | **The screener's most oversold leader-pullback on the board (RSI 28.9/33.7 ⚠`C21`) is also the name dragging the sector this run just kept at `OW`** — flow −0.322, `OBV 중립`, `rs60` −7.4. Two instruments, opposite directions; this run took neither side | `GOOGL` `rs60` **crosses above 0 with `OBV` 매집** on a settled sweep | 2026-09-30 |

🚫 **Not re-filed** (already in the ledgers with live recheck dates): `MSTR` (`H.밸류소진`, 09-16) ·
`XOM` (`K.본문반증`, 09-09) · `ORCL` (`Q.확신부족`, 09-16) · `KLAC` (`M.숏리스트탈락`, 09-16) ·
`SLB` and `STX` (both written at **EVENT_ALPHA** this run, 09-30 / 09-14).
★ **Nothing was RE-FILED-as-removed**: no name on this sheet had its flow gate pass while only its
story broke, so the "re-file, don't delete" rule had no instance this run. **Stated because a rule
with no instance and a rule not applied look identical in a file that does not say so.**

---

## ✅ EXIT CHECK
- [x] **ONE file, per-sector sections §A–§E** for all four DEEP sectors + a cross-sector §5. Filename unchanged.
- [x] **Candidate set is the wide net**: DEEP thesis leaders ∪ `us_setup_screener --sector` (all four run) ∪ `US_LIVE_SHORTLIST` (its 8 names given their own section).
- [x] **Thesis-confirmation gate checked first** — and it has **zero applicable rows** (scoreboard is 5 KR names, 08-21). Stated, with `module_report_tags` cross-checked so "no row ≠ no coverage" was tested rather than assumed.
- [x] §A numbers cross-checked **XBRL ↔ yfinance (4/4 quarters within 5% on all five names pulled)**; blanks stated as blanks; **`PEG 142.98` rejected as an artifact rather than quoted**.
- [x] **`L2` peak-margin applied to the two sectors it bites** — and **no cheapness claim is made anywhere in this file**, because no margin percentile was measured (`C3`). `MPC` is written up as the trap's textbook form (`M1082`).
- [x] **Revision tables used only as denominator direction**, never as a leading indicator, and explicitly **not as evidence on `AVGO`/`ANET`** where the measured confound lives.
- [x] Epicenter-starter module: **not triggered** (no GAP), with the ✅'s two blind spots named (`RTX` threshold unset, optical unregistered).
- [x] **Every name set aside has a ledger row with both required fields**; misses filed separately; the machine-enforced boundary respected; already-filed names named as such rather than duplicated.
- [x] **Zero buy/sell language.** No position size appears anywhere; G6 bars IC-backed sizing and no fraction is quoted.


---

# §7 · ALPHA (Stage 10) — freshness gate, positioning gate, momentum-only flags

> Written by Stage 10 into this file, per the protocol (`BET_SHEET §B` tags are ALPHA's output).
> The action tickets and both-sides brackets are in `llm_outputs/2026-08-29/ACTION_TICKETS.md`
> (§ ADDENDUM). P4 — no sizing, no buy/sell.

## §7.1 · 🚫 The freshness verdict is WITHHELD, not issued — and that is the rule, not a shortfall

**The stage's own rule: *"when G1 FAILs, ALPHA may not issue a freshness verdict at all."***
**G1 FAILED**, and this run's own sweep is worse than the baseline PREFLIGHT scored:
`SECTOR_FLOW_US.json §scoring.vel_coverage` = **0.0%** (0 of 299).

⇒ **Every §B freshness tag in §1–§5 above reads `UNMEASURED (G1 FAIL)`. No name receives
🟢LIVE / 🟡PARTIAL / 🔴RESOLVED on a freshness basis this run.**

**The falsification probe was run first, as the stage demands — and it passed, which is what makes
the zero readable as an instrument fact rather than as a quiet market:**
`fts search <name> --days 7 --count --scope foreign` → **Nvidia 5,353 · Broadcom 549 · Marvell 482 ·
Eli Lilly 191 · Nucor 23 · Marathon Petroleum 19.** Two orders of magnitude between loudest and
quietest, and two of the probes sit below the coverage frontier (`NUE` #217, `MPC` #177).
⇒ **The pipe is alive and discriminating. The 0.0% is the counter, not the news.**

★★ **`M1088`** `[measured]` **And the F1 gate is structurally unfireable on this board — measured on
the US side, not inherited from KR.** `theme_age --scope foreign` on this run's live themes returns
**`age >= 90` on every one**:

| theme | verdict | age | 7d mean | acceleration | total |
|---|---|---|---|---|---|
| `Jackson Hole` | 🟡ACCELERATING | **>=90** | 159.9 | **29.06×** | 1,290 |
| `Hormuz` | ⚪ECHO | **>=90** | 129.9 | 0.75× | 9,669 |
| `take-private` | ⚪ECHO | **>=90** | 2.9 | 1.36× | 114 |
| `memory commitment` | (same shape) | **>=90** | — | — | — |

🟢FRESH requires **age ≤14d AND acceleration ≥2×**. **The age field is saturated at `>=90` for every
theme queried**, so the age conjunct **cannot be satisfied regardless of the market** — not even by
`Jackson Hole`, which is accelerating **29.06×**. ⇒ **F1's long zero is a property of the gate's age
term, not of the universe.** This reproduces the KR run's 2026-08-29 finding **independently on the
US feed**, so it is a measurement here and not a `W1` transfer.
🚫 **Not repaired** (P4 — a gate change is a code/human decision). Registered as **`D410`**.

## §7.2 · Positioning gate — issued, because it does NOT depend on the news axis

| name | positioning read (`us_flow` FINRA short-z, `asof 2026-08-27`) | gate |
|---|---|---|
| `MSTR` | z **−1.41** — ✅ low-short / covering | clean rise — **but the name is already REJECTED** (`H.밸류소진`, recheck 09-16). Gate result recorded, not acted on |
| `A` (Agilent) | z **−0.92** — ✅ low-short / covering | clean rise, and it sits inside the promoted `HLTH` sector |
| `CRM` | z **−0.76** — ✅ low-short / covering | clean rise |
| `COIN` | z −0.24 — △ normal | 🚨 carried **only** with its denominator attached: CY consensus **+1.01 → −2.06 = −303.5%** |
| `FCX` | z +1.09 — △ normal | ⚠ inside a `UW` sector; **Copper spec at the 100th percentile of one year** |
| `SNPS` · `INTU` | z +1.29 / +0.20 — △ normal | — |
| **`TGT`** | **z +1.74 — ⚡ crowded-short** | 🚨 **turn-conditional squeeze fuel, NEVER a standalone entry — hard-stop stamp required.** And it sits in a `UW` sector |
| `AVGO` (held) | short **1.2% float, covering**, DTC 3.0; **P/C 2.05 put-heavy** | prints **09-02**; bracketed by `S132` |
| `HPE` (held) | short **5.1% float, covering**, DTC 3.5; **P/C 0.24 call-heavy ⇒ complacent** | prints **09-03**; 🚨 **no bracket** — open item for the next run's PREMORTEM |

## §7.3 · Momentum-only flags — graded before acting (`D6`)

| name | RS / volume | accumulation axis | grade of the disagreement | flag |
|---|---|---|---|---|
| `TGT` | `rs60` **+33.2**, 🟢가속, `vol_surge` 1.30 | `OBV 매집` — **agrees** | no disagreement | ⚡ crowded-short ⇒ **hard-stop stamp** on the positioning axis, not the accumulation one |
| `MSTR` | `rs20` **+36.6**, `vol_surge` 1.54 | `OBV 매집` — agrees; but **`rs60` −0.5 ≈ flat** | — | 20-day move on a flat 60-day; already rejected |
| `SNPS` | `rs20` +20.9, 🟢가속 | `OBV 매집` — agrees; **`rs60` −10.1** | — | turn shape, not a runner |
| `PYPL` | `rs60` **+36.5** | 🚨 **`OBV 중립` — disagrees** | **C-grade** (OBV alone; r≈0.49 vs real flow, no leading power, t=1.00) | ⇒ **downgraded to 🟡 and reported as a disagreement**; it does **not** by itself convert the name into a tape trade. ⚠ The independent, non-OBV reason it is EXHAUSTED is the **withdrawn Advent/Stripe offer, dated 08-28**, and that is what carries the verdict |
| `TMUS` | `exc1` **+2.264pp** on 08-28 | 🚨 **`OBV 분산`, `rs60` −7.4 — disagrees** | **C-grade** | ⇒ **🟡 with the disagreement reported**; one good session inside a distribution pattern, not carried |

⚠ **No C-grade disagreement was upgraded into a verdict**, per the carry rule. Both names are
flagged, not converted.

## §7.4 · 🟡PARTIAL residuals — every one is a dated appointment, handed to `carryover`

| name / claim | residual | **re-check date** |
|---|---|---|
| `MPC` `PSX` `VLO` (CONFIRMED-EARLY, `EVENT_ALPHA` Card 3) | the crack's **second** QoQ rate decline is the kill; one has occurred (+58.92% → +27.34%) | **2026-09-30** (Q3 close) |
| `VRT` (CONFIRMED-EARLY, thin) | `rs60` must cross above 0 from −21.0 with `OBV` still 매집 | **2026-09-30** |
| `ABT` (chain-hop survivor) | `rs20` (+1.7) must catch up to `rs60` (+26.8) | **2026-09-14** (with `S133`) |
| `COHR` (turn candidate, unmeasurable cycle) | `rs20` +14.6 must hold above 0 with `OBV` 매집 | **2026-09-30** |
| `HPE` (held, prints 09-03, **unbracketed**) | the next PREMORTEM registers a bracket or states why not | **2026-09-02** |
| `PYPL` · `TMUS` (C-grade disagreements, §7.3) | see the flags above | **2026-09-30** |

★ **Tags follow the NAME, not the sector's turn in the rotation.** All six rows are handed to the next
run's inheritance packet even where their sector loses its DEEP slot — the measured failure this rule
exists for is a tagged name going untracked while its sector rested.

## §7.5 · Names carried into ALPHA that received NO tag — written down, not dropped silently

**Zero.** Every name that reached §1–§5 received either a positioning gate (§7.2), a momentum flag
(§7.3), a dated residual (§7.4), or a ledger row (§6). ⚠ **Stated explicitly because "no untagged
names" and "nobody checked for untagged names" look identical in a file that does not say so** —
that is the F1 asymmetry the missed ledger exists to close.

## ✅ EXIT CHECK (Stage 10 · ALPHA)
- [x] **The falsification probe was run BEFORE any freshness reading** — six live CLI probes spanning >2 orders of magnitude, two below the coverage frontier. **The 0.0% is written as the instrument, never as "no news."**
- [x] 🚫 **No freshness verdict issued at all**, per the stage's own G1-FAIL rule. All §B tags read `UNMEASURED (G1 FAIL)`.
- [x] **The F1 zero was diagnosed rather than re-counted** — `theme_age` returns `age >= 90` on every theme queried, so the 🟢FRESH conjunct is unsatisfiable by construction (`M1088`, `D410`), measured on the **US** feed rather than transferred.
- [x] Positioning gate issued (it does not depend on the news axis); **⚡crowded-short (`TGT`) stamped hard-stop and explicitly not a standalone entry.**
- [x] **Momentum-only disagreements GRADED before acting** — both are **C-grade (OBV-only)**, downgraded to 🟡 and reported as disagreements, not converted into tape trades.
- [x] **Every 🟡PARTIAL carries an explicit re-check date** and is handed to `carryover`; tags follow the name, not the sector.
- [x] **Untagged names: zero, and the check itself is recorded** rather than left implicit.
- [x] `ACTION_TICKETS.md` written by `action_bracket.py` — **with an ADDENDUM recording that the generator named a binary and then reported none (`M1087` / `D409`)**, so its empty body cannot be read as "no binary to bracket." The four both-sides brackets are registered in `handoff/SCENARIOS_US.md` + the `SCENARIOS.md` MASTER INDEX.
