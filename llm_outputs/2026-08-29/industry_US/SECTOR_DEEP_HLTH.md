# SECTOR_DEEP_HLTH — Health Care · industry_US · 2026-08-29 (Stage 8 / L1·DEEP)

> **CONTINUOUS TRACK** — HLTH held a continuous slot in the prior run and is still top-OW today
> (`SECTOR_ROTATION §3`, anti-thrash continuity). **This file LEADS with the delta**; the unchanged
> structural map is carried by reference to `REPORT/industry_US/SECTOR_DEEP_HLTH.md` (2026-08-27).
> ⚠ Fan-out executed **in-line, not as parallel subagents** (see `BLINDSPOT_PREMORTEM §0` for the
> reason and what it costs). P4 — analytical only, no sizing.

## §0 · The mandate ROTATION handed this file, answered in one line

> *"The board's best money-flow breadth (`eqflow` +0.102) sits under the board's worst 5-day return
> (`XLV` `exc5` −2.456pp vs `SPY`). Early, or trap?"*

**Answer: neither — the two numbers are measuring different objects, and the measurement that
separates them is the constituent table.** `XLV` is cap-weighted and its `top1` `LLY` is a
**`top1_flips_sign` bucket**; `eqflow` is not cap-weighted. **13 of the sector's 16 highest-`rs60`
names are `OBV 매집`, spanning FIVE distinct GICS industries** — that is not a sub-sector rotation
wearing a sector label, and it is not one name. **The promotion stands, and `S133` is registered to
falsify it.**

## §1 · The delta — what changed since 2026-08-27

| | 08-27 file said | today, measured `asof 2026-08-27` settled | delta |
|---|---|---|---|
| sector verdict | `N` | **`OW`** | 🔴 **CHANGED** (`SECTOR_ROTATION §2`, on `eqflow` only) |
| `eqflow` | not the lead number | **+0.102 = highest of 11** | new |
| flipper status | HLTH flagged 08-28 baseline | **still a flipper** (`LLY`, `wflow` −0.027 vs `wflow_ex_top1` +0.053) | unchanged |
| the sector's one 🟢 (`MRK`) | *"trades ABOVE its mean target, upside −4.4%"* | **carried unchanged, `L2` still applies** | unchanged |
| new dated event | — | **`LLY` Mounjaro approved to reduce cardiovascular risk (FDA), 08-28, 7 outlets** | 🆕 |

## §2 · Flow — and the number that answers §0

**Constituent table, `asof 2026-08-27` settled, top 16 by `rs60`** (`SECTOR_FLOW_US.json`, n=32):

| ticker | GICS industry | `rs60` | `rs20` | OBV | flow |
|---|---|---|---|---|---|
| `REGN` | Biotechnology | **+32.4** | +5.4 | **매집** | +0.436 |
| `AMGN` | Biotechnology | **+31.6** | +8.8 | **매집** | +0.567 |
| `TMO` | Life Sciences Tools | **+29.3** | +5.4 | **매집** | +0.349 |
| `BDX` | HC Equipment | **+28.7** | +9.7 | **매집** | +0.506 |
| `MRK` | Pharmaceuticals | **+27.8** | +11.3 | **매집** | +0.761 |
| `VRTX` | Biotechnology | **+27.3** | +9.7 | **매집** | +0.417 |
| `ABT` | HC Equipment | +26.8 | +1.7 | **매집** | +0.226 |
| `BMY` | Pharmaceuticals | +21.4 | −0.7 | **매집** | +0.060 |
| `DHR` | Life Sciences Tools | +21.0 | +6.0 | **매집** | +0.361 |
| `HUM` | Managed Care | +20.8 | +3.1 | **매집** | +0.329 |
| `MDT` | HC Equipment | +20.5 | +1.0 | 중립 | −0.030 |
| `COR` | Distributors | +19.8 | −1.1 | 중립 | −0.274 |
| `MCK` | Distributors | +19.3 | −1.1 | **분산** | −0.396 |
| `CAH` | Distributors | +19.1 | −0.4 | **매집** | +0.012 |
| `ABBV` | Biotechnology | +18.3 | −3.7 | 중립 | −0.487 |
| `JNJ` | Pharmaceuticals | +17.7 | −0.1 | **매집** | +0.201 |

★★ **`M1068`** `[measured]` **Every one of the sector's top 16 by `rs60` is positive (+17.7 to
+32.4), and 11 of 16 are `OBV 매집`, across five GICS industries** — Biotechnology, Life Sciences
Tools, HC Equipment, Pharmaceuticals, Managed Care. Lens **B5** (state the dispersion inside the
sector) applied and it **passes in the unusual direction**: the spread between sub-industries
(+17.7 to +32.4 = 14.7pp) is **narrower than the spread inside** several other sectors on the board.
**The sector label is, for once, the right unit of analysis here** — and that is the finding.

★ **`M1069`** `[measured]` **`LLY` — the name that owns the sector's `wflow` sign — does not appear in
the sector's top 16 by `rs60` at all.** The bucket whose sign one company controls is a bucket whose
sign is set by a name that is not participating in the sector's actual move.

🚨 **Where the money is NOT: the distributors.** `MCK` **OBV 분산**, `COR` 중립, `CAH` marginal — all
three with **negative `rs20`** despite `rs60` near +19. **The one sub-node with a coherent negative
signal is drug distribution**, and it is the node most exposed to a drug-pricing policy shock.

## §3 · The 08-28 session — and it argues AGAINST the basket I just registered

Settled 08-28 vs `SPY` (**benchmark named inline**, `SPY` −0.227% on the day):

| name | `exc1` | | name | `exc1` |
|---|---|---|---|---|
| `JNJ` | **+1.081pp** | | `MRK` | −0.569pp |
| `ABT` | +1.016pp | | `ABBV` | −0.807pp |
| `BDX` | +0.966pp | | `AMGN` | −0.819pp |
| `MCK` | +0.550pp | | `VRTX` | −0.843pp |
| **`LLY`** | **+0.100pp ≈ flat** | | `TMO` | −1.124pp |
| `UNH` | −0.305pp | | `REGN` | **−1.447pp** |

🚨 **`M1070`** `[measured]` **Five of the six names in `S133`'s basket were NEGATIVE on 08-28** —
`REGN` −1.447, `TMO` −1.124, `VRTX` −0.843, `AMGN` −0.819, `MRK` −0.569, only `BDX` +0.966 ⇒
**EW6 `exc1` ≈ −0.64pp.** ⇒ **the basket registered this morning as the sector's leadership
underperformed the index on the last settled session.**
- ✅ **This does not contaminate `S133`** — its window starts at the **08-28 close**, so this session
  is prior, not inside. **Stated because suppressing it would hide that the row starts against it.**
- ★ **And it re-frames `XLV`'s `exc1` of −0.018pp (≈flat)**: the sector's flat day was `JNJ`/`ABT`
  offsetting the biotech leaders. **`LLY` was flat.** The sector's **5-day** −2.456pp is where the
  damage sits, not the last session.
- ⇒ **The honest position: the promotion rests on 60-day accumulation, and the 1-day and 5-day tapes
  are both against it.** That is exactly why `S133` was written with **branch B graded HIGH**.

## §4 · Value chain — 6 nodes, left → right, binding constraint marked

`discovery → clinical/trials → manufacture (biologics/device) → distribution → payer/PBM → patient`

| # | node | who (universe) | state |
|---|---|---|---|
| 1 | discovery / platform | `VRTX` `REGN` `AMGN` `BMY` | **all OBV 매집, `rs60` +21 to +32** |
| 2 | **tools & instruments (the picks-and-shovels)** | `TMO` `DHR` | **both 매집, `rs60` +29.3 / +21.0** — the node that gets paid whoever wins the drug |
| 3 | device / equipment | `BDX` `ABT` `MDT` | 매집 / 매집 / 중립 |
| 4 | large-cap pharma | `MRK` `JNJ` `LLY` `ABBV` | mixed; **`ABBV` 중립 with `rs20` −3.7** despite $382B cap |
| 5 | 🚨 **distribution** | `MCK` `COR` `CAH` | **the binding constraint on this thesis** — see below |
| 6 | payer / managed care | `UNH` `HUM` | `HUM` 매집 `rs60` +20.8; `UNH` not in the top 16 |

🚨 **Binding constraint = node 5, distribution — and it is a *policy* constraint, not a demand one.**
Strong demand is not a bottleneck (the L1's own warning). What can actually stop this sector is the
**payer/pricing leg**, and node 5 is where that shows up first: **all three distributors carry
negative `rs20` and `MCK` is the sector's only `OBV 분산`.** The money is leaving the node that sits
between the manufacturer and the payer while it accumulates on both sides of it.

## §5 · Chain-hop — body-proximate, never headline-named

`chain-hop "GLP-1" biotech "drug pricing" --days 7 --scope foreign` (title-count 0 + body proximity ≥2):

| candidate | proximity / body hits | flow cross-check (`asof 08-27`) | verdict |
|---|---|---|---|
| `VRTX` | 7 / 7 | **OBV 매집, `rs60` +27.3, `rs20` +9.7** | 🚫 **Not a chain-hop — it is already in `S133`'s basket.** Recorded and excluded |
| `ABT` | 5 / 11 | OBV 매집, `rs60` +26.8, **`rs20` only +1.7** | ✅ **Genuine candidate**: accumulating, `rs60` high, **`rs20` has NOT caught up** — the tool's own definition of chain-hop alpha. **Flagged to BET as a candidate, not a name** |
| `BMY` | 2 / 8 | OBV 매집, `rs60` +21.4, **`rs20` −0.7** | ✅ Same shape, weaker. Example body: ***"Biotech Stocks Facing FDA Decision In September 2026"*** |
| `WMB` `SPGI` `KO` `WFC` `BLK` `CCEP` | 2–7 | — | 🚫 **Rejected as tool noise** — the example bodies are cross-topic articles (*"Photronics Is Quietly Becoming a Key Winner From the AI Boom"* surfaced under `WMB`). **A news co-mention alone is not a candidate**; none reaches BET |

⚠ **`BMY`'s example body is a dated-catalyst warning, not a bullish one**: *"Biotech Stocks Facing
**FDA Decision In September 2026**"* — which is **`S133`'s VOID condition** (an FDA action with a
named date at ≥2 of the six, inside 08-28 → 09-14). **Handed straight back to the bracket.**

## §6 · Customers and their disclosed spend (rule A6)

**Named**: the buyers of this sector's output are **payers (`UNH`, `HUM`, CMS) and hospital systems**,
not corporates with a capex line. ⚠ **Their disclosed spend is `unknown` from this run's instruments**
(`C3` — marked unknown rather than concluded around): no payer capex/medical-loss-ratio series was
pulled, and `UNH`'s next print is not inside the DEEP window.
✅ **What IS disclosed and dated**: **`LLY`'s Mounjaro won FDA approval for cardiovascular risk
reduction on 2026-08-28 (7 outlets)** — a **label expansion**, which is the mechanism by which this
sector's demand-side actually widens (more covered indications → more reimbursed volume).
🚫 **Not turned into a `LLY` view** — `LLY` is the flipper whose `wflow` this run is barred from using
(G3), and it is absent from the sector's `rs60` leadership (`M1069`).

## §7 · Inherited lead/lag claims (rule A5)

**None carried into this file.** The 08-27 file's structural map is referenced, not re-asserted, and
**no lead/lag relationship is claimed** between discovery, tools and pharma here. Any future claim
that "tools lead pharma" must be tested against a correlation table before it is written — the class
`RESEARCH.md` records as most likely to be repeated on authority alone.

## §8 · Track KPIs and anti-signals — stated as observables

| # | KPI / anti-signal | observable | kills what |
|---|---|---|---|
| 1 | **`S133`** | `EW{REGN,AMGN,TMO,BDX,MRK,VRTX}` exc10 vs **`XLV`**, 08-28 → **09-14**; B ≤ −3.00pp | **the `OW` promotion made today** |
| 2 | breadth persistence | the count of HLTH names with `OBV 매집` on the next settled sweep. **Today: 11 of the top 16.** Falls **below 6** ⇒ the accumulation was a snapshot | `M1068`, the whole §0 answer |
| 3 | **distribution node** | `MCK` `OBV` turning 매집 **with** `rs20` > 0 ⇒ the binding constraint releases and the `OW` strengthens. `COR`/`CAH` joining `MCK` in 분산 ⇒ **the policy leg is tightening and node 5 becomes the sector's kill** | §4's bottleneck |
| 4 | **`L2` peak-margin, not waived** | `MRK` **trades ABOVE its mean target (upside −4.4%)** and is the sector's only prior 🟢. **The multiple has no margin percentile attached in this run** ⇒ per the rule, **no cheapness claim is made about any HLTH name here** | any valuation argument |
| 5 | 🚨 **VOID** | an **FDA action or trial readout with a named date at ≥2 of the six** in `S133`'s window. **Base rate NOT remote**: `LLY` took one on 08-28, the same day's head layer carried three other FDA items, and `chain-hop` surfaced *"Biotech Stocks Facing FDA Decision In September 2026"* | `S133`'s scoreability |

## §9 · Instrument note found while writing this file

🚨 **`M1067`** `[measured]` **`module_chart` and `sector_flow` disagree on OBV for the same name on
the same nominal date, and the cause is measured, not guessed.** `MRK`: sweep says **매집**,
`module_chart --read` says **분배 (20d slope −23%)**. Same for `ANET` (매집 vs 분배 −19%) and `CMCSA`
(매집 vs 중립 −7%); `MPC` agrees (매집 / 누적 +18%). ⇒ **3 of 4 disagree.**
**Cause**: `module_chart` reads the **08-28 phantom bar** (its RSI, Bollinger and 20d-momentum fields
all return `nan`, PREFLIGHT G7), while this run's sweep was **truncated to the 08-27 settled bar**
(`M1059`). **The two tools are reading different terminal bars.**
🚫 **No `module_chart` OBV verdict is cited anywhere in this run's four DEEP files.** `D6` already
grades OBV as a **C-grade signal**; this adds that its two implementations do not currently agree.

## ✅ EXIT CHECK (this file)
- [x] flow → players (large-cap ∪ sub-industry spread) → IR/dated event → chain map (6 nodes, bottleneck marked) → chain-hop (flow-crossed, 6 of 9 rejected as noise) → KPIs + anti-signals.
- [x] **Continuous-track: LED with the delta** (§1); the unchanged structural map is referenced, not reprinted.
- [x] **ROTATION's flagged divergence has an explicit resolution verdict** (§0), and the evidence that would refute it is registered as `S133` **with branch B graded HIGH**.
- [x] Dispersion inside the sector stated (**B5**, §2) — and it passes in the unusual direction.
- [x] `L2` peak-margin applied and **not waived** (§8, KPI 4); no cheapness claim made without a margin percentile.
- [x] **The evidence against this file's own conclusion is written down, not omitted** (§3, `M1070`).
- [x] Not applicable and stated: **no commodity/price-cycle node in this sector**, so no QoQ rate series is required here (see `SECTOR_DEEP_ENRG.md §2` for the run's price-cycle series).
