# BLINDSPOT_PREMORTEM — industry_US · 2026-09-02 (Wed) · Stage 7 / L1·PREMORTEM ★US-only

> Four adversarial lenses argue **against this run's own tilt** before the DEEP budget is committed.
> Inputs: `SECTOR_ROTATION.md` (draft tilt + 2 filled DEEP slots), `SECTOR_FLOW_US_REPAIRED.json`
> (asof **2026-09-01**), `MACRO_REPORT.md §E`, `EVENT_ALPHA.md`, `../CATALYST_WATCH.json`,
> `../CYCLE_EXPOSURE.json`, `preflight/PREFLIGHT.md`. **No sizing (P4).**

## 0 · ⚠ Declared execution constraint — 8th consecutive run

**The four lenses ran IN-CONTEXT and serially, not as a parallel adversarial agent fan-out.** This is
a standing session constraint, not a choice made this run, and it is declared rather than hidden
because it changes what the output is worth: **serial in-context lenses share the author's priors,
so they are weaker adversaries than four independent agents would be.** Every finding below should be
read with that discount. (Recorded in the carry on 08-26 … 09-01; this is the 8th.)

## 0b · The draft tilt these lenses are attacking

`ENRG OW · HLTH OW · MATR N · IT N · STPL N · FIN UW · RE UW · DISC UW · UTIL UW · INDU UW− ·
COMM no-verdict` · **DEEP filled 2 of 4 (`ENRG`, `HLTH`), two rotating slots deliberately empty and
offered to this stage.**

## 0c · The binaries this stage MUST bracket both ways (`CATALYST_WATCH.json`, `--days 10`)

| catalyst | date | ≤48h? | source |
|---|---|---|---|
| **`AVGO` earnings** | **2026-09-02 (D-0, after today's close)** | **YES** | earnings |
| **August Employment / NFP** | **2026-09-04 (D-2)** | **YES** | `bls≈` |
| August PPI | 2026-09-10 (D-8) | no | `bls✓` |
| August CPI | 2026-09-11 (D-9) | no | `bls~est` |
| Iran "Strait of Hormuz open" statement | **undated** | n/a | `[news👁]` — **no date is invented** |

---

## Lens 1 · UNDER-COMPUTED LEGS — what we did not deep-dive that has a catalyst ≤~5 trading days

**Nine of eleven sectors got no DEEP slot.** Four have a dated catalyst inside ~5 trading days.

### 1a · `IT` / semis — **PROMOTE-TO-DEEP (5th slot)**
**The strongest bull case this run refused to make.** ROTATION declined `IT N → UW` on a measured
contradiction and routed it here for a **4th** consecutive run; the promotion mechanism is the whole
point of that routing, so it is exercised rather than deferred a 5th time.

- **Return breadth already fired against the desk**: `S124` **FIRED-A at 41 of 56** from a
  **13/56 = 5.6th-percentile** registration state. That is a settled, pre-registered measurement of
  IT breadth, and it points the opposite way to the flow breadth (**1 green of 55**).
- **The positioning is hedged into the print, not complacent**: `AVGO` P/C **1.62**, short **1.2% of
  float and COVERING**, DTC 3.0; `NVDA` P/C **2.16** with skew **+26.1** — the most put-heavy reading
  on the board. **Crowded fear is squeeze fuel on a beat** (⚠ and never a buy signal alone, `D6`).
- **The memory sub-leg is accumulating while semicap is not**: `SNDK` +0.312 / `rs20` +8.9 and `MU`
  `rs20` +5.8 against `AMAT` **−0.861**, `KLAC` −0.850, `LRCX` −0.842, `TER` −0.878. ⇒ **"IT is weak"
  is a semicap statement, not an IT statement** (`W5`).
- **The book's largest concentration sits here**: `NVDA` **21.3%** + `ANET` **14.6%** = **35.9%** of
  real invested capital.
- Dated catalyst: **tonight**.

**Verdict: PROMOTE-TO-DEEP.** Mandate handed to DEEP: *"Is IT's weakness a semicap-and-hardware
weakness with an accumulating memory/software core, or is it the whole label?"*

### 1b · `UTIL` / AI-power — **PROMOTE-TO-DEEP (6th slot)**
- **UW sectors cannot take a DEEP slot by rule, so this stage is the only path to it** — and the
  structural unreachability has now cost a live question for a **2nd** run.
- **Recency gap 8 runs** — the longest on the board for a 4th consecutive run.
- **The two admissible flow axes disagree and the gap is wide**: Δ **+0.267 (board's 2nd-best)** and
  exc1 **+1.47 vs `SPY` (2nd-best)** against `eqflow` **−0.223 (rank 8)** and **0 greens of 15**.
- **A named, dated falsifier and a named, dated confirmer landed on the same day** (EVENT_ALPHA
  Card 4 / Card 1): the Texas data-center power halt over *"ghost demand"* [**6 outlets**, up from 3]
  against *"Gas Turbine Prices Are on Track to Nearly Triple"* [REIGNITED **5→3→8**], the largest
  geothermal deal ever [6], a $2.2bn Army micro-reactor programme [4], and the California wildfire
  bill's postponement [6, utilities bounced].
- Catalyst: no *dated* event of its own inside 5 days — **stated, not invented.** It is promoted on
  the axis disagreement + the recency gap + the live falsifier, and that reason is written down.

**Verdict: PROMOTE-TO-DEEP.** Mandate: *"Is the AI-power leg an offtake story (`VST`/`CEG`/`NEE`) or
an equipment story (`GEV`/`PWR`/`ETN`) — and does the Texas halt attack the demand denominator or
only the siting?"*

### 1c · `FIN` — **WITHIN-RUN-WATCH** (not promoted)
ROTATION demoted it to **UW today**, and the strongest fact on the board points the other way:
**`hy_oas` 2.63 = the 0.4th percentile of its trailing 252**, `ig_oas` flat at 0.80, `NFCI` easing
9.3 bp over 21 observations. `COIN` +0.689 and `AJG` +0.463 are inside the bucket with `rs60` +12.7
and +17.9. **Demoting a sector into a one-year credit tight is the shape of an error**, and it is
named here rather than argued away. **Not promoted** because both slots are spent on stronger legs
and because it is already double-bracketed: `S134` (09-04) and `P128` (09-08).

### 1d · `INDU` — **WITHIN-RUN-WATCH** (not promoted)
Board-worst on every number (`eqflow` −0.447, Δ −0.222, **31 reds of 50**) — the *bear* case is fully
computed. The **under**-computed leg is the bull one: `RSG` **+0.739 with `vol_surge` 1.13** sits
inside it, and **`HWM`'s `vol_surge` 1.49 is the highest reading in the whole Card-1 exposure set** —
i.e. the bucket is being traded heavily, not abandoned. Bracketed at the NFP by `S126` + `P114`.

---

## Lens 2 · REGIME-FLIP / BOTH-SIDES — the against-us branch of every binary

### 2a · `AVGO`, tonight (D-0) — **the against-us branch, and it is not currently bracketed at SECTOR level**

**Our tilt**: `IT` held at N with a declined demotion; `AVGO` read as **🔴분산, OBV −0.264, `rs60`
−7.4, Δ −0.095** i.e. distributed into its own print; `ANET`/`NVDA` read as distribution + short-build.
**The branch that hurts**: `AVGO` beats and raises AI guidance ⇒ `SMH` gaps, `S124`'s 41/56 breadth
extends, the memory sub-leg confirms, and **three of this run's readings break at once** (the IT
decline, the Card-6 "late money" tag, and the `ANET` EXHAUSTED re-tag in Lens 3).

| existing rows | what they cover | gap |
|---|---|---|
| `S138` (±11.00pp, settles first close after the print) | the **name**, threshold **outside** the implied move | — |
| `S127` (A ≥ +5.928 / B ≤ −4.922, 09-08) | the **name**, 5-session | bands **inside** the implied move |
| `S132` (±9.00pp, 09-03) | the **name** | ★ **pre-declared NO-INFORMATION** (`M1197`) — restated here so a later reader cannot mistake its `C` for evidence (`D451`) |
| — | **the SECTOR** | 🚩 **nothing measures whether the print moves `SMH`** |

★ **Implied-move check, and its honest failure.** `module_flow AVGO --positioning` reads
**예상변동 ±9.5% (expiry 2026-09-02, D0)** — confirming for a **second** run that `S138`'s ±11.00pp
sits **outside** the priced move and `S132`'s ±9.00pp sits **inside** it.
⚠ **But for the new sector row the check cannot be performed**: `SMH`'s only straddle reads
**±0.9% at a D0 expiry that lapses before the observable window opens**, so it bounds today's residual
move, not tomorrow's print reaction. ⇒ **`S141` is registered as `implied-move UNCHECKED`, not as
"outside the implied move"** (`C3` — the column is unknown, not zero). **This is the first row this
desk has had to label that way, and labelling it is the point.**

⇒ **`S141` registered** (§5).

### 2b · August NFP, 09-04 (D-2) — **already both-sided; nothing new is registered** (`D343`)
The against-us branch is symmetric and both halves are covered:

| branch | what rips | who brackets it |
|---|---|---|
| **hot NFP** → hike odds jump | banks / the curve — hits **today's new `FIN UW`** | `S134` (Financials, 09-04) · `P125` (which leg moves, 09-04) |
| **weak NFP** → hike odds collapse | `XLU` + `XLRE` + `XLP` rip **together** — hits three of our UWs at once | `S139` (`EW{XLU,XLRE,XLP}`, 09-04) — ★ registered precisely because `S112` **measured** this pattern (`FIRED-B`, −2.331pp) |
| industrials leg | `XLI` into / out of the print | `S126` (into) · `P114` (out of) |

⚠ **Lens-2's own field note reproduces**: `RE`, `STPL` and `UTIL` are **three UW verdicts that are one
duration bet in disguise** — `S135` (09-11) exists to test exactly whether they are one or three, and
`S112` already fired B on the basket. **The correlated-UW pattern is measured, not hypothesised**, and
this run's `FIN UW` makes it **four** correlated legs if the hike-odds path is the driver.

### 2c · August CPI, 09-11 — **the run's own frame has no equity bracket, and that is the gap**
This run's spine is `P125`: *is the repricing the policy path or inflation?* `P125` settles at the
**NFP (09-04)** on a `[FRED]` observable. **The CPI print on 09-11 — the direct test of the inflation
leg — has no equity-transmission bracket at all**, and the sector it would hit hardest is the one
demoted today.
⇒ **`S142` registered** (§5): `XLF` 3-session excess vs `SPY` at the 09-11 close, i.e. **the
against-us branch of today's `FIN UW`, priced on an inflation print**.
**Information grading (B4)**: A (banks rip) **falsifies today's demotion**; B (banks lag) **confirms
it against the credit-tight counter-argument**. Both branches change a conclusion ⇒ worth bracketing.

### 2d · The undated Hormuz binary — **bracketed, and one row settles tonight**
`S109` (`FRO`) settles on **tonight's** close; `P126` (barrel-EW minus refiner-EW, 09-09) and `S136`
(09-09) both carry it. **No new row** (`D343`).
⚠ **Information grading applied and one candidate DROPPED**: a bracket on `XLE` itself was considered
and **rejected** — with `XLE` exc1/exc5/exc20 all board-best and `eqflow` +0.551, **neither branch of
an `XLE`-vs-`SPY` row would change the `ENRG OW`**. *"If neither branch would change the conclusion,
the event is not worth bracketing"* — the bracket is spent on `S142` instead.

---

## Lens 3 · MOMENTUM-CONTINUATION — "already ran" is not "avoid". Re-tag every runner.

Flow asof **2026-09-01**, repaired file; `rs` vs **`SPY`**.

| name | `rs60` | `rs20` | OBV | surge | Δ | **re-tag** | flip condition |
|---|---:|---:|---:|---:|---:|---|---|
| **`MPC`** (held) | **+42.9** | +23.8 | **+0.425** | 1.09 | −0.005 | **EXTENDED-BUT-LIVE** | crack narrows on two consecutive settled prints while `CL=F` holds |
| **`VLO`** | **+38.2** | +18.5 | +0.210 | **0.87** | +0.122 | **EXTENDED-BUT-LIVE, volume fading** | `vol_surge` < 0.80 with OBV < 0 |
| **`PSX`** (held) | **+34.4** | +23.6 | +0.301 | **0.99** | +0.089 | **EXTENDED-BUT-LIVE** ⚠ misses the 🟢 gate by **0.01** (`C5`) | same as `MPC` |
| **`DASH`** | **+40.6** | +12.7 | **+0.525** (the universe's highest in this set) | 0.83 | −0.034 | **EXTENDED-BUT-LIVE** — **4th consecutive run**, and it sits inside a `UW` bucket ROTATION is barred from deepening | OBV < +0.20 |
| **`ABNB`** | +33.4 | **+23.0** | +0.219 | **1.09** | −0.033 | **EXTENDED-BUT-LIVE** | `rs20` < +8 |
| **`NEM`** | +19.7 | **+26.7** | +0.270 | **1.21** | +0.033 | **EXTENDED-BUT-LIVE**, and `[FINRA]` z **−3.14** (shorts leaving a **rising** name) confirms it | z returns above +1.0 |
| **`CRM`** | +35.7 | **+36.4** | +0.340 | **2.00 — the board's highest** | 0.000 | **EXTENDED-BUT-LIVE on the tape** ⚠ **but it carries a STANDING rejection** (08-31 `B.모멘텀only`, revives 09-30) and **`D463` bars ALPHA from tagging it** | revival needs `rs20 > +20` ✅ **and** a new dated catalyst inside 30 days ❌ |
| **`MSTR`** | +0.4 | **+29.1** | **+0.404** | 1.18 | −0.044 | **EXTENDED-BUT-LIVE on 20 sessions, DEAD on 60** — the two windows disagree | standing `L.vehicle없음` rejection (09-01) |
| **`ANET`** (held, 14.6%) | **+19.4** | **+0.6** | +0.070 | **0.64** | **−0.494** | 🚩 **EXHAUSTED** | `rs20` > +8 **with** `vol_surge` > 1.0 on a settled close |

★ **The lens's finding: only one runner in nine re-tags as EXHAUSTED, and it is a held name at 14.6%
of real invested capital.** `ANET`'s 60-session leadership (+19.4) has decayed to **+0.6 over 20
sessions** while volume fell to **0.64×** and `[FINRA]` short-pressure built to **z +2.25, the board's
largest**. **That is the exact shape the lens exists to separate from "already ran."**
⚠ And the inverse warning applies with equal force: **`DASH` and `ABNB` are EXTENDED-BUT-LIVE for a
4th consecutive run inside a `UW` bucket**, i.e. the desk's *avoidance* is the structural error on
that side, not its enthusiasm.
⚠ **`B3` applied**: none of these is a date-clustered move — they are 20/60-session readings, so the
n≈1 trap does not bite here. Stated because the lens is required to check it.

---

## Lens 4 · CYCLE-EXPOSURE — registry vs coverage vs the REAL book

`cycle_exposure` verdict: **✅ no GAP** — AI-compute epicenter **16.8%** (floor 12.0%, `NVDA`+`ANET`),
Energy/refining **10.3%** (floor 8.0%, `MPC`+`PSX`), missile-defense 3.75% (`RTX`, no floor set).
Real book: **9 US names, $5,154 invested of ≈$10,980 ⇒ 53.1% cash.**

🚩 **The instrument's ✅ is not the whole answer, and three things sit under it:**

1. ★★ **The registry cannot see the cycle this run spent two lenses on.** **AI-power has no registry
   row** (`D416`, **6th run**), so `cycle_exposure` cannot compute a GAP for it. The book's only
   exposure is **`ETN` 7.5%**, tagged *adjacent*, and `ETN` is **🔴분산 with Δ −0.633 (4th-worst in the
   universe)** and `[FINRA]` z **−1.72** — shorts leaving a **falling** name. ⇒ **the desk is 7.5%
   exposed to a cycle it cannot measure, through the layer's weakest name.** **Manually flagged as a
   GAP the instrument is structurally unable to raise.**
2. ★ **The rank-1 cycle's epicenter is half-exhausted.** Of the 16.8%, **`ANET` is 14.6% of invested
   capital and Lens 3 just re-tagged it EXHAUSTED**; `NVDA` (21.3%) is **OBV −0.195 distribution on
   `vol_surge` 1.38** with `[FINRA]` z **+2.18**. **A floor met by two names that are both being
   distributed is a floor met on paper.** ⚠ **This does NOT justify cutting core exposure** — the
   rule is explicit that a 🔴 tape gates ADD *timing* and never justifies zero core in a multi-year
   cycle. It is a **timing** flag, not a **weight** flag, and it is handed to BET as such.
3. ⚠ **16.6% of invested capital carries no thesis at all** — `T` **13.8%** (2nd run) and **`CBRE`
   2.8%** (**newly surfaced this run** in `SWEEP_READ §3`). `T` is **OBV +0.409 매집** with `rs60`
   +11.0; `CBRE` is **🔴분산**. **A position with no thesis cannot be re-justified when its thread
   ends, because it never had one.** Assigning them is a human call (P5).

**Cleanest epicenter expressions, named as the lens requires** (analysis only, no sizing, P4):
AI-compute — `NVDA` (epicenter, distributed), `AVGO` (epicenter, prints tonight, 🔴분산);
Energy/chain — `SLB` **🟢**, `WMB` **🟢** (the only two names on the board that are green *and*
short-covering); Energy/barrel — `FANG` and `EOG` carry **the universe's two largest positive Δ**
(+0.840, +0.693) and are **filed as misses**, not held.

---

## §5 · Brackets registered — both-sided, thresholds frozen, IDs live-scanned

> IDs from `module_evidence next-id` (live scan of `handoff/*.md` · `llm_outputs/**` · `REPORT/**`):
> highest existing `S140` at write time ⇒ this stage takes **`S141`**, **`S142`**.
> Both are written into `handoff/SCENARIOS_US.md` **and** indexed in the `handoff/SCENARIOS.md` spine.

### `S141` — ★★★ Does the `AVGO` print move the SECTOR? (the D-0 binary's sector leg)

| | |
|---|---|
| **Frozen observable** | `SMH` **1-session excess return vs `SPY`**, settled closes, **2026-09-02 close → 2026-09-03 close** |
| **Branch A (the print re-rates semis)** | **≥ +1.802pp** (trailing-252 **p85**) — `S124`'s breadth repair extends, IT's decline this run was wrong, and Card 6's "late money" tag breaks |
| **Branch B (the print de-rates semis)** | **≤ −1.732pp** (trailing-252 **p15**) — the distribution reading was right and `IT N` was too generous |
| **Branch C** | between |
| **`D93` before freezing** | trailing 252 of `SMH` exc1 vs `SPY`: mean **+0.212** · sd **1.911** · p05 −3.249 · **p15 −1.732** · p50 +0.367 · **p85 +1.802** · p95 +3.418 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **−1.363 = the 17.9th percentile** (09-01 settled) — **just outside branch B**, so B is nearer and A is farther. Disclosed |
| ★ **Implied-move check — UNCHECKED, and why** | `AVGO` reads **±9.5% (expiry 09-02, D0)**, which confirms `S138` is outside and `S132` is inside. **`SMH`'s only straddle is ±0.9% at a D0 expiry that lapses before this row's window opens**, so it bounds today's residual move rather than the print reaction. **This row is therefore labelled `implied-move UNCHECKED` rather than "outside the implied move"** (`C3`) |
| **Anti-signal (VOID)** | a market-wide halt, or a dated US export-control announcement on advanced semiconductors, inside the window. ⚠ **Base-rate note**: the 09-01 KR run recorded a *near-miss* of exactly this class (a tungsten/black-mass export action on a different product class, and a **draft** rule reported by tomshardware) — **neither would void this row**, and saying so now prevents an improvised void at scoring |
| **Information content (L3)** | **A and B both change a conclusion**; C does not and is the disclosed favourite. **A is the informative branch** because the registration state already sits near B |
| **Owner** | `industry_US` |

### `S142` — ★★ The against-us branch of today's `FIN UW`, on the inflation print

| | |
|---|---|
| **Frozen observable** | `XLF` **3-session excess return vs `SPY`**, settled closes, at the **2026-09-11** close (August CPI) |
| **Branch A (banks rip — the demotion was wrong)** | **≥ +1.315pp** (trailing-252 **p85**) — a hot print steepens/repricings pays the bucket ROTATION demoted today, and `hy_oas`'s 0.4th-percentile tight was the signal the flow axes could not see |
| **Branch B (banks lag — the demotion holds)** | **≤ −1.516pp** (trailing-252 **p15**) — `eqflow` −0.220, Δ −0.220 and 0 greens of 47 were right, and the credit tight was not information |
| **Branch C** | between |
| **`D93` before freezing** | trailing 252 of `XLF` exc3 vs `SPY`: mean **−0.118** · sd **1.397** · p05 −2.329 · **p15 −1.516** · p50 −0.175 · **p85 +1.315** · p95 +2.071 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **+0.034 = the 56.7th percentile** — ★ **dead centre; the most balanced starting state this desk has registered in weeks**, and therefore the most informative of the two rows written today |
| **Implied-move check** | `XLF` reads **±0.4% (expiry 09-02, D0)** — again a straddle that lapses before the window. **Labelled `implied-move UNCHECKED`.** ⚠ Note the qualitative reading it *does* carry: P/C **0.04** = *"안일 (연료 적음)"*, i.e. almost no put protection in the bucket the desk just demoted |
| **Anti-signal (VOID)** | an unscheduled Federal Reserve action, a bank-specific failure or emergency facility, or a CPI release-date change, inside 09-08 → 09-11 |
| **Information content (L3)** | **A falsifies a verdict this run just made; B defends it against the single strongest counter-argument on the board.** Both change a conclusion ⇒ the bracket is spent here rather than on `XLE`, which was considered and **dropped because neither of its branches would have changed `ENRG OW`** |
| **Owner** | `industry_US` |

### Brackets considered and NOT registered, with the reason
- **`XLE` vs `SPY`** — dropped: `XLE` is board-best on exc1/exc5/exc20 with `eqflow` +0.551;
  **neither branch would change `ENRG OW`** (B4).
- **A 4th `AVGO` name row** — dropped: `S138`/`S127`/`S132` already point at the name (`D343`).
  The gap was the **sector**, and `S141` fills it.
- **An NFP row** — dropped: `S126`/`S134`/`S139`/`P114`/`P121`/`P125` already bracket it from six
  directions, and `S139` was itself written from a **measured** correlated-UW pattern.

---

## §6 · Outputs handed forward

| to | what |
|---|---|
| **DEEP** | **Final DEEP set = 4 of 4: `ENRG` · `HLTH` (from ROTATION) + `IT` · `UTIL` (promoted here).** Mandates written in Lens 1a / 1b |
| **ALPHA** | `S141` and `S142` as action brackets; **`AVGO` D-0 and NFP D-2 are both dated binaries inside the window** — ⚠ `D294` has reproduced **6 times** (the tool names the binary and then prints "no dated binary in window"); if it does so again, the tickets are written by hand and the reproduction is logged |
| **BET** | the cycle flag: **AI-power exposure is 7.5% through `ETN`, a 🔴분산 name with Δ −0.633, inside a cycle the registry cannot measure** — a **timing** flag, never a weight cut (the rule is explicit) · `ANET` **EXHAUSTED** re-tag · `DASH`/`ABNB` EXTENDED-BUT-LIVE inside a barred `UW` |
| **book desk** | 16.6% of real invested capital (`T` 13.8% + `CBRE` 2.8%) carries **no thesis** — human call (P5) |

## ✅ EXIT CHECK
- [x] **4 lenses run, each returning named tickers + dated catalysts** — ⚠ **serially and in-context,
      not as a parallel agent fan-out; declared in §0, 8th consecutive run**, with the discount that
      implies stated rather than implied.
- [x] **Every bracket names its observable + frozen threshold + date and carries BOTH branches.**
      `S141` and `S142` are registered in `handoff/SCENARIOS_US.md` and indexed in the
      `handoff/SCENARIOS.md` spine. **No one-way bracket exists in this run**, and both ≤48h binaries
      (`AVGO` D-0, NFP D-2) carry both-sided coverage — `AVGO` at the name level (`S138`, outside the
      measured ±9.5%) **and now at the sector level** (`S141`), NFP from six directions.
- [x] **Every magnitude threshold stated against the implied move, including where the check FAILS.**
      `S138` ±11.00pp **outside** ±9.5%; `S132` ±9.00pp **inside** ⇒ restated as **NO-INFORMATION**
      (`D451`); `S141`/`S142` labelled **`implied-move UNCHECKED`** because the only straddles
      available expire **before** their windows open (`D±n` read, not ignored).
- [x] **Each branch graded by information content**, and **one candidate bracket was dropped for
      failing that grade** (`XLE` — neither branch would change `ENRG OW`), with the reason written.
- [x] `BLINDSPOT_PREMORTEM.md` written — legs · brackets · re-tags · cycle GAP.
- [x] **Every catalyst-bearing leg promoted or logged**: `IT` and `UTIL` **PROMOTED**; `FIN` and
      `INDU` logged as WITHIN-RUN-WATCH with their brackets named. **Nothing silently dropped.**
- [x] **DEEP set updated and stated: `ENRG` · `HLTH` · `IT` · `UTIL` — 4 of 4, the budget now full.**
- [x] No sizing anywhere (P4).
