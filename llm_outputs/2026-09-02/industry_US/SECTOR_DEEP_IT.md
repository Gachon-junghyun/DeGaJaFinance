# SECTOR_DEEP_IT — Information Technology · industry_US · 2026-09-02 (Wed) · Stage 8 / L1·DEEP · ★ **PREMORTEM-PROMOTED (slot 3 of 4) — DELTA-LED, narrow disjoint mandate**

> Deep-dived **2026-09-01 (0 runs ago)** ⇒ **delta-led**; structure carried by reference to
> `llm_outputs/2026-09-01/industry_US/SECTOR_DEEP_IT.md`.
> Promoted here because ROTATION **declined** `IT N → UW` for a **4th** consecutive run on a measured
> contradiction it is barred from resolving. Flow asof **2026-09-01**. Benchmark **`SPY`** inline.
> **No sizing (P4).** ⚠ `AVGO` prints **tonight** — every number below predates it.

## 0 · Mandate from PREMORTEM (Lens 1a) — deliberately narrow and disjoint from ROTATION's
**"Is IT's weakness a semicap-and-hardware weakness with an accumulating memory/software core, or is
it the whole label?"**
The contradiction that produced the mandate: **flow breadth 1 green of 55** against **`S124`'s settled
return breadth 41 of 56** (`FIRED-A`, 08-31, from a 13/56 = 5.6th-percentile registration state).

## ★ 1 · THE ANSWER — it is not the whole label, and the split is 1.28 wide

**`M1251` [measured] — the IT bucket splits cleanly in two, and the gap between the halves is larger
than the gap between most whole sectors.**

| sub-node | n | EW flow | EW OBV | EW `rs20` vs `SPY` | EW Δ | greens |
|---|---:|---:|---:|---:|---:|---:|
| **software / services / security** | 21 | **+0.092** | **+0.042** | **+4.73** | −0.204 | **1** |
| memory / storage | 4 | −0.206 | −0.056 | −1.03 | −0.127 | 0 |
| hardware / EMS | 4 | −0.306 | −0.086 | −3.50 | −0.241 | 0 |
| networking / optical | 7 | −0.370 | −0.019 | −5.83 | −0.359 | 0 |
| **semiconductors** | 13 | **−0.507** | −0.119 | −6.71 | −0.237 | 0 |
| **semicap / test** | 6 | **−0.759** | **−0.221** | **−9.82** | **−0.374** | 0 |

★ **Software +0.092 vs semicap −0.759 = a 0.851 gap; software vs semis = 0.599.** The whole sector's
`eqflow` is **−0.252**, which is a weighted average of a **positive** node and a **deeply negative**
one. ⇒ **"IT is weak" is a semicap-and-semis statement.** The 21-name software node is **positive on
flow, positive on OBV, and beating `SPY` by 4.73 over 20 sessions.**

**`M1252` [measured] — the dispersion is the widest on the board and it decisively exceeds the sector
move.** Spread **+1.000 (`CRM`) to −0.878 (`TER`) = 1.878** against `XLK` exc5 **+1.58** / exc20
**−0.51 vs `SPY`**. ⇒ **`B5` fires hardest here: the sector label is the wrong unit of analysis, and
this file says so.** `XLK` (+1.58) and `SMH` (−1.37) disagree by **2.95pp on exc5** and **3.55pp on
exc20** — the two ETFs are measuring the two halves above.

⇒ **This resolves the flow-vs-return breadth contradiction without needing `vol_surge`'s IC**:
`S124`'s 41-of-56 counted **positive 5-session excess return**, which the software node supplies;
the flow breadth counted **all three price axes positive simultaneously**, which the surge axis
denies to almost everyone (universe median `vol_surge` **0.81**, only 54 of 298 above 1.0).
**They were never measuring the same thing, and neither is wrong.**

## 2 · The name the desk holds most of, and what its own instruments say

| | `NVDA` (real book **21.3%**) | `ANET` (real book **14.6%**) | `AVGO` (paper book; prints tonight) |
|---|---|---|---|
| flow / tag | +0.036 🟡 | −0.029 🟡 | **−0.711 🔴분산** |
| OBV · `rs20` · `rs60` · surge | **−0.195** · +3.8 · +2.7 · **1.38** | +0.070 · **+0.6** · +19.4 · **0.64** | **−0.264** · −10.4 · −7.4 · 0.92 |
| Δ | −0.210 | **−0.494** | −0.095 |
| `[FINRA]` short z (09-01) | **+2.18 🔴** | **+2.25 🔴 (board's largest)** | +0.79, 5v5 **+3.8▲** |
| `module_chart --read` | PULLBACK-TO-SUPPORT · **bearish divergence (price HH, RSI LH)** · OBV **−89%** · coiling 9.6% | — | **FALLING-KNIFE (진입 금지)** · **RSI 24.5** · **0/4 MA** · OBV **−84%** |
| fwd P/E · 90d next-yr estimate Δ · target | 14.43 · **+22.2%** · **+46.7%** | 36.11 · **+16.0%** · +29.8% | **18.82** · **+6.0%** · **+42.8%** |
| price vs 52w high | −6.0% | −13.3% | **−25.6%** |

★★ **`M1253` [measured] — this sector is the exact inverse of Energy on the same fact.** In Energy,
estimates revised up **48–82%** sit on prices **at** 52-week highs with **consensus targets BELOW the
price**. In IT, estimates revised up **6–22%** sit on prices **6–26% below** their highs with
**consensus targets 30–47% ABOVE** the price. **The same "estimates rising" input is priced in
opposite directions in the two sectors this run deep-dived.** ⇒ **lens `B2`'s peak-margin trap loads
in Energy and does not load here** — and that is a *measurement of where the trap is*, not a
preference between the sectors.
⚠ **But `D395`/`D428` bind**: the estimate-revision table is **a description of the denominator's
direction, not a leading indicator**, and it carries **no independent weight on an IT name** — the
2026-08-09 control measured the whole revision effect as an **IT loading** (ex-IT Q5−Q1 **−1.1pp**).
**So the revision numbers above are used to say what the denominator is doing and for nothing else.**

## 3 · `AVGO` — the primary source finally opened, and the answer is "nothing"
🚨 **`M1254` [measured] — `module_disclosure_us AVGO --days 45` returns 0 filings, 0 8-Ks.**
The desk carried *"`AVGO`'s contracted share is `unknown`, the module was never opened"* as an open
gap for **two** runs. **It is opened now and the gap does not close — it converts**: the contracted
share is `unknown` **because there is nothing filed to read**, not because nobody looked (`C3`).
⇒ **Any claim about `AVGO`'s contracted AI revenue tonight is press-sourced, not filing-sourced**, and
this file says so before the print rather than after.

**The positioning into the print, for the record**: `module_flow AVGO --positioning` reads short
**1.2% of float, COVERING**, DTC 3.0, **P/C 1.62**, skew −0.8, **예상변동 ±9.5% (expiry 09-02, D0)**.
⇒ hedged and put-heavy, not complacent. **Crowded fear is squeeze fuel on a beat and never a buy
signal alone (`D6`).** `S138`'s ±11.00pp is **outside** that implied move; `S132`'s ±9.00pp is
**inside** it and is **restated as pre-declared NO-INFORMATION** (`D451`).

## 4 · Memory — the QoQ rate-of-change and the contractual band (B1 + the 2026-08-10 escape hatch)
- `MU` flow **−0.031**, OBV −0.016, `rs20` +5.8, **`vol_surge` 0.57 (the node's lowest)**, Δ −0.301;
  `SNDK` +0.312 / `rs20` +8.9; `WDC` **−0.844** / `rs60` −15.3; `STX` −0.263.
  ⇒ **the memory node is not one bet either** — spread 1.156 across four names.
- **`B1` applied**: the inherited contract-price QoQ series is **+90~95% → +58~63% → +13~18%**, i.e.
  two consecutive declines in the *rate*.
- 🚨 **And the escape hatch is applied before the second derivative is read as demand.** The
  2026-08-10 reading of `MU`'s FY26Q3 10-Q found **take-or-pay agreements with binding multi-year
  volumes, a ceiling at ~the 2Q CY2026 market price and a floor for the term**. ⇒ **a decelerating
  QoQ rate inside a floor/ceiling band is arithmetic hitting a cap, not demand weakening**, and this
  file does **not** read it as a demand signal. ⚠ **The contracted share is still `unknown`** — the
  10-Q was read for the *terms* but the *share* was never quantified (`C3`, carried).
- `MU` fwd **6.06×** on a next-year estimate revised **+50.9% in 90 days** — the canonical `B2` shape,
  **but the band above is exactly the escape hatch `B2` requires**, so the trap is **checked and does
  not cleanly apply**. `R111`'s floor/ceiling band is on record and is honoured.

## 5 · Customers named and their spend checked (rule A6)
- **Semicap's customers are the foundries and IDMs**, and their own flow is the node above:
  `TSM` is **not in `us_top300`** (`N.유니버스부재`, a standing gap), `INTC` −0.397, `MU` −0.031,
  `ASML` −0.500 (itself semicap). ⇒ **the buyer side of the semicap node is unmeasurable on this
  desk's universe**, and that is stated rather than concluded around.
- **Software's customer is enterprise IT budget**, and the node's own prints corroborate: `CRM`
  current-yr estimate **+14.0% in 90 days**; `NOW` +0.689 flow with `rs60` +23.8; `ADBE` +0.594 with
  OBV +0.312. ⇒ the demand side of the positive node is confirmed from three independent names.
- **`NVDA`'s customers signed dated cheques this week**: *"Anthropic signs $35 billion cloud deal with
  Nvidia-backed Lambda"* [5 outlets] and *"Nvidia pours $3.5 billion into MediaTek — NVLink Fusion"*
  [6 outlets]. ⇒ **buyer spend is disclosed and rising**, which is why `NVDA` is 🟡 and not 🔴 despite
  OBV −0.195.

## 6 · Chain-hop and the frame-transfer question
- `chain-hop` on the AI-power/turbine terms (run at EVENT_ALPHA) surfaced **no un-named IT
  beneficiary** inside the universe; the body-proximate names were `GOOGL`/`GOOG` (from an unrelated
  pollution article) and `BRK-B`. **Nothing promoted.**
- **Frame transfer, answered**: the **take-or-pay / contracted-floor** frame this desk trusts in
  midstream and merchant power **does apply here — to `MU`'s customer agreements** (§4), and it was
  the 2026-08-10 finding that *"zero of 21 files applied it to memory."* ⇒ **it is applied here for a
  second run.** It does **not** apply to `AVGO` on the evidence available, because **there is nothing
  filed to read** (§3). *"Checked, does not apply"* is the pass; not asking is not.

## 7 · Bottleneck, KPIs, anti-signals
- **Binding constraint: participation, not price.** Universe-wide `vol_surge` median **0.81**, 54 of
  298 above 1.0; IT's own greens fell to 1 of 55 while its return breadth is 41 of 56. **The bucket
  is up on price and down on participation** — `M1208`'s reading, replicated on a fresh session.
- **Track KPIs**: (i) `SMH` − `XLK` exc5 gap (now **2.95pp**) — if the split is real it widens;
  (ii) `ANET` `rs20` (now **+0.6** from a `rs60` of +19.4) — the EXHAUSTED re-tag's own test;
  (iii) `NVDA`'s bearish RSI divergence resolving up or down; (iv) `S140` (does IT's breadth HOLD)
  settles **09-08**; (v) `S141` (does the print move `SMH`) settles **09-03**.
- **Anti-signals**: a dated US export-control announcement on advanced semiconductors (⚠ **base rate
  pre-stated**: the recent tungsten/black-mass action and the *draft* rule reported by tomshardware
  are **different product classes and not voiders**); an `AVGO` guidance cut that takes the software
  node down with it (which would falsify §1's split).
- ⚠ **Lead/lag tested, not inherited**: the "EDA leads semis by 12–18 months" claim is **measured and
  dead** — 199 monthly observations gave same-month **+0.63** vs lag-12 **+0.05** ⇒ coincident.
  `SNPS` (−0.626 Δ) and `CDNS` (−0.454 Δ) are therefore read as **coincident** with the semis node,
  not as leading it.

## 8 · Verdict on the mandate
**Not the whole label.** IT is **a positive 21-name software node (+0.092 flow, +4.73 `rs20`, the
sector's only green) inside a deeply negative semis-and-semicap node (−0.507 / −0.759)**, with a
1.878 dispersion that exceeds the sector's own move. **ROTATION's decline was right for the wrong
reason** — it declined for lack of an IC on `vol_surge`; the actual reason a sector demotion is wrong
here is that **the bucket is two bets and a demotion would carry the positive one with it.**
⇒ **Handed back to ROTATION for the next run: the verdict unit for IT should be the sub-node, not the
sector.** ⚠ Every number above predates tonight's `AVGO` print; `S141` and `S138` are the rows that
will say whether it survives.

---
> Refs: `SECTOR_FLOW_US_REPAIRED.json` · `module_fundamentals_us NVDA|AVGO|ANET|MU|CRM` ·
> `module_disclosure_us AVGO` (**0 filings/45d**) · `module_flow AVGO --positioning` ·
> `module_chart --read NVDA|AVGO` · `us_flow` FINRA 09-01 · prior structure
> `llm_outputs/2026-09-01/industry_US/SECTOR_DEEP_IT.md`.
