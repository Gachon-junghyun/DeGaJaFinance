# SECTOR_DEEP_ENRG — Energy · industry_US · 2026-09-02 (Wed) · Stage 8 / L1·DEEP · **CONTINUOUS TRACK (C1) — DELTA-LED**

> Deep-dived **2026-09-01 (0 runs ago)** ⇒ **this file leads with the delta.** Unchanged structure —
> the 5-node chain map, the take-or-pay frame, the player union — is carried **by reference** to
> `llm_outputs/2026-09-01/industry_US/SECTOR_DEEP_ENRG.md` and is not reprinted.
> Flow: `SECTOR_FLOW_US_REPAIRED.json`, asof **2026-09-01**, 3-axis `nonews`. Benchmark **`SPY`**
> named inline. **No sizing (P4).**

## 0 · Mandate from ROTATION — one question
**"Energy is OW on every admissible number. Is the escalation now paying the BARREL, or is the OW
still a CHAIN-POSITION bet?"** (`P126`, settles 09-09; `S136`, 09-09; `M1201`/`M1202` are the
inherited claim under test.)

## ★ 1 · THE DELTA — and it answers the mandate

**`M1245` [measured] — the barrel leg turned on Δ while the chain leg still leads on level, and the
two orderings are now opposite.**

| sub-node | n | EW flow | EW OBV | EW `rs20` vs `SPY` | **EW Δ (08-31 → 09-01)** |
|---|---:|---:|---:|---:|---:|
| **refining** (`MPC`,`PSX`,`VLO`) | 3 | **+0.657** | **+0.312** | **+21.97** | **+0.069** |
| services (`SLB`,`BKR`) | 2 | +0.593 | +0.223 | +9.05 | **+0.050** |
| midstream (`WMB`,`OKE`,`KMI`,`TRGP`) | 4 | +0.565 | +0.137 | +8.53 | +0.272 |
| **barrel** (`XOM`,`CVX`,`COP`,`EOG`,`FANG`,`DVN`,`OXY`) | 7 | +0.485 | +0.191 | +10.44 | **+0.377** |

★ **On LEVEL the order is refining > services > midstream > barrel — the inherited `M879`/`M1201`
ordering, replicated a 6th time. On Δ it is exactly reversed: barrel > midstream > refining >
services.** The universe's **two largest positive Δ of all 298 names are `FANG` +0.840 and `EOG`
+0.693**, and `XOM` is +0.536 — all three barrel names. `MPC` is **−0.005 (flat)** and `BKR` −0.005.

⇒ **Verdict on the mandate: the OW's centre of gravity is MOVING upstream, and it has not arrived.**
The chain still owns the level; the barrel owns the change. **`P126` is registered on exactly this
and it is the honest place to leave it** — a one-session Δ used as a **direction, not a magnitude**
(PREFLIGHT G2), on a baseline that is yesterday's holed primary.

**`M1246` [measured] — the sector is uniform in a way nothing else on the board is: 16 of 16 names
carry positive flow and there are ZERO reds.** No other sector on the board has zero red names.
Dispersion +0.894 (`SLB`) to +0.287 (`XOM`) = **0.607**, i.e. **smaller than the sector's own 20-day
move vs `SPY` (`XLE` exc20 +11.92)** ⇒ **for once the sector label IS the right unit of analysis**
(`B5`, and this is the only DEEP file this run where that is true).

## 2 · What changed in the fundamentals since 09-01 — the peak-margin trap deepened

`module_fundamentals_us`, 2026-09-02:

| name | fwd P/E | 90d current-yr estimate Δ | price vs 52w high | **consensus target vs price** |
|---|---:|---:|---:|---:|
| **`MPC`** (held) | **11.93** | **+81.9%** | **−0.4%** | **−16.1% BELOW** |
| **`PSX`** (held) | **12.31** | **+48.2%** | **−0.25%** | **−12.8% BELOW** |
| `XOM` | 15.33 | +5.7% | −7.5% | +4.0% above |
| `SLB` | 17.70 | **−4.2%** | −5.5% | +8.5% above |

★ **`M1247` [measured] — lens `B2` is now unambiguous on the two held refiners, and the gap WIDENED.**
Both sit within **0.4%** of their 52-week highs on estimate lines revised up **48–82% in 90 days**,
and **both carry consensus targets BELOW the live price** (`MPC` −16.1%, wider than 09-01's −13.9%;
`PSX` −12.8%). **A forward multiple of 11.9× on a denominator revised up 82% is not cheapness — it is
a peaking denominator**, and the desk's own record already establishes that **refining has no
take-or-pay** (`M831`).

### 🚨 The frame-transfer question, answered rather than skipped
**Frame this desk trusts elsewhere: take-or-pay / contracted-volume floors** (applied well to `KMI`'s
$35.67B RPO, `LNG`'s tolling, `VST`'s 20-year PPA, and — after 2026-08-10 — to `MU`'s multi-year
customer agreements).
**Does it apply to refining? Checked: NO, and the check is on record.** `M831` established that
refining margin is a **spot crack**, with no contracted floor and no ceiling. ⇒ **lens `B2` has no
escape hatch on `MPC`/`PSX`**, which is the opposite of the `MU` case where the filing supplied one.
**Does it apply to midstream? YES** — and that is where the frame points next: `WMB`'s and `OKE`'s
revenue is fee-based/contracted, which is why they can carry a 🟢 tag on a **+6.4 / +10.4** `rs20`
rather than on a `+23.8` one. **The frame is aimed at a second target this run rather than at one.**

## 3 · Contract terms read from the filing, not the press
- **`SLB`** — `module_disclosure_us SLB --days 45`: **18 filings**, including an **8-K Item 7.01
  (Reg FD / guidance) dated 2026-08-31** — *the exact session the name ignited* — plus an 8-K Item
  2.02 (07-24 results), a 10-Q, **4 Form 4s and 4 Rule 144 notices**. ⚠ **The Rule 144s are notices
  of intent to sell; share counts were not read** — carried as `unknown` (`C3`), 2nd run.
- **`AVGO`-style gap closed here**: the primary source was opened for the sector's ignition name and
  the guidance 8-K is dated **inside** the ignition window. That is a dated, primary-source fact, not
  a press inference.
- ⚠ **`MPC`'s Q2 revenue +52.0% QoQ remains unattributed for a 2nd run** — the 10-Q line was not
  opened. **Named again as the sector's largest unexplained number**, not concluded around.

## 4 · Customers named and their spend checked (rule A6)
- **Refining's customer is the crack, not a company** — so the "customer" check is the **product
  side**: `refining margin` runs **⚪ECHO at 0.66× on a base of 302** (`theme-age`, 90d/foreign,
  direct query outside the sweep window), **decelerating for a 2nd consecutive run**, while
  `Strait of Hormuz` runs **+9.0%** and `crude oil` **+4.0%** week on week. ⇒ **the margin story is
  not the crowded one; the barrel story is.**
- **Services' customers are the E&Ps**, and their capex direction is visible in the estimate line:
  `XOM` **+5.7%**, `EOG`/`FANG` carry the universe's two largest flow Δ. ⇒ **`SLB`'s own estimates
  being cut −4.2% while its customers' Δ leads the board is the sharpest internal contradiction in
  this sector** — recorded, and it is why `SLB` is filed 🟡PARTIAL rather than as a clean lead.
- **Midstream's customers are contracted shippers** — fee-based, so their spot spend is not the
  binding variable (the take-or-pay frame, §2).

## 5 · Chain-hop, flow cross-checked
`chain-hop` on the Hormuz terms returns `CVX` (6 proximity / 12 body) and `WMB` (1 / 4) as
body-proximate names, both already in the universe and both already accumulating (`CVX` OBV +0.342
with `rs20` +12.1 vs `SPY`; `WMB` +0.181 with `rs20` +6.4 and `vol_surge` 1.31) ⇒ **no un-named
beneficiary was surfaced inside the universe.** **RULE D6 — the OBV figures here are paired with
`rs20` and `vol_surge` and no proposition rests on OBV alone.**
🚩 **The two names the body-read DID identify are outside it**: `FRO` (freight / ton-mile) and `LNG`
(the gas leg — *"LNG Exports Through Hormuz Remain Stalled Even as Oil Flows Rebound"* [bloomberg
08-31] against *"more than 17 million barrels transited Hormuz on Monday"* [cnbc 09-02]).
**Both filed to `missed_ledger` as `N.유니버스부재`.** `LNG` has now been untaggable for 10+ runs.

## 6 · Bottleneck, KPIs, anti-signals
- **Binding constraint: transit distance and insurance, not barrels.** The oil flow is being restored
  under naval escort while **tanker transits remain below normal** and **LNG force majeure persists**.
  ⇒ the constraint has moved from *volume* to *ton-mile and gas-specific routing*, and **the desk's
  instruments cannot price either** (§5).
- **Track KPIs**: (i) the 3-2-1 crack on settled prints; (ii) `CL=F` holding **$88**; (iii) `refining
  margin` theme-age leaving ⚪ECHO; (iv) `[FINRA]` `MPC` z (now **+1.49**, 5v5 **+8.6▲** — shorts
  pressing the winner) vs `XOM` 5v5 **−11.3▼** (pressure leaving the laggard).
- **Anti-signals (what kills it, as observables)**: an OPEC+ emergency production decision; a US SPR
  release; a dated Iranian "Strait open" statement (`catalyst_calendar`, **undated — no date invented**);
  two consecutive negative 5-session crack rates on settled bars **with** `CL=F` above $88.
- ⚠ **Lead/lag**: no inherited lead/lag claim is used in this file. The "crack leads the refiner
  equity" relation is **[unverified]** — it has not been measured by this desk — and is tagged rather
  than asserted (`A5`).

## 7 · Sub-node dispersion (B5) — stated, and it does NOT exceed the sector move
Spread **0.607** across 16 names vs `XLE` exc20 **+11.92 vs `SPY`**. ⇒ **the sector label is the right
unit here.** ★ This is the only one of this run's four DEEP sectors where that holds — `HLTH` (1.583)
and `IT` (1.878) both fail it.

## 8 · Verdict
**`ENRG OW` stands, and the mandate is answered with a moving target rather than a static one:**
the OW is **still chain-positioned on level** and **turning barrel-ward on Δ**. `P126` (09-09) is the
row that settles which. **The peak-margin trap on the two held refiners deepened this run** (targets
now 12.8–16.1% below price on 48–82% upward revisions) and **has no contractual escape hatch** —
that is handed to BET as a *quality-of-the-leg* fact, not as a size instruction (P4).

---
> Refs: `SECTOR_FLOW_US_REPAIRED.json` · `EVENT_ALPHA.md` Card 2 · `MACRO_REPORT.md` §C, §E ·
> prior structure `llm_outputs/2026-09-01/industry_US/SECTOR_DEEP_ENRG.md`.
