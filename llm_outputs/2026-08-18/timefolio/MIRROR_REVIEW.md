# TIMEFOLIO MIRROR — 2026-08-18 (화) 10:35~10:40 KST · **집행 보류, 사유 3건**

> **주문 0건 제출. `--execute` 0회.** 드라이런 + 검증만 했다.
> CDP: Chrome **9222 LISTENING**(PID 16436, 전용 프로필 `chrome-debug-carmacro`) — 내가 띄웠다.

## 1. 계좌 상태 (읽기)

| 항목 | 값 |
|---|---|
| NAV | **988.587** · 수익률 **+0.78%** |
| 순위 | **378위 / 2,048명** |
| **주간 회전율** | **0.0%** (콘테스트 한도 대비 여유) |
| **누적 위반** | 🚨 **1건** |
| 지수 대비 | KOSPI 당일 +2.32% · **기간 −14.01% · 대비 +12.87%** |

## 2. ✅ holdings 파서 검증 — **2026-07-06 사고(빈 `{}` 반환)는 오늘 재발하지 않았다**

메모리 규칙(`feedback_timefolio_verify_holdings_before_execute`)대로 **집행 전에 먼저 검증**했다.
스크린샷 대조보다 강한 방법을 썼다 — **독립적으로 파싱된 세 수량의 정합성**:

| 검증 | 계산 | 결과 |
|---|---|---|
| 평가금액 합 | 19종 합 = **67,425만원** | |
| 비중 합 | **68.10%** | |
| ⇒ 총자산 역산 | 67,425 ÷ 0.681 = **99,009만원** | |
| NAV 텍스트(별도 파싱) | 988.587백만 = **98,859만원** | |
| **비율** | **1.0015** | ✅ **정상** (0.15% 차이는 비중 표시가 0.1% 단위로 반올림된 탓) |

⇒ **보유 19종(투자 68.1% · 현금 31.9%)은 신뢰할 수 있다.** 파서 버그 시그니처(빈 dict) 없음.
잔량 3종(016360 삼성증권 18만 · 042700 한미반도체 24만 · 078930 GS 24만)은 **비중 0.00% 먼지**다.

## 3. 🚨🚨 집행 보류 사유 ① — **노출 규칙이 자기 자신과 모순된다 (20pp 차이)**

같은 스크립트·같은 분·같은 벤치인데 **두 서브커맨드가 다른 답을 준다:**

| 호출 | 시각 | 상태 | **목표 투자비중** | 근거 |
|---|---|---|---|---|
| `exposure_rule.py **state**` | 10:37:14 | **정상** | **95%** | *"⚠전이 보류(정상→과열) — **최소유지 2/3세션**"* |
| `exposure_rule.py **target --json**` | 10:38 | **과열** | **75%** | *"20일MA 대비 +10.2% ≥ 10.0"* — **그게 `why` 의 전부다** |

**`module_timefolio/_sync.py:100` 이 호출하는 것은 `target` 이다.**
⇒ **미러 데스크는 휩소 가드(`min_hold_sessions: 3`)를 우회하는 쪽 서브커맨드에 물려 있다.**

★ **그리고 그 가드는 정확히 이 계좌를 위해 존재한다.** `data/exposure_bands.json` 의 자체 주석:
> *"이 값이 0이면 '공격' 밴드는 120세션에 전이 36회·누적 회전 **1040pp = 주당 43.3pp** 를 노출 규칙 **혼자서** 쓴다.
> 콘테스트의 그 주 회전율이 26.9%, 누적 위반 1건이므로 **종목 매매를 하기도 전에 규정을 깬다.**"*

**이 계좌는 이미 누적 위반 1건이다.** ⇒ **가드를 우회한 타깃으로 15건을 제출하는 것은 규정 위반 경로에 직접 올라타는 것이다.**

## 4. 🚨 집행 보류 사유 ② — **그 「과열」은 미완봉 위에서 임계선을 0.2pp 차로 넘나든다**

| 시각 | 20일MA 대비 | 임계 | 판정 |
|---|---|---|---|
| 09:47 | **+11.0%** | 10.0 | 초과 |
| 10:37 | **+10.1%** | 10.0 | 초과 (마진 0.1pp) |
| 10:38 | **+10.2%** | 10.0 | 초과 (마진 0.2pp) |

**입력은 08-18 장중 봉이다.** 오늘 PREFLIGHT **G0 FAIL** — 833종목 중 789개가 개장 26분 미완봉이고,
벤치도 미정착이다(`⚠미정착봉(장중·KIS실시간)` 경보가 `exposure_rule` 출력에 그대로 찍혀 있다).
**+3.8% 갭업 아침에 20일MA 이격이 임계선을 0.1~0.2pp 차로 오르내리는 것**은
「레짐이 과열로 바뀌었다」가 아니라 **「미정착 봉이 선을 스치고 있다」**로 읽는 게 오늘 측정에 부합한다.

## 5. 🚨 집행 보류 사유 ③ — **plan 의 15건이 오늘 데스크 산출물과 정면 충돌한다**

`plan` 은 **BUY 11건 / SELL 4건**을 낸다. 오늘 이 데스크는 **BET 후보 승격 0건 · 등급 변경 0칸 · OW 0칸(9런 연속)** 이었다.
충돌을 종목별로 적는다:

| 주문 | 오늘 산출물이 말한 것 | 충돌 |
|---|---|---|
| **BUY 010950 S-Oil +0.71%** | **🔴분산** · flow −0.353 · RS20 −6.8 · **마진 FY2025 3.0% = 11기 중 9th 백분위** · EVENT_ALPHA 카드2 **STORY-ONLY**, *"BET 후보로 넘기지 않는다"* · 오늘 `missed_ledger` 에 `Q.확신부족` 등재 | 🚨 **정면** |
| **SELL 000660 SK하이닉스 −1.34%** | 오늘 **+6.87% = 지수 최대 기여**. `M-76`①이 **오늘 15:30 정착 종가**로 채점하기로 사전공약 | 🚨 **미정착 봉에 파는 것 = G0 가 금지한 바로 그것** |
| **SELL 005930 삼성전자 −3.74%**(최대 주문) | 오늘 +2.73%. `M-68`: 랠리 4세션 **외국인·기관 쌍끌이, 개인 순매도** | 🚨 동일 |
| **BUY 051900 LG생활건강 +1.35% · 090430 아모레 +0.79% · 483650 달바 +0.89%** | **화장품 브랜드 2/2 🟡중립**(vol_surge 0.76·1.08) vs **ODM 5/5 🟢**(2.38~7.90). **`S63-KR` 이 08-20(D−2)에 바로 이 질문을 정산한다** | 🚨 **점화 안 한 쪽을 사고, 브래킷 정산 전에 산다** |
| **BUY 068270 셀트리온 +2.03%**(2위 주문) | **`S52-KR` 이 내일(08-19) 「제약 = 섹터냐 한 이름이냐」를 정산한다.** BET 은 *"그 전에 승격하면 사전공약을 사후 무력화하는 것"* 이라며 보류했다 | 🚨 **D−1 에 매수** |
| **BUY 011200 HMM +1.79%** | 운송·창고 **eqflow −0.270 · breadth 0.00 (0/24)** | ⚠ 약함 |
| BUY 034020 · 042660 · 028260 · 089860 · 103140 · 003230 | **오늘 어느 스테이지도 분석하지 않은 이름** | ⚠ 근거 부재 |
| SELL 028050 −1.49% / SELL 316140(해당없음) | 028050 = **보유·4Phase 5런째 부재**(dig #2). 316140 = 오늘 **진입조건 발화**(외국인 5세션 +98.6만주) ↔ **가계대출 하방 노출층**, 두 축이 반대 | ⚠ **미해소 모순 위에서 감량** |

**078930 GS 타깃 0.0%** — 메모리 규칙(`feedback_timefolio_orphan_hold`)대로 계보를 봤다:
현재 비중 **0.00%(24만원 먼지)**, 손익 +41.6%. **오펀이 아니라 잔량**이고, `plan` 은 이 3종(016360·042700·078930)에 **주문을 내지 않았다.** 조치 불요.

## 6. ⇒ 오늘의 조치 — **제출 0건. 대신 사람이 결정할 것 3가지**

**내가 하지 않은 것**: `sync --execute` · `order --execute` · 어떤 주문도 제출하지 않았다.
`TIMEFOLIO_EXECUTE=1` 이 **이미 arming 돼 있다**(모든 `exposure_rule` 출력에 🚨🚨 로 찍힌다) — 즉 `--execute` 를 붙이면 **바로 나간다.**

**사람이 정해야 하는 것:**

1. **`exposure_rule target` 이 `min_hold_sessions` 를 적용하도록 고칠 것인가** (코드 변경, P5).
   지금은 `state`(95%)와 `target`(75%)이 갈리고, **미러가 보는 쪽이 가드 없는 쪽**이다.
2. **고치기 전까지 미러를 어느 숫자로 굴릴 것인가** — 95%(state·가드 적용) / 75%(target·현행) / 동결.
   현재 실제 투자비중은 **68.1%** 라 95% 면 대폭 매수, 75% 면 소폭 매수, 동결이면 무행동이다.
3. **정착 종가(15:30) 이후로 미룰 것인가.** 오늘 브래킷 2개가 **내일·모레** 정산된다(`S52-KR` 08-19 · `S63-KR`·`S57-KR` 08-20),
   그리고 `M-76`① 이 **오늘 종가**를 관측면으로 이미 사전공약했다. **이틀만 기다리면 세 브래킷이 답을 준다.**

**내 권고(하나로 압축)**: **오늘은 동결하고 15:30 정착 종가 뒤에 다시 본다.**
사유는 취향이 아니라 숫자다 — **집행 근거가 되는 노출 상태 자체가 두 값으로 갈려 있고(95 vs 75),
그 판정이 미완봉 위에서 임계선을 0.2pp 차로 스치고 있으며, 15건 중 6건이 오늘 문서가 명시적으로 보류한 이름이다.**
그리고 **회전율 0.0% · 누적 위반 1건**이라, 서두를 이유가 계좌 쪽에도 없다.
