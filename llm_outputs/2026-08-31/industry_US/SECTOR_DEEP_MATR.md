# SECTOR_DEEP_MATR — Materials · industry_US · 2026-08-31 (Mon) · Stage 8 / L1·DEEP

> **CONTINUOUS slot C1 — DELTA-LED.** Last DEEP **2026-08-30 (yesterday)**. Structure carried by
> reference to that file; this one leads with what changed.
> All flow figures `repaired sweep, asof 2026-08-28`; Δ is `08-27 → 08-28, 3-axis nonews`.
> ⚠ Serial, not parallel, fan-out — see `BLINDSPOT_PREMORTEM §0` (`D434`).

## 0 · Mandate — one question, and the answer overturns yesterday's frame

ROTATION asked: *`C24` — the board's rank-1 `eqflow` (+0.164, **0 reds of 12**, Δ **+0.197**) against
**copper speculative net at the 100th percentile of its year**. Yesterday's DEEP resolved WHAT the
signal is (`FCX` + `NEM`); it did not resolve WHICH INSTRUMENT to believe.*

**Answer: the question has a false premise, and finding that is worth more than answering it.**
**Today's board-leading Δ is not the metals at all.** `FCX` and `NEM` contributed **negatively** to
it. The Δ is **industrial gases + construction materials + ag-chem**, and **more than half of it is
one low-beta name.** ⇒ **`C24` is not resolved — it is narrowed**: the copper contradiction now sits
on the sector's **level**, while the sector's **change** is a different trade entirely.

## 1 · ★ The delta — attributed, because the sector's headline number is one name

Sector Δ **+0.197 — the largest positive delta on the board.** Attribution, cap-weighted:

| name | weight | Δ | contribution | share of sector Δ |
|---|---:|---:|---:|---:|
| **LIN** (Industrial Gases) | **24.7%** | **+0.41** | **+0.1011** | 🚨 **51.4%** |
| **CTVA** (Fertilizers & Ag Chem) | 5.5% | **+0.77** | +0.0421 | **21.4%** |
| **CRH** (Construction Materials) | 7.8% | +0.37 | +0.0287 | **14.6%** |
| **APD** (Industrial Gases) | 6.5% | +0.33 | +0.0217 | **11.0%** |
| **MLM** (Construction Materials) | 3.8% | +0.47 | +0.0179 | 9.1% |
| **NUE** (Steel) *(held)* | 5.8% | **−0.28** | **−0.0161** | **−8.2%** |
| **FCX** (Copper) | 8.2% | −0.08 | −0.0066 | **−3.4%** |
| **NEM** (Gold) | 9.2% | −0.03 | −0.0028 | **−1.4%** |

★★ **Three findings, and each is checkable:**
1. **`LIN` alone is 51.4% of the sector's entire day-over-day improvement.** One name.
2. **`FCX` and `NEM` — yesterday's answer to "what is the signal" — contributed NEGATIVELY** (−3.4%
   and −1.4%). **They are the level, not the change.**
3. **The positive Δ block is `LIN` + `CTVA` + `CRH` + `APD` + `MLM` = 107.5% of the sector Δ**, i.e.
   industrial gases, construction aggregates and agricultural chemicals — **three businesses with
   nothing in common with the copper trade.**

🚨 **And the flip guard cannot see any of this.** `top1_flips_sign` is **False** for Materials
(`wflow` +0.225 → ex-`LIN` **+0.233**, so the sign holds), which is a statement about the **level**.
**The guard does not look at Δ at all.** ⇒ **`D437` registered**: *the `top1_flips_sign` guard is
computed on `wflow` only; a sector's **Δ** can be >50% one name while the guard reads clean. A run
citing a sector Δ attributes it the same way it attributes a level.*
⚠ This is the **`D424` family** (registered by the KR desk today for the level case) extended to the
change — and it fires here on the board's most-cited positive number.

## 2 · What `LIN`'s +0.41 actually is — checked, and it is not a fundamental event

| | LIN |
|---|---|
| forward P/E | 25.09 · trailing 31.64 · P/B 5.79 |
| **beta** | **0.726** |
| dividend yield | 1.31% |
| price / 52-week range | 491.03, range 387.78–548.20 (**−10.4% off the high**) |
| consensus upside | +11.4% (target mean 547.24) |
| **revenue QoQ series** | **−2.9 · +4.7 · +1.4 · +1.9 · +5.8** — steady, no inflection |
| **+1q EPS, 90-day** | **4.2358 → 4.2305 = −0.1%, 1 up : 1 down** — **flat** |
| next print | **2026-10-30** — no catalyst inside any bracket window |
| flow detail | +0.199, OBV **+0.258 매집**, `rs20` **−0.7**, `rs60` **−5.6** vs `SPY`, `vol_surge` 0.81 |

⇒ **A low-beta (0.726) industrial-gas name with flat estimates, steady single-digit revenue growth,
no catalyst for two months, and NEGATIVE relative strength vs `SPY` on both windows (`rs20` −0.7 ·
`rs60` −5.6, benchmark named inline per `C1`) — being accumulated on volume BELOW its own average
(`vol_surge` 0.81).**
★ **That is the signature of a defensive rotation, not a commodity-cycle signal.** `APD` (the other
industrial gas, Δ +0.33, OBV +0.196, `rs20` +1.5) reads the same way.
⇒ **The sector's rank-1 Δ is a flight into low-beta industrial staples that happen to be classified
as Materials.** Stated as a reading of the numbers, and it is falsifiable — see `§7 K1`.

## 3 · `C24` — narrowed, not resolved, and the narrowing is the deliverable

The contradiction as inherited: **rank-1 flow vs copper spec net at the 100th percentile.**
What today adds:

| leg | today's number | what it says |
|---|---|---|
| Copper positioning | **COT spec net +85,266, +5,518▲ wk, 1-yr percentile 100, 30.1% of OI** | unchanged and still extreme |
| **`FCX` flow** | **+0.767 (rank 1 in sector), OBV +0.276 매집, `rs20` +19.1** — **but `rs60` only +6.2** and **Δ −0.08** | 20-day mover with almost **no 60-day leg**, and it deteriorated day-over-day |
| **`FCX` fundamentals** | forward P/E 18.32, **trailing 37.14**, consensus **upside −4.9% (price ABOVE target)**, **0q EPS 0.741 → 0.730 = −1.5% on 3 up : 6 down** | **estimates being cut** into a 100th-percentile long |
| **`FCX` revenue QoQ** | **−15.6 · +32.4 · −8.0 · −10.6 · +12.8** | 🚨 **violently oscillating with no trend** — `B1`'s rate series is **uninformative here**, and saying so is the honest read |
| **`HG=F` copper** | 6.56 (08-28 settle) → **6.72 live 08-31**, i.e. +2.4% into the escalation ⚠ **quarantined, pre-open/intraday, not citable** | — |
| Gold, for contrast | COT **66th percentile — NOT crowded**; `NEM` +0.644 with OBV **+0.506** and `rs20` **+33.5** | ⚠ **must not be netted with copper** — different positioning states |

★ **The narrowing**: the contradiction is **`FCX`-specific, not sector-wide.** `FCX` is the one name
where a 100th-percentile speculative long, a **negative** consensus upside, and a **cut** estimate
book all sit under a rank-1 flow score. **`NEM` is not in that state** (66th-percentile positioning,
+0.506 OBV, +33.5 `rs20`) — but `NEM` has its own contradiction carried from 08-30: **0q EPS
2.577 → 1.916 = −25.7% in 90 days on 0 up : 7 down**, i.e. **estimates cut while price leads.**
⇒ 🚫 **Still not resolved by picking a side** (`C24` stays open, as the carry requires). **What is
new is that the two metals fail in two different ways and should never again be quoted as one signal.**

## 4 · Steel — the sector's other contradiction, and the desk HOLDS one side of it

*"Steel Hits 2½-Month Highs"* [2 outlets] printed today. The equities did not agree.

| | **NUE** *(held)* | **STLD** |
|---|---:|---:|
| flow (Δ) | **−0.307 (−0.28)** | **−0.187 (−0.09)** |
| OBV | +0.050 | +0.089 |
| `rs20` / `rs60` vs `SPY` | **−5.6 / −4.8** | **−9.6 / −16.7** |
| forward P/E | **13.22** (trailing 20.09) | **12.43** (trailing 21.36) |
| P/B | 2.58 | 3.59 |
| price vs 52-week high | 251.76 / 280.11 = **−10.1%** | 235.43 / 288.74 = **−18.5%** |
| consensus upside | **+12.3%** | **+15.2%** |
| **revenue QoQ series** | **+10.7 · +8.0 · +0.8 · +11.4 · +9.5** | **+0.6 · +4.5 · +5.8 · +7.8 · +17.0** |
| **0q EPS, 90-day** | **4.111 → 5.886 = +43.2%, 5 up : 1 down** | **4.490 → 5.413 = +20.5%, 5 up : 1 down** |
| **+1q EPS, 90-day** | 3.232 → **4.924 = +52.4%, 4 up : 1 down** | 4.074 → **5.119 = +25.7%, 2 up : 3 down** |
| next print | 2026-10-27 | 2026-10-20 |

★ **`B1` — the second derivative, read properly.** `STLD`'s revenue **rate is ACCELERATING** through
the whole five-quarter window (0.6 → 4.5 → 5.8 → 7.8 → **17.0**). `NUE`'s is **strong and stable**
(around +9–11% with one dip). **Neither shows the two-consecutive-declines signal.** ⇒ the `B1`
warning is **checked and does not fire** — the level is not the distraction here.
★ **`B2` — peak-margin trap?** Forward multiples are 12–13× on denominators revised **up 43% / 21% in
90 days**. **That is the `B2` shape** — but with a difference from refining: **both names trade
10–19% BELOW their 52-week highs and BELOW consensus target** (+12.3% / +15.2% upside), whereas
`MPC`/`VLO` trade **at** their highs and **above** target. ⇒ **same trap shape, opposite crowding
state.** Recorded as the distinction, not collapsed into one verdict.
⚠ **`NUE` is HELD and is on the wrong side of its own sector's strength** — it is the sector's
**second-largest negative Δ contributor (−8.2%)** on the day the sector led the board.

### 🚨 Frame-transfer question, answered (the EXIT CHECK requires one)

**Frame: take-or-pay / contractual floors** — load-bearing for this desk on `KMI` (RPO), `LNG`
(tolling), `VST` (PPA floor), and, after `R111`, on **memory**.
**Does it apply to steel or copper? — Partly, and the distinction is the answer.**
- **Copper (`FCX`): NO.** Carried unchanged from the 08-30 DEEP's **FY2025 10-K read**: US cathode is
  priced off *"COMEX monthly average settlement… plus a premium"*, South America off **LME monthly
  averages 1–4 months forward**. **No floor, no ceiling — index-linked pass-through.** ⇒ revenue
  amplitude tracks the cycle **unsmoothed**, and — the operative consequence — **no contractual
  ceiling can be producing `FCX`'s oscillating QoQ series.** The oscillation is the cycle itself.
- **Steel (`NUE`/`STLD`): `unknown` (C3).** Neither name's contracted-share is extracted this run.
  US sheet steel does trade partly on annual contract, which is exactly the structure that would
  smooth a QoQ series — **so the accelerating `STLD` rate could be contract resets rather than spot
  demand, and this file does NOT read it as demand.** ⚠ **`B1`'s escape hatch applies and the share is
  unmeasured**: marked `unknown`, not assumed. Extracting it is the first item for the next MATR DEEP.
- ⇒ **The general lesson `R111` taught is executed here**: *when a frame is load-bearing in one
  sector, ask which other position it should be pointed at.* It was pointed at both metals and steel;
  it **answers** for copper and **flags an unmeasured gap** for steel.

## 5 · Chain map — delta-led, structure by reference to the 08-30 file

The 08-30 DEEP's node map (mine → concentrate → smelt/refine → semis/mill → fabricate → end-market)
is **carried unchanged by reference**. What moved:

| node | 08-30 read | today |
|---|---|---|
| **Mine / metals** | "the signal is `FCX`+`NEM`" | **contributed NEGATIVELY to today's Δ** (§1); `FCX`'s estimates are being cut and its price is above target |
| **Industrial gases** | not a node in the prior map | 🚨 **now 62.4% of the sector's Δ** (`LIN` 51.4% + `APD` 11.0%) — **a node the prior map did not contain** |
| **Construction materials** | not emphasised | **`CRH` +0.37, `MLM` +0.47, `VMC` +0.28 Δ** — all three with **negative `rs20` and `rs60`** ⇒ **a turn off a weak base**, not continuation |
| **Steel mill** | `STLD` "fundamentals up, money out" | **`NUE` joins it** (§4), and `NUE` is held |
| **End-market** | — | the construction-materials turn and the **Industrials construction-&-engineering collapse** (`PWR` −0.622, `FIX` −0.789, `FER` −0.767 — see `SECTOR_DEEP_INDU §1`) **point opposite ways on the same end-market.** ⚠ Recorded as a cross-sector contradiction; **not resolved** |

**Sub-sector dispersion, stated (`B5`)**: within a 12-name sector, `FCX` **+0.767** to `SHW`
**−0.398** = a **1.17 spread** against a sector move of **+0.225** ⇒ **the spread is 5.2× the sector
move; the sector label is the wrong unit of analysis and this file says so.**

## 6 · Customers named, with disclosed spend or print dates (`A6`)

- **`LIN`/`APD` (industrial gases)** → customers are **refining, chemicals, electronics, healthcare** —
  i.e. **`MPC`/`PSX`/`VLO` are `LIN`'s customers**, and those three are this run's Energy story.
  ⚠ **Their capex is not extracted** ⇒ `unknown` (C3). Prints: `LIN` **2026-10-30**, `MPC` 11-03,
  `VLO` 10-22.
- **`FCX` (copper)** → customers are **grid, construction, EV**. 🚨 **The grid leg is measurable and it
  is negative**: `GEV` −0.640, `ROK` −0.850 🔴, `PWR` −0.622 🔴, `ETN` Δ −0.39 (`SECTOR_DEEP_INDU §3`).
  ⇒ **copper's largest structural demand story is being sold in the sector next door**, which is the
  strongest single argument against the 100th-percentile long that this run can produce **from
  measured data rather than from positioning alone.**
- **`NUE`/`STLD` (steel)** → customers are **construction and autos**. Construction reads two ways
  (§5). ⚠ `DAN`/auto exposure not extracted ⇒ `unknown`.
- **Print dates all fall outside every bracket window registered today** — hence `§7`'s observables
  are market spreads, not prints.

## 7 · Track KPIs and anti-signals

| # | KPI / anti-signal | threshold | date |
|---|---|---|---|
| **K1** | **The defensive-rotation reading (§2) is falsifiable**: if `LIN`+`APD`'s Δ persists **while their `rs20` vs `SPY` stays negative**, it is rotation; if `rs20` turns positive with rising estimates, it is a fundamental re-rating | `LIN` `rs20` vs `SPY` crossing above 0 by **09-11** | 2026-09-11 |
| K2 | **`FCX`** — the `C24` name | consensus upside returning **above 0** (currently −4.9%), **or** 0q revision breadth flipping to net-up (currently 3 up : 6 down) | 2026-10-22 |
| K3 | **`NEM`** | 0q EPS breadth (currently **0 up : 7 down**) flipping to any up-revision while `rs20` holds > +20 | 2026-10 |
| K4 | **`NUE`** *(held)* — "fundamentals up, money out" | `rs20` vs `SPY` crossing above 0 **with** OBV > +0.15 ⇒ the money agrees with the estimates; **or** a second consecutive negative Δ ⇒ it does not | 2026-09-18 |
| A1 | **Anti-signal — copper COT falling out of the ≥80th percentile** | would remove `C24`'s positioning leg entirely and the contradiction dissolves without either instrument being wrong. **Next COT print 2026-09-04** | **2026-09-04** |
| A2 | **Anti-signal — a China stimulus or property-policy print** | *"What do China's new housing presale rules mean for real estate developers?"* [2 outlets] printed today; a construction-demand shock moves steel and copper for a reason unrelated to the AI/grid story | live |
| A3 | **Anti-signal — `LIN`/`APD` are index-heavy defensives** | if `RSP` − `SPY` fires `S131`-A (broadening, ≥+1.30pp) on the MSCI rebalance, part of §2's Δ is a **passive-flow artifact**, not a rotation | **2026-08-31 settle**, readable next run |

★ **A3 is the sharpest anti-signal in this file**: **today is the MSCI quarterly review effective
date**, and `LIN` is a 24.7%-weight index constituent of its own sector. **A one-day cap-weighted Δ
of +0.41 on a low-beta index heavyweight, on a rebalance date, has a mechanical candidate cause** —
and `S131` (ARMED, settles on the 08-31 close) is the row that will let the next run tell the two
apart. **Registered here as a pre-declared confound, before the score, not after it** (`D242`).

## 8 · Verdict on the mandate

**`OW` is KEPT** — rank-1 `eqflow` (+0.164), **zero reds in twelve**, ex-top1 **+0.233** (broad, the
top name is a mild drag on the level).
**But yesterday's answer to `C24` is superseded on the change axis**: the metals are the sector's
**level** and industrial gases + construction materials + ag-chem are its **change**, with **`LIN`
alone at 51.4% of the Δ** and a **rebalance-date confound sitting under it**.
**`C24` remains open**, narrowed to `FCX` specifically, and the strongest new evidence against the
copper long is **not positioning — it is that copper's grid demand chain is being distributed in the
sector next door.**
🚫 **No sizing here — BET owns it, and every sizing line must name its book (`C23`).**
