# BLINDSPOT_PREMORTEM — industry_US · 2026-08-31 (Mon) · Stage 7 / L1·PREMORTEM ★US-only

> Four adversarial lenses argue **against our own tilt** before the deep budget is committed.
> Draft tilt entering this stage: `ENRG OW · HLTH OW · MATR OW · FIN OW− · COMM OW− · IT N · STPL N ·
> DISC UW · INDU UW− · RE UW · UTIL UW`, DEEP = **continuous [MATR, HLTH] · rotating [ENRG] · R2 unfilled**.

## 0 · How the fan-out was executed, and why it is stated

The L1 specifies an **Agent fan-out, 4 lenses in one parallel message**. **This run executed the four
lenses in-context instead**, and that is a deviation, logged rather than hidden. Two reasons:
1. **This session carries a standing constraint against invoking the Agent tool** unless the user asks
   for it; the scheduled task delegates to the protocol but the constraint is a session-level rule and
   the safe reading is to satisfy the **structure** (four named adversarial lenses, each returning
   named tickers and dated catalysts) rather than the **harness**.
2. **`D419`, measured on the 08-30 run of this exact stage**: *three of five* subagents produced a
   factual error of one class (mislabelled denominator, superseded filing, an invented defect), all
   catchable by re-deriving from the artifact the agent itself cited.
⚠ **What is lost is real and is named**: independence. Four lenses run by one author share one prior,
so the adversarial value is lower than a true fan-out. **Every lens below is therefore required to
land on a number that contradicts the draft tilt, or to say explicitly that it could not.**
⇒ Registered as **`D434`**: *a stage that specifies a parallel fan-out and is executed serially says
so in its own output and states what independence was lost.*

⚠ **Clock note**: this stage ran **ET 09:5x, ~25 minutes after the NYSE open**. `PREFLIGHT` (written
at ET 09:1x, pre-open) barred all 08-31 tape claims. **That bar is kept**: every intraday number below
is labelled and none carries a verdict. An intraday print is not a settled close (`D401`/`M1045`).

---

## Lens 1 — UNDER-COMPUTED LEGS

**Question**: what is the strongest bull (or bear) case for a sector/sub-leg we did **not** deep-dive
that has a catalyst within ~5 trading days?

Not deep-dived this run: `IT` · `FIN` · `COMM` · `DISC` · `INDU` · `STPL` · `RE` · `UTIL`.

| leg | catalyst ≤5 trading days | the case the draft tilt does not make | verdict |
|---|---|---|---|
| **★ INDUSTRIALS** | **Aug NFP, 2026-09-04 (D-4)** | **The desk demoted it to `UW−` TODAY on Δ −0.129 / 17 reds / `eqflow` rank 9 — and then gave it no slot.** That is a verdict change with no owner. Three specific things the demotion does not account for: (i) **`ETN` is booked here** and it carries **the worst Δ of any held name (−0.39)** while its **OBV is +0.202 매집** — flow and accumulation point opposite ways on a position we own; (ii) **`RTX` (held)** reads flow −0.255 with `rs20` −4.6 vs `SPY` against **`rs60` +20.7** — a decaying leader inside a freshly-deepened UW; (iii) the sector contains **`PAYX` +0.321 (OBV +0.389), `TRI` +0.343 (OBV +0.403), `ADP` +0.393 (OBV +0.287), `AXON` +0.417** — four names with OBV accumulation above +0.28 inside a 17-red bucket, i.e. **the red count is not uniform**. `S126` (XLI exc5 **into** NFP) and `P114` (**out of** NFP) bracket the date but neither reads inside the sector. | **PROMOTE-TO-DEEP** |
| **UTILITIES** | none ≤5d — its option book peaks at the **09-11 CPI** expiry (D-11), where `XLU` put OI **3,594 vs call 22 (163×)** | Board's **worst `eqflow` (−0.524)** and **worst red-rate (66.7%)** with a **6-run recency gap — the longest on the board** — and it is **structurally unreachable by the DEEP selection rule for a 2nd consecutive run**, because UW sectors do not take slots. The rule is producing a permanent blind spot at the board's most extreme point. | **WITHIN-RUN-WATCH** — and the rule defect is escalated as `D435` |
| **IT** | **`AVGO` print 09-02 or 09-03 (D-2/D-3)** | Routed here by ROTATION (1名 `NVDA`, `wflow` +0.033 vs `eqflow` −0.034). ⚠ **But IT was deep-dived on 08-29 AND 08-30**; a third consecutive IT DEEP would spend the promoted slot on the most-covered sector on the board. The `AVGO` binary is **already double-bracketed** (`S132` + `S127`). | **WITHIN-RUN-WATCH**, not promoted — and the reason is recency, not disagreement |
| **FINANCIALS** | **NFP 09-04 (D-4)** · `S134` settles 09-04 | **0 greens in 47 — the largest bucket on the board with no green name**, against Δ **+0.073 (3rd-best)**. ROTATION declined the demotion because the two signals disagree; that disagreement is un-owned. ⚠ Covered **0 runs ago**. | **WITHIN-RUN-WATCH** (`S134` owns the date) |
| **CONS. DISC.** | none ≤5d | Holds **the two largest runners in the entire 299-name universe** (`DASH` `rs60` +51.1, `ABNB` +39.8 vs `SPY`) inside a `UW`. See Lens 3. | **WITHIN-RUN-WATCH** |
| **REAL ESTATE / STAPLES / COMM** | none ≤5d | RE: Δ **+0.078** (2nd-best) off a deep base, top-1 **holding it up**. STPL: rank 7 mid-table, Δ +0.080. COMM: `eqflow` **+0.080 positive** under a `wflow` of −0.422. | **WITHIN-RUN-WATCH** ×3 |

★ **Lens 1's contradiction of the draft tilt**: **the run demoted Industrials and then declined to
look at it**, while filling its slots with the two sectors it looked at yesterday. That is the
selection rule producing coverage inversely to where the verdict moved.

⇒ **DEEP set updated. Final: continuous `[MATR, HLTH]` · rotating `[ENRG]` · **PREMORTEM-promoted
`[INDU]`** = 4/4 filled.**

---

## Lens 2 — REGIME-FLIP / BOTH-SIDES

**Every binary in the window, its against-us branch, and whether a bracket already owns it.**
⚠ Protocol rule: **a one-way tilt into a known binary is a violation.** Result: **6 of 6 binaries
carry a both-sides bracket.** Two are re-derived below; four are inherited and named.

### B1 · MSCI quarterly review — **D-0, 2026-08-31, the mandatory ≤48h binary**
- **Owned by `S131`** (ARMED): `RSP` − `SPY` **1-session excess**, A ≥ **+1.30pp** / B ≤ **−1.30pp**,
  both outside `RSP`'s ±1.06% implied at registration. ✅ Both-sided, threshold frozen 08-29.
- **The against-us branch, stated**: **A (broadening).** If passive flow **broadens** the tape, then
  (i) the desk's `IT N` — justified on `eqflow −0.034` and cap-weight narrowness — is measuring a
  condition that is about to reverse, and (ii) three of its four `UW` sectors are the equal-weight
  side of the very trade that would rip. **Names that rip on A**: the `UW` breadth laggards —
  `XLU`(−0.549), `XLRE`(−0.338), `XLI`(−0.317) — plus `RSP` itself. **Trigger**: `RSP` − `SPY` ≥
  +1.30pp on the 08-31 settle. **Invalidation**: ≤ −1.30pp ⇒ concentration deepens and the `UW`s get
  worse, i.e. the desk is right for the uncomfortable reason.
- 🚨 **New information this stage, and it cuts against the row's premise**: **the dominant MSCI story
  today is India, not the US.** *"India's Closing Auction Faces **$5 Billion** MSCI Reshuffle Test"*
  [`bloomberg`], *"MSCI Rebalancing Threatens to Turn 'Messy' in New Indian Auction"*, *"Adani Shares
  Lose **$15 Billion** as Closing Auction Fuels Swings"*. **MSCI 1-day count 36 vs 7-day 208.**
  ⇒ **`S131`'s observable is unaffected** (it reads `RSP`−`SPY`), **but its narrative premise — that
  this print is a US concentration event — may be measuring an offshore event's echo.** 🚫 **The
  threshold is NOT moved** (`D242`). Recorded as `M1141` so the score is read with it.
- **Information content (`B4`)**: **HIGH both ways** — A falsifies the desk's breadth reading, B
  confirms it on the mechanism it named. Worth its bracket.

### B2 · `AVGO` earnings — **D-2/D-3** ⚠ the date itself is disputed
- **Owned twice**: `S132` (**±9.00pp** 1-session excess vs `SPY`, settles 09-03) and `S127` (09-08).
  A third row would be `D343`.
- ★ **Implied move re-derived today, and it is the third independent reading**: 09-04 expiry,
  **K = 370.0, straddle 29.82, spot 368.91 ⇒ implied ±8.08%**, against **±8.11% (08-29)** and
  **±8.09% (08-30)**. **Three days, three chains, 3 bp apart.** ⇒ **`S132`'s ±9.00pp threshold sits
  OUTSIDE the implied move and therefore carries information.** Stated as the rule requires.
- ★ **`M1142` — the option book FLIPPED side between 08-30 and 08-31.** Same expiry, same-ish strike:
  **08-30 call OI 645 / put OI 354**; **today call OI 5,105 / put OI 1,892 (2.70× call-heavy)**, and
  the near-dated 08-31 expiry reads straddle 5.04 = ±1.37% with call OI 1,175 / put 776. ⇒ positioning
  turned **call-heavy into the print** while the underlying reads **flow −0.717 🔴분산 · OBV −0.147 ·
  `rs60` −25.1 vs `SPY`**. **Distribution in the tape against call-buying in the options book** — the
  cleanest conflict on the board, and neither is a direction on its own.
- **The against-us branch**: `AVGO` is **held**. A ≥ +9.00pp beat-rip would say the desk's `IT N` and
  its own holding's 🔴 flow tag were both wrong about the same name.
- 🚨 **`D420` still unresolved**: `module_fundamentals_us` and `yfinance` say the print is **09-03**;
  `catalyst_calendar` says **09-02**; **`S132` settles 09-03 on that basis** and may therefore measure
  the session **before** the information. **Carried, not fixed** (a calendar edit is a human item).

### B3 · Aug NFP — **09-04 (D-4)**
- **Owned three ways**: `S126` (`XLI` exc5 **into** NFP) · `P114` (`XLI` exc5 **out of** NFP) ·
  `S134` (Financials, 09-04) · and `P115` carries it as an **AMBIGUOUS clause** (a ≥100k miss in
  either direction voids the row rather than scoring it).
- **The against-us branch**: a **hot** print. It would confirm the hawkish repricing, deepen the
  `UTIL`/`RE` UW — **and simultaneously break the `INDU UW−` that was set today**, because Industrials
  is a cycle bet, not a duration bet. ⇒ **`S126`-A and `P115`-A firing together while `INDU` rips is
  the specific pattern that would show the desk booked one macro call as two sector verdicts.**
  **Starter list on that branch**: `XLI` · `PAYX` · `ADP` · `TRI` (the three OBV-accumulating names
  inside the UW). **Invalidation**: `XLI` exc5 vs `SPY` < 0 through 09-11.
- ⚠ **Base rate live**: the preliminary benchmark revision marked job growth **down 79,000**, i.e. the
  labour series is being restated.

### B4 · Aug PPI — **09-10 (D-10)**
- 🚫 **Dropped on information grade for a 4th consecutive run — BY DECISION, restated so it does not
  decay into an omission.** PPI is a **nowcast update to the CPI print one day later**, not an
  independent binary; **neither branch would change a verdict** that `P116` (HY OAS at the first close
  covering 09-18) and `P120` (IG OAS, 09-25) do not already carry. **The bracket is spent elsewhere.**

### B5 · Aug CPI — **09-11 (D-11)**
- **Owned by `P108`** (HY OAS ≤2.60 / ≥2.95 at the first close covering 09-11), **`P116`** (HY, 09-18)
  and **`P120`** (IG, 09-25, registered today). ✅ Both-sided.
- **The against-us branch**: a **cool** CPI. It unwinds the hawkish repricing, rips exactly the three
  duration `UW`s, and would put `P115` on branch **B**. ⚠ **This is the 2026-07-14 postmortem's own
  named miss ("cool-CPI semi rip missed") and it is why the branch is written out here.**
  **Starter list**: `XLU` · `XLRE` · `SMH` (the `exc60` −15.31pp vs `SPY` laggard).
  **Trigger**: `DGS2` ≤ 4.12 (`P115`-B). **Invalidation**: HY OAS ≥ 2.95 (`P108`-B) — a cool print
  that still widens credit is a growth scare, not a relief.
- 🚨 **Positioning is one-sided into this exact date**: `XLU` **put OI 3,594 vs call OI 22 (163×)** and
  `NVDA` **put 4,602 vs call 379 (12.1×)** at the 09-11 expiry (`M1101`, carried). **The crowded hedge
  is on the branch the desk is already on.**

### B6 · Hormuz / "Strait open" — **undated**
- **Owned four ways**: `P107` (BZ=F ≥96.00 through 09-08) · `P112` (through 09-11) · `P117`
  (BZ=F ≤85.00 / ≥95.00 through 09-18) · **`P118`** (`DCOILWTICO` spot, A ≥91.00 / B ≤82.00,
  09-11 — registered today specifically because it is **roll-immune**).
- 🚨 **The against-us branch is now the live one and it is NOT "reopening"**: after today's strikes,
  the branch that hurts is **A on `P118`** — a barrel spike that lifts everything Energy, including
  the E&P names this run just filed to `reject_ledger` (`XOM`/`EOG`/`FANG`) **and** `SLB`, whose card
  is built on a data-centre thesis. ⇒ **`P118`-A firing makes card 8 AMBIGUOUS by its own written
  clause**, which is why that clause exists.
- ⚠ **`S8` remains dateless for a 33rd run** — it is the one row in this family that cannot be scored
  in either direction. **A human must `VOID` it or give it a date (P5).**

### ★ Two NEW brackets registered by this stage

Both observe things **no existing row reaches**, and both are set on **spread** observables so the
08-28 ghost bar and the `BZ=F` roll cannot make them unscoreable.

#### `S136` — ★★★ Is the Energy escalation a BARREL event or a CHAIN-POSITION event?
- **Object**: `EW{SLB, MPC, PSX, VLO, COP}` **minus** `EW{XOM, EOG, FANG}`, **5-session sum of daily
  returns**, from the first settled close after 2026-09-01 through **2026-09-08**.
- **Why it exists**: this run's **principal flow claim** is that the money left E&P and sat in
  services/refining **before** the strikes (§2 of ROTATION). **Nothing brackets it**, and `P118`
  measures the barrel, not the split.
- **Branches**: **A ≥ +3.90pp** ⇒ the chain-position split is real and widening; the sector label is
  the wrong unit and Energy should be booked as two legs. **B ≤ −3.90pp** ⇒ the escalation lifts the
  barrel and E&P catches up; the split was a pre-event artifact and this run's headline flow finding
  is wrong. **C** between ⇒ disclosed as the modal outcome.
- **Band derivation (`C5`)**: measured 5-session-sum sigma of this exact spread over 60 sessions is
  **2.598pp**, realised range **−4.39 to +7.29**. **±3.90pp = ±1.50σ**, and it is **reachable in both
  directions inside the realised range** (`D216`). ⚠ **Asymmetry disclosed and deliberate: none** —
  the band is symmetric because the realised distribution straddles zero.
- ⚠ **Anti-signal (AMBIGUOUS)**: an **M&A event on any of the eight names** inside the window
  (`SLB`/Kelvion already closed one **today**; a second would contaminate). ⚠ Second: an **OPEC+ quota
  decision**.
- **Information content**: **HIGH on B** — B kills this run's own headline reading. A confirms a
  direction already visible in the 08-28 tape and is worth less.
- **Non-redundancy (`D343`)**: `P107`/`P112`/`P117`/`P118` all observe **the barrel**. This observes
  **relative position within the sector** and can disagree with all four; if it does, the disagreement
  is the finding.
- **Settles 2026-09-09** · **ARMED**.

#### `S137` — ★★ Is optical a SEPARATE leg from semis, or `SMH` beta? *(`D250`'s 16th run, finally bracketed)*
- **Object**: `EW{LITE, COHR}` **minus `SMH`** (benchmark named inline, `C1`), **5-session sum**, from
  the first settled close after 2026-09-01 through **2026-09-08**.
- **Why it exists**: `D250` has asked for an optical **registry row** for 16 runs and never got one, so
  *"exposure is unmeasurable, not zero."* **A bracket does not need the registry** — it measures the
  question directly, which is why it is registered now instead of waiting a 17th run.
- **Branches**: **A ≥ +9.00pp** ⇒ optical is its own leg and `SMH`'s `exc60` **−15.31pp vs `SPY`** does
  not describe it; the registry gap is a real exposure gap. **B ≤ −9.00pp** ⇒ optical is high-beta
  semis and the registry is right to fold it in; `D250` should be **closed as not-a-gap**. **C** ⇒
  contested.
- **Band derivation (`C5`)**: measured 5-session-sum sigma of this spread over 60 sessions is
  **8.639pp** (realised range **−16.69 to +24.69**). **±9.00pp ≈ ±1.04σ**, reachable both ways.
  ⚠ **This is a wide, volatile spread and the band is wide because of it** — a tighter band would
  fire on noise, which is the `S61` failure.
- ⚠ **Anti-signal (AMBIGUOUS)**: an **earnings print by `LITE` or `COHR`** inside the window (a
  single-name event, not a leg verdict); and **`AVGO`'s 09-02/03 print**, which sits inside `SMH` and
  moves the denominator for a reason unrelated to optical. ⚠ **The second is NOT remote — it is
  certain** ⇒ disclosed at registration as a known contaminant of the denominator, and the row is
  registered anyway **because a leg that cannot survive its index's largest print is not a leg**.
- **Information content**: **HIGH on B** — B would let the desk **close** a 16-run open dig, which is
  worth more than confirming it.
- **Settles 2026-09-09** · **ARMED**.

---

## Lens 3 — MOMENTUM-CONTINUATION ("already ran" ≠ avoid)

Re-tagged with the flip condition on each. Cycle KPI = `rs20` vs `SPY` still positive **and** OBV
still 매집. All figures `repaired sweep, asof 08-28`.

| name | `rs60` / `rs20` vs `SPY` | OBV | re-tag | flip condition |
|---|---|---|---|---|
| **DASH** | **+51.1** / +17.6 | **+0.679 — the board's highest** | **EXTENDED-BUT-LIVE** | `rs20` < 0 on two consecutive settled closes by **09-18** |
| **ABNB** | +39.8 / **+22.0** | +0.275 | **EXTENDED-BUT-LIVE** | same date |
| **SHOP** | +33.4 / **+27.6** | **+0.436** | **EXTENDED-BUT-LIVE** — ★ **not previously tagged by this desk** | `rs20` < +10 by 09-18 |
| **MPC** *(held)* | +36.0 / +13.6 | +0.329 | **EXTENDED-BUT-LIVE**, at its 52w high | `rs20` < 0, or a >5% loss of the 52w high |
| **PSX** *(held)* | +30.1 / +12.3 | +0.082 | **EXTENDED-BUT-LIVE**, at its 52w high | same |
| **VLO** | **+32.7** / +9.6 | +0.090 | **EXTENDED-BUT-LIVE**, and **un-held** — see `missed_ledger` | `rs20` < 0 |
| **CRM** | +32.4 / **+36.3** | +0.305 | **EXTENDED, POST-CATALYST** — its print fired **08-27**, so this is drift, not a pending event; **+41.7% above its 50DMA**, upside +2.6% | `rs20` < +15 |
| **PLTR** | +29.0 / **+48.4 — the board's highest 20-day** | +0.243 | **EXTENDED-BUT-LIVE** | `rs20` < +20 |
| **AJG** | +30.1 / **+4.3 (decaying)** | +0.251 | **EXTENDED-BUT-LIVE**, unchanged from 08-30 | `rs20` < 0 — **it is 4.3 away** |
| **NEM** | +17.0 / **+33.5** | **+0.506** | **EXTENDED-BUT-LIVE on price, CONTRADICTED on fundamentals** — 0q EPS **−25.7% in 90d on 0 up : 7 down** | `rs20` < +15 or OBV < +0.30 |
| **FCX** | **+6.2** / +19.1 | +0.276 | ⚠ **NOT a runner — a 20-day mover with almost no 60-day leg**, and the only name on the sheet whose consensus target is **below** its price | this is `C24`'s subject, not a momentum tag |
| **MSTR** | **−1.4** / **+33.5** | **+0.471** | 🔄 **TURN, not continuation** | fails if `rs60` still < 0 by 09-18 |
| **LITE** | **−6.6** / **+22.3** | +0.287 | 🔄 **TURN** — `S137`'s subject | as `S137` |
| **SNDK** | **−20.9** / +19.3 | +0.100 | 🔄 **TURN**, withheld (`P111`/`S4` own it) | — |
| **PANW** | +30.6 / +9.0 | **−0.123 분산** | 🔴 **EXHAUSTED (early)** — OBV distributing under elevated trailing strength; **carried unchanged from 08-30 and its re-flip window (09-02) is now 2 days out** | re-flips only on OBV → 매집 **and** `rs20` > +15 by **09-02** |
| **TRV** · **ALL** | +25.5 / −4.2 · +22.5 / −4.3 | −0.149 · −0.223 | 🔴 **EXHAUSTED** — two Financials names with strong 60-day legs and **🔴분산** | re-flip on OBV → 매집 |
| **CMG** | +30.3 / **−0.8** | −0.011 | 🔴 **ROLLING OVER**, unchanged from 08-30 | — |

★ **Lens 3's contradiction of the draft tilt, and it is the same one as last run**: **the two
strongest continuation names on the entire board (`DASH` +51.1, `ABNB` +39.8) sit inside `DISC`,
which the desk holds at `UW`** — and ROTATION *declined* to deepen that UW today partly for this
reason. **The label and the names disagree, and the label is kept.** ⚠ `SHOP` is a **third** such name
and it had **never been tagged by this desk** — `rs60` **+33.4** and `rs20` **+27.6 vs `SPY`** carry
this line (**`RULE D6`**: OBV **+0.436** is quoted beside them as a C-grade corroborant only and does
not support the tag alone) — filed to `missed_ledger` in §5.

⚠ **`B3` (date-clustered moves are one observation)**: the `rs60` column is a 60-session window, so
these are **not** one clustered day and the tags are admissible. Stated because the rule binds here.

---

## Lens 4 — CYCLE-EXPOSURE (registry vs coverage vs the REAL book)

`cycle_exposure.py`, **REAL KIS account**, read-only:

| cycle | rank | epicenter % | floor | held epicenter | GAP |
|---|---:|---:|---:|---|---|
| AI-compute / semiconductors | 1 | **16.91%** | 12.0% | `NVDA`, `ANET` (+`ETN` adjacent) | ✅ margin +4.91pp |
| Energy / oil-refining | 2 | **10.17%** | 8.0% | `MPC`, `PSX` | ✅ margin +2.17pp |
| Missile-defense / rearmament | 3 | 3.82% | — | `RTX` | ⚪ no floor set |

**Invested $5,188.76 of $11,045.42 ⇒ 53.0% cash.** **No top-rank GAP.**

★ **But the lens's job is to attack that verdict, and there are three ways it is weaker than it looks:**

1. 🚨 **A cycle with no registry row cannot produce a GAP by construction.** **AI-power/grid has no
   ranked row** (`D416`) and **optical has none** (`D250`, 16th run). ⇒ *"0% exposure to AI-power"* is
   **not a statement this desk's instruments can make**, and today an **Energy** name (`SLB`) bought
   **$3.4bn of data-centre cooling** — a cross-cycle move the registry has no field for.
2. 🚨 **Two of the rank-1 cycle's own epicenter names are outside the universe**: **`TSM` and `SMCI`**
   are in `cycle_registry.json`'s AI-compute epicenter list and **absent from `us_top300`** (`LNG` is
   the long-running third). ⇒ the epicenter percentage is computed over a **subset** of the epicenter
   the registry itself defines. **Reproduced from 08-30, unfixed.**
3. 🚨 **`C23`: the "no GAP" verdict is about the REAL book, and two other books exist.** `PREFLIGHT G5`
   validated the universe against the **paper** book (13 names); `exposure_rule` reads a **third**
   (KR contest) at **85.3% invested vs a 95% target**. **The real US account is 53.0% cash at the same
   moment.** ⇒ **any sizing sentence downstream must name its book**, and `T` (~13.6% of real invested
   capital) still appears in **zero** cycle rows.

**Cleanest epicenter expressions, if a GAP were to open** (named per the rule, **not** a recommendation
and **not** sized): AI-compute — `NVDA`, `ANET` (held) · AI-power/grid — `ETN` (held, adjacent tier)
and, unregistrable today, `SLB` after the Kelvion purchase · optical — `LITE`, `COHR` (neither held,
neither in the registry).
⚠ **Rule restated**: a 🔴/crowded tape gates **ADD timing**; it never justifies 0% core in a top-rank
cycle. **No cycle currently sits at 0% core**, so that rule is not load-bearing this run.

---

## §5 · What this stage injects downstream

| destination | injection |
|---|---|
| **DEEP (Stage 8)** | **Final DEEP set: `[MATR, HLTH, ENRG, INDU]` — 4/4, with `INDU` PROMOTED by Lens 1.** Its mandate: *does the `UW−` set today survive reading inside the bucket, given `ETN`'s worst-Δ-with-accumulating-OBV, `RTX`'s decaying leadership, and four OBV-accumulating names above +0.28 inside a 17-red sector?* |
| **ALPHA (Stage 10)** | **6 of 6 binaries carry both-sides brackets** (B1–B6). Action-bracket inputs: the **MSCI D-0** row `S131`, the **`AVGO` D-2/D-3** rows `S132`/`S127` with the ±8.08% implied and the `D420` date dispute, and the **09-11 CPI** one-sided option book (`XLU` 163× puts). |
| **BET (Stage 9)** | Cycle audit: **no GAP**, but with three stated weaknesses (§Lens 4). **Epicenter starters named, none sized.** ⚠ **Every sizing line must name its book** (`C23`). |
| **`handoff/SCENARIOS_US.md` + `SCENARIOS.md` MASTER INDEX** | **`S136`** (Energy chain-split, 09-09) and **`S137`** (optical vs `SMH`, 09-09), both ARMED, both frozen at registration. |
| **`handoff/RESEARCH.md` Part C** | **`D434`** (serial execution of a fan-out stage says so) · **`D435`** (the DEEP selection rule cannot reach a UW sector, so the board's most extreme point has a 6-run recency gap). |
| **`missed_ledger`** | **`SHOP`** — a third `rs60` > +30 runner with OBV +0.436 that this desk has **never** tagged. |

## ✅ EXIT CHECK
- [x] **4 lenses executed, each returning named tickers and dated catalysts.** ⚠ **Executed serially,
      not as a parallel Agent fan-out** — the deviation, its two reasons and **what independence was
      lost** are stated in §0 and registered as `D434`. **Each lens landed on a number that
      contradicts the draft tilt** (L1: the demoted sector got no slot · L2: the MSCI print's dominant
      story is offshore · L3: the two best runners sit inside a `UW` · L4: two epicenter names are
      outside the universe).
- [x] **Every bracket names its observable + frozen threshold + date, with BOTH branches.** Six
      inherited (`S131`·`S132`/`S127`·`S126`/`P114`/`S134`·`P108`/`P116`/`P120`·`P107`/`P112`/`P117`/`P118`)
      and **two new** (`S136`, `S137`). **Zero one-way brackets.** Registration into
      `SCENARIOS_US.md` and the `SCENARIOS.md` MASTER INDEX is performed in the handoff writeback.
- [x] **Every magnitude threshold stated against the implied move where one exists** — `AVGO`
      **±8.08%** (09-04 expiry, K=370, straddle 29.82, D+3), so `S132`'s ±9.00pp is **outside** it and
      carries information. `S136`/`S137` are **spread** observables with no traded straddle, so their
      bands are derived from **measured 5-session realised sigma** (2.598pp / 8.639pp) and say so.
- [x] **Each branch graded by information content.** **Aug PPI dropped for a 4th run with the reason
      restated** (neither branch would change a verdict `P116`/`P120` do not already carry).
- [x] `BLINDSPOT_PREMORTEM.md` written — legs · brackets · re-tags · cycle GAP.
- [x] **Every catalyst-bearing leg promoted or logged**: `INDU` **PROMOTED**; `UTIL`, `IT`, `FIN`,
      `DISC`, `RE`, `STPL`, `COMM` logged as WITHIN-RUN-WATCH with reasons. **None silently dropped.**
- [x] **DEEP set updated and stated: `[MATR, HLTH, ENRG, INDU]`, 4/4.**
