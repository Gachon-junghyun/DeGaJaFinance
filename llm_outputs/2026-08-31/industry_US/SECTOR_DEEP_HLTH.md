# SECTOR_DEEP_HLTH — Health Care · industry_US · 2026-08-31 (Mon) · Stage 8 / L1·DEEP

> **CONTINUOUS slot C2 — DELTA-LED.** Last DEEP **2026-08-30 (yesterday)**; anti-thrash continuity
> applied. Structure carried by reference to that file; this one leads with what changed.
> All flow figures `repaired sweep, asof 2026-08-28`; Δ is `08-27 → 08-28, 3-axis nonews`.
> ⚠ Serial, not parallel, fan-out — see `BLINDSPOT_PREMORTEM §0` (`D434`).

## 0 · Mandate — one question

*The `D424` case: `wflow` **+0.021** against **ex-`LLY` +0.095**. Is the sector's breadth
(`eqflow` +0.135 rank 2, only **2 reds of 32**) real, or is it an artifact of `LLY` being the sole
large drag?*

**Answer: the breadth is REAL and it is not an `LLY` artifact — but the reason `LLY` drags is not
the reason the `D424` shape usually appears, and that difference matters.**
**The drag is a LEVEL effect only. On the CHANGE axis `LLY` is the sector's largest POSITIVE
contributor (27.9% of the Δ).** ⇒ `LLY` is simultaneously the biggest drag on where the sector *is*
and the biggest push on where it *went*. Two axes, opposite signs, one name.

## 1 · The `D424` case, decomposed on both axes

**Level.** `wflow` **+0.021** · `wflow_ex_top1` **+0.095** · `top1_flips_sign` **False** (the sign
never changes, which is why the guard is silent) · `LLY` weight **19.4%** of the bucket at a **$980bn**
market cap.
`LLY` flow **−0.288**, OBV **−0.082**, `rs20` **−0.8**, `rs60` +6.8 vs `SPY`. ⇒ **a mildly negative
mega-cap dragging a positive bucket. Confirmed.**

**Change.** Sector Δ **+0.047**. Attribution, cap-weighted:

| name | weight | Δ | contribution | share of sector Δ |
|---|---:|---:|---:|---:|
| **LLY** | **19.4%** | **+0.07** | **+0.0132** | **27.9%** |
| ABBV | 7.6% | +0.16 | +0.0121 | 25.6% |
| SYK | 2.3% | **+0.47** | +0.0110 | 23.3% |
| JNJ | 10.9% | +0.07 | +0.0077 | 16.4% |
| MDT | 2.0% | **+0.36** | +0.0073 | 15.5% |
| HCA | 1.6% | −0.33 | −0.0054 | −11.4% |

⇒ 🚨 **`LLY` is the largest single contributor to the sector's improvement.** The `D424` shape here is
**level-only**, and a run that had checked only the level would have carried "the top-1 drags" as the
whole story. **Both axes are reported, which is the `C2`-both-halves discipline applied to a flow
number rather than to an earnings print.**

**Breadth, independently of both**: **1 green, 2 reds, 29 neutral of 32.** The two reds are
**`UNH` −0.765** and **`CVS` −0.828** — *the same two names as 08-30*, i.e. **managed care**, not the
sector. **30 of 32 names are non-red.** ⇒ **the breadth claim survives without any reference to
`LLY`.** That is the mandate's answer.

## 2 · Sub-industry map — where the breadth actually lives

| sub-industry | names (flow) | read |
|---|---|---|
| **★ Life Sciences Tools & Services** | **`A` +0.906 🟢** · `WAT` +0.426 · `DHR` +0.397 · `TMO` +0.392 | **4 of 4 positive — the single most coherent block in the sector**, and the desk's only 🟢 sits in it |
| **Biotechnology** | `ALNY` +0.583 · `AMGN` +0.556 · `GILD` +0.506 · `VRTX` +0.406 · `REGN` +0.224 · `BMY` +0.032 (OBV **+0.449**) · `ABBV` −0.327 (Δ **+0.16**) | 6 of 7 positive |
| **Pharmaceuticals** | `MRK` +0.678 (`rs60` **+27.3**) · `PFE` +0.489 (OBV +0.369) · `JNJ` +0.272 · **`LLY` −0.288** | 3 of 4 positive; the negative is the mega-cap |
| **Health Care Equipment** | `BDX` +0.467 · `MDT` +0.335 (Δ +0.36) · `ABT` +0.275 · `BSX` +0.256 · `EW` +0.169 · `ISRG` +0.108 · `IDXX` −0.222 · `SYK` −0.262 (Δ **+0.47**) | 6 of 8 positive |
| **🚨 Managed care / services** | **`UNH` −0.765 🔴** · **`CVS` −0.828 🔴** · `CI` −0.104 (Δ +0.34) · `ELV` +0.156 · `HUM` +0.297 | **the sector's only two reds are both here** |
| **Distributors** | `COR` −0.109 · `CAH` −0.192 · `MCK` −0.342 | 3 of 3 negative |
| **Facilities** | `HCA` −0.176 (Δ **−0.33**) | the largest negative Δ contributor |

**Sub-sector dispersion, stated (`B5`)**: `A` **+0.906** to `CVS` **−0.828** = a **1.73 spread**
against a sector move of **+0.021**. ⇒ **the spread is ~80× the sector move; the sector label is
emphatically the wrong unit of analysis and this file says so.** The usable units are
**Life Sciences Tools** (uniformly positive) and **managed care/distributors** (uniformly negative).

## 3 · The delta this run adds — `LLY` did a deal, and the drag name is the one doing deals

**`[news]` 2026-08-31, head layer: *"Eli Lilly buying Merida Biosciences for up to $2.875 billion"* —
5 outlets / 8 articles.** Body layer: *"New Phase 3b data on Lilly's **Taltz (ixekizumab)** and
**Zepbound (tirzepatide)** used together showed improved and durable efficacy"* [2 outlets].

**Fundamentals, read as the carry rules require:**

| | **LLY** |
|---|---|
| forward P/E | **24.40** · trailing **38.62** · **P/B 30.33** |
| beta | 0.506 |
| price / 52-week range | 1,152.51, range 712.05–1,292.65 ⇒ **−10.8% off the high** |
| consensus upside | +14.1% (target mean 1,315.04) |
| **revenue QoQ series** | **+11.3 · +22.2 · +13.1 · +12.5 · +16.0** |
| **0q EPS, 90-day** | 9.396 → **9.871 = +5.1%, 13 up : 1 down** |
| **★ +1q EPS, 90-day** | 9.934 → **9.976 = +0.4%, but 5 up : 10 DOWN** |
| next print | **2026-10-29** |

★ **`B1` — the second derivative.** The revenue **rate** is **not decelerating**: +11.3 → +22.2 →
+13.1 → +12.5 → **+16.0**, with the last quarter accelerating off the prior two. **The
two-consecutive-declines signal has not fired.**
★ **`B2` — peak-margin/low-multiple trap: does NOT apply.** 24.4× forward at **P/B 30.3** is not a
cheap-looking multiple; there is no low multiple to be trapped by. ⚠ **The opposite risk applies and
is stated**: a 30× book multiple has **no valuation cushion** if the revision book turns.
★ **Revision momentum, used only as a description of the denominator's direction** (never as a
leading indicator; `LLY` is not an IT name, so the measured IT-loading confound does not apply):
🚨 **the near quarter is 13 up : 1 down while the FORWARD quarter is 5 up : 10 DOWN.** **The breadth
flips sign one quarter out.** ⇒ **the first fundamental correlate of `LLY`'s flow drag that this desk
has found**, and it is a **breadth** signal, not a level one (the +1q level barely moved, +0.4%).

⚠ **`C2` — both halves.** The Merida acquisition is quoted as **"up to $2.875bn"**, i.e. a headline
value including contingent milestones; **the upfront consideration is not disclosed in the news
layer** ⇒ marked **`unknown` (C3)**. **No claim is made about its earnings impact.**

## 4 · The other side of the sector — the two reds, and the frame that fails on them

**`UNH` −0.765 🔴 (OBV −0.114, `rs20` −8.2) and `CVS` −0.828 🔴 (OBV −0.232, `rs20` −13.9)** carry
**the largest consensus upside on the whole 299-name board** (+20.9% / +24.7%, carried from 08-30).

### 🚨 Frame-transfer question, answered (the EXIT CHECK requires one)

**Frame: regulated-return / contractual floor** — the desk trusts it on `VST`'s PPA floor, `KMI`'s
RPO, `LNG`'s tolling, and (after `R111`) on memory take-or-pay.
**Applied to managed care: CHECKED, and it FAILS — and the 08-30 run already did the primary read.**
`UNH`'s own 10-K MD&A states Medicare Advantage rates ran *"well below the industry forward medical
cost trend"*, and the **Medical Loss Ratio floor CAPS insurer margin rather than guaranteeing it**.
⇒ **an MLR floor is a ceiling on the insurer, not a floor** — the frame **inverts** when transferred.
**Carried unchanged from 08-30; not re-derived, and cited rather than re-discovered** (`D398`-class
discipline: the desk checked `module_report_tags` and this is a known finding, not a first).
⇒ **The +20.9%/+24.7% "upside" is not protected by any contractual structure**, and both names remain
**rejected (`A.flow미도착`)** rather than promoted on valuation.

## 5 · The one green, and why it is weaker than a 🟢 suggests

**`A` (Agilent) — the universe's only Health Care 🟢 and one of just 4 greens in 299 names.**

| | A |
|---|---|
| flow / OBV / `vol_surge` | **+0.906** · **+0.330 매집** · **1.43** — the volume confirmation the other greens lack |
| `rs20` / `rs60` vs `SPY` | +8.1 / +9.9 |
| forward P/E | 22.56 · trailing 29.98 · P/B 5.83 |
| consensus upside | +14.5% |
| price / 52w | 152.28 of 108.35–163.75 ⇒ **−7.0% off the high** |
| **revenue QoQ series** | 🚨 **+6.5 · −0.8 · +4.2 · +3.5 · +2.1 — DECELERATING across the last three** |
| **+1q EPS, 90-day** | 1.5159 → **1.5011 = −1.0%, 0 up : 2 DOWN** |
| **next print** | 🚨 **2026-11-26 — no catalyst for ~3 months** |

★ **`B1` fires here and it is the file's cleanest rate signal**: **`+4.2 → +3.5 → +2.1` is two
consecutive declines in the RATE**, which is precisely the signal the lens says to act on, and the
**level** (still positive growth) is the distraction. ⚠ **Check for a contractual ceiling before
reading a rate as demand (`R111`'s lesson)**: `A` sells instruments and consumables under
**catalogue/spot pricing, not take-or-pay bands**; **no floor/ceiling structure is disclosed** in the
sources read this run ⇒ the deceleration is **not** arithmetic hitting a cap. Marked as read, with the
contracted share itself `unknown` (C3).
⇒ **The block around it is what carries the signal, not the name**: `TMO` +0.392, `DHR` +0.397,
`WAT` +0.426 — **all four Life Sciences Tools names positive.** **A breadth signal, not a lone spike**
(carried from 08-30 and re-confirmed), **but the leader's own rate is decelerating and it has no
catalyst until 11-26.**

## 6 · Customers named, with disclosed spend or print dates (`A6`)

- **Life Sciences Tools (`A`/`TMO`/`DHR`/`WAT`) → customers are pharma and biotech R&D budgets.**
  Those customers are **in this same sector and are measurably positive**: `MRK` +0.678, `AMGN`
  +0.556, `GILD` +0.506, `VRTX` +0.406, `PFE` +0.489. ⇒ **the tools block's demand side is visible and
  is not deteriorating** — the strongest single support for the breadth claim. ⚠ Their **R&D spend
  lines are not extracted** this run ⇒ `unknown` (C3); the flow tags are a proxy and are labelled as
  one.
- **Managed care (`UNH`/`CVS`) → customers are employers and CMS.** **CMS rate-setting is the
  binding constraint** and it is a policy variable, not a market one. Prints: `UNH` mid-Oct, `CVS`
  early-Nov — **outside every bracket window registered today**.
- **`LLY` → customers are payers and PBMs**, i.e. `CVS`/`UNH` — 🚨 **the sector's two 🔴 names are
  `LLY`'s own counterparties.** ⇒ **the GLP-1 pricing negotiation runs between the sector's biggest
  drag and its two worst flows.** Recorded as a structural observation; **no direction claimed.**
- **Print dates**: `LLY` **2026-10-29** · `A` **2026-11-26** · `MRK` late-Oct. **All outside this
  run's windows**, which is why `§8`'s rows are market observables.

## 7 · Lead/lag — tested or tagged

- *"Life-science tools orders lead pharma R&D spend by 2–3 quarters"* — **`[unverified]`**. Carried as
  a tag, **not used as evidence** above (`A5`). The demand-side argument in §6 rests on **coincident**
  flow tags, and says so.
- ✅ **Tested this run**: the mandate's hypothesis — *"the breadth is an `LLY` artifact"* — is
  **falsified by construction**: `eqflow` **+0.135** is equal-weighted and therefore contains no
  `LLY` weighting at all, and **30 of 32 names are non-red**. **The breadth does not need `LLY`
  removed to exist.**

## 8 · Track KPIs and anti-signals

| # | KPI / anti-signal | threshold | date |
|---|---|---|---|
| K1 | **`S133`** (ARMED) — *is the sector its constituents or its ETF?* | per its frozen row. ⚠ **This file deliberately does NOT pre-commit its answer**, per ROTATION's mandate | **2026-09-14** |
| **K2** | **★ `LLY`'s +1q revision breadth** — currently **5 up : 10 down** while 0q is 13 up : 1 down | a second consecutive month with net-down forward breadth **while `rs20` stays ≤ 0** confirms the drag has a fundamental cause; a flip to net-up says the drag was flow-only | **2026-10-29** |
| K3 | **`A`'s revenue rate** — currently **+4.2 → +3.5 → +2.1**, two consecutive declines | a third consecutive decline at the 11-26 print would make the sector's only 🟢 a `B1` sell-signal in its own right | **2026-11-26** |
| K4 | **The tools block as a unit** — `EW{A, TMO, DHR, WAT}` `rs20` vs `SPY` | all four staying positive keeps the breadth claim; two turning negative retires it | rolling, check **09-18** |
| A1 | **Anti-signal — CMS or drug-pricing policy print** | would move `UNH`/`CVS`/`LLY` together for a reason unrelated to any flow read, and would make `S133` AMBIGUOUS | live |
| A2 | **Anti-signal — `LLY` M&A follow-on** | the Merida deal's **upfront consideration is `unknown`**; a second deal in the window changes the balance sheet story the flow is reading | live |
| A3 | **Anti-signal — `A` has no catalyst until 11-26** | 🚨 **a 🟢 with no scheduled event for three months cannot be confirmed or falsified by a print inside any current window.** Its only observable is flow, and flow is the thing being tested | **2026-11-26** |

## 9 · Verdict on the mandate

**The breadth is REAL.** `eqflow` **+0.135 (rank 2/11)** is equal-weighted and therefore cannot be an
`LLY` artifact by construction; **30 of 32 names are non-red**; the positive block is coherent
(**Life Sciences Tools 4 of 4**, Biotech 6 of 7, Equipment 6 of 8) and the negative block is confined
to **managed care and distributors**.
**`OW` is KEPT** — and `MACRO` has no notch above it, which is stated so the absence is not read as
indifference.
**What this file adds beyond the mandate**: (i) **`LLY` drags the level and LEADS the change** —
27.9% of the sector's positive Δ — so the `D424` shape here is **level-only**; (ii) **`LLY`'s forward
revision breadth has turned (5 up : 10 down)** while its near quarter is 13 up : 1 down, which is the
first fundamental correlate of the drag; (iii) **the sector's only 🟢 (`A`) has two consecutive
declines in its revenue RATE and no catalyst for three months**, so the green is weaker than the tag
implies; and (iv) **the regulated-return frame INVERTS on managed care** — an MLR floor caps the
insurer — which is why the board's two largest consensus upsides stay rejected.
🚫 **No sizing here — BET owns it, and every sizing line must name its book (`C23`).**
