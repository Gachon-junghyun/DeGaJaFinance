# 🎯 타임폴리오 미러 — 2026-07-13 (월, FULL)

**모드: 집행 완료 (파서 버그 수정 후 밀린 청산 실행)**
**결론 한 줄: `holdings` 파서 버그의 근본원인(종목코드 'A' 접두)을 잡아 수정 → plan이 올바르게 오펀 2종 청산을 산출 → 유저 승인 하에 집행, 콘테스트를 책(flat)과 정합화. 밀린 하드스탑 미러를 뒤늦게 완결.**

---

## 1. 근본원인 규명 + 수정 (핵심)

- **증상:** `module_timefolio holdings`가 실보유가 있는데 빈 `{}` 반환 → `plan`이 "현재보유 0 → 매매없음" 오판(일주일째).
- **근본원인:** KRX UI 보유잔고 표의 종목코드가 **`A012450` 접두형**으로 렌더된다. `read_holdings`의 `re.fullmatch(r"\d{6}", code)`가 7자 `A012450`을 거부 → 전 데이터행 스킵 → 빈 반환. (컬럼 인덱스 5=비중/6=평가/8=손익%는 애초에 정확했음.)
- **수정:** `module_timefolio/__init__.py` `read_holdings` —
  `re.fullmatch(r"A?(\d{6})", code)`로 'A' 접두 허용 + `code = m.group(1)`로 6자리 정규화(책 `code6()` 키와 매칭). 이후 holdings/plan/sync 전부 정상.

## 2. 미러 계획 (수정 후)

| 종목 | 책 타깃 | 콘테스트 현재(수정후 정상 판독) | 액션 |
|---|---|---|---|
| 한화에어로스페이스(012450) | 0% | 3.4% (−17.0%) | **SELL 3.4% → 청산** |
| SK하이닉스(000660) | 0% | 0.8% (−14.2%) | **SELL 0.8% → 청산** |

- 책 한국모의(SSOT)는 flat(0종, 100% 현금). 이 두 종목은 07-09 book 하드스탑 청산됐으나 파서 버그로 콘테스트 미러가 누락돼 방치된 **레거시 오펀**. 정확한 미러 = 전량 청산.

## 3. 집행

- **arming:** `.env` 영구수정 없이 이번 실행에만 `TIMEFOLIO_EXECUTE=1` 인라인 → `sync --execute` (향후 스케줄런 자동발사 방지). 유저 명시 승인.
- **결과:** 둘 다 **제출됨**. 화면 확인 — "주문 추가 완료" x2 + "[TMS] 전량청산 처리" 모달. **보유 잔고 전부 0.0%.**

## 4. 콘테스트 계좌 (집행 후)

- NAV 990.28 (−0.14%), **순위 674 → 648위 / 2054명** (개선).
- 주간 회전율 **0.0% → 2.1%** (청산이 회전율에 기여, 가이드 ≥5%엔 아직 미달).
- 누적 위반 1 (증가 없음), 현재 가이드라인 위반 항목 없음.
- 보유: **flat (100% 현금)** = 책과 정합.

## 4.5. 장중 추가 진입 — GS 반타작 스타터 (유저 GO)

- **맥락:** 데스크는 GS를 "추격가(엔진 G4 SKIP)"로 대기시켰으나, 유저가 소액·공격 성향으로 부분 진입 GO. 트리거(79,000 눌림) 위 83,800이라 **풀사이즈 대신 반만 스케일-인**.
- **책 등록:** POSITION #55 한국모의 · GS(078930.KS) 3주 @83,800 · 스탑 75,240 · weight 2.5% · 폐기선 74,000. (책=SSOT 먼저 등록 후 미러 → 오펀 발산 방지)
- **미러 집행:** `sync --execute`(arming 인라인) → BUY 078930 2.52% **제출·체결**. 콘테스트 GS 297주 @83,992, 비중 2.5%(−0.3%). 가이드라인 위반 없음, 회전율 3.3%.
- **리스크:** 스탑까지 −10.2%, 책 기준 손실노출 ~0.26%(3주). 추격 대가는 파산 아닌 평단.
- **다음 액션:** GS **79,000 눌림 시 +3주 추가**(알람 #199 대기) → 평단↓·5% 완성. 74,000 이탈 시 테제 재검토·청산.

## 5. 후속

- 파서 수정으로 **자동 미러(sync) 신뢰 회복** — 다음 스케줄런부터 정상 미러 가능(안전차원 집행 전 스크린샷 대조 습관은 유지).
- 회전율 금주 2.1% → 신규 진입 트리거 발동 시 자연 상승. 위반 관리는 데스크 진입 페이스에 종속.

---
*스크린샷: 집행 전 `holdings_20260713.png` · 집행 후 `holdings_after_20260713.png` (둘 다 `llm_outputs/timefolio/`)*
