# SWEEP_READ — industry_kr · 2026-08-25 (화) · Stage 4/9 · L1·SWEEP

> **읽기이지 리포트가 아니다.** 숫자는 `SECTOR_FLOW_KR.json`(§scoring · §sector_rotation · §names)과
> `KR_LIVE_SHORTLIST.json` 에 있고 이 파일은 **그것들이 말할 수 없는 것만** 담는다.
> 섹터 행·종목 행 재인쇄 0건.

## 0 · 🚨 계기 건강 라인 — 숫자를 읽기 전에

`SECTOR_FLOW_KR.json §scoring`: **`vel_axis` false · `vel_coverage` 0.0605(50/826) · `n_axes` 3 ·
`scored` 806 · `dropped_missing_axis` 20.**

- **커버리지 6.05% < 80% ⇒ 이 런에서 뉴스축은 죽었다.** 원인은 **파이프이지 침묵이 아니다** — 프로브를 실제로 돌렸다:
  **스윕 전 6/6 성공**(삼성전자 d7 1,464) → **스윕 직후 0/2**(`URLError`, `curl` 000/exit35) →
  **냉각 +1분 2/3** → **냉각 +5분 3/3**(삼성전자 1,498). **`curl` 401 → 000 → 401.**
  ⇒ **자기유발 차단 4일 연속 재현.** 🚫 **`velocity` 필드 인용 전면 금지**(쇼트리스트의 `뉴스` 열 포함).
- **`dropped_missing_axis` 20종은 집계에서 제외됐다** — 옛 구현의 `clip(nan)=+1.0`(결측이 최대 양수) 경로는 작동하지 않았다.
- 🚨 **이 런의 더 큰 계기 문제는 뉴스축이 아니다**: **벤치 `^KS11` 의 08-24 봉이 없어 `asof` 라벨이 08-21 로 잘못 붙었고
  (내용은 08-24), 내장 `Δ` 열의 기준선이 08-19 로 밀렸다.** 상세·수치는 `preflight/PREFLIGHT.md §①`.
  ⇒ **이 파일의 모든 Δ 는 어제 파일 대비 직접 차분이다. 🚫 JSON 의 `delta` 필드 금지.**

## 1 · 유니버스 헤드라인 (3 숫자)

**n = 806** · 섹터평균 `wflow` **−0.020**(어제 −0.088) · 🟢 **86** / 🔴 **108**(어제 80 / 134).

## 2 · MACRO 매트릭스와의 교차검증 — **이 파일이 존재하는 이유**

### 2-a · 확증 3건

1. **제약 = 서사와 돈이 같은 방향.** 매트릭스는 제약을 ★★순풍으로 적었고, 쇼트리스트는 **한미약품·한미사이언스가
   둘 다 `flow_score` 최고치 + `✅진짜손`** 으로 나왔다. **그리고 오늘 머리층에 「한미약품 제넨텍 3.2조 기술수출」[6건/5매체]** 이 있다.
   ⇒ **오늘 이 보드에서 서사·가격축·KIS 실측이 셋 다 같은 방향인 유일한 섹터다.**
2. **운송·창고 = `wflow` 금지를 우회하는 정당한 경로가 열렸다.** 이 섹터는 플리퍼(HMM 35.6%)라 `wflow` 로 승강 불가인데,
   **KIS 실측(A급 축)이 독립적으로 확증한다**: HMM **외국인 +433만·기관 +250만·개인 −676만**,
   팬오션 **외국인 +526만·기관 +303만·개인 −794만**. **두 이름 모두 외국인·기관이 대량으로 사고 개인이 판다.**
   ⇒ **ROTATION 은 이 섹터를 `wflow` 가 아니라 KIS 실측으로 다룰 수 있다.**
3. **리츠 = 매트릭스의 역풍 판정이 쇼트리스트에서도 확인.** 네 지표(wflow·eqflow·참여율·실Δ)가 모두 음이고
   **쇼트리스트 0종**, 1조+ 후보도 2종뿐. **오늘 방향을 깨끗하게 말할 수 있는 두 칸 중 하나.**

### 2-b · 🚨 모순 2건 — **그리고 둘 다 돈이 아니라 게이트 쪽에 있다**

1. **건설: 매트릭스는 ★순풍(참여율 53.8%, `wflow` +0.204), 쇼트리스트는 0종.**
   **1조+ 후보가 8종이나 있는데 하나도 안 걸렸다.** 원인은 **🟢 태그 게이트**다 —
   건설의 **🟢율은 4.0%** 이고, 🟢 는 `_synthesize.py:16` 의 **`vol_surge >= 1.2`** 를 통과해야 켜진다.
   ⇒ **`M-101` 이 예측한 형태 그대로**이고, **`R91`(「건설에는 돈이 없다」) 회수의 원인이 쇼트리스트 층에서 재현됐다.**
   **돈은 있고 게이트가 막는다.**
2. **전기·전자: 매트릭스는 참여율 36.4% 하위권으로 적었는데 쇼트리스트 최다 3종.**
   ⇒ **섹터는 약한데 개별 이름 셋은 강하다** — `W5`(하위섹터 분산)의 교과서 사례.
   **ROTATION 은 이 칸을 섹터로 다루면 틀린다.**

### 2-c · 오늘 세션 자체가 매트릭스에 주는 한 줄
**실Δ 기준 22/28 섹터가 상승했고 🔴가 134 → 108 로 줄었는데, 같은 날 지수는 −3.12% 였다.**
⇒ **「지수는 밀렸고 종목의 폭은 개선됐다」.** 대형주(삼성전자 −8.70%)가 지수를 눌렀다.
**이 둘을 섞어 「시장이 나빴다」로 쓰면 806종 중 700종 이상을 잘못 서술하게 된다.**

## 3 · 쇼트리스트 구성 — **부재를 읽는다** (이름은 JSON 에 있다)

**15종 통과 · `✅진짜손` 13 · `❌약한손` 2.**

**0종 섹터 중 진단이 갈리는 것들:**

| 섹터 | 참여율 | 1조+ 후보 | 부재의 진단 |
|---|---:|---:|---|
| **건설** | 53.8% | **8** | 🚨 **게이트 아티팩트** — 🟢율 4.0%. **증거 아님** |
| **기계·장비** | 25.0% | **10** | **게이트 + 실제 약세 혼합** — 🟢율 4.0% 이지만 참여율도 하위권. **부분 증거** |
| **유통** | 37.3% | **15** | **실제 부재에 가깝다** — 후보 최다인데 0종. 🟢율 7.0% |
| **증권** | 33.3% | 7 | 실제 부재 |
| **통신** | 20.0% | 3 | 실제 부재(n=5, 섹터 판정 자체가 약함) |
| **전기·가스** | **60.0%** | 2 | ⚠ **참여율 2위인데 0종** — 🟢율은 20%다. **🟢 종목이 1조 플로어 아래에 있다는 뜻** ⇒ **시총 플로어 아티팩트** |
| **비금속** | **61.9%(1위)** | **1** | ⚠ **플로어 아티팩트** — 1조+ 후보가 1종뿐. **증거 아님** |
| **섬유·의류** | 55.6% | 1 | **플로어 아티팩트. 증거 아님** |

★ **부재 8칸 중 4칸이 필터 아티팩트다**(건설=게이트 · 전기가스/비금속/섬유의류=시총 플로어).
🚫 **ROTATION 은 이 넷의 0종을 「돈이 없다」로 읽으면 안 된다.**

## 4 · ⚠ `✅진짜손` 라벨을 읽는 법 (`R94`·`R95` 구속)

라벨은 **20일 누적 순매수 부호**로 붙는다. **20일 누적은 방향이 아니다** — 어제 이 데스크는 006360·005930 에서
**앞반/뒤반 10세션을 가르면 부호가 뒤집히는 것**을 측정하고 두 건을 회수했다.
⇒ **`✅진짜손` 13종은 「20일 누적 부호가 양」이라는 뜻이고, 「지금 사고 있다」는 뜻이 아니다.**
**DEEP 이 이름을 집으면 그 이름의 일별 계열을 다시 봐야 한다.**
⚠ **`❌약한손` 2종 중 롯데렌탈(089860)은 `S49-KR`(ARMED → 2026-10-30)의 대상 종목**이다 — 브래킷과 함께 읽어야 한다.

## 5 · 보유-유니버스 불변식
**보유 KR 2종 전부 유니버스 안**(316140 rank 30 · 028050 rank 75 / 833행) ⇒ **🚨 없음.**
⚠ **다만 유니버스는 41일 경과**(2026-07-15 빌드)라 **시총가중 서술은 「41일 전 가중」 조건부**다.

---
*읽기 산출물. 매수·매도·사이즈 언어 0건(P4). 섹터 표·종목 표 재인쇄 0건 — 숫자는 두 JSON 에 있다.*
