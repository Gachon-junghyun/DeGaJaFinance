# SECTOR_DEEP_HLTH — Health Care · industry_US · 2026-08-24 (Mon) · Stage 8 / L1·DEEP
### Continuous track (last covered 08-23) ⇒ **DELTA-LED.** Structure carried by reference to the 08-23 file.

> All flow figures `asof 2026-08-21`; `exc` = excess return vs **`SPY`** (named inline).
> ⚠ Declared deviation, 7th run: in-context serial execution, not a parallel agent fan-out.

## 0 · ROTATION's mandate, answered in one line

*"Rank 1 on `wflow`, `eqflow` and `exc5`, rank 2 on `participation` — and ZERO narrative coverage.
Is the leadership a mechanism or a one-week rotation artifact? Produce the direction body-read
EVENT_ALPHA could not."*

★ **Answer: it is a mechanism, and the mechanism is that TWO-THIRDS of this sector is not the sector
this desk keeps arguing about. The leadership is 24 pharma/biotech/tools names; the eight managed-care
and distribution names are a separate, negative business — and the ROTATION grade covers both.**
🚫 **The narrative gap is NOT closed** and this file does not pretend it is (§4).

## 1 · ★★★ THE DELTA — the sector is TWO businesses, and now the split is measured on four axes (`M878`)

The 08-23 file asserted *"the sector is TWO businesses and only one of them is the OW."* That was
stated. **Today it is measured, on the same frame, across four independent instruments:**

| Node | n | wflow | eqflow | **exc5 vs `SPY`** | **exc20 vs `SPY`** | participation |
|---|---:|---:|---:|---:|---:|---:|
| **Pharma / biotech / tools / devices** | **24** | **+0.349** | **+0.436** | ★ **+5.30** | ★ **+6.95** | ★ **75.0%** |
| **Managed care / services / distributors** | **8** | **−0.524** | **−0.391** | −0.35 | **−5.67** | **12.5%** |
| *Sector as graded* | 32 | +0.196 | +0.229 | +3.89 | +3.79 | 59.4% |

**Gap: 87.3 basis-point-equivalents on `wflow`, 12.62pp on `exc20`, and 62.5pp on participation.**
⇒ **Every one of the sector's rank-1 readings is produced by the 24 and diluted by the 8.**
The eight: `ELV` +0.233 · `COR` −0.206 · `CAH` −0.252 · `HUM` −0.349 · `MCK` −0.403 · `CI` −0.651 ·
**`UNH` −0.719 🔴 (7.2% weight, exc20 −10.91)** · **`CVS` −0.783 🔴 (exc20 −17.29).**
**Both of the sector's only two 🔴 names are in the 8.**

⚠ **The weight is where the dilution bites**: `LLY` is **19.4%** of the sector and prints only
**+0.160** flow with `exc20 vs SPY +1.34` and rs20 **+1.3**, OBV **중립** *(RULE D6 — grade-C, paired with flow, rs20 and exc20)* — i.e. **the largest name is the weakest of the 24 on
flow.** `wflow_ex_top1` **+0.205** vs `wflow` +0.196 confirms it: `top1_flips_sign = false`, so
`LLY` is not a flipper — but it is **not carrying the sector either.**

## 2 · ★★ Where the leadership actually is — and it is broad, not a single name

Of the 24: **18 carry OBV 매집 with rs20 > 0.** The top of the flow column is **not** the mega-caps:
`MRK` **+0.978 🟢** (5.6% wt) · `A` +0.711 (0.7%) · `BSX` +0.683 (1.3%) · `AMGN` +0.639 · `HCA` +0.611
· `BDX` +0.589 (rs20 **+19.2**) · `PFE` +0.583 · `MDT` +0.583 · `TMO` +0.568 (rs60 **+35.9**) ·
`DHR` +0.552 · `REGN` +0.550 (rs20 **+23.5**, rs60 +30.8).
★ **Life-Sciences Tools is the cleanest sub-cluster**: `A` (rs60 +35.2), `TMO` (+35.9), `DHR` (+24.4),
`WAT` (+18.0) — **four of four accumulating, all rs60 > +18**, and none of them is a household name in
this desk's files.
⇒ **This is breadth, not a rotation into one defensive mega-cap.** A one-week rotation artifact would
show up as `exc5 ≫ exc20`; here **`exc5 +5.30` and `exc20 +6.95` are the same order**, i.e. the move
has persisted for a month, not a week. **That is the mandate's discriminator and it points to mechanism.**

## 3 · `MRK` — the sector's only 🟢, read against `L2` and against consensus

`MRK` **$150.47** (52-w high $154.49, **2.6% below**) · flow **+0.978**, OBV 매집 *(RULE D6 — grade-C, never alone: paired on this line with rs20 **+12.8** and `vol_surge` **1.56**)*, `vol_surge` **1.56**
(clears the gate), **`exc5 +13.67` — the highest single-name 5-session excess this run measured on any
board** · exc20 +12.76 · exc60 +24.84 · Beta **0.21**.
**Valuation, multiple beside the denominator's history as `L2` requires:** forward P/E **15.77**,
trailing P/E **120.37** on trailing EPS **$1.25** vs forward EPS **$9.54**.
⇒ ★ **The trailing multiple is meaningless — it is a charge year.** The forward 15.77× is the only
readable number, and the **7.6× gap between trailing and forward EPS is the whole story**: the
denominator is expected to normalise by an order of magnitude. **A multiple built on a 7.6× EPS
recovery is a forecast, not a valuation** (`C3`: the realised path is `unknown` from this instrument).
🚨 **Consensus mean target $147.77 = −1.8% BELOW the current price** (median $150.00, high $186, low
$105; 5 Strong Buy / 15 Buy / 8 Hold / 0 Sell).
★★ **Cross-sector observation (`M881`): both DEEP sectors' best-flow names trade ABOVE their consensus
mean targets — `MPC` −10.1% and `MRK` −1.8%.** The money is accumulating into names the sell side has
stopped raising targets on. **That is the "already-priced" shape, and it is the single most useful
valuation gate this run hands to BET.**

## 4 · 🚨 The narrative gap is NOT closed, and this file says so rather than backfilling one

The mandate asked for the direction body-read EVENT_ALPHA could not produce. **This DEEP could not
produce it either, and the reason is instrumental, not analytical:**
- `brief --date 2026-08-23 --scope foreign` head (34 clusters): **zero Health Care clusters.**
- `thread --days 7 --scope foreign` top-20: **zero HC threads.**
- The term table has **no HC bucket** — and 265 of 280 single-outlet foreign events went unseen
  (`D339`), shown as a random 15, so **"no HC news" is not a measurement this desk can make today.**
⇒ **The sector's leadership is `[measured]` on price and flow, and `[unmeasured]` on cause.**
**Registering an HC term in the living table is a prerequisite for the next run**, and `MRK` stays in
the miss ledger (`Q.확신부족`, `--enters-if` an HC term reads 🟡ACCEL/🟢FRESH ∧ OBV 매집 ∧ rs20>0,
recheck **2026-09-03**) rather than being promoted on price alone.

## 5 · Value chain, 6 nodes, binding constraint marked

`discovery / tools` → `clinical trials` → **`regulatory approval` ← BINDING** → `manufacturing` →
`distribution (`MCK` `COR` `CAH`)` → `payer / managed care (`UNH` `CI` `ELV` `HUM`)`

- ★ **The chain's two right-hand nodes are exactly the eight negative names.** The sector's money is in
  the left half; the weakness is in the half that pays for it. **That is a coherent mechanism and it is
  the one thing this DEEP can offer in place of a news read**: cost pressure at the payer node is the
  mirror image of pricing power at the manufacturer node.
- ⚠ **Stated as a hypothesis, not a finding** — the desk has not measured payer cost trend, and
  `A6` (name the node's customers and check their disclosed spend) is **unmet**: the manufacturers'
  customers *are* `UNH`/`CI`/`ELV`, and their spend disclosures were not opened this run.
- `APPROVAL` is marked binding because it is the only node where a single dated event re-rates a name;
  **no dated FDA catalyst is on `CATALYST_WATCH` for any of the 32** (the calendar carries 5 binaries,
  none HC).

## 6 · Track KPIs and anti-signals (observables, dated)

| KPI | Reading now | Kills the thesis if |
|---|---|---|
| 24-name `exc20` vs `SPY` | **+6.95** | ≤ **0.00** by **2026-09-03** ⇒ the month-long persistence was a rotation after all |
| 24-name participation | **75.0% (18/24)** | < 50% while `exc5` stays positive ⇒ narrowing into a few names |
| Split gap (24 vs 8) on `exc20` | **12.62pp** | ≤ 3pp ⇒ the two-business reading dissolves and the sector must be graded as one |
| `MRK` vs consensus | **+1.8% above mean target** | `MRK` OBV turns 분산 while rs20 < 0 |
| **HC term registered in the living table** | **DOES NOT EXIST** | *(this is the prerequisite, not a kill)* — until it exists, no HC freshness claim is admissible |
| `LLY` (19.4% wt) | flow +0.160, OBV **중립** | `LLY` turns OBV 분산 ⇒ a fifth of the sector's weight leaves and `wflow` rank 1 goes with it |

## 7 · Verdict for BET (analysis only — no sizing, P4)

**HLTH OW is unchanged and is now specified as an OW on the 24, not on the 32.** Any BET sentence
must name **which half** it means; grading `UNH`/`CVS` inside an OW is the `W5` error this file exists
to prevent.
⚠ **Two constraints travel with it:**
1. **The leadership has no measured cause** (§4). It is `[measured]` on flow and `[unmeasured]` on
   mechanism, and the tags do not merge.
2. **`MRK` trades above its consensus mean target and 2.6% below its 52-week high**, on a forward
   multiple that assumes a **7.6×** EPS recovery.

---
*Analytical output only. No buy/sell recommendation, no sizing (P4).*
