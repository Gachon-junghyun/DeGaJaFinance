# SECTOR_DEEP_STPL — Consumer Staples · industry_US · 2026-08-24 (Mon) · Stage 8 / L1·DEEP
### Rotating slot ② — last covered **2026-08-17 ≈ 7 runs, the WORST recency on the board** ⇒ **FULL FRESH MAP.**

> All flow figures `asof 2026-08-21`; `exc` = excess return vs **`SPY`** (named inline).
> ⚠ Declared deviation, 7th run: in-context serial execution, not a parallel agent fan-out.

## 0 · ROTATION's mandate, answered in one line

*"Flipper-blocked at the sector level, so the DEEP is the only place it can be examined. `TGT` +0.950 🟢
vs `WMT` −0.333 🔴 = 17.1pp on one window — and BOTH clear the `vol_surge` gate, so this is not a
filter artifact. Plus Canada's 09-08 retaliation list names dairy and agricultural goods."*

★ **Answer: the `TGT`/`WMT` split is real, it is the largest same-industry divergence on the entire
board, and it is NOT the sector's main problem. The sector's main problem is that removing `WMT`
turns `wflow` positive but leaves `exc20` at −3.91 — i.e. the weakness is broad and `WMT` is only the
loudest instance.**

## 1 · The flipper, quantified from both sides (`M880`)

| Cut | n | wflow | eqflow | exc5 vs `SPY` | **exc20 vs `SPY`** |
|---|---:|---:|---:|---:|---:|
| **STPL as graded** | 19 | **−0.013** | +0.048 | +1.81 | **−4.17** |
| **ex-`WMT`** | 18 | **+0.116** | +0.069 | +2.40 | **−3.91** |
| Staples **retail only** (`TGT` `WMT` `COST` `KR`) | 4 | −0.214 | +0.058 | +0.82 | **+1.09** |

★★ **The two halves of the flipper test say opposite things.** On `wflow`, removing `WMT` flips
**−0.013 → +0.116** — the textbook 🚨1名 case, and the reason ROTATION was barred from moving the grade
(3rd consecutive run). **But on `exc20` vs `SPY`, removing `WMT` moves −4.17 only to −3.91.**
⇒ **`WMT` owns the sector's flow SIGN and owns almost none of its 20-session underperformance vs `SPY`.**
**The weakness is broad; `WMT` is the loudest instance of it, not its cause.**
★ **And the retail sub-node is the sector's ONLY positive `exc20` cut (+1.09)** — i.e. the drag is in
food, beverage, tobacco and household products, not in the stores.

## 2 · `TGT` vs `WMT` — the board's largest same-industry divergence, and both pass the volume gate

| | `TGT` | `WMT` |
|---|---|---|
| Weight in sector | 1.8% | **28.9%** |
| flow / tag | **+0.950 🟢가속** | **−0.333 🔴분산** |
| OBV | **매집** | **분산** |
| rs20 / rs60 | **+17.3 / +26.9** | **−8.9 / −14.6** |
| `vol_surge` | **1.51** | **1.68** |
| exc5 / exc20 / exc60 vs `SPY` | **+8.46 / +17.33 / +26.88** | **−8.67 / −8.90 / −14.55** |
| delta (d/d) | +0.061 | −0.173 |

★ **Both clear `vol_surge ≥ 1.20` — `WMT` at 1.68 is the highest in the sector.** ⇒ **This is not the
`vol_surge` filter artifact that explains ENRG's and COMM's zero-🟢; it is two companies in one GICS
industry moving 26.0pp apart on 20 sessions and 41.4pp on 60, on heavy volume both ways.**
**`W5` in its strongest form on this board.**
⚠ **Direction is `[measured]` on flow and `[carried]` on cause**: the inherited reading is that `TGT`'s
beat contained **$1.65 of $4.11 EPS = 40.1% tariff refund** while `WMT` fell on its print. **This run
did not re-open either filing**, and says so rather than restating the number as fresh.
🚫 **`S93` is VOIDed** and `S100` settled — **the sector currently has no live bracket pointed at the
`TGT`/`WMT` split.** That is the gap this DEEP surfaces.

## 3 · The sector's other anomaly — a name accumulating into a −52% relative collapse

`MNST`: flow **+0.333**, **OBV 매집**, `vol_surge` **1.64** — and **rs20 −52.5, rs60 −47.4,
exc20 −52.51, exc60 −48.48.**
★ **This is the exact inverse of the `D306` exhaustion-geometry class**: not a spike on a losing base,
but **accumulation on heavy volume into a collapse.** `KMB` shows a milder version (OBV 매집,
rs20 −4.0, rs60 +7.1).
⚠ **`D6` binds: OBV is a grade-C signal and must not be cited alone.** Two axes disagree here
(`OBV 매집` + `vol_surge` 1.64 vs `rs20 −52.5`), so this file records the anomaly and **draws no
verdict** — the honest output is a dated observation, not a bottom call.
⇒ **Filed as an observation with a recheck, not as a candidate.** No name to BET.

## 4 · Value chain, 6 nodes left → right, binding constraint marked

`agricultural input (`ADM`)` → `packaged manufacture (`MDLZ` `HSY` `KO` `PEP` `KDP` `MNST`)` →
`household & personal care (`PG` `CL` `KMB` `KVUE`)` → `distribution (`SYY`)` →
**`retail shelf: `WMT` `COST` `TGT` `KR`` ← BINDING** → `consumer`

- **Binding constraint = the retail shelf**, because it is the node that sets price realisation for
  every node to its left, and it is the node where the sector's only positive `exc20` cut sits (+1.09).
- ★★ **The chain's LEFT edge is its worst name**: `ADM` (Agricultural Products) flow **−0.689 🔴,
  OBV 분산, rs20 −10.2** — the sector's second 🔴 and the only one that is not `WMT`.
  ⇒ **Input and shelf are diverging**, which is the shape a margin transfer down the chain produces.
- 🚨 **NEW OBJECT, dated, and it enters at the left edge**: **Canada's retaliatory tariffs take effect
  2026-09-08 and the published target list names *dairy* and *agricultural goods*** (`dw` 08-22 body,
  `aljazeera` 08-23 body — direct calls outside a sweep window, ≥2 outlet bodies).
  ⇒ **`ADM` is the sector's most exposed name to a dated event that no bracket on this board reaches**
  (`D342`: `P92`/`P93` both settle 08-28, eleven days early; **`S123`** was registered today to span
  09-08 but is keyed to **Industrials**, not to Staples).
  ★ **This DEEP's recommendation to PREMORTEM for the next run: the Staples/agricultural leg of the
  09-08 event is unbracketed.**
- ⚠ **`A6` partially met**: the customers of the packaged node are the four retailers, and their
  disclosed direction is measurable — `TGT` +17.33 vs `WMT` −8.90 vs `COST` −2.27 on `exc20`. **Their
  capex/spend disclosures were not opened**, so the demand side is `[unmeasured]` and dated to their
  next prints.

## 5 · Track KPIs and anti-signals (observables, dated)

| KPI | Reading now | Kills the thesis if |
|---|---|---|
| `exc20` ex-`WMT` | **−3.91** | ≥ **0.00** by **2026-09-08** ⇒ the "weakness is broad" reading is void and it was `WMT` all along |
| `TGT` − `WMT` `exc20` spread | **+26.23pp** | ≤ **+8.00pp** by 09-08 ⇒ the divergence was a print artifact, not a franchise divergence |
| Retail sub-node `exc20` | **+1.09** (sector's only positive cut) | turns negative while the sector's stays negative ⇒ the binding constraint moved off the shelf |
| `ADM` | **−0.689 🔴, OBV 분산** | ★ recheck **2026-09-09** (the session after Canada's tariffs take effect) — this is the sector's dated exposure |
| `WMT` `top1_flips_sign` | **true, 3rd run** | prints `false` ⇒ ROTATION's flipper block lifts and the grade becomes movable |
| `MNST` | OBV 매집 ∧ `vol_surge` 1.64 ∧ rs20 **−52.5** | recheck **2026-09-08**: if rs20 > 0 with OBV still 매집, the accumulation was informed; if OBV turns 분산, it was distribution mislabelled |

## 6 · Verdict for BET (analysis only — no sizing, P4)

**STPL stays `N`, flipper-blocked, and this DEEP adds three things the grade does not carry:**
1. **`WMT` owns the sign but not the weakness** — `exc20` is −4.17 as graded and **−3.91 without it**.
   Any "Staples is weak because of Walmart" sentence is falsified by 0.26pp.
2. **The only positive `exc20` cut in the sector is the retail shelf (+1.09)**, which is also the
   chain's binding constraint — the money and the mechanism agree on which node matters.
3. 🚨 **A dated, unbracketed exposure**: **Canada's 09-08 tariff list names dairy and agricultural
   goods**, and the sector's left-edge name `ADM` is already its second 🔴. **No row on this board
   spans that date for Staples.**
⚠ **And the constraint that travels with all three**: this DEEP produced **no live bracket** for the
`TGT`/`WMT` split because `S93` VOIDed and `S100` settled. **The board's largest same-industry
divergence is currently unmeasured going forward.**

---
*Analytical output only. No buy/sell recommendation, no sizing (P4).*
