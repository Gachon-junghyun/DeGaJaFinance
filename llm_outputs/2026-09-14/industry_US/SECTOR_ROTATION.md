# SECTOR_ROTATION — industry_US · 2026-09-14 (Mon, pre-open) · Stage 6 / L1·ROTATION

> Delta-only. MACRO §E owns the 11-sector verdict; `SECTOR_FLOW_US.json §sector_rotation` owns the flow
> numbers. **DEEP budget N = 4** (protocol line, unchanged).

## 🚨 Instrument state governing every number below
`§scoring` (load-bearing `--no-news` file): **`n_axes` 3 (`nonews`) · `vel_coverage` 0.0% · scored 299 ·
dropped 0** ⇒ **zero deltas cite velocity or freshness.** ★ **`asof` 2026-09-11 = the 09-12 run's close: the
sweep is a REPRINT** — `flow_score` 0/299 changed, `tag` 0/299 changed, Δ (09-04 → 09-11 weekly) identical.
**Every flow number this stage could cite was already spent by the 09-12 run.** Sweep #1 (news axis
half-alive) is quarantined — its 36 `new_green` and 33 🟡→🟢 promotions are an artifact (PREFLIGHT G1,
SWEEP_READ §0) and **no line below reads them**. Cap weights 61 d stale ⇒ `eqflow` is the citable cut.
**Flip list (reproduced in full, §2).**

## §1 · Inherited — one line, verbatim from MACRO §E
`MACRO holds: ENRG OW (split inside: crack leg vs barrel leg, moving apart live) · IT N (N+ candidate on breadth) · COMM no-verdict · UTIL N− · INDU UW− · STPL UW (eqflow/breadth only) · RE UW− · MATR N− · FIN N (N− candidate) · DISC UW (eqflow) · HLTH N (P150 armed)`.

## §2 · Deltas — **0 of 11.** Flow confirms the matrix on all 11 — because the flow is the same flow.

A delta must be carried by a flow number; **there is no flow number today that the 09-12 stage did not
already read on the same close.** The 09-12 run's two deltas (`FIN N → N−`, `UTIL N− → UW`) were both
*applied by MACRO §E* on 09-12 and are inherited in §1 as the standing verdicts — they are not re-issued.

**Flip list, 11/11** (`top1_flips_sign` · issuer rescan): Energy F (`XOM` 30.5%, ex-top1 +0.486) · Materials F
(`LIN` 24.7%) · IT F (`NVDA` 19.4%) · **Comm. Services F by flag, YES by issuer (Alphabet 2 classes 76.6%:
−0.148 → +0.400)** · Health Care F (`LLY` 19.4%) · **Consumer Staples T (`WMT` 28.9%: −0.186 → +0.023)** ·
Cons. Disc. F (`AMZN` 40.2%, −0.240 → −0.037 same sign) · Utilities F (`NEE` 17.7%) · Financials F (`BRK-B`
13.9%) · Real Estate F (`WELL` 16.9%) · Industrials F (`CAT` 8.7%). ⇒ **STPL and COMM may not be promoted or
demoted on `wflow`** — no delta touches them.

**Attempts declined, each with the reason (all are non-flow arguments today):**
- `ENRG OW → OW+` **and** `ENRG OW → OW−`: both attempted from MACRO §A-4's **live** tape (barrel +2.85%,
  distillate crack ≈ −6.6 $/bbl at 13:39 KST) and the second-chokepoint titles (≥9 / ≥6 outlets). **Declined
  — an unsettled bar and a headline count are not flow numbers** (`D577`; G1). The bucket's flow is unchanged
  (`eqflow` +0.431 rank 1, 11/16 OBV accumulating — RULE D6 exempt: the OBV count here diagnoses the 🟢-gate filter, it carries no verdict; 0 🟢 = gate artifact). The *inside* of the OW is the live question
  (crack leg vs barrel leg vs freight leg) and that is **the DEEP-ENRG brief** (§3), not a notch.
- `IT N → N+`: declined a second run — flow still disagrees with the tape (`eqflow` −0.207 rank 6, 23 🔴 of
  56, Δ −0.012 flat) and today adds `NQ=F` −1.25% live (unsettled, not a flow number). `P111`/`S148` settle
  tonight; PREMORTEM decides whether IT takes a slot again.
- `FIN N− → UW`: the only new input is the weekend "rate-hike fever" title cluster (≥9 outlets) and `MET`'s
  FINRA z +1.95 — **narrative + a name-level short-pressure read, not sector flow**; declined. `eqflow` −0.269
  rank 8 unchanged. `P151`/`P124` decide.
- `HLTH N → N−` / `→ N+`: level rank 3 vs Δ −0.250 sign-split unchanged (`C5`); `S133` tonight, `P150` 09-18.
- `MATR N− → UW`: `eqflow` −0.200, 2/12 — numbers unchanged; `C24` (copper COT 100th, 6th run) — declined,
  no new number.
- `INDU` · `RE` · `DISC` · `UTIL` · `STPL`: no new number; no attempt.
- `COMM`: barred (Alphabet 76.6% at issuer level, 20th run); `META` carried as a name to BET §B.

**Macro re-arguments attempted and declined: 2** (ENRG on the live product/barrel split; FIN on the hike
cluster). **Logged, not used.**

## §3 · DEEP picks + DEEP_LOG

**Rule at N = 4 (2 continuous + 2 rotating):**
- ① Continuous-track ⌈N/2⌉ = 2 = today's top OW ranks. **Only one OW sector exists (ENRG)** → **ENRG keeps
  its continuous slot** (held 09-12; still the sole OW). Second continuous slot: **no second OW — not padded.**
- ② Rotating ⌊N/2⌋ = 2 = next-highest OW not deep-dived in ~3 runs. **No OW candidate exists** → **empty, not
  padded** (the rule forbids N/UW padding).
⇒ **N = 1/4 filled by rule** — `M1371` (US DEEP budget under-filled) **8th consecutive run**, human item.
**Three slots are offered to PREMORTEM**, whose promotion rule is the only path for a non-OW sector today.
Candidates **in this order, with today's reason**:
1. **IT** (N, last DEEP 09-12 = 0 runs) — tape/flow divergence carried a 2nd run; `P111` + `S148` settle
   tonight; `NQ=F` −1.25% live; the weekend's loudest cluster (AI governance/IPO supply) is *not* a demand
   print (EVENT_ALPHA Card 4) while the assembler/optics node is 🟢 (Card 6). Book's largest concentration.
2. **FIN** (N−, last DEEP 09-08 = 2 runs) — FOMC 09-16 inside 48 h of *trading*; the insurer/exchange split
   is the live object (Card 3), the book holds both sides (`MET` z +1.95 🔴 vs `NDAQ` 🔴).
3. **HLTH** (N, last DEEP 09-08 = 2 runs) — `S133` settles tonight; `P150` 09-18.

**DEEP-ENRG brief (the sole slotted sector) — the question, not the answer:** the OW is **three legs**
today — (a) crack/refining (held `MPC`/`PSX`; crack ≈ −6.6 live from a 99th-pctile level; `S156`-A is the
against-us bracket), (b) barrel/E&P (0% held; `P149`; `XOM` revived on the ledger today; barrel +2.85%
live on the Saudi East-West shutdown), (c) freight/tankers (0% held, 0% measurable in the universe;
`P152` registered today; basket 🟢 ×5 at the 91.7th pctile). **DEEP must say which leg the OW's evidence
actually sits on tonight**, and whether (a) and (b) are still one bet when they move apart.

## DEEP_LOG 2026-09-14: continuous=[ENRG] rotating=[] · **N=1/4 filled by rule (only one OW sector exists; padding forbidden); three slots offered to PREMORTEM with candidates IT · FIN · HLTH in that order** · **verdict deltas 0 of 11 — the sweep is a byte-equivalent REPRINT of the 09-12 close (`asof` 09-11; `flow_score` 0/299 changed; Δ 09-04→09-11 identical); every flow number was already spent by the 09-12 stage; the 09-12 deltas (`FIN N→N−`, `UTIL N−→UW`) are inherited via MACRO §E, not re-issued** · **attempts declined 6, each with the reason: `ENRG OW→OW+/OW−` (live unsettled tape + headline counts are not flow numbers — routed to DEEP-ENRG as the three-leg question), `IT N→N+` (2nd run; `eqflow` −0.207 rank 6, 23 🔴/56, Δ flat; `NQ=F` −1.25% live is not a flow number), `FIN N−→UW` (title cluster + `MET` z +1.95 = narrative + name-level, not sector flow), `HLTH N→N−/N+` (level/Δ sign-split, `C5`), `MATR N−→UW` (no new number; `C24` 6th run), `COMM` (barred, Alphabet 76.6% issuer flip, 20th run)** · **macro re-arguments attempted and declined: 2 (ENRG live split; FIN hike cluster)** · **flip list: by flag 1/11 (STPL/`WMT`/28.9%); by issuer 2/11 (+COMM/Alphabet/76.6%)** · 🚨 **instrument state: `n_axes` 3 (`nonews`), `vel_coverage` 0.0% on the load-bearing file; sweep #1 (62.9% coverage, half-alive pipe) QUARANTINED — its `flow_tag` promoted 33 names 🟡→🟢 on a `velocity ≡ 4.29` local-pool artifact while `flow_score` correctly dropped the axis (`D603`/`D604`); cap weights 61 d stale ⇒ `eqflow` cut on every line** · ★★ **the structural line: this is a settlement-and-bracket run — 13 brackets settle on tonight's close (`S133` `S146` `S147` `S148` `S150` `S152` `P111` `P136` `P139` `P143` `P145` `P146` + `P148` 09-17), and the next ROTATION is expected to move on VERDICTS, not on narrative; the run's new information is entirely non-sweep (second chokepoint ≥9/≥6 outlets, live barrel-up/product-down, G7 hike leg, SpaceX/NDX 09-18 mechanism)** · **uncovered=[IT(N, last DEEP 09-12 = 0 runs, offered #1 — `P111`/`S148` tonight, Cards 4/6), FIN(N−, last DEEP 09-08 = 2 runs, offered #2 — FOMC inside 48h of trading, Card 3), HLTH(N, last DEEP 09-08 = 2 runs, offered #3 — `S133` tonight, `P150` 09-18), UTIL(UW, last DEEP 09-07 = 3 runs, `S146` tonight — UW, not slotted), INDU(UW−, last DEEP 09-08 = 2 runs, `S150` tonight; Houthi/Saudi defense-demand titles ≥6 outlets, `RTX` 🔴), MATR(N−, last DEEP 09-01 = 6 runs, `C24` 6th run, copper thread 0 outlets 09-11/12/13), COMM(no-verdict, last DEEP 09-01 = 6 runs, barred 20th run), STPL(UW, last DEEP 08-27 = 10 runs, 🚨1名), DISC(UW, last DEEP 08-27 = 10 runs), RE(UW−, last DEEP 08-26 = 11 runs — longest gap, 8th consecutive run unreachable by rule, `S152`/`TLT` tonight, named)]** · **MANDATORY brackets handed to PREMORTEM: (1) FOMC + SEP 2026-09-16 14:00 ET ≈ 62 h from run start / 2 sessions from tonight's open — `P148` (slope) + `P151` (level) span it; PREMORTEM re-confirms anti-signals and does NOT add a third FOMC row unless it measures something neither does (`D343`); (2) BoJ "next week" (titles; date `[blank]`, `D607`) — `P153` is written to contain it; PREMORTEM decides whether a US-equity leg is needed; (3) S&P rebalance / quad witching 09-18 now carries a named mechanism (SpaceX Nasdaq-100 weighting boost, Bloomberg title) — a counted-supply row is admissible only on a published pro-forma (EVENT_ALPHA Card 5); (4) the Saudi East-West shutdown is a dated escalation INSIDE `P149`/`S156`/`P152` windows — PREMORTEM must state which existing anti-signal it trips (none by pre-declaration) rather than register a new oil row**

## ✅ EXIT CHECK — ROTATION
- [x] §1 is one line, verbatim from MACRO §E.
- [x] §2 delta count **0**, stated with the reason (reprint); every declined attempt carries its number
      or is named as a non-flow argument and declined.
- [x] No promotion/demotion rests on a flipper bucket; flip list reproduced (1/11 by flag, 2/11 by issuer).
- [x] Axis count stated (`n_axes` 3); no velocity/freshness citation; Δ read only against the same-mode
      snapshot and labelled as the 09-04 → 09-11 week.
- [x] Every matrix×flow divergence named with an owner (IT → PREMORTEM/DEEP; ENRG legs → DEEP-ENRG;
      FIN split → `P151`/`P124`; HLTH → `S133`/`P150`).
- [x] N = 1/4 by rule; no padding; uncovered sectors named in DEEP_LOG with recency.
- [x] Linter: run on this file (`_lint_rotation.txt`).
- [x] DEEP_LOG appended.
