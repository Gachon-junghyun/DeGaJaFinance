# BLINDSPOT_PREMORTEM — industry_US · 2026-08-16 · Stage 7/11 (L1·PREMORTEM) ★US-only

> Four adversarial lenses argue **AGAINST our own tilt** before the deep budget is committed.
> Anti-tunnel. Benchmark **`SPY`** named inline (C1). No sizing (P4).

## §0 · Execution note — a declared deviation, logged rather than hidden

The L1 specifies this stage as a **4-lens Agent fan-out in one message**. **This session carries a
standing operator constraint that the Agent tool is not to be invoked unless the user requests it**,
and this is an unattended scheduled run with no one to ask. ⇒ **The four lenses were executed in-run,
sequentially, against the same inputs the fan-out would have received** (the draft tilt, the 4 DEEP
picks, `SECTOR_FLOW_US.json`, `CATALYST_WATCH.json`, `CYCLE_EXPOSURE.json`, `EVENT_ALPHA.md`).
⚠ **State the cost honestly**: sequential self-execution loses the fan-out's independence, which is the
property that makes the lenses adversarial. **Treat this stage's dissent as a lower bound.**
Resolved per the unattended-run rule; logged here and in the run summary.

⚠ **And a second constraint binds every lens below**: PREFLIGHT revokes *"the tape has gone quiet on
this risk"* as evidence — **measured, 0 of 80 silent names across two runs were actually quiet.**
No lens may argue from absence of coverage.

---

## §1 · LENS 1 — UNDER-COMPUTED LEGS

**Question**: which sector or sub-leg did we NOT deep-dive that has a catalyst inside ~5 trading days?

DEEP committed to **ENRG · INDU · IT · UTIL**. Uncovered: **HLTH · MATR · FIN · COMM · DISC · RE · STPL**.

| Uncovered leg | Dated catalyst ≤5 sessions | Flow `asof 2026-08-14` | Verdict |
|---|---|---|---|
| ★★ **The CONSUMER COMPLEX (DISC ∪ STPL)** | **`HD` ~08-19 · `WMT` ~08-20 · `TGT` ~08-20** — three prints inside the window, and **`CATALYST_WATCH` carried ZERO of them** | `WMT` 🟡 (RS60 **−19.9** vs `SPY`) · `HD` **🔴분산** · `COST` **🔴분산** · **`TGT` 🟡 but OBV 매집, RS20 +6.2, RS60 +15.6** · `ABNB` **🟢가속** RS60 **+34.5** | ★ **PROMOTE-TO-DEEP (5th slot)** |
| **RE** | **`S80` settles 08-19** (is UTIL/RE/STPL one duration bet?) | 4 of 12 accumulating = `M131`'s data-centre unit | **WITHIN-RUN-WATCH** — `S80` scores it without a DEEP |
| **HLTH** | `S76` **08-19** · `S82` **08-20** (**already above its branch A at +3.091**) | 15 of 32 accumulating; Δ −0.103 worst of eleven | **WITHIN-RUN-WATCH** |
| **MATR** | `S77` **08-19** | 6 of 12 accumulating; flipper `LIN` | **WITHIN-RUN-WATCH** |
| **FIN** | `S78` **08-19** | `KKR` the only admissible 🟢 | **WITHIN-RUN-WATCH** |
| **COMM** | **`Reddit` joins the S&P 500 on 08-18** — a dated index rebalance in a Comm-Services name | `NFLX` is the run's only `new_green` and it is **velocity-produced** ⇒ not an ignition | **WITHIN-RUN-WATCH** — `RDDT` is outside `us_top300` so no axis exists |

### The promotion, argued

**Bull case for the consumer complex that this run did NOT make.** Three dated prints land inside five
sessions on a complex the desk has **not** deep-dived since **08-03 (DISC) / 08-10 (STPL)** — and
`STPL` is the **longest-uncovered sector on the board at 6 runs**, declined a slot by ROTATION on the
grounds that *"its mandate is empty."* **That judgement is now wrong**, and this lens says so:
1. **The straddle is genuinely event-priced.** `WMT` implied **±5.0%, expiry 2026-08-21 (D5)** — it
   **covers** its own print. That is rarer than it sounds (see §2's `NVDA` counter-case).
2. **The label splits inside itself, both ways.** In Staples, `TGT` is **OBV 매집 with RS20 +6.2 and
   RS60 +15.6 vs `SPY`** while `WMT` (**−19.9**) and `COST` (**−18.0**) disperse. In Discretionary,
   `ABNB` (**+34.5**) and `HD` (**🔴분산**) sit in one bucket. ⇒ **`W5` fires on both labels
   simultaneously**, which is exactly the condition a sector-level verdict cannot express.
3. **A dated macro driver already printed**: *"US retail sales post first decline in nine months in
   July"* (08-14) and *"'Nobody escaped': Walmart, Bank of America, TransUnion raise major red flag over
   US consumers"* (08-14).
4. **ROTATION's own decline is refutable on its own terms**: it held DISC at N− because **Δ +0.038 is
   positive** — but Δ is a *frozen* number this run (zero sessions), so "the delta blocks it" is a
   statement about the calendar, not about demand.

⇒ **PROMOTED to a 5th DEEP slot: `CONSUMER` (DISC ∪ STPL as one object).**
**Mandate**: resolve whether the goods/services and mass/big-ticket splits are one factor or two, using
the three prints as the test — **not** the GICS labels. **Registered as `S93` below.**

---

## §2 · LENS 2 — REGIME-FLIP / BOTH-SIDES ★ MANDATORY

`CATALYST_WATCH.json` carries exactly **one 🔀binary in window**:
**`Iran 'Strait of Hormuz open' statement (TACO trigger) · axis=oil · undated`.**
🚨 **It is the object `S8` has failed to date for 14 consecutive runs.** The protocol makes a both-sides
bracket **mandatory** for any binary ≤48h out, and a one-way tilt into a known binary a **protocol
violation**. **This run's #1 sector tilt (ENRG OW) is a one-way tilt into exactly this binary unless
this section brackets it.**

### 2a · The against-us branch, argued at full strength

**Our tilt**: ENRG OW, carried by refining (`MPC` 🟢, `PSX`/`VLO` filtered-out leaders), justified by
`P66`'s supply-destruction mechanism.
**The branch that hurts**: **a Hormuz de-escalation or an Iran accommodation.** Its evidence is already
on the tape and was **not** buried by this run:
- **08-13** *"Crude Prices Undercut as Persian Gulf Tensions Ease Slightly"* [`nasdaq`]
- **08-13** *"Oil falls on weaker demand outlook and higher US stocks"* [`yahoo_finance`]
- **08-12** *"Kyiv stops strikes on Russian port after request from JD Vance"* [`semafor`] — ★ **a named
  US actor braking the OTHER leg of the same thesis in the same week**
- **`Iran` term velocity 0.88× against `Hormuz` 1.15×** — the country story is the decelerating one
- **`WTI` speculative net at the 18th percentile SHORT** (COT Tue 08-11) ⇒ **the crowd is already
  positioned for this branch**, which means the pain trade on a de-escalation is *ours*, not theirs

**What rips on that branch**: airlines, consumer discretionary (the complex §1 just promoted), the
rate-sensitives our UW side holds (UTIL, RE), and any name whose cost line is fuel.
**Which of our OWs gets hit**: **ENRG, the board's top OW**, and specifically the refining leg —
`MPC`(held) · `PSX`(held) · `VLO`. `XOM` is the partial hedge **only** because it has **0% refining
exposure** (`M146`), which is also why it ranks 5/6 in its own bucket.

### 2b · ★ B4 — grade the branches by information content BEFORE the event

**The branches are NOT symmetric, and this is the finding that makes the bracket worth registering:**

| Branch | What it can do to the thesis |
|---|---|
| **Hormuz OPENS / Iran accommodation** | Kills the **war-premium** leg. ⚠ **Leaves the Russia supply-destruction leg (`P66`) completely intact** — Salavat is still down, Russia is still importing Indian fuel, diesel exports are still at a multiyear low. ⇒ **CONFIRM-only against a partial thesis; it cannot falsify the refining case** |
| **Hormuz CLOSES / US declares territory** | Confirms the war-premium leg **and** the crack leg together ⇒ **also confirm-only** |
| ★ **The genuinely falsifying event neither branch contains** | **A US-brokered halt to Ukrainian strikes on Russian energy infrastructure.** *That* breaks `P66`'s named mechanism, and it is the branch with a **dated first datapoint already on the board** (`semafor` 08-12) |

⇒ **The Hormuz binary alone would be a bracket where neither branch changes the conclusion — which the
carry rules say is a bracket not worth spending.** **So this lens registers the bracket the desk
actually needs**: `S92` brackets **Hormuz AND the strike-halt together**, because only their
**conjunction** can falsify the Energy OW.

### 2c · Starter list, trigger, invalidation (P4 — this is exposure mapping, not sizing)

| If the against-us branch fires | Cleanest expressions | Trigger | Invalidation |
|---|---|---|---|
| Hormuz de-escalation | `ABNB` (🟢가속, RS60 +34.5 vs `SPY`, clean-rise short z −0.94) · `TGT` (OBV 매집, RS60 +15.6) | `CL=F` closes **< 78.00** on a settled session | `CL=F` **> 82.40** (the 08-14 close) with a dated US action on the Strait |
| Refining leg unwind | Reduce reliance on `wflow`-free reads: `MPC`'s own **`vol_surge` 1.25** is the only 3-axis leg it has | `HO=F` − `CL=F` 20d gap **≤ 0pp** at the 08-21 settle | gap **> +4pp** |

### 2d · ⚠ The correlated-UW check the field note demands

**UTIL + RE + STPL are one bet in disguise, and `S80` (settles 08-19) already brackets exactly that.**
★ **This run adds the reason it matters now**: `P65` measures the long-end move as a **BULL steepener**
(30y−10y **0.53 → 0.58** while `DGS30` **FELL** and `DGS2` fell **10bp**), not a term-premium level
rise. **A falling front end is a tailwind for all three legs of that UW simultaneously.**
⇒ **Three of the desk's five UW/UW− verdicts may be one rate bet whose sign just changed.** Not
re-bracketed (that would duplicate `S80`); **named here so `S80`'s scoring on 08-19 is read as a
three-sector event, not a one-sector one.**

---

## §3 · LENS 3 — MOMENTUM-CONTINUATION ("already ran +100%" ≠ avoid)

Re-tagged on RS60 **and** RS20 vs **`SPY`** together, `asof 2026-08-14`. The flip condition is stated
for each.

| Name | RS60 | RS20 | Flow | Re-tag | Flip condition |
|---|---|---|---|---|---|
| `ABNB` | **+34.5** | **+21.6** | 🟢 surge 1.55 | ★ **EXTENDED-BUT-LIVE** — both windows positive, 3-axis producible, clean-rise short | RS20 turns negative while RS60 stays positive |
| `ANET` *(held)* | **+34.6** | +13.5 | 🟡 OBV 매집, surge **0.85** | **EXTENDED-BUT-LIVE**, blocked on volume only | OBV flips to 분산 |
| `MU` | +33.3 | +10.0 | 🟡 OBV **중립**, surge 0.64 | **LIVE BUT THINNING** — the OBV leg has already gone neutral | OBV 분산 |
| `MPC` *(held)* | +29.3 | +9.3 | 🟢 surge 1.25 | **EXTENDED-BUT-LIVE** | `HO=F`−`CL=F` gap ≤0 |
| `STX` | +26.9 | +19.1 | 🟡 OBV 매집 | **EXTENDED-BUT-LIVE** | — |
| `VLO` · `PSX` | +24.3 · +22.3 | +5.9 · +8.5 | 🟡 OBV 매집, surge 0.84 · **1.01** | **LIVE, filtered out** (`D251`, 2nd run) | — |
| `PLTR` | +22.9 | **+27.0** | 🟡 surge 0.90 | **ACCELERATING** — RS20 **exceeds** RS60, the opposite of exhaustion | RS20 < RS60 |
| `RTX` *(held)* | +22.0 | +10.8 | 🟡 OBV 매집, surge 0.64 | **EXTENDED-BUT-LIVE** | — |
| `BAC` | +21.4 | **+0.8** | 🟢 **velocity-produced** | ⚠ **DECAYING** — RS20 at zero, and its 🟢 is not 3-axis producible | already flipped |
| `MRVL` | +20.1 | +13.2 | 🟡 surge **0.48** | **LIVE, thin** — lost its 🟢 this run on velocity alone | volume, not price |
| `AMD` | +18.4 | **−0.7** | 🟡 | ⚠ **DECAYING** | already flipped |
| `AMAT` | +18.8 | **−8.7** | **🔴분산** | 🚫 **EXHAUSTED** — RS60 is a decaying stock of past excess (`M149`'s shape) | already flipped |
| `KLAC` | +11.2 | **−8.7** | 🟡 OBV 중립 | 🚫 **EXHAUSTED** | already flipped |
| `TSLA` | −21.1 | −14.6 | 🔴분산 | not a runner | — |

### ★ The lens's actual finding — the desk's two best names are the OPPOSITE shape from what it screens for

| Name | RS60 | RS20 | Reading |
|---|---|---|---|
| **`COHR`** | **−13.7** | **+12.9** | 🟢 surge **1.67**, flow **+0.99 = board top** |
| **`LITE`** | **−1.8** | **+21.9** | 🟢 surge 1.27, flow +0.817 |

**Both are TURNS, not runners** — negative-to-flat 60-day, strongly positive 20-day. They are the
**only two admissible 🟢 in Information Technology**, and the desk's "already ran ⇒ avoid" instinct
**does not apply to them at all**. ⚠ EVENT_ALPHA Card 2 dates their greens to their own **08-11/08-12
earnings prints** ⇒ **reaction volume, not accumulation** — so this lens does **not** promote them; it
records that **the screen that would have rejected them was aimed at the wrong shape**, and that their
green needs a **post-reaction** re-read once the print volume rolls off (≈08-25).

⚠ **`D48` note against this lens's own draft**: it first tagged `MU` "EXTENDED-BUT-LIVE" by analogy to
`STX`. Re-reading the row refuted it — **`MU`'s OBV is 중립, not 매집**, so its 20-day leg has no
accumulation under it. Corrected to **LIVE BUT THINNING** and left visible.

---

## §4 · LENS 4 — CYCLE-EXPOSURE (registry vs coverage vs the REAL book)

| Cycle | Rank | Epicenter % | Floor | Held | Verdict |
|---|---|---|---|---|---|
| AI-compute / semiconductors | 1 | **16.54%** | 12.0% | `AVGO` `NVDA` `ANET` | ✅ clears |
| **Energy / oil-refining** | **2** | **7.1%** | **8.0%** | `MPC` `PSX` | 🚨 **GAP −0.898pp** |
| Missile-defense / rearmament | 3 | 5.77% | **none set** | `RTX` | ⚪ n/a |

**On the GAP.** ⚠ **The number is identical to the 08-15 run to three decimals** — the book did not
trade and prices did not move, so this is **the same GAP measured twice, not a widening one**, and the
desk should not read a second appearance as a strengthening signal.
★ **What IS new is the tension around it**: the GAP sits on the **rank-2 cycle whose narrative
strengthened most this run** (`P66` — five dated Russian refining strikes in six days), while the same
cycle's **positioning is `WTI` spec at the 18th percentile SHORT, dated 08-11 — before the escalation**.
Per the standing rule: **a crowded/🔴 tape gates ADD timing; it never justifies 0% core.** Cleanest
epicenter expressions the desk can actually measure: **`MPC`** (the sector's only admissible 🟢, FINRA
clean-rise z −0.71) and **`PSX`/`VLO`** (the #2/#3 RS60 in the sector, both filtered out on `vol_surge`).

### 🚨 The lens's real finding: **the registry cannot see this run's own cycles**

`data_build/cycles/cycle_registry.json` is **29 days stale** (`D250`), its rank-3 floor is **0.0** so
that check is **OFF**, and it has **NO ENTRY** for either cycle this run's own EVENT_ALPHA identified:

| Cycle the run found | Where it was found | Registry entry | Book exposure | Measurable? |
|---|---|---|---|---|
| **Optical / interconnect** | The **only two admissible 🟢 in IT** — `COHR` (flow **+0.99**, board top) and `LITE` (+0.817) | **NONE** | **ZERO** | ✅ both in `us_top300` |
| **Custom AI silicon** | Card 2 — Maia 300, 300k-unit target, *"Microsoft Rethinks Its Nvidia Reliance"*, `MRVL` named | **NONE** | Indirect only (`AVGO` held) | ✅ |

⇒ **This is the 2026-07-14 postmortem's failure mode reproduced structurally, not by judgement.** That
postmortem's headline was *"zero exposure to the #1 cycle's epicenter."* Today the check that exists to
prevent it **runs on a registry that does not contain the cycles**, so **a zero-exposure GAP on
optical/interconnect is invisible by construction** — the instrument returns ✅ because it has nothing
to compare against.
**Registered as `S94` below.** The remedy is a **human** one (the registry is human-maintained, P5) and
is **not** a promotion of `COHR`/`LITE` — §3 already flagged their greens as print-reaction volume.

---

## §5 · Brackets registered this run — `S92` · `S93` · `S94`

> ID 3-grep at write time across `handoff/SCENARIOS.md` · `SCENARIOS_US.md` · `SCENARIOS_KR.md` ·
> `llm_outputs/` · `REPORT/`: highest existing **S91** (suffixed: `S63-KR`) ⇒ this run takes **S92–S94**.
> All observables and thresholds are **frozen at registration**. Benchmark `SPY` named inline.
> Prices settled **2026-08-14**, one provider (`yfinance`, `auto_adjust=False`).

### S92 — ★★★ The MANDATORY Hormuz bracket, widened to the branch that can actually falsify us · ARMED · → settle **2026-08-31**

**Why it exists.** `CATALYST_WATCH` carries one 🔀binary and the desk's #1 tilt (ENRG OW) is a one-way
tilt into it. **`S8` has been undated for 14 runs and cannot serve.** ★ **And §2b measured that the
Hormuz binary ALONE is not worth bracketing** — both its branches are confirm-only against `P66`'s
supply-destruction leg. So the bracket is **two-dimensional by construction**.

| Branch | Frozen observable | Meaning |
|---|---|---|
| **A (escalation)** | A dated US action on the Strait (declaration / escort regime / interdiction) reported by **≥3 independent foreign outlets** by **2026-08-31**, **AND** `CL=F` settled close **> 82.40** (the 08-14 close) | War premium re-prices. ENRG OW confirmed on the leg it was **not** primarily argued from |
| **B (de-escalation)** | `CL=F` settled close **< 78.00** on any session through 08-31 with **no** dated US action | War premium prices out. ⚠ **`P66`'s Russia leg SURVIVES this branch** — recorded at registration so branch B is not over-read |
| **★ C (the falsifier)** | A **US-brokered halt to Ukrainian strikes on Russian energy infrastructure** reported by **≥3 independent foreign outlets** by 08-31 | **This is the only branch that breaks the Energy OW's stated mechanism.** First datapoint already on the board: *"Kyiv stops strikes on Russian port after request from JD Vance"* [`semafor` **08-12**] |
| **D** | none of the above | no information |

**State at registration**: `CL=F` **82.40** (08-14 close) · `WTI` spec **18th percentile SHORT** (COT,
Tue 08-11, i.e. **before** the 08-14 Trump statement) · `Hormuz` term velocity **1.15×** vs `Iran`
**0.88×**.
**🚨 Anti-signal**: an **OPEC+ production decision** inside the window makes the barrel
non-attributable ⇒ branches A and B are **VOID**; branch C scores independently.
⚠ **Threshold provenance (C5)**: 78.00 and 82.40 are the **08-14 settled close ±≈5%**, chosen because
`MPC`'s **implied move is ±4.4% (expiry 2026-08-21, D5)** — the bracket must sit **outside** what is
already priced. Stated at registration.

### S93 — ★★ The consumer complex: is the split one factor or two? · ARMED · → settle **2026-08-21**

**Why it exists.** Lens 1 promoted `CONSUMER` (DISC ∪ STPL) to a 5th DEEP over ROTATION's decline.
**Three dated prints land inside the window and the desk's catalyst instrument carried none of them.**

| Branch | Frozen observable (settled closes, 5-session excess vs **`SPY`**, 2026-08-14 → 2026-08-21) |
|---|---|
| **A (one factor — "the consumer is rolling")** | `WMT` **and** `HD` **and** `TGT` all post **negative** 5-session excess vs `SPY` |
| **B (two factors — the label is the wrong object, `W5`)** | `TGT` **positive** while **both** `WMT` and `HD` are negative, **OR** `ABNB` − `HD` 5-session excess spread vs `SPY` **> +5pp** |
| **C** | any other combination |

**State at registration (2026-08-14 settled)**: `WMT` 🟡 RS60 **−19.9** vs `SPY` · `HD` **🔴분산**
RS20 −4.4 · `TGT` 🟡 **OBV 매집**, RS20 +6.2, RS60 **+15.6** · `COST` 🔴분산 RS60 −18.0 ·
`ABNB` **🟢가속** RS20 +21.6, RS60 **+34.5**.
**★ Implied move, taken from the options market not from judgement**: `WMT` **±5.0% (expiry 2026-08-21,
D5)** — it **covers** the print. `WMT`'s single-name threshold is therefore **not** used; the bracket is
built on **cross-sectional sign agreement**, which the straddle does not price.
**🚨 Anti-signal**: a **tariff announcement affecting consumer goods** inside the window ⇒ **VOID**.

### S94 — ★★★ The cycle registry cannot see the cycles this desk finds · ARMED · → settle **2026-08-31**

**Why it exists.** Lens 4 measured that `cycle_registry.json` (**29 days stale**, rank-3 floor **0.0**)
has **no entry** for **optical/interconnect** or **custom AI silicon**, while `COHR` (flow **+0.99**,
the board's top score) and `LITE` (+0.817) are the **only two admissible 🟢 in IT** and the book holds
**neither**. ⇒ **The instrument built to prevent "zero exposure to a live cycle's epicenter" returns ✅
because it has nothing to compare against.**

| Branch | Frozen observable |
|---|---|
| **A (the gap was real)** | At the **2026-08-31** settle, the equal-weight 5-session excess of **`COHR` + `LITE`** vs **`SPY`** is **> +3pp**, **while** `cycle_registry.json` still carries **no** optical/interconnect entry |
| **B (the registry's silence was right)** | The same equal-weight excess is **< −3pp** ⇒ the desk was not missing a cycle; the greens were print-reaction volume, as §3 suspected |
| **C** | between ±3pp — no information |

**State at registration**: `COHR` RS20 **+12.9** / RS60 **−13.7** vs `SPY`, `vol_surge` **1.67** ·
`LITE` RS20 **+21.9** / RS60 **−1.8**, `vol_surge` 1.27. **Both are TURN shapes, not runners** (§3).
**🚨 Anti-signal**: ⚠ **both greens are dated to their own 08-11/08-12 earnings prints** (Card 2) ⇒ if
the window's move is still inside the print-reaction volume decay, branch A is **confounded**. The row
therefore also requires **`vol_surge` ≥ 1.0 on both names at the 08-31 settle** for branch A to score —
stated at registration, not discovered afterwards.
⚠ **This bracket does NOT promote either name.** It tests the **registry**, which is a human-maintained
file (P5).

---

## §6 · ⚠ A structural defect this stage re-measured, and it disqualifies one threshold source

**`NVDA` implied move: ±1.5%, expiry 2026-08-17 (D1).** `NVDA` **prints 2026-08-26**.
⇒ **The straddle expires NINE days before the event it would be used to bracket.** Any threshold taken
from it would be **fabricated** — this is `M47`'s structural defect reproduced exactly (*"META ±4.4% ·
MSFT ±2.9% · VLO ±3.2% all expire before their events — they are not event-priced"*).
**No bracket in §5 uses an `NVDA` threshold.** Recorded so a later stage does not reach for one.
★ By contrast **`MPC` ±4.4% (D5)** and **`WMT` ±5.0% (D5)** genuinely cover their windows, and both
were used as the *provenance* for §5's thresholds rather than as the thresholds themselves.

⚠ **One more instrument reading, logged**: `module_flow NVDA` returned news velocity **1.09×** at
~22:5x KST against the sweep's **1.16×** measured ~20:1x and yesterday's **1.20×**. **Three readings,
three values, one frozen tape** — `D271`'s flickering bridge, now observed inside this stage too.

---

## ✅ EXIT CHECK

- [x] **All four lenses executed** — and the **deviation from the Agent fan-out is declared in §0**,
      with its cost stated (loss of independence ⇒ treat the dissent as a lower bound).
- [x] **Every under-computed leg is PROMOTED or logged as WITHIN-RUN-WATCH — none silently dropped.**
      One promotion (**`CONSUMER`, a 5th DEEP slot**) against ROTATION's own decline, argued on four
      numbered grounds including a refutation of ROTATION's stated reason.
- [x] 🚨 **The binary in `CATALYST_WATCH` is bracketed BOTH ways** — `S92`. ★ And **B4 was applied
      before registration**: the Hormuz binary alone was measured to be **confirm-only on both
      branches**, so the bracket was **widened to include branch C (the strike-halt)**, which is the
      only branch that can falsify the Energy OW. **A one-way tilt into a known binary did not happen.**
- [x] **Momentum re-tags carry a flip condition each** (§3), and the lens's own draft was **refuted
      in-run** (`MU`: OBV 중립, not 매집) and left visible per `D48`.
- [x] **Cycle-GAP flagged with core expressions** (§4) → handed to ALPHA's action bracket and BET's
      epicenter module. ⚠ The GAP is disclosed as **identical to the prior run**, not as widening.
- [x] 🚨 **The lenses did NOT all agree with the draft** — Lens 1 overturned a ROTATION decline, Lens 2
      widened the mandatory bracket beyond the registered binary, Lens 3 found the desk's two best
      names are the **opposite shape** from what its screen targets, and Lens 4 found the anti-tunnel
      instrument **cannot see this run's own cycles**.
- [x] **Observables and thresholds frozen at registration**, with **threshold provenance stated**
      (C5) and the options-implied move used as the *floor* for informativeness — including the
      **disqualification of `NVDA`'s straddle** in §6.
- [x] **Correlated-UW pattern checked** (§2d) — **UTIL + RE + STPL may be one rate bet whose sign just
      changed under `P65`**; not re-bracketed because `S80` (08-19) already owns it, but named so
      `S80`'s scoring is read as a three-sector event.
- [x] **No sizing, no buy/sell language** (P4).
