# BLINDSPOT_PREMORTEM — industry_US · 2026-09-12 (Sat) · Stage 7 / L1·PREMORTEM

> Four adversarial lenses fanned out **in parallel as subagents** (one message, 22:30 KST; returned
> 22:32–22:36), each given the draft tilt (MACRO §E + ROTATION deltas), the DEEP pick (`ENRG` only),
> the 09-11 sweep, EVENT_ALPHA cards and `CATALYST_WATCH.json`. Instrument state inherited: news pipe
> dead ⇒ every narrative fact the lenses used is `[WebSearch]` and tagged; prices `[yfinance]` settled
> 09-11. **The desk's tilt going in**: ENRG OW (refiners held) · UTIL/RE/STPL UW (one duration object)
> · INDU UW− · FIN N− · IT N · HLTH N. Held: `NVDA ANET AVGO HPE ETN MET MPC NDAQ NUE PSX RTX`.

## 0 · The binaries this stage must bracket (`CATALYST_WATCH.json`, `--days 10`, 22:12)
| when | event | rule | handled |
|---|---|---|---|
| **2026-09-16 14:00 ET (2 sessions from Monday's open)** | FOMC decision + SEP | 🔀 binary ⇒ both-sides bracket mandatory | `P148` (slope, registered 09-08) **spans it**; anti-signals re-checked post-CPI: `hy_oas` 2.70 < 3.10 ✓, no unscheduled action ✓, date unchanged ✓. **But `P148` no longer discriminates the consensus case** (§2) ⇒ companion LEVEL row **`P151`** registered below |
| undated | Iran "Hormuz open" statement | 🔀 binary, undated | a **dated tell now exists** (`[WebSearch]`: Iran–Oman transit agreement "expected to be signed" in Muscat; Arab FMs met Iran's FM in Salalah; White House says no direct talks). Cannot be registered on a date (`[blank]`, never guessed) ⇒ the **crack** is bracketed on a dated window instead — **`S156`** |
| 2026-09-18 | quad witching / S&P rebalance (`ILMN` inclusion at the close `[WebSearch]`) | structural | **not bracketed (`B4`)**: no branch changes a sector conclusion; it amplifies whichever of the two above resolved. Named as the terminal day of `P149`/`P150`/`S156` |

## Lens 1 — UNDER-COMPUTED LEGS (subagent verdicts, ranked)
| # | leg | tickers | dated catalyst (09-14…18) | flow says | verdict |
|---|---|---|---|---|---|
| 1 | **IT · AI-hardware + optical-interconnect hop** | `HPE` 🟢 · `DELL` 🟢✅ · `QCOM` 🟢 · `GLW` 🟡 Δ+0.83 · `COHR`/`CIEN` 🔴 · `LITE` 🟡 · `MRVL` 🟡 · `AMD` 🟡 | **09-15 Oppenheimer AI-Infrastructure + Truist Tech conferences** `[WebSearch]`; FOMC 09-16; witching 09-18 | sector `eqflow` −0.207, 23 🔴 — **but this leg's 🔴s are rs60 artifacts** (`COHR` rs60 −22.1, `CIEN` −20.6, `QCOM` −16.9 while the week ran +10…+15%): names coming off 60-day lows read as distribution on a 60-session-heavy axis | ★ **PROMOTE-TO-DEEP** (the sub-leg, not the sector) |
| 2 | **HLTH · the drawdown was three idiosyncratic shocks** | broken: `AMGN`·`BSX`·`SYK`; winners: `ELV` 🟡 +0.68 · `HUM` · `COR` · `HCA` +5.4% | **09-14…16 Morgan Stanley Global Healthcare Conf** (where BSX/SYK would quantify); `ILMN` S&P inclusion 09-18 | `eqflow` −0.059 still rank 3/11; managed care/distributors OBV 매집 | **WITHIN-RUN-WATCH** — DEEP would duplicate the live `P150`; **the cause correction goes into `P150`'s notes (§5)** |
| 3 | **FIN · insurers as the hike's asset-yield leg** | `AIG` 🟢 · `PGR` 🟡 OBV +0.40 (FIN's highest) · `MET` 🟡 · `TRV` | FOMC 09-16 | 1 🟢 / 14 🔴; insurers the only accumulating cluster — **but insurers also fell** (`KIE` −3.8%, `AIG` −2.0%, `MET` −2.1% wk `[yf]`): "green" is a flow tag, not price | **WITHIN-RUN-WATCH** |

★ **The cause the desk could not see (MACRO §B-2 "unknown") is now read `[WebSearch]`**: `AMGN` −10.1%
on 09-08 = **Novartis/Ionis pelacarsen Lp(a) Phase 3 miss → read-through to olpasiran** + Tavneos
UK/EU suspension + BMO downgrade; **`BSX` and `SYK` = two separate cyber attacks** (BSX filed 09-08 it
will miss Q3/FY; SYK flagged backorders). **Three name-specific breaks in three heavyweights produced
4/32 breadth by sympathy.** ⇒ `P150`'s branch-A label ("rotation") mis-describes what A would mean;
the observable and thresholds are **frozen and untouched** (`D242`) — the *reading* is corrected in §5.

## Lens 2 — REGIME-FLIP / BOTH-SIDES
**2a · The book is one trade wearing six sector labels** (`[yf]` 60-session 1-day correlation vs
`IEF`): `XLU` +0.29 · `XLRE` +0.33 · `XLP` +0.27 · `XLF` +0.25 · `XLI` +0.27 · **`XLE` −0.46**. Every
UW/N− bucket co-moves with duration and the sole OW anti-correlates with it; refiners' 20-session gains
(`VLO` +13.9, `PSX` +11.6, `MPC` +11.1) mirror the oil-loser legs (`JETS` −11.6, `XLI` −7.2, `XHB` −10.1).
**Net position = short Treasuries + long crude via equities.** The correlated-UW pattern the field note
warns about is not two sectors — it is **five UW/N− buckets and the OW, all one "higher-for-longer ×
oil" object.** `S135`-A already measured the trio as one bet; this extends it to FIN N− and INDU UW−.
⇒ handed to BET as the size-context fact: the book's diversification across labels is nominal.

**2b · Against-us branches per binary**
| binary | against-us branch | what rips | held names hit | starter list | trigger | invalidation |
|---|---|---|---|---|---|---|
| **FOMC 09-16** | **no hike / hike with dovish dots / no dots** — Goldman calls a September hike "very unlikely" `[WebSearch]`; one read says Warsh drops the dot plot `[WebSearch]`, contradicting the calendar's "SEP" (unresolved, flagged) | 2y unwinds part of the +93 bp wedge; duration trio bounces first; `NDAQ`/`KKR`/`SPGI` squeeze; `ITB`, `IWM` rip; bear-flattening → bull-steepening | **`MET`** (asset-yield story dies; shortZ +1.95 squeezes the wrong way for the thesis), `NDAQ` becomes the winner the desk is tilted against | `XLU` `XLRE` `ITB` `NDAQ` `IWM` | `SHY` 1-session ≥ +0.134% (p95) on 09-16; 30y−5y re-steepens ≥ +8 bp | hike delivered ∧ `SHY` ≤ −0.073% (p15) ⇒ dead by 09-17 close |
| **Hormuz signing (undated)** | a Muscat transit agreement prints inside FOMC week | WTI gives back at the margin (1d p05 −5.0%); **crack collapses from a record**; airlines (`DAL`/`UAL` 20d −12.5%), `JETS`, `XLI` transports, chemicals rip; **refiners are the exception that carried the crack, not the barrel** | **`MPC`, `PSX`** (1d p05 −3.3/−2.8%; 20d +11 both, crack-driven per `P140`-B); `P149` VOIDs on its own anti-signal, leaving Energy OW with **no bracket** — which is why `S156` exists | `DAL` `UAL` `JETS` `XLI` `LYB` | ≥3-outlet "Muscat agreement signed", or `CL=F` 1-session ≤ −4.98% (p05) ∧ crack 1-session ≤ p05 same session | `CL=F` reclaims 102.48 within two sessions, or transits stay ≤10/day a week after signing |
| **09-18 witching** | mechanical selling into the week's momentum (`HPE` +19.4%, refiners) | — | `HPE`, `ANET` (shortZ +1.50), `AVGO` 🔴 | — | 09-18 volume ≥2× 20d ADV ∧ `HPE`/`ANET` close < 09-17 low | 09-21 close > 09-18 close on both |

**2c · Does `P148` still discriminate?** `[yf]` `IEF−SHY` 2-session is **already −0.650% (2.8th pctile)
on 09-09→09-11, below A's −0.3469 line before the meeting.** A hike delivered as consensus yields near-zero
*spread* change ⇒ `P148` scores **C while the level moves**; the slope only catches tail surprises. ⇒
**companion LEVEL row `P151`** (§4). ⚠ `IEF` now has a **09-18 straddle (±0.8%, D6)** that covers the
meeting — the 09-08 registration's "expires five days before" note is superseded; `SHY` ±0.7% (09-18, D6).

**Most under-priced against-us scenario (lens 2's one line):** *a signed Muscat transit deal landing
inside FOMC week — the crack collapses while rates stay high, so `MPC`/`PSX` bleed and the duration trio
does not bounce: the book is long the leg that lost and short the leg that did not win.*

## Lens 3 — MOMENTUM-CONTINUATION ("already ran" ≠ avoid; date-cluster rule applied first)
**3a · Date-cluster test** (σ = 20 daily returns ending 09-04; week = 4 sessions, z on n=4):
`HPE` +19.4% = **z +2.93**, but **66% of it is 09-11 (+12.4%), a day on which 7 IT names printed z>2**
(`ON` 3.8, `ADI` 3.3, `NXPI` 3.2, `IBM` 2.5, `DELL` 2.3, `TXN` 2.2) on **Oracle's FY capex guide $90–95bn
`[WebSearch]`** ⇒ **n≈1**, not seven. `AMGN` −13.7% = **z −4.92**, 72% on 09-08, same day `NVS` −14% in the
same drug class ⇒ fundamental, not factor. `QCOM` +7.8% = z +2.34 with the biggest day only **41%** of the
move (4/4 sessions up) ⇒ **multi-day, not a headline**. `VLO`/`MPC`/`PSX` z +1.46/+0.41/+0.45 — a quiet week.
**The "40/56 IT breadth rip" is 29/56 through 09-10 and 36/56 on 09-11 alone** — two headline days, `XLK`
week z +0.31 ⇒ **the sector-level fact is the AI-hardware sub-basket, not IT** (agrees with lens 1).

**3b · Re-tags** (structure: 20d high · MA50 slope · acc20 = r20 − prior r20 · OBV; KPI; flip condition):
| name | tag | why | **flip condition (observable · date)** |
|---|---|---|---|
| `VLO` | **EXTENDED-BUT-LIVE** | at 52w high, MA50 +15%, acc20 −0.4, OBV +0.37; KPI = distillate crack **$108.2**, one tick under its 20d high **109.9** (60d low 54.8) | → EXHAUSTED if crack < **$94.2** (20d low) or close < MA20 **$359** by **09-18** |
| `MPC` (held) | EXTENDED-BUT-LIVE, decelerating | −0.9% off high, acc20 −5.4, **OBV +0.48 = strongest on the board**, vol 0.89× | crack rail above; or close < MA20 **$374** by 09-18 |
| `PSX` (held) | EXTENDED-BUT-LIVE, decelerating | acc20 −4.0, OBV +0.32 | close < MA20 **$247.5** by 09-18 |
| `DELL` | EXTENDED-BUT-LIVE, **Friday-dependent** | 52w high, +26% vs MA50, acc20 −11.6, flow Δ −0.21, shortZ −0.95 clean; KPI = Oracle capex | → EXHAUSTED if close < **$506.6** (09-10 close = fills the Oracle gap) by 09-18 |
| `HPE` (held) | EXTENDED-BUT-LIVE, **weakest conviction of the greens** | acc20 **−28.8** (August did the running), OBV only +0.10, surge 1.47 = one day, shortZ +0.58 rising; KPI = 09-02 FY26 guide raise + Oracle capex | → EXHAUSTED if close < **$55.2** (09-10 close) by 09-18; confirms LIVE if 09-18 sweep OBV > +0.25 |
| **`CRM`** | ★ **EXTENDED-BUT-LIVE — the cleanest add-on-dip structure on the list** | r20 **+23.0**, acc20 **+6.4 (still accelerating)**, −6.3% off 20d high on **light volume (0.79×)**, OBV 매집, rs60 +51.3; the week's −4.4% = **−0.41σ = noise** | → EXHAUSTED if close < MA20 **$230.6** on vol >1.2×, or rs20 < 0 on 09-18 |
| `NOW` | **EXHAUSTED** (as a runner) | at MA20, −31% off 52w high = bear-market rebound; acc20 −18.2 | LIVE only on close > **$148** (20d high) by 09-25 |
| `MSFT` | EXHAUSTED / stalled | r20 −0.3, acc20 −24.1, vol 0.43× | LIVE > **$513.5**; broken < **$453.7** (MA50) by 09-25 |
| `NEM` | EXHAUSTED-leaning | at MA20; **gold $4,366 at its 20d low**, r20 +0.1% — `NEM` ran +17% on a flat KPI with real yields rising | LIVE only if `GC=F` > **$4,641**; confirmed exhausted on two closes < **$126.9** by 09-18 |
| `QCOM` | **LAGGARD IGNITION (early, not extended)** | 20d high but −27.5% off 52w, MA50 slope −9% = base breakout, acc20 +13.8, 4/4 up | kill if close < **$168.7** (09-04 close = the gap) by 09-18; confirm on rs60 > 0 by **10-09** |

**3c · Health Care, bounce vs broken**: reversal-bounce candidates (fell with the sector, trend intact,
no own news; RS-60 = the A-grade axis, OBV = corroborant only): **`REGN`** (rs60 +25.3, z −1.31) · **`VRTX`** (rs60 +11.9, z −1.16, OBV 매집) · **`GILD`** (rs60 +11.1, z −1.33,
OBV 매집). Broken (own fundamental hit, OBV 분산): `AMGN` (z −4.92) · `SYK` (z −2.43, PV/hip slowdown, vol 1.74×)
· `BSX` (z −1.98, cyber + guide pulled). **Lens 3 votes `P150` = C leaning B for the survivors, A-irrelevant
for the three broken names.**
**The runner the desk most wrongly treats as "done": `CRM`** — filed in §E's losing "software" tier next
to `SHOP`/`NOW`/`ADBE`, yet the only large runner whose 20-day leg is still accelerating on a light-volume
pullback with OBV accumulating.

## Lens 4 — CYCLE-EXPOSURE (registry vs book)
**4a · 🚨 Measurement defect first**: `CYCLE_EXPOSURE` counts `epi_names = [NVDA, ANET]` for rank 1 while
the registry lists `AVGO` as an epicenter **and the book holds it** (RISK_UNITS 14:54, PREFLIGHT G5 11/11)
⇒ the **17.18% is not trustworthy** until re-run; `HPE` (held, 🟢, +19.4%) sits in **no** registry layer, so
`any-layer %` under-counts too. Registry file dated **2026-07-17 = 57 days stale**. → **`D592`**.

**4b · Registry audit — what the tape prices that the registry does not know**
| candidate | tape (09-11) | register? |
|---|---|---|
| **Custom inference silicon / ASIC multi-sourcing + 1.6T optics** | `QCOM` 🟢, `GLW` Δ +0.83, `AVGO` 🔴 = the losing leg | **yes — as a fork inside rank 1**: the layer receiving incremental dollars moved from training GPU/ASIC-incumbent to inference ASIC + optics |
| **AI-server assembler re-rating** | `HPE` 🟢 rs60 +26.5, `DELL` 🟢 ✅ | **yes — adjacent-downstream layer of rank 1** |
| **Hike cycle / higher-for-longer** | 2y−FF +93 bp, 87% hike, UST2Y COT 80th | **yes — rank 3, macro**, above missile-defense; winners `AIG`/`MET`/`PGR`/`TRV`, losers `NDAQ`/`NEE`/trio — **the book holds both sides by accident** |
| **Second chokepoint (Mokha / Bab al-Mandeb)** | `[yf]` `FRO` +8.3%, `DHT` +9.0%, `INSW` +6.5% wk (+14–22% 20d vs `SPY` −1.7%) | **yes — ton-mile leg of rank 2**; named in the registry (`FRO`/`STNG`/`INSW`/`DHT`) but **outside `us_top300`** ⇒ flow-unmeasurable, 0% held — must carry an *unmeasurable* flag, not a silent 0 |
| US gas disconnect | `NG` −4.8%, COT 0th | **no** — watch/kill-switch lane only (COT-contrarian REJECTED, `D6`) |

**4c · Epicenter vs one-layer-off**: rank 1 — `NVDA` still the training epicenter by dollars but 🟡 (rs20 −1.4, rs60 +3.4; OBV
분산 as corroborant); `ANET` = networking, one layer off, **and really the `ETN` unit** (4d); `AVGO` = the losing leg of
Card 1. **Incremental-dollar epicenter = inference ASIC + optics: `QCOM`, `MRVL`, `AMD`, `LITE` (the only
optics name with OBV 매집), `GLW`**; out of universe `TSM`, `ALAB`, `CRDO`, `SMCI`. Rank 2 — `MPC`/`PSX` are
the crack epicenter (correct), but the week's incremental pricing went to the **barrel and ton-mile legs**
(`VLO`, `DVN`, `CVX`, `COP`, `EOG` all 🟡 매집; tankers +8–9%); book 0% barrel/E&P, 0% tankers. Rank 3 —
`RTX` 🔴 shortZ +1.40, `LMT`/`NOC`/`GD` all 🔴: **dormant this week**, `S150` (09-14) decides.
**4d · Zero-epicenter cycles**: (1) inference-silicon/optics fork **0%**; (2) ton-mile leg **0%** and
unmeasurable; (3) barrel/E&P leg **0%**; (4) hike cycle **net ≈ 0 by accident** (`MET` vs `NDAQ`);
(5) AI-power as a *distinct* cycle ≈ 0 in risk terms — **RISK_UNITS merges `ANET+ETN` (resid corr 0.366)
and `AVGO+NVDA` at 750d**: `ETN` co-moves with `ANET`, not with `GEV`/`VST`/`CEG` ⇒ the "AI-power adjacent
3.87%" is a relabelled slice of the AI-electrical unit — **double-counted**. Real power-cycle names (`VST`
🟡 매집, `CEG` 🟡) unheld; `GEV` 🔴.
**Single biggest gap (lens 4's one line)**: *0% at the inference-silicon/optics fork where the rank-1
cycle's incremental dollars just moved — while the counted "AI-compute core" is a distributing `NVDA`, an
`ANET` that is really the `ETN` unit, and an `AVGO` that fell out of the count entirely.*

## 4 · Registrations — both indexed in `handoff/SCENARIOS.md`, detail in `SCENARIOS_US.md` (writeback at run end)

### `P151` — ★★★ FOMC 09-16, the LEVEL companion: was the hike a surprise in either direction? · settle **2026-09-16**
| field | value |
|---|---|
| **Why** | `P148` measures the slope and is **already inside branch A (−0.650%, 2.8th pctile) before the meeting**; with the hike itself at 87% the slope cannot tell "as priced" from "hawkish". The level can |
| **Frozen observable** | **`SHY` 1-session return, 2026-09-15 close → 2026-09-16 close** (`auto_adjust=False`), percentage |
| **A (hawkish beyond consensus — the front end sells off further)** | `SHY` **≤ −0.236%** (trailing-252 1-session **p05**) ⇒ the duration trio UW, FIN N− (exchanges 🔴 / insurers asset-yield), and the "hike cycle" registry entry are confirmed on the Fed's own print |
| **B (dovish surprise — no hike, or hike with a dovish statement/dots)** | `SHY` **≥ +0.134%** (**p95**) ⇒ **falsifies** the UW trio's driver, FIN N−, and `MET`'s thesis at once (lens 2's against-us branch) |
| **C** | between = *"hike delivered, nothing learned"* — the disclosed favourite (≈90% by construction; the tails were chosen deliberately so A/B mean *surprise*, not lean) |
| **`D93`** | trailing 252 settled 1-session `SHY` returns to 09-11: p05 −0.236 · p15 −0.073 · p50 +0.006 · p85 +0.085 · p95 +0.134 · sd 0.109. ±0.1% ≈ ±5 bp on the 2y |
| **Implied move** | `SHY` **±0.7% to 2026-09-18 (D6)** — a 6-session straddle; per-session equivalent ≈ ±0.29%. **Both thresholds sit INSIDE the priced 6-session path** ⇒ pre-declared: this row grades the print against `SHY`'s own 1-session history, **not** against the options market; it is an information row, not a "trigger" (`C3`) |
| **Information (`B4`)** | **B is the informative branch** (it falsifies three live verdicts); A confirms. If neither fires, `P148` and `P151` together say the meeting was priced — which is itself the finding |
| **Anti-signal (VOID)** | unscheduled Fed action before 09-16 · FOMC release date changed · US bank failure · `hy_oas` ≥ 3.10 on a close 09-15/16 (then it is credit) · ⚠ **if no SEP/dots are published (`[WebSearch]` contradictory) the row still scores — the observable does not depend on the dots** |
| **Non-redundancy (`D343`)** | `P148` = slope 09-15→09-17 · `P142` = `DGS2` level (blocked) · this = ETF front-end level, 1 session |
| **Owner** | `industry_US` |

### `S156` — ★★ The crack after the record: does the product scarcity survive a Hormuz headline week? · settle **2026-09-18**
| field | value |
|---|---|
| **Why** | The desk's Energy OW driver is the distillate crack (`P140`-B: level **108.24 = 99.2nd pctile**, 60-session change **+51.14 = 96.8th**); `P140` is settled and `P149` VOIDs on a Hormuz reopening — leaving the OW unbracketed exactly when its against-us branch (Muscat signing) has a dated tell. **This row does not VOID on the signing: it measures it** |
| **Frozen observable** | **distillate crack = `HO=F` × 42 − `CL=F` ($/bbl), 5-session change, 2026-09-11 close (108.24) → 2026-09-18 close** |
| **A (the crack collapses — reopening/against-us priced)** | change **≤ −4.25** (trailing-252 5-session **p15**) ⇒ refiners' driver breaks; `MPC`/`PSX` lens-3 rails (MA20 $374 / $247.5) are the name-level tells |
| **B (the scarcity extends through the headlines)** | change **≥ +8.70** (**p85**) ⇒ a second record; the equity leg's lag (`P149`) becomes the anomaly, not the barrel |
| **C** | between — disclosed favourite |
| **`D93`** | trailing 252 settled 5-session changes to 09-11: p05 −8.94 · **p15 −4.25** · p50 +0.18 · **p85 +8.70** · p95 +12.77; **current (09-03→09-11) +6.61 = 81.3rd pctile** ⇒ enters leaning B, disclosed |
| **Both halves (`C2`)** | gasoline crack (`RB=F`×42−`CL`) **38.85 = 61.9th pctile level, 20-session change −11.27 = 4.0th pctile** — the product split is distillate-up / gasoline-down; this row measures distillate only, stated |
| **Implied move** | no listed straddle on the spread — **UNCHECKED** (`C3`); `XLE` ±1.3% (09-14, D2) does not cover the window |
| **Anti-signal (VOID)** | a **front-month `HO`/`CL` roll inside the window** (October `CL` expires ~09-22 — outside; `HO` October expires 09-30 — outside; stated) · an exchange trading halt |
| **Information (`B4`)** | **A is the informative branch** — it would be the first measured break in the desk's OW driver since 08-21 and it fires on the against-us headline the desk cannot date |
| **Owner** | `industry_US` |

## 5 · Corrections and re-labels injected (not silently noted)
- **`P150` reading corrected, thresholds untouched (`D242`)**: branch A ("rotation") would in fact measure
  *"the sympathy drawdown persists after three idiosyncratic breaks are known"*; B measures the `S128`-class
  bounce in the survivors (`REGN`/`VRTX`/`GILD`/`TMO`). **MACRO §B-2's "cause unknown" is superseded by
  `[WebSearch]` reads** (§Lens 1) — appended to MACRO_REPORT as a PREMORTEM addendum, earlier text left standing (`D48`).
- **`S133` (09-14) heads-up**: `AMGN` is one of its six and carries a **fundamental** break; the row's
  anti-signal (FDA action/readout at ≥2 of six) — a Phase 3 miss at a *competitor* is not an enumerated
  voider; recorded at D−1, not argued (`D242`).
- **MACRO §E IT line**: the "40/56" is two headline days (lens 3); the sector-level fact is the
  AI-hardware/optics sub-basket ⇒ **DEEP is scoped to the sub-leg**, not to IT.

## 6 · DEEP set — updated
**Final: `ENRG` (continuous) + `IT-HW` (PREMORTEM-promoted, lens 1 + lens 4 — scoped to AI-hardware /
inference-silicon / optics; software excluded) = 2 of 4.** `HLTH` and `FIN` are **WITHIN-RUN-WATCH** with
their brackets (`P150`, `S133`; `P151`, `P124`) doing the work. **Two slots left unfilled by decision** —
no OW candidate, and neither watch leg has a question a live bracket does not already ask.
The `IT-HW` DEEP's mandate: *resolve tape-vs-flow as lag-vs-distribution on the sub-leg; re-map the
rank-1 epicenter after the QCOM×AWS fork; name the 09-15 conference datapoints that would confirm/kill.*

## 7 · Hand-offs
- **To ALPHA (action bracket)**: `P151`, `S156` (new) + `P148`, `P149`, `P150` — five dated rows through
  09-18; lens-2 triggers/invalidations per binary (§2b); lens-3 flip rails on `MPC`/`PSX`/`HPE`.
- **To BET (epicenter-starter / size context)**: (i) the book is **one "higher-for-longer × oil" object**
  across six labels (§2a); (ii) **0% at the inference-silicon/optics fork** (`QCOM`/`MRVL`/`LITE`/`GLW`),
  0% barrel/E&P, ton-mile unmeasurable; (iii) `ANET+ETN` one unit, `AVGO+NVDA` one unit at 750d ⇒ the
  "AI-power adjacent" exposure is double-counted; (iv) `CYCLE_EXPOSURE` mis-counts `AVGO` (`D592`).
- **To DEEP**: mandates above; `CRM` re-tag (lens 3) as an IT-HW-adjacent counter-example the DEEP must
  either absorb or reject.

## 8 · Registered by this stage (writeback at run end)
| id | type | statement |
|---|---|---|
| **`P151`** · **`S156`** | brackets | §4 |
| **`M1478`** | measured | **The HLTH week was three idiosyncratic breaks, not a sector object** `[WebSearch]`: `AMGN` −10.1% 09-08 (Novartis/Ionis pelacarsen Ph3 miss → olpasiran read-through; Tavneos suspension; z −4.92 on its own 20d σ), `BSX` (cyber; Q3/FY guide pulled), `SYK` (cyber; backorders). Survivors `REGN`/`VRTX`/`GILD` fell ≤1.4σ with rs60 +11…+25 |
| **`M1479`** | measured | **09-11 was one observation, not seven**: 7 IT names z>2 on Oracle's $90–95bn capex guide (`HPE` 3.8 · `ON` 3.8 · `ADI` 3.3 · `NXPI` 3.2 · `IBM` 2.5 · `DELL` 2.3 · `TXN` 2.2); `HPE`'s +19.4% week is 66% that day; IT breadth 29/56 through 09-10, 36/56 on 09-11 alone |
| **`M1480`** | measured | **The book is one object across six labels**: 60-session 1d corr vs `IEF` — `XLU` +0.29 · `XLRE` +0.33 · `XLP` +0.27 · `XLF` +0.25 · `XLI` +0.27 · `XLE` **−0.46**; `IEF−SHY` 2-session −0.650% (2.8th pctile) already inside `P148`-A before the meeting |
| **`D592`** | defect | **`cycle_exposure.py` dropped a held registry epicenter (`AVGO`) from its count and has no layer for `HPE`/`DELL`**; registry file 57 days stale. Prescription: assert `held ∩ registry_epicenter ⊆ epi_names` and print the diff; add the inference-silicon/optics fork and the AI-server layer to the registry (human item) |

## ✅ EXIT CHECK — PREMORTEM
- [x] 4 lenses fanned out in parallel; each returned named tickers + dated catalysts.
- [x] Every bracket names observable + frozen threshold + date, both branches; `P151`/`S156` to be indexed in the spine + detailed in `SCENARIOS_US.md` at writeback (receipt = `grep P151 handoff/`).
- [x] Every magnitude threshold stated against the implied move (`SHY` ±0.7% D6 — thresholds inside, pre-declared as history-graded; crack — no straddle, UNCHECKED; `XLE` ±1.3% D2 does not cover).
- [x] Each branch graded (`B4`); quad witching dropped with the reason (no branch changes a conclusion).
- [x] File written: legs · brackets · re-tags · cycle GAP.
- [x] Every catalyst-bearing leg promoted (IT-HW) or logged (HLTH, FIN watch); brackets → ALPHA; GAP → BET.
- [x] DEEP set updated: **`ENRG` + `IT-HW` = 2/4**, two slots unfilled by decision.

> P4 — no sizing, no buy/sell language; starter lists are the against-us branch's observables, not calls.
