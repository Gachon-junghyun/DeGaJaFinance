# SECTOR_ROTATION — industry_US · 2026-09-12 (Sat) · Stage 6 / L1·ROTATION

> Delta-only. MACRO §E owns the 11-sector verdict; `SECTOR_FLOW_US.json §sector_rotation` owns the
> flow numbers. **DEEP budget N = 4** (protocol line, unchanged).

## 🚨 Instrument state governing every number below
`§scoring`: **`n_axes` 3 (`nonews`) · `vel_coverage` 0.0% · scored 299 · dropped 0**. ⇒ **zero deltas
cite velocity or freshness.** Δflow is legal (prior same-mode snapshot 09-04, `nonews`, 299/299) and is
the **09-04 → 09-11 weekly** change, labelled so on every line. Cap weights 59 days stale ⇒ `eqflow` is
the citable cut. `asof` **2026-09-11 settled** — the first new price information in five runs.
**Flip list (reproduced in full, §2).**

## §1 · Inherited — one line, verbatim from MACRO §E
`MACRO holds: ENRG OW · IT N (N+ candidate on breadth) · COMM no-verdict · UTIL N− · INDU UW− · STPL UW (eqflow/breadth only) · RE UW− · MATR N− · FIN N (N− candidate) · DISC UW (eqflow) · HLTH N (from N+, P150 armed)`.

## §2 · Deltas — **2 of 11**, both carried by non-cap-weighted flow numbers on non-flipper buckets

| sector | matrix said | flow evidence (wflow · eqflow · breadth · Δwk · 🟢/🔴) | new verdict | who resolves |
|---|---|---|---|---|
| **Financials** | N (N− candidate) | `wflow` −0.288 · **`eqflow` −0.269 = rank 8/11** · breadth 0.02 · **Δ −0.170 (3rd-worst on the board)** · 1 🟢 / **14 🔴** of 47 · `top1` `BRK-B` **13.9%, flip False**, ex-top1 −0.291 (same sign) | **N−** | DEEP if PREMORTEM promotes (rates split: exchanges/brokers/alt managers 🔴 vs insurers 🟢/🟡 — `AIG` new-🟢, `MET` OBV 매집 with rs60 +9.3 and shortZ +1.95); else `P124` (09-18) |
| **Utilities** | N− | `wflow` −0.268 · **`eqflow` −0.273 = rank 9/11** · breadth 0.00 · **Δ −0.340 = the worst on the board** · 0 🟢 / 9 🔴 of 15 · `top1` `NEE` 17.7%, flip False, ex-top1 −0.259 | **UW** | `S146` (09-14) · `P148` (09-17, the curve); UTIL/RE/STPL are ONE object per `S135`-A — the three now carry the same UW family (UW / UW− / UW) |

**Declined (9), each with its number:**
- `IT N → N+`: **declined — flow disagrees.** Tape EW exc5 **+3.40pp vs `SPY` on 40/56** (MACRO §E), but
  `eqflow` **−0.207 (rank 6/11)**, Δ **−0.012 (flat)**, 3 🟢 / **23 🔴** of 56. A promotion here would be
  a *price* argument against the stage's own flow axis; the divergence (5-session tape led, 60-session
  RS/OBV lag) is **the #1 question for a DEEP** — offered to PREMORTEM (IT is the book's largest
  concentration; `S148` settles 09-14). `top1` `NVDA` 19.4%, flip False.
- `HLTH N → N−`: **declined — the level does not support it.** Δ **−0.250 (2nd-worst)** and 0 🟢 / 8 🔴 say
  demote; **`eqflow` −0.059 = rank 3/11** says the sector is still mid-table on the 60-session axes. Sign-
  disagreement between level and change ⇒ side not picked (`C5`); `P150` (09-18) settles it on price.
  `top1` `LLY` 19.4%, flip False, ex-top1 −0.025.
- `ENRG OW → OW+`: no notch above OW; `eqflow` +0.431 rank 1, Δ +0.030, **0 🟢 / 1 🔴 of 16** — the
  0-🟢 is a gate artifact (SWEEP_READ §2), not a demotion argument; `XOM` 30.5% top-heavy, **not a flipper**
  (ex-top1 +0.486).
- `COMM` any: **barred** — Alphabet **76.6% at issuer level**, −0.148 → +0.400 ex-issuer (PREFLIGHT G3;
  the per-ticker flag prints False, `D459`). Stated for the record: **Δ +0.241 is the board's largest
  positive weekly change and `META` is a new 🟢** — this sector's sign is Alphabet at 76.6%, so no
  verdict is issued; `META` goes to BET §B as a name, not as a sector.
- `STPL UW → UW−` / `→ N`: **🚨1名 `WMT` 28.9%** flips −0.186 → +0.023 ⇒ `wflow` barred; `eqflow` −0.059
  (rank 3–4) and Δ −0.004 are flat ⇒ no change either way. Verdict stays where MACRO put it (the
  `S135`-A duration-object reason).
- `INDU UW− → lower`: no notch below; `eqflow` −0.470 = worst, **34 🔴 of 50 (68%)**, `CAT` 8.7% not a flipper.
- `MATR N− → UW`: `eqflow` −0.200 rank 5/11 (mid-table), Δ −0.060, 0 🟢 / 6 🔴, breadth 2/12 on price —
  the price cut supports it, the flow level does not; **declined for the same level-vs-change split as
  HLTH**; `P102`-B (settled today) removed the promoting artifact, nothing demoting was added.
- `DISC UW → UW−`: `eqflow` −0.338 rank 10 agrees with the sign; Δ **+0.015** (flat, slightly positive)
  ⇒ not newly worse. `AMZN` 40.2% top-heavy, flip **False** this run (ex-top1 −0.037).
- `RE UW− → lower`: no notch; `eqflow` −0.351 rank 10/11, Δ −0.203, 0 🟢 / 6 🔴, `WELL` 16.9% not a flipper.

**Macro re-arguments attempted and declined: 1** — a `FIN` demotion could also be argued from the
bear-flattening (`P138`-B); it was **not needed** (the flow numbers carry it) and is not cited as the reason.

**Flip list — full**: by the flag **1 of 11 — Consumer Staples / `WMT` / 28.9%** (−0.186 → +0.023). By
issuer (share classes merged): **2 of 11 — adds Communication Services / Alphabet / 76.6%** (−0.148 →
+0.400). All other nine survive top-1 removal with the same sign (Energy `XOM` 30.5% and Discretionary
`AMZN` 40.2% are top-heavy, not flippers — stated so they are not rejected for concentration).

## §3 · DEEP picks + DEEP_LOG

**Rule**: continuous ⌈N/2⌉ = 2 from today's top OW ranks; rotating ⌊N/2⌋ = 2 from the next OW not
covered in ~3 runs. **Never pad with N/UW.**

**OW sectors today: 1 (`ENRG`).** ⇒ by rule, **N = 1 of 4 filled.**
- **Continuous ①: `ENRG`** — held the slot 09-08 (and every run since 08-31) and is still OW rank 1.
  Anti-thrash continuity applies. **The DEEP's #1 question is the (a)-type divergence**: matrix OW,
  price OW, **ignition unmeasurable** (0 🟢 because the velocity leg is dead) — and the barrel/equity
  gap at the **9.9th pctile** (`P149`). *Early, or the equity market's discount?*
- **Continuous ②: vacated.** `HLTH` held it 09-08 at N+; today it is **N** (its promoting evidence,
  `eqflow` +0.190, reversed to −0.059 in one week; the week's worst basket on 4/32). Not OW ⇒ cannot keep
  the slot. **Stated, not padded.**
- **Rotating ① ②: no OW candidate exists.** Recency read from DEEP files on disk: `ENRG` 09-08 (0 runs)
  · `HLTH` 09-08 (0) · `FIN` 09-08 (0) · `INDU` 09-08 (0) · `IT` 09-07 (1) · `UTIL` 09-07 (1) · `MATR` 09-01
  · `COMM` 09-01 · `STPL` 08-27 · `DISC` 08-27 · `RE` 08-26 (the longest gap, 9 runs; UW− ⇒ unreachable
  by rule, named).

**Three unfilled slots offered to PREMORTEM with named candidates**, in the order the divergences bite:
1. **`IT`** — the week's tape leader (+3.40pp on 40/56) against a flat/negative flow axis; the book's
   largest concentration (`NVDA`, `AVGO`, `ANET`, `HPE`); the QCOM×AWS card (EVENT_ALPHA 1) puts a held
   name (`AVGO`) on the losing side; `S148` settles 09-14. **The tape-vs-flow divergence is the
   textbook "early vs trap" question.**
2. **`HLTH`** — the one sector that moved *as a sector* this week (breadth at the p05 level), cause
   unknown on this desk, `P150` (09-18) and `S133` (09-14) both live, `AMGN` (an `S133` member) −13.7%.
3. **`FIN`** — demoted today on flow; the rates split (insurers vs exchanges) is a **sub-sector
   dispersion** question (`W5`) with two held names on opposite sides (`MET` 🟡 매집 / `NDAQ` 🔴).

`M1371` (N = 2/4 for five runs) → **today N = 1/4 by rule, a sixth run below budget** — the escalation
to the human (P5) on the US DEEP budget stands; this run does not change N.

## DEEP_LOG 2026-09-12: continuous=[ENRG] rotating=[] · **N=1/4 filled by rule (only one OW sector exists; `HLTH` vacated its continuous slot on N+→N; padding forbidden); three slots offered to PREMORTEM with candidates IT · HLTH · FIN in that order** · **verdict deltas 2 of 11: `FIN N → N−` (`eqflow` −0.269 rank 8, Δ −0.170 3rd-worst, 14 🔴 of 47, `BRK-B` 13.9% not a flipper) · `UTIL N− → UW` (`eqflow` −0.273 rank 9, Δ −0.340 worst on the board, 0/9, `NEE` 17.7% not a flipper)** · **9 declines each with its number: `IT N→N+` (flow disagrees: `eqflow` −0.207, Δ −0.012, 23 🔴 vs tape +3.40 on 40/56 — routed to PREMORTEM), `HLTH N→N−` (level rank 3 vs Δ −0.250 sign-split, `C5`, `P150` decides), `ENRG OW→OW+` (no notch; 0-🟢 = gate artifact), `COMM` (barred, Alphabet 76.6% issuer flip; Δ +0.241 largest positive and `META` new-🟢 noted), `STPL` (🚨1名 `WMT` 28.9%; `eqflow`/Δ flat), `INDU` (no notch; 34 🔴/50), `MATR N−→UW` (level rank 5 vs price 2/12 split), `DISC` (Δ +0.015 not newer-worse; `AMZN` 40.2% not a flipper this run), `RE` (no notch)** · **macro re-arguments attempted 1 (FIN on the curve) — not used** · **flip list: by flag 1/11 (STPL/`WMT`/28.9%); by issuer 2/11 (+COMM/Alphabet/76.6%)** · 🚨 **instrument state: `n_axes` 3 (`nonews`), `vel_coverage` 0.0% (pipe 404 ×8, 14:52→22:10) ⇒ zero velocity/freshness citations; Δ = 09-04→09-11 weekly; cap weights 59 d stale ⇒ `eqflow` cut on every line; `asof` 09-11 settled = first new tape in five runs** · ★★ **the structural line: 129 🔴 of 299 (from 92) with only 5 🟢 (from 11) — the flow axes read the week as broad distribution while the cap-weighted tape was −0.77%; the only positive `eqflow` sector is ENRG, and its ignition cannot be measured** · **uncovered=[IT(N, last DEEP 09-07 = 1 run, offered to PREMORTEM #1 — tape/flow divergence, book's largest concentration, `S148` 09-14), HLTH(N, last DEEP 09-08 = 0 runs, offered #2 — sector-level move on 4/32, `P150` 09-18 · `S133` 09-14), FIN(N− DEMOTED TODAY, last DEEP 09-08 = 0 runs, offered #3 — insurer/exchange split, `P124` 09-18), UTIL(UW DEMOTED TODAY, last DEEP 09-07 = 1 run, `S146` 09-14, not slotted — UW), INDU(UW−, last DEEP 09-08 = 0 runs, `S123` settled C today, `S150` 09-14), MATR(N−, last DEEP 09-01 = 4 runs, `P102`-B settled today, `C24` 6th run), COMM(no-verdict, last DEEP 09-01 = 4 runs, barred 19th run), STPL(UW, last DEEP 08-27 = 8 runs, 🚨1名), DISC(UW, last DEEP 08-27 = 8 runs), RE(UW−, last DEEP 08-26 = 9 runs — longest gap, 6th consecutive run unreachable by rule, named)]** · **MANDATORY brackets handed to PREMORTEM: (1) FOMC + SEP 2026-09-16 is 2 sessions from Monday's open — `P148` spans it; PREMORTEM must confirm its anti-signals post-CPI and decide whether a LEVEL companion is needed now that a hike is the 87% base case `[WebSearch]`; (2) the undated Hormuz-reopening 🔀binary now has a dated tell — Iran–Gulf talks in Oman (09-11 `[WebSearch]`) — `P149`'s anti-signal names it; (3) quad witching 09-18 sits inside both new windows (`P149`, `P150`) — named, `P124`'s date**

## ✅ EXIT CHECK — ROTATION
- [x] §1 is one line, verbatim from MACRO §E.
- [x] Every §2 delta cites `eqflow`/breadth/Δ on a non-flipper bucket; the one macro re-argument (FIN/curve) is logged as not used.
- [x] No promotion/demotion rests on a flipper; flip list reproduced (STPL/`WMT` 28.9%; COMM/Alphabet 76.6%).
- [x] Axis count stated (3, `nonews`); no delta cites velocity/freshness; Δ read only against the 09-04 `nonews` snapshot.
- [x] Every divergence named with an owner (DEEP via PREMORTEM promotion, or a dated bracket).
- [x] N = 1/4 by rule; no padding; OW sectors without a slot: **none** (there is only one OW); uncovered sectors named in DEEP_LOG with recency.
- [x] Linter: `report_lint.py` run — 1 D6 finding fixed (rs60 added beside the OBV cite); re-run clean.
- [x] DEEP_LOG appended.
