# SECTOR_DEEP_HLTH — Health Care · industry_US · 2026-09-01 (Tue) · Stage 8 / L1·DEEP
### CONTINUOUS TRACK (slot held) — **leads with the DELTA**, prior structure by reference

> Flow: `SECTOR_FLOW_US_REPAIRED.json`, 3-axis `nonews`, **asof 2026-08-31**. Benchmark `SPY` inline (`C1`).

## 0 · Mandate from ROTATION — one question
> *`eqflow` rank 2 (+0.123) and Δ +0.095 against `XLV` `exc5` **−2.85pp vs `SPY`** (2nd-worst on the
> board) and `exc20` **+3.88pp** (2nd-best). **Rollover, or one week inside a good month?***

## ★ 1 · Answer: ONE WEEK — and the evidence is that the weakness is CONCENTRATED, not broad

**`M1207` [measured] — three names supply almost all of the sector's five-session damage, and they
are one sub-node.**

| name | flow | OBV | `rs20` vs `SPY` | sub-node |
|---|---:|---:|---:|---|
| **`UNH`** | **−0.818 🔴** | −0.213 | **−7.5** | Managed care |
| **`CVS`** | **−0.811 🔴** | −0.197 | **−12.1** | Health services |
| **`SYK`** | **−0.718 🔴** | −0.169 | −6.3 | Devices |
| `CI` | −0.438 | −0.065 | −3.4 | Health services |
| `ELV` | +0.055 | +0.115 | +1.3 | Managed care |

**Against that, the sector's breadth is strongly positive**: of 32 names, **23 have a positive
`flow_score`** and **26 have OBV ≥ 0**. `eqflow` **+0.123 = rank 2 of 11** and the engine's own
like-for-like Δ is **+0.095 (positive)**.

⇒ **The `exc5` −2.85pp is a payer/services drawdown wearing a sector label.** The mandate's answer is
**one week**, and specifically *one sub-node's* week.

### 1b. The `D424` shape has NARROWED — the delta this continuous file exists to report
| run | `wflow` | ex-`LLY` | gap |
|---|---:|---:|---:|
| 2026-08-31 | +0.021 | +0.095 | **0.074** |
| **2026-09-01** | **+0.069** | **+0.115** | **0.046** |

`LLY` (19.4% of the sector's cap, $980B) is still a **drag** — it reads **−0.123 🟡** with OBV
**−0.073** — but the gap between the headline and the ex-top-1 number has closed by a third.
⇒ **the sector is becoming less of a one-name story, not more.** ⚠ This is the `D424` direction the
flip guard does not look for, and it is now reported for a second consecutive run.

## 2 · Sub-node map — 6 nodes, dispersion stated (lens B5)

| node | names (flow · OBV · `rs20`/`rs60` vs `SPY`) | read |
|---|---|---|
| **Life-science tools** | **`A` +0.817 🟢** (the sector's ONLY 🟢, `vol_surge` **1.27**) · `WAT` +0.478 OBV **+0.339** · `TMO` +0.424 · **`rs60` +26.7** · `DHR` +0.376 | ★ **the strongest node, and it is the picks-and-shovels layer** |
| **Big pharma** | `MRK` +0.633 · **+14.4/+21.6** · `PFE` +0.539 OBV **+0.388** · `JNJ` +0.371 · `BMY` +0.161 OBV **+0.404** · **`LLY` −0.123** | broad-positive **except the 19.4% top-1** |
| **Biotech** | `AMGN` +0.550 · **+12.2/+23.1** · `ALNY` +0.539 · `GILD` +0.500 OBV +0.316 · `VRTX` +0.428 OBV **+0.370** · **`REGN` +0.378 OBV +0.381 · `rs60` +25.8** · `ABBV` +0.152 **Δ +0.64** | uniformly accumulating |
| **Devices** | `BDX` +0.495 · **`rs60` +24.5** · `BSX` +0.276 · `EW` +0.137 · `ABT` +0.097 · **`rs60` +20.3** · `MDT` +0.060 · `IDXX` −0.257 · **`ISRG` −0.128** (`rs60` −11.3) · **`SYK` −0.718 🔴** | split, with the two largest the weakest |
| **Distributors** | `COR` +0.277 **Δ +0.55** · `MCK` −0.083 (`rs60` +15.6) · `CAH` −0.209 | mid |
| **Payers / facilities / services** | **`UNH` −0.818 🔴 · `CVS` −0.811 🔴 · `CI` −0.438 · `HCA` −0.392 (Δ −0.54)** · `HUM` +0.185 · `ELV` +0.055 | ★ **the entire drawdown** |

**Dispersion**: `flow_score` spans **+0.817 (`A`) to −0.818 (`UNH`) = 1.635** — the widest of the five
DEEP sectors — against a sector `eqflow` of +0.123. **The label is the wrong unit; the node is.**

## 3 · The news / IR delta this window
- **`LLY` buying Merida Biosciences for up to $2.875bn** [5 outlets / 12 articles, 08-31] — a
  dated, primary-ish capital-allocation event on the sector's top-1 **on the day its flow read
  −0.123**. ⚠ Deal size is **0.29% of `LLY`'s $980B cap** ⇒ **materially small; it does not explain
  the flow** and is not used to.
- ⚠ **`module_disclosure_us` was NOT called for any Health Care name this run** — the budget went to
  `SLB` (Energy). **Stated as a gap**, not inferred around.
- ⚠ **`chain-hop` returned no Health Care theme worth running**: the `"gold miners"` and
  `"data center power"` probes surfaced `AMGN`/`DIS` only from **generic market-wrap** articles
  (`"Nvidia earnings and PCE inflation data take centre stage…"`), which are **co-mentions, not chain
  proximity**. ⇒ **zero admissible chain-hop candidates**, stated.

## 4 · Frame-transfer question — answered
> *Does the take-or-pay / contracted-revenue frame apply to this node?*

**Checked, and it applies in an inverted form the desk has not used before.**
- **Payers (`UNH`, `CVS`, `CI`, `ELV`, `HUM`)** are the sector's *contracted* layer — premiums are
  set annually and locked, so **revenue has a floor and COST does not.** That is the **`MPC` shape,
  not the `MU` shape**: a contract that floors the top line while the input (medical loss ratio)
  floats is a **margin risk**, not a margin protection.
  ⇒ ★ **the node the desk would naively call "contract-protected" is exactly the node that is
  🔴, and the frame explains why.** Recorded as the transferable point.
- **Life-science tools and pharma** sell into capex and patent-protected demand — no contractual
  floor, and none claimed.
⚠ **The contracted share is `unknown` (`C3`) for every name here** — no 10-K/10-Q was opened this run.
**No "the margin must mean-revert" claim is made anywhere in this file**, precisely because that share
was not read.

## 5 · Customers named, and their disclosed spend (rule A6)
- **Life-science tools (`A`, `TMO`, `DHR`, `WAT`)**: customers are **pharma/biotech R&D budgets**.
  Their proxy in this very file is the biotech node, which is **uniformly accumulating** (`REGN` OBV
  +0.381 with `rs60` **+25.8 vs `SPY`** · `VRTX` +0.370 / `rs60` +22.0 · `GILD` +0.316 / `rs60` +12.0 —
  RULE D6, OBV paired with RS on every name because OBV is C-grade and cannot carry a line alone)
  ⇒ **the buyer layer's flow supports the seller layer's flow.**
  **This is the one place in the run where a node and its customers were both measured and agreed.**
- **Payers' customers are employers and CMS.** No disclosed-spend series is wired for either; next
  relevant dates are the Q3 prints (mid-October) and the CMS rate cycle. **Given as dates.**
- **`LLY`/`MRK`/`PFE`'s customers are payers** — ⚠ **and the payer node is the sector's 🔴 layer.**
  ⇒ a named, measured tension: the pharma node's demand-side buyer is the weakest thing in the
  sector. **Recorded; not resolved by this file.**

## 6 · Valuation discipline (lens B2) — deliberately NOT applied
⚠ **No "cheap on forward multiple" claim is made in this file, so no margin percentile is owed.**
`module_fundamentals_us` was run for `MPC`/`NEM`/`LIN`/`AVGO`/`MU` this run and **not** for Health
Care names. **Stated as a scope choice, not an oversight** — the mandate was rollover-vs-week, which
is a flow-and-tape question.

## 7 · Track KPIs and anti-signals
| KPI | now | what flips it |
|---|---|---|
| `XLV` `exc5` vs `SPY` | **−2.85pp** (2nd-worst) | a return above 0 while `UNH`/`CVS` stay 🔴 ⇒ confirms the payer-only diagnosis |
| Payer node (`UNH`+`CVS`+`CI`) OBV | **−0.213 / −0.197 / −0.065** | ★ **any turn to 매집** ⇒ the drawdown's source repairs |
| `A` (the sector's only 🟢) `vol_surge` | **1.27** | **< 1.0** ⇒ the sole ignition dies and the sector has none |
| `wflow` − ex-`LLY` gap | **0.046** (from 0.074) | widening again ⇒ back to a one-name story |
| `S133` | settles **2026-09-14** | *"is the sector its constituents or its ETF?"* — this file's §1/§2 is its pre-settle groundwork |

**Anti-signals that would kill the "one week" reading**: (i) the positive breadth (23 of 32) falling
below half; (ii) the life-science-tools node turning 분산 — that is the node whose customers this
file used as corroboration; (iii) a payer-node *recovery* accompanied by a broad decline, which would
invert the diagnosis entirely.

## 8 · Handed forward
- **To BET (CONFIRMED-EARLY, unsized)**: **`A` (Agilent)** — the sector's only 🟢, `vol_surge` 1.27,
  OBV +0.240 with `rs20` **+8.6 vs `SPY`** (RULE D6 — paired, not OBV alone), and the shortlist's only
  Health Care name. ⚠ **`[FINRA]` short-z was not pulled for
  `A`** — stated as a gap on the same line.
- **Watch, not handed**: `REGN` · `VRTX` · `BMY` · `PFE` — the four highest OBV readings in the sector
  (+0.381 / +0.370 / +0.404 / +0.388) all sitting at 🟡 because `vol_surge` is **0.55–0.78**.
  ★ **The same `vol_surge` gate that emptied five sectors' shortlists (SWEEP §3) is what keeps this
  node out.**
- **To the next ROTATION**: the mandate is answered — **one week, one node** — and `HLTH OW` is
  supported by breadth (23 of 32 positive) rather than by its top-1, whose drag is **narrowing**.
