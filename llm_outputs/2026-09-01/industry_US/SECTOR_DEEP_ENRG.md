# SECTOR_DEEP_ENRG — Energy · industry_US · 2026-09-01 (Tue) · Stage 8 / L1·DEEP
### ROTATING TRACK (R1) — **FULL FRESH MAP**

> Flow: `SECTOR_FLOW_US_REPAIRED.json`, 3-axis `nonews`, **asof 2026-08-31** (the strike session).
> Benchmark `SPY` named inline (`C1`). Prices tagged **[live]** are partial bars (NYSE open).

## 0 · Mandate from ROTATION — one question
> *Is the OW a **BARREL** bet or a **CHAIN-POSITION** bet — and does the tape separate them?*

## ★ 1 · Answer: it is a CHAIN-POSITION bet, and the tape separates them cleanly

**`M1201` [measured] — remove the two integrated majors and the sector gets STRONGER, not weaker.**

| cut | n | `wflow` | `eqflow` |
|---|---:|---:|---:|
| all Energy | 16 | **+0.302** | **+0.387** |
| ex-`XOM` (30.5% of cap) | 15 | **+0.471** | +0.418 |
| ex-integrateds (`XOM` 30.5% + `CVX` 18.4% = **48.9% of the bucket's cap**) | 14 | **+0.444** | +0.409 |

★ **Half the sector's market cap is two integrated majors, and they are the drag.** `XOM` is the only
Energy name with a **negative** flow (−0.083) and **negative OBV** (−0.092). The `D424` shape
(a non-flipper whose top-1 pulls the *wrong* way) is at its strongest here on the whole board.

⚠ **`eqflow` (+0.387) > `wflow` (+0.302) ⇒ breadth-led, not mega-cap-narrow.** Energy is the **only**
sector on the board with `breadth` above zero (**0.120**) and **0 reds of 16**.

## 2 · Flow — the chain, node by node, with the split quantified

| chain node | names (flow · OBV · `rs20`/`rs60` vs `SPY` · surge) | node read |
|---|---|---|
| **① Services & equipment** | **`SLB` +0.822 🟢** OBV **+0.498** (highest in the sector) · +20.6/+2.3 · **1.28** · Δ +0.25 · ★ **`new_green`** · `BKR` +0.251 🟡 | **ignition node** |
| **② E&P (upstream)** | `COP` +0.617 · `OXY` +0.487 · `DVN` +0.483 · `EOG` **−0.208** · `FANG` **−0.311** | **split** — the two pure shale names are the negatives |
| **③ Integrated** | `CVX` +0.546 (OBV +0.227) · **`XOM` −0.083** (OBV −0.092) | **`XOM` alone is the sector's only negative flow** |
| **④ Refining (downstream)** | **`MPC` +0.711** OBV **+0.427** · **+20.4/+38.5** · **`PSX` +0.617** · +18.4/**+32.6** · **`VLO` +0.550** · +15.5/**+37.3** | ★ **the return leaders of the whole sector, by a wide margin** |
| **⑤ Midstream / storage** | **`WMB` +0.710 🟢** · **1.28** · Δ **+0.46** · ★ **`new_green`** · `OKE` +0.522 Δ +0.46 · `KMI` +0.322 (1.11) · `TRGP` +0.154 | **second ignition node** |

★ **`M1202` [measured] — the sector's only two `new_green` ignitions in the entire 299-name universe
are `SLB` (services) and `WMB` (midstream). Neither is a barrel.** The barrel's own proxies (`EOG`,
`FANG`, `XOM`) are the three weakest rows in the sector. ⇒ **the mandate's answer, in one line: the
money is in the *positions along the chain that get paid regardless of where the barrel settles*, not
in the barrel.**

⚠ **Dispersion (lens B5)**: `rs60` spans **`MPC` +38.5 to `FANG` −2.5 = 41.0pp** inside one sector
label, against `XLE`'s own `exc20` of **+7.56pp vs `SPY`**. **The spread is 5× the sector's move ⇒ the
sector label is the wrong unit**, exactly as `S136` (settles 09-09) pre-registered.

## 3 · Positioning — and it is the sharpest asymmetry the run found
`[COT 08-25]` **WTI spec net +31,099 = the 43rd percentile of its year**, +1,935 WoW, 15.0% of OI —
**mid-range, NOT crowded**, into a two-day multi-state military escalation. `Nat Gas` spec is at the
**2nd percentile 🔴 crowded-short** (rebound ammunition, not a signal).

`[FINRA 08-31]` short-volume, z vs own 20-day base:
**`MPC` 74.5% · z +2.90 🔴** (5v5 **+5.1▲**) · `SLB` 62.1% · z +1.13 (5v5 **+8.7▲**) ·
`PSX` 59.1% · z +0.87 · **`XOM` 43.2% · z −0.26 (5v5 −18.3▼)**.

★ **`M1203` [measured] — shorts pressed INTO the winners and left the laggard.** `MPC` rose **+2.69%**
(08-27 → 08-31, official) on its most extreme short-volume reading of the trailing month, while short
pressure **left** `XOM`, the only negative-flow name. **The two Energy legs are being positioned in
opposite directions**, which is `S136`'s bracket stated in the order-flow the news cannot see.

## 4 · IR / primary filings — and one thing that cuts against the story
`module_disclosure_us SLB` (SEC primary, trailing window):

| date | form | note |
|---|---|---|
| 2026-07-29 | **10-Q** | latest quarterly, period 2026-06-30 |
| **2026-08-26 · 08-27 · 08-31** | **Form 4 ×3** | insider transactions, on **three of the last four sessions** |
| **2026-08-26 · 08-27 · 08-31** | **Rule 144 ×3** | **notices of proposed sale**, same three dates |
| 2026-07-29 | **IRANNOTICE** | Section 13(r) Iran-related disclosure — ⚠ **filed by the very name the desk is calling the sector's ignition, in the window the Iran escalation defines** |

★ **`M1204` [measured] — `SLB` filed three Rule 144 proposed-sale notices and three Form 4s across
08-26/27/31, i.e. in the exact window it became the board's #3 flow name and one of only two
`new_green` ignitions.** ⚠ **Rule 144 is a notice of intent, not a completed sale, and the share
counts were not read** — this is recorded as a **dated primary-source counterweight**, not as a
verdict. **It is the kind of fact a flow-only read cannot produce.**
⚠ `module_disclosure_us` returned **no 10-K in the window** for `SLB`; the FY risk factors were **not
opened** — stated as a gap.

## 5 · Fundamentals — the peak-margin / low-multiple test (lens **B2**) on `MPC`
| | `MPC` |
|---|---|
| price **[live]** | $377.06 (52w **161.93 – 381.15**) — within 1.1% of its 52-week high |
| Trailing / **Forward P/E** | 13.07 / **11.71** |
| Beta | **0.51** |
| target mean | **$324.56 = −13.9% BELOW the live price** |
| recommendations (30d) | 4 SB / 6 B / **8 H** / 1 S |

**Estimate momentum — 90-day change** (`module_fundamentals_us` §추정치 모멘텀):
**current quarter +127.0%** (8.59 → 19.49) · **next quarter +91.0%** · **current year +81.9%**
(27.61 → 50.23) · **next year +44.4%** (22.31 → 32.20).
**Revision breadth 30d: current quarter 14↑ / 0↓ · current year 12↑ / 0↓.**

★ **`M1205` [measured] — `MPC` is the textbook peak-margin / low-multiple shape and the desk names it
as such.** A forward **11.71×** whose current-year EPS estimate has risen **+81.9% in ninety days on
12 upgrades and zero downgrades** is **consensus chasing a peaking denominator**, not cheapness
(lens **B2**). ⚠ And the desk's own rule bars using the revision table as a leading indicator — it is
used here **only** as a description of the denominator's direction, which is what it is valid for.
⚠ **The margin percentile is `unknown` (`C3`)** — no gross-margin history is wired for US names
(`scripts/margin_history.py` is KR-only and **exits 1**, PREFLIGHT G7). **A multiple without a margin
percentile is not a valuation, and this one is therefore not called cheap.**
★ **The consensus target sitting 13.9% BELOW the price** is the same statement from the other side:
price has outrun the analyst set even as the analyst set raced upward.

**QoQ rate beside the level (lens B1, `C2` both halves):** revenue 33.80 → 34.81 → 32.57 → 34.20 →
**51.99 $B**; QoQ **+3.0% → −6.4% → +5.0% → +52.0%**; the last quarter is **+53.83% YoY**.
⚠ **A +52% sequential jump is not a normal refining quarter** and this file does **not** attribute it
— the 10-Q line item was not opened this run. **Flagged as the single largest unexplained number in
the sector, and handed forward as a dig rather than narrated.**

## 6 · Frame-transfer question — answered, and the answer is on record
> *Does take-or-pay / contracted revenue apply to refining?*

**Checked, and it does NOT — and the desk already proved it.** `M831` (2026-08-23) read `MPC`'s 10-Q
directly: **zero take-or-pay, zero fixed-fee margin structure, zero long-term contracts**, and the
only long-term structure runs the **wrong way** (minimum-volume commitments to MPLX that *"negatively
impact segment adjusted EBITDA when throughput is lower"*). ⇒ **`MU`'s contracts floor REVENUE;
`MPC`'s floor COST.** Lens **B2** has **no escape hatch in refining**, which is why §5 does not call
11.71× cheap.

★ **Where the frame DOES transfer, and it is a different node**: **midstream**. `WMB` (+0.710 🟢,
`new_green`, Δ +0.46), `OKE` (+0.522, Δ +0.46) and `KMI` (+0.322) sell **contracted capacity**, and
`KMI`'s **$35.67B RPO** is already in the desk's own files as a "thing the Fed can't reach". ⇒ **the
sector's second ignition node is the one node in Energy where the contractual floor is real** —
and the desk owns **neither** midstream name.
⚠ `R89` + `P83` still bar any refining-margin-vs-barrel **separation** claim; §1–§3 make a
**chain-position** claim from flow and positioning, not a margin-separation claim. Stated so the two
are not conflated.

## 7 · Customers named, and their disclosed spend (rule **A6**)
- **Refiners' customer is the crack spread**, i.e. product demand. `[news 08-31, 5 outlets]`
  *"Goldman Sachs Sees Diesel Refining Margins Soaring to $63 a Barrel"* — ⚠ but
  `theme-age "refining margin"` reads **⚪ECHO, accel 0.72× (decelerating)** on a base of 297.
  ⇒ **the margin call is being published into falling attention**, which is the opposite of the
  barrel (`theme-age Larak` **58.21×**). **The two legs have different crowds.**
- **`SLB`'s new customer set is data centers** — *"SLB Makes $3.4 Billion Bet on AI Data Center Boom"*
  [oilprice, 08-31] (the Kelvion acquisition). **Their disclosed spend is the AI-power capex line, and
  `SECTOR_DEEP` Lens 4 measured that every AI-power equity is 🟡 or 🔴 with Texas halting new
  interconnects.** ⇒ **`SLB`'s demand-side check comes back negative on the tape** even as its own
  flow is the sector's best. **Named, dated, and contradictory — recorded, not smoothed.**
- **Midstream's customers** are contracted shippers under multi-year agreements (see §6).

## 8 · Chart structure (`module_chart SLB`, restored this run — PREFLIGHT G7)
`SLB`'s render completes (0/3 → 3/3 today). Reading the panels: **RSI is in the >70 overbought band at
the right edge** while the **OBV-20d panel has just turned positive after a long negative stretch** —
read beside its `rs20` **+20.6 vs `SPY`** and `vol_surge` **1.28** (RULE D6 — OBV is C-grade and does
not carry the reading alone; here all three agree).
⇒ **overbought on price, freshly accumulating on volume** — the EXTENDED-BUT-LIVE shape, with the
overbought reading as the near-term caution. ⚠ Any window spanning **08-28** inherits the data hole
for the 259 unbackfilled names; `SLB` is one of them, so **the 08-28 candle in this render is
reconstructed by the tool's own dropna, not observed.** Stated.

## 9 · chain-hop — body-proximate candidates only
`chain-hop "refining margin" --days 7 --scope foreign` (34 articles scanned):
- **HEADLINE-NAMED (already crowded)**: `XOM` (7 title / 4 body) · `CVX` · `VLO` · `MPC` · `PSX` · `TRGP`.
- **CHAIN-HOP candidate**: **`NVDA`** (3 proximity / 5 body) — ⚠ **rejected**: the article is
  *"2 Oil Refiners Outperforming NVIDIA: Are They Better Buys Now?"*, i.e. a **comparison**, not a
  chain relationship. **A news co-mention alone is not a candidate** and it does not reach BET.
- ⇒ **zero admissible chain-hop candidates from Energy this run**, stated.

## 10 · Track KPIs and anti-signals
| KPI | now | flips the verdict |
|---|---|---|
| `CL=F` settled | **85.76** (08-31) · 88.13 **[live 09-01]** | `P122`-B: **≤ 80.00** through 09-08 |
| WTI spec 1-year %ile `[COT]` | **43** | `P122`-A: **≥ 65** with `CL=F` ≥ 88.00 ⇒ the asymmetry is gone |
| `EW{SLB,MPC,PSX,VLO,COP}` − `EW{XOM,EOG,FANG}` | `S136` settles **09-09**, ±3.90pp | **B (≤ −3.90pp)** kills this file's headline chain-position claim |
| `theme-age Larak` | **58.21×** | **< ~5×** with `CL=F` still bid ⇒ the bid is no longer the Strait |
| `MPC` short-vol z | **+2.90 🔴** | a fall below +1.0 with price held ⇒ the short leg capitulated |

**Anti-signals**: an **OPEC+ quota decision** or **M&A on any of the eight `S136` names** inside the
window (⚠ base rate **not remote — `SLB` closed one on 08-31**); a **front-month roll** in `CL=F`
(`R116`'s class, and `P122` carries the clause explicitly).

## 11 · Handed forward
- **To BET (CONFIRMED-EARLY, unsized)**: **`SLB`** and **`WMB`** — the universe's only two `new_green`
  ignitions, both in the node where the contractual floor is real or the demand is contracted.
  ⚠ **`SLB` carries the §4 insider-notice counterweight and the §8 overbought RSI on the same line.**
- **Held-name read**: `MPC` and `PSX` are **EXTENDED-BUT-LIVE** (OBV 매집, `rs60` +38.5 / +32.6 vs
  `SPY`), with `MPC`'s valuation flagged **peak-margin, not cheap**, and its consensus target
  **13.9% below** the live price. **No sizing here** (P4).
- **To the next ROTATION**: the mandate is answered — **chain-position, not barrel** — and the
  measurement that carries it is `M1201` (ex-integrated `wflow` **+0.471** vs headline +0.302).
  ⇒ **"Energy OW" should be booked as two legs**, which is precisely what `S136` settles on 09-09.
