# SECTOR_ROTATION — industry_US · 2026-09-01 (Tue) · Stage 6 / L1·ROTATION

> **Delta-only.** MACRO §E owns the 11-sector verdict; `SECTOR_FLOW_US_REPAIRED.json` owns the flow
> numbers. Both are on disk. This file writes **only what changes and why**.

## 🚨 Instrument state governing every line below
`SECTOR_FLOW_US.json §scoring`: **`n_axes` 3** · `vel_coverage` **16.05%** · `scored` 299 · asof
**2026-08-31**. **The news axis is dead** (cause probed = the pipe: 6/6 pre-sweep, 4/4 post-sweep,
**10/10** direct falsification) ⇒ **no delta below cites theme freshness or news velocity.**
🔴 **The primary file's OBV axis is revoked** (259 of 299 names carry a two-session hole; 29%
label-flip rate). **Every number here comes from `SECTOR_FLOW_US_REPAIRED.json`**, 3-axis `nonews`,
299 scored. **Δ is legal this run and is a TWO-session quantity** — prior same-mode snapshot is
**08-27 `nonews` n=299**, 299/299 common names; the 08-28 snapshot holds 1 key and cannot be an
operand. Every Δ below carries the label **`repaired sweep, 08-27 → 08-31`**.

**🚨 The flip list, reproduced in full even though it changes little:**
- **Repaired (citable): 1 of 11** — **Consumer Discretionary · `AMZN` · 40.2%** (−0.282 → **+0.081**).
- **Primary: 2 of 11** — adds **Financials · `BRK-B` · 13.9%** (−0.040 → +0.004).
- ★ **`IT` is NO LONGER a flipper** — `NVDA` 19.4%, +0.048 → **+0.001**, sign held. It flipped on
  08-29, 08-30 and 08-31; this is the first run in four in which it does not. Stated because the
  three prior runs' declines rested on that bar.
- ⚠ **`D424` cases (non-flippers that are still one-name stories, opposite direction): TWO** —
  **Energy** (+0.302 vs ex-`XOM` **+0.471**) and **Comm. Services** (`wflow` −0.547 vs `eqflow`
  **−0.033**, `GOOGL` 38.3%). **Materials is the mirror**: +0.148 vs ex-`LIN` **+0.115** ⇒ `LIN` is
  holding it **up**.

---

## §1 · Inherited — one line, verbatim from MACRO §E

`MACRO holds: ENRG OW · HLTH OW · MATR OW · FIN OW− · COMM OW− · IT N · STPL N · DISC UW · INDU UW · RE UW · UTIL UW.`

⚠ **One inherited inconsistency, named rather than silently adopted**: the 08-31 run demoted
**Industrials to `UW−`** (its only delta, carried by three non-cap-weighted numbers) and MACRO §E
today printed **`UW`**. **The notch stands at `UW−`** — nothing this run reverses it, and today's
numbers are worse, not better (23 reds of 50 = the board's most in absolute terms; Δ **−0.225** =
2nd-worst). Recorded so the un-notching is not inherited by accident.

---

## §2 · Deltas — 1 of 11 attempts, and 10 declines each with its number

| sector | matrix said | flow evidence (`repaired sweep, asof 08-31`) | new verdict | who resolves |
|---|---|---|---|---|
| **Financials** | **OW−** | ★ **The disagreement that blocked this move on 08-31 is gone — both non-cap-weighted signals now point the same way.** `eqflow` **−0.186 = rank 7 of 11** · **0 greens of 47 — the largest bucket on the board with none, for a 2nd consecutive run** · **13 reds** · and **Δ −0.047**, one of only three negative Δs on the board (last run it was **+0.073 = 3rd-best**, which is precisely why the demotion was declined then). **Non-flipper in the citable file** (`BRK-B` 13.9%, −0.191 → −0.142, sign held) ⇒ the delta rests on `eqflow` + red-count + Δ, **not on `wflow`** — which PREFLIGHT G3 bars for this sector | **N** | **`S134`** (settles **2026-09-04**, `XLF` exc5 vs `SPY`, A ≥ +2.00 / B ≤ −2.00). ⚠ The tape still disagrees (`XLF` `exc2` **+0.23pp vs `SPY`**) and that is exactly what `S134` brackets |

### Declines — every attempt, with the number that killed it

| attempted | number | why declined |
|---|---|---|
| **`IT N → OW`** | `wflow` **+0.048** (and **no longer a flipper**: ex-`NVDA` +0.001) — but `eqflow` **−0.014 (negative)**, **3 greens against 9 reds**, and the board's two worst names are both IT (`AMAT` −0.889, `WDC` −0.861) | 🚫 **Declined: the non-cap-weighted number is negative.** ★ **And the case FOR promotion is not a flow number** — `S124` fired **A at 41 of 56** positive 5-session excess vs `SPY`, which is a **return** measurement. Promoting on it would be the return re-argument this stage's own rule forbids (the 2026-07-21 `RE` precedent). ⇒ **routed to PREMORTEM for a 3rd consecutive run**, and made **DEEP mandate #1** |
| **`MATR OW → N`** | `eqflow` **+0.030 (positive**, rank 3 of 11) · **Δ +0.120 (positive)** · ex-`LIN` **+0.115** · **0 greens / 2 reds of 12** | 🚫 **Declined: both admissible numbers are positive and a demotion cannot ride a positive number.** ⚠ **The only number pointing down is a cross-file comparison** — yesterday's repaired `eqflow` was +0.164 (rank 1) and today's is +0.030 (rank 3) — and **PREFLIGHT G2 does not license subtracting two separate reconstructions**. The engine's own like-for-like Δ is **+0.120**. ★ **"0 greens of 12" is carried forward as Materials' standing question, not as a verdict** — same treatment `FIN` got on 08-31, and the same question is now **one run old** |
| **`UTIL UW → UW−`** | `eqflow` **−0.454 = rank 11/11 (worst)** · red-rate **10 of 15 = 66.7% (worst)** — but **Δ +0.076 (positive**, 6th of 11) | 🚫 **Declined for a 3rd consecutive run, and on a third distinct reason** — 08-30 declined it **on method** (the only new argument was a macro re-argument), 08-31 **on newness** (an unchanged extreme is restatement), today **on Δ direction** (the like-for-like two-session move is *positive*). ⚠ **EVENT_ALPHA Card 5 (Texas halts data-center power) is a macro/story argument and is explicitly NOT used here.** This is now the longest-declined move on the board |
| **`COMM OW− → N`** | `eqflow` **−0.033 = rank 5 of 11, mid-table** · `wflow` −0.547 **barred** (`D297` 13th run + `GOOGL` 38.3%) · Δ **−0.287 = worst on the board** | 🚫 **Declined: the only admissible non-cap-weighted level is mid-table.** Δ is the worst on the board and that is stated, but a **single** negative number against a mid-table level does not carry a notch when the sector's headline is barred by rule. ⇒ **taken as a DEEP slot instead** (§3), with a mandate narrow enough to be answerable |
| **`DISC UW → UW−`** | red-rate **12 of 28** · `eqflow` −0.226 rank 8 · Δ −0.146 | 🚫 **Declined — 🚨1名 bucket.** `AMZN` at **40.2%** is the largest single-name share on the board and the sector's entire negative sign is that one company (ex-`AMZN` **+0.081**). The red-rate would support it; the flip bar forbids it |
| **`RE UW → UW−`** | `eqflow` −0.267 rank 9 · 5 reds of 12 · **Δ +0.168 = 2nd-best on the board** · ex-`WELL` **−0.334** (top-1 holds it up) | 🚫 Declined — Δ points the wrong way for a demotion |
| **`STPL N → UW`** | `eqflow` −0.157 **rank 6 = mid-table** · Δ **+0.107 (positive)** · ex-`WMT` −0.115 (better without the top-1) | 🚫 Declined — mid-table plus a positive Δ |
| **`HLTH OW → N`** | `eqflow` **+0.123 = rank 2** · Δ **+0.095 (positive)** · 1 green / 3 reds of 32 | 🚫 **Declined — both admissible numbers are positive.** ⚠ The case for a cut is the **tape** (`XLV` `exc5` **−2.85pp vs `SPY`**, 2nd-worst on the board, against `exc20` **+3.88**), which is a return re-argument. ⇒ **handed to DEEP as its mandate** |
| **`ENRG OW → OW+`** · **`MATR`/`HLTH OW → OW+`** | ENRG rank 1 on both axes, **0 reds of 16**, the **only** sector with `breadth` > 0 (0.120), **Δ +0.353 = best on the board by 2×**, and **both of the universe's only two `new_green` ignitions** (`SLB`, `WMB`) | 🚫 **No notch above `OW` exists in this desk's scale.** Recorded so the absence is not read as indifference — Energy is the most confirmed verdict on the board and the scale cannot say so |
| **`INDU UW− → lower`** | 23 reds of 50 (most in absolute terms) · `eqflow` −0.365 rank 10 · Δ **−0.225 = 2nd-worst** | 🚫 **No notch below `UW−` exists.** The numbers deepened; the scale is exhausted. `S126` + `P114` bracket 09-04 |

### §2b · Divergences named, each with a resolution owner
- **(a) matrix-OW but flow-absent — `MATR`.** OW with **0 greens of 12** and `eqflow` +0.030.
  → **DEEP mandate**: *early, or trap?*
- **(a) matrix-OW but tape-absent — `HLTH`.** `eqflow` rank 2 against `exc5` −2.85pp vs `SPY`.
  → **DEEP mandate**: *is the sector rolling over, or is one week inside a +3.88pp month?*
- **(b) flow-led, matrix under-rated — none.** Energy is already OW and the scale has no higher notch.
- **(c) new-🟢 ignition — `SLB` and `WMB`, and they are the ONLY two in 299 names.** Both Energy.
  → confirmation of the existing OW; **`SLB` also carries EVENT_ALPHA Card 5's power-layer question**.
- **★ The board's sharpest divergence is inside IT and it is between two of this desk's own
  instruments**: `S124`'s **41 of 56** positive 5-session excess vs `SPY` against the sweep's
  `eqflow` **−0.014 / 3🟢 vs 9🔴`. **IT is up on price and down on participation.**
  → **DEEP mandate #1**, and routed to PREMORTEM in parallel.
- ⚠ **The sweep is `asof 08-31`, the last settled close** — unlike the previous three runs it is
  **not** behind the escalation; the 08-31 session **is** the strike session. Stated because the
  08-31 run's decisive Energy decline rested on the opposite fact.

---

## §3 · DEEP picks + DEEP_LOG

**DEEP budget: N = 4** (protocol line: 2 continuous + 2 rotating; unchanged 2026-07-31).

**Recency read from DEEP FILES ON DISK**, not from `DEEP_LOG` lines:

| sector | last DEEP file | runs since |
|---|---|---:|
| ENRG · HLTH · INDU · MATR | 2026-08-31 | **0** |
| COMM · FIN · IT | 2026-08-30 | **1** |
| DISC · STPL | 2026-08-27 | **3** |
| RE | 2026-08-26 | **4** |
| UTIL | 2026-08-23 | **6** |

**OW set after §2's delta: `ENRG`, `HLTH`, `MATR`, `COMM−`** (FIN left the OW set).

- **Continuous (⌈4/2⌉ = 2) — `MATR`, `HLTH`.** Both held a continuous slot last run and both are
  still top-N OW today ⇒ **anti-thrash continuity applies and they KEEP the slots** (stated).
- **Rotating slot R1 — `ENRG`.** ⚠ **Declared 0-run recency violation** under the recency-starved
  fallback: every OW sector has been covered inside 3 runs, so the rule's own fallback is
  "next-highest OW regardless, and say recency-starved." Justified independently — rank 1 on both
  axes, the only sector with breadth > 0, both `new_green` ignitions, the largest Δ on the board, and
  the run's dominant news cluster (22 outlets) is its driver.
- **Rotating slot R2 — `COMM`.** ⚠ **Declared 1-run recency violation.** ★ **This is a deliberate
  change from 08-31, which rejected `COMM` as a pad on the grounds that `D297` makes the bucket
  un-measurable.** That reasoning, repeated, makes `COMM` **structurally unreachable forever** — the
  same defect the 08-31 log itself flagged about `UTIL` ("the 2nd consecutive run in which the board's
  most extreme sector is unreachable by the selection rule"). ⇒ **the slot is filled and the mandate
  is narrowed to a question n=12 CAN answer**: not *"is COMM OW?"* but *"the cap-vs-equal split is
  −0.547 vs −0.033, the widest on the board — is that `GOOGL` alone, and what is underneath it?"*
  **N = 4/4 filled. No padding with N/UW.**
- **`IT` is routed to PREMORTEM** as a candidate 5th slot (the 08-30 precedent), because the case for
  it is a **return** measurement that this stage may not act on but a pre-mortem may interrogate.

### Mandates handed to DEEP — one question each, being the question the sector's own numbers could not answer

| sector | mandate |
|---|---|
| **MATR** (C1) | **Early or trap?** An OW with **0 greens of 12**, `eqflow` +0.030, `LIN` holding the headline **up** (ex-`LIN` +0.115), against **copper spec at the 100th percentile of its year** `[COT 08-25]` — `C24`'s third reproduction. Its two best names (`NEM` +0.772 rank 6/299, `FCX` +0.700) are both 🟡 **by 0.01–0.14 of `vol_surge`**. Resolve whether the sector is accumulating ahead of a story or is the crowded end of one |
| **HLTH** (C2) | **Rollover or one week?** `eqflow` rank 2 (+0.123) and Δ +0.095 against `XLV` `exc5` **−2.85pp vs `SPY`** (2nd-worst on the board) and `exc20` **+3.88pp** (2nd-best). The `D424` shape has **narrowed** (+0.069 vs ex-`LLY` +0.115, from +0.021 vs +0.095). `S133` settles 09-14 |
| **ENRG** (R1) | **Is the OW a BARREL bet or a CHAIN-POSITION bet — and does the tape separate them?** `ex-XOM` **+0.471** vs headline +0.302; `MPC` +0.711 / `PSX` +0.617 / `VLO` +0.550 (`rs60` **+38.5 / +32.6 / +37.3 vs `SPY`**) against `EOG` −0.208 / `FANG` −0.311; `XOM` Δ **+0.468** is now among the board's best while its short pressure fell **−18.3** on the 5v5 trend. `S136` settles 09-09 and this is its pre-settle groundwork |
| **COMM** (R2) | **Is the −0.547 `wflow` `GOOGL` alone, and is there anything underneath?** `eqflow` **−0.033** vs `wflow` **−0.547** — a **0.514 gap, the widest on the board**; `GOOGL` is **−0.778 🔴분산** (OBV −0.237, `rs20` −10.4 vs `SPY`) at 38.3% of a 12-name bucket with only **3 reds**. Answer with the other 11 names, not with the bucket |

## DEEP_LOG 2026-09-01: continuous=[MATR, HLTH] rotating=[ENRG, COMM] · N=4/4 filled · **OW sectors WITHOUT a slot: none — all 4 OW sectors got one** · **verdict deltas 1 of 11 attempts: `FIN OW− → N`, carried by `eqflow` −0.186 (rank 7), 0 greens of 47 for a 2nd run, 13 reds, and Δ −0.047 — all non-cap-weighted, on a bucket that is a NON-flipper in the citable file; ★ the 08-31 decline of this same move rested on the two signals disagreeing (level said demote, Δ +0.073 said the opposite) and today they AGREE, so the move is made on a NEW number rather than by restating an old one** · **10 declines each with its number: `IT N→OW` (eqflow −0.014 negative, 3🟢/9🔴 — and the case FOR it, `S124` firing A at 41/56, is a RETURN measurement this stage may not act on ⇒ routed to PREMORTEM, 3rd consecutive run), `MATR OW→N` (both admissible numbers POSITIVE: eqflow +0.030, Δ +0.120; the only bearish number is a cross-file eqflow comparison G2 does not license), `UTIL UW→UW−` (3rd consecutive decline on a 3rd distinct reason — method / newness / Δ +0.076 pointing the wrong way; EVENT_ALPHA's Texas-halt finding is a macro re-argument and was NOT used), `COMM OW−→N` (eqflow −0.033 rank 5 mid-table; wflow barred by `D297` 13th run), `DISC UW→UW−` (🚨1名 `AMZN` 40.2%, ex-top1 +0.081), `RE UW→UW−` (Δ +0.168 = 2nd-best, wrong direction), `STPL N→UW` (rank 6 mid-table, Δ +0.107), `HLTH OW→N` (eqflow rank 2 and Δ both positive; the bearish case is the tape, a return re-argument), `ENRG`/`MATR`/`HLTH OW→OW+` (no notch above OW exists), `INDU UW−→lower` (no notch below UW− exists; numbers deepened to 23 reds of 50 and Δ −0.225)** · **flip list 1 of 11 in the citable file — DISC/`AMZN` 40.2% — DOWN from 3 on 08-30 and 08-31; ★ `IT` is NO LONGER a flipper (`NVDA` 19.4%, +0.048 → +0.001, sign held) for the first time in four runs, which removes the bar the three prior IT declines rested on, and IT was still declined — on `eqflow` this time; ⚠ the PRIMARY file lists 2 flippers (adds FIN/`BRK-B` 13.9% on a 0.004 margin) and the two instruments therefore DISAGREE about Financials, logged as `C26`** · **`D424` cases: TWO — ENRG (+0.302 vs ex-`XOM` +0.471) and COMM (`wflow` −0.547 vs `eqflow` −0.033); MATR is the mirror (`LIN` holds it UP, +0.148 vs ex-`LIN` +0.115); HLTH's case NARROWED (+0.069 vs +0.115, from +0.021 vs +0.095)** · 🚨 **instrument state: the ghost bar HEALED at the head — 08-31 Close NaN 1 of 301 (the 1 is `EA`) after two runs of `scored=0` — and the NATIVE sweep scored 299/299 again; but 08-28 froze into a PERMANENT partial hole (259 of 301 still NaN at T+4) which zeroes the two most recent sessions out of the OBV window for those names, measured at 0.065 mean / 0.404 max `obv_norm` shift with a 29% label-flip rate, so the PRIMARY file's OBV axis is revoked and every number above comes from `SECTOR_FLOW_US_REPAIRED.json` (258 names patched from the 5m endpoint; residual OBV error 0.009 mean / 0.026 max, 0 of 42 label flips)** · **`n_axes` 3, `vel_coverage` 16.05% ⇒ ZERO deltas cite velocity or freshness; Δ IS legal (prior same-mode snapshot 08-27 `nonews` n=299, 299/299 common) and every Δ carries the label `repaired sweep, 08-27 → 08-31` — a TWO-session quantity, not a one-day move** · ★ **the structural line: `eqflow` is positive in exactly 3 of 11 sectors (ENRG +0.387, HLTH +0.123, MATR +0.030) and the whole universe holds SIX 🟢 names in 299, of which the underlying axes say 88 names are accumulating AND beating `SPY` over 20 sessions — the difference is entirely `vol_surge`, so the 6 is a threshold artifact and not a breadth measurement (SWEEP §3)** · ★ **`COMM` R2 is a DELIBERATE reversal of 08-31's pad-rejection: repeating `D297`'s un-measurability makes the bucket permanently unreachable, so the slot is filled with a mandate n=12 can actually answer** · **uncovered=[IT(N — routed to PREMORTEM, 08-30 = 1 run, the book's largest concentration, and the ONE sector where a settled bracket fired against the desk this run: `S124` FIRED-A at 41 of 56 from a 13/56 registration state), FIN(N — DEMOTED TODAY, 08-30 = 1 run, `S134` settles 09-04 and brackets exactly the flow-vs-tape disagreement that survives the demotion), DISC(UW, 08-27 = 3 runs, 🚨1名 `AMZN` 40.2%), STPL(N, 08-27 = 3 runs, rank 6 mid-table), RE(UW, 08-26 = 4 runs, Δ +0.168 = 2nd-best on the board), INDU(UW−, 08-31 = 0 runs, 23 reds of 50 = board's most, bracketed 09-04 by `S126`+`P114`), UTIL(UW, 08-23 = **7 runs — the longest recency gap on the board for a 3rd consecutive run**, board's worst `eqflow` −0.454 and worst red-rate 66.7%, NOT slotted because UW sectors do not take DEEP slots; ★ EVENT_ALPHA Card 5 supplies a NAMED falsifier for the AI-power story that would otherwise argue for promoting it, so the structural unreachability is now costing a live question)]**

---

## ✅ EXIT CHECK
- [x] **§1 is ONE line**, inherited verbatim from MACRO §E. No unchanged sector gets a row —
      the only addition is the named `INDU UW−` inconsistency, which is a **correction**, not a restatement.
- [x] **Every §2 delta cites a flow number.** The single delta (`FIN OW− → N`) rests on `eqflow`
      −0.186, 0 greens of 47, 13 reds and Δ −0.047. **Two attempts were declined specifically because
      their only support was a non-flow argument** (`IT` — a return measurement; `UTIL` — EVENT_ALPHA's
      macro cause), and both are logged as declined rather than taken.
- [x] 🚨 **No promotion or demotion rests on a `top1_flips_sign` bucket.** `DISC` was declined on
      exactly that ground with `AMZN` 40.2% written out; the flip list is reproduced in full (§0)
      **including the fact that it shrank from 3 to 1 and that `IT` left it.**
- [x] **Axis count stated** (`n_axes` 3, `vel_coverage` 16.05%); **no delta cites freshness or
      velocity**; Δ read only against a same-mode snapshot (08-27 `nonews`, 299/299 common).
- [x] Every matrix×flow divergence named with a resolution owner (§2b) — `MATR`→DEEP, `HLTH`→DEEP,
      `IT`→DEEP #1 + PREMORTEM, `ENRG`→DEEP + `S136`, `FIN`→`S134`.
- [x] **4 DEEP targets by the rule**: continuity applied and stated (`MATR`, `HLTH` keep their slots),
      recency read **from files on disk**, both rotating slots' recency violations **declared with
      justification**, **no padding**, and every uncovered sector named in `DEEP_LOG` with its recency.
- [x] **Linter run on this stage's own output** — `report_lint.py .../SECTOR_ROTATION.md` ⇒ **0 findings** (C1·C2·S6·D6). ⚠ Form only; a clean run is not a correct report.

- [x] `DEEP_LOG` line appended.

---
> Next: `python pipeline/run_protocol.py industry_us --next` → **PREMORTEM** (Stage 7).
> ⚠ PREMORTEM inherits: **`IT` routed for a 3rd run** (and a settled bracket fired against the desk
> there); a **≤48h binary at D-1 (`AVGO`)** already bracketed by `S132`; and **the oil axis lost its
> bracket when `S92` scored** — `P122` was registered at MACRO to replace it, and PREMORTEM must check
> that replacement rather than assume it.
