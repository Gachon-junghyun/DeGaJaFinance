# BLINDSPOT_PREMORTEM — industry_US · 2026-08-19 · Stage 7/11 ★US-only

> Four adversarial lenses fanned out in parallel against this run's own tilt, BEFORE the deep budget
> is committed. Every number is settled to **2026-08-18**, benchmark named inline, `velocity`/
> `breadth` unused (G1). Analytical only — zero buy/sell, zero sizing (P4).

## 0 · Verdict — the lenses did not agree with the draft, and two of them broke this run's own work

**Fan-out: 4 lenses, all four returned named tickers and dated catalysts. Not one rubber-stamped.**
Three findings are severe enough to change what this run may claim:

| # | Finding | Lens | Status |
|---|---|---|---|
| **1** | 🚨 **`S98`'s branch A and `S98`'s VOID trigger are the same observation.** Its anti-signal reads *"an **FOMC-dated communication** or a CPI/PPI print inside the window drives all four together ⇒ VOID"*, and **FOMC minutes released 2026-08-19, inside its 08-13→08-20 window.** Branch A **is** "all four carry the same sign." ⇒ **An A-fire is unscoreable.** | 2 | **VERIFIED against the registered text** |
| **2** | 🚨 **`P73`, registered by this run's own MACRO 40 minutes ago, is void at registration.** Its anti-signal names *"a Warsh testimony headline that moves ≥3 outlets"* — and **§D-1 of the same report carries Warsh at 10 articles / 6 outlets.** | 2 | **VERIFIED — self-inflicted** |
| **3** | 🚨 **G5 audited the wrong book.** The gate read `module_paper_book`; `cycle_exposure` reads the real KIS account. The two differ by **5 names**. | 4 | **VERIFIED by independent re-fetch** — correction appended to `PREFLIGHT_US.md` |

---

## 1 · §4c — claims this run asserted and then refuted

**Left standing, with what broke them appended. Not edited away.**

1. **MACRO's headline said `S98` branch A "would retire a four-sector tilt."** It cannot. Branch A
   fires exactly when the row's own VOID condition is satisfied. **The correct 08-20 outcome for an
   A-shaped print is `VOID` + construction defect, not a tilt retirement.** The beta artifact `P74`
   named is still real and still binds — it now sits *on top of* an unscoreable branch, which makes
   the row worse, not better.
2. **MACRO wrote of `P73`'s anti-signal: *"this anti-signal is already live and is reachability-checked,
   not decorative."*** It noticed the Warsh print and **failed to draw the conclusion from it.** A live
   anti-signal at registration does not make a bracket robust; it makes it **void**. `P73` is withdrawn
   and replaced by **`S102`** below, whose anti-signal is reachability-checked *and* not already fired.
3. **PREFLIGHT G5 reported "11/11 US holdings inside `us_top300` AND scored."** The real book holds
   **12**, including `LITE` (**the #2 position, 12.01% of invested**) and `T` (9.59%), neither of which
   was on the list G5 checked. **Re-run against the real book: 12/12 covered and scored — the verdict
   survives, the evidence did not.**

---

## 2 · LENS 1 — UNDER-COMPUTED LEGS

**Three legs. One promoted.**

- ★ **LEG A — Consumer Staples big-box `TGT`+`WMT`. PROMOTE-TO-DEEP (5th slot).**
  **`TGT` reports today 2026-08-19; `WMT` reports 2026-08-20** — two dated issuer binaries inside 24h,
  in the sector this run moved (UW−→UW) and then **excluded from DEEP on recency**.
  `TGT` is Staples' top flow name (**+0.713**, OBV 매집 +0.177, RS20 **+7.5 vs `SPY`**, RS60 +17.5,
  `vol_surge` 1.12) and `WMT` is one of the run's **two new-🟢** (Δ **+0.446**).
  EW{`TGT`,`WMT`}: `eqflow` **+0.466**, 2/2 accumulating, 2/2 positive RS20 vs `SPY` — against a sector
  `eqflow` of **−0.020**.
  🚨 **The decomposition refutes ROTATION §2b's own reasoning**: of the sector's +0.325 Δ, `WMT` alone
  contributes **+0.129 (40%)** at a 28.9% weight while **`TGT` contributes +0.004 at a 1.8% weight** —
  so `TGT`'s +0.210 Δ and +0.713 score are **invisible in every cap-weighted instrument this run
  read.** That is the definition of under-computed, and "mega-cap-narrow" was the wrong diagnosis.
- **LEG B — Health Care ex-payers. WITHIN-RUN-WATCH.** Splitting out the 5 payers (`UNH ELV HUM CVS CI`,
  13.7% of sector cap): ex-payers `eqflow` **+0.298** · 19/27 accumulating · median RS20 **+6.70 vs
  `SPY`**; payers `eqflow` **−0.567** · 0/5 positive RS20. **A 2.35pp β-adjusted spread inside one
  sector.** ⇒ **The N+ granted today was made on `LLY`-removal (+0.163 → +0.149); payer-removal is
  nearly double that effect and nobody ran it.** Not promoted **only because the clock fails** — no
  Health Care print inside 5 sessions (first is `A` on 08-27).
- **LEG C — Financials alt-managers + exchanges. WITHIN-RUN-WATCH.** `KKR APO BX BLK SCHW CME ICE`
  (10.6% of sector cap): `eqflow` **+0.512**, **7/7 accumulating, 7/7 positive RS20 vs `SPY`**, median
  **+10.10**. Against the insurance complex (10.5% of cap) at `eqflow` **−0.246**. ⇒ **The −0.087
  `eqflow` that justified holding FIN at N− is the average of two opposite nodes of near-identical
  weight.** G3 blocked the `wflow` move; nobody looked underneath it. Δ is flat (+0.011), hence watch.

---

## 3 · LENS 2 — REGIME-FLIP / BOTH-SIDES

### 3a · The against-us branch is not the one the desk expected

Regressing each sector's daily excess vs `SPY` on Δ10y (bp), controlling for Brent, **120 settled
sessions to 08-18**:

| leg | pp per bp of 10y | read |
|---|---|---|
| `XLU` | **+0.0049** | no measurable rate beta |
| `XLRE` | **−0.0072** | no measurable rate beta |
| `XLP` | **+0.0224** | no measurable rate beta |
| `XLV` | **+0.0185** | no measurable rate beta |
| **`XLE`** | **+0.1534** (R² 0.550 w/ Brent; univariate r **+0.602**) | **the only rate-exposed tilt on the board** |

🚨 **This falsifies the transmission the whole duration underweight was built on.** `P51-A` said high
real yields press UTIL/RE/STPL; the four legs have **no measurable rate beta**, while the desk's
**overweight** does — with a **positive** sign. `P73`'s direction A (`DGS2` ≥4.30) would *help* the OW.

**What actually rips against us is the risk-off channel**, and it fires three registered rows at once:
`S&P500` spec at the **88th %ile crowded long** de-risks → `XLK` (β 1.74) leads down → every low-beta
defensive prints positive **raw** excess by construction ⇒ **`S98` branch A ∧ `S80` branch A ∧ `S82`
branch B simultaneously**, while ENRG loses both legs together (**−0.153pp per bp of yield decline and
−0.233pp per 1% Brent**, same regression). In the book: **`MPC` −0.305 pp/bp, `PSX` −0.295.**
- **Trigger**: `DGS2` ≤4.08 at the first `[FRED]` close covering 08-21 **and** `SPY` 5-session return
  ≤ −1.0% (08-14→08-21).
- **Invalidation**: `SPY` 5-session return inside ±0.5% (no directional tape ⇒ no beta channel), or
  `DGS2` in 4.08–4.30 (`P73`'s pre-declared favourite; the realised 9-observation range **4.15–4.25**
  sits entirely inside it).

### 3b · Hormuz — and the invalidation is the crack, quantified

Excess vs `SPY` per 1% `BZ=F`, 120 sessions: `XLE` **+0.313** (r 0.688) · `XOP` +0.409 · `MPC` **+0.345**
· `PSX` **+0.314** · `VLO` +0.363. `S95` branch B (Brent 91.02 → ≤86.50 = −4.97%) maps to `XLE`
**−1.56pp**, `MPC` **−1.71pp**, `PSX` **−1.56pp**. The duration UW returns only **+0.24pp** against
that ⇒ **the ENRG OW is a naked one-way exposure and the UW complex does not hedge it.**

★ **But the crack is the load-bearing invalidation, and it is now measurable**: `HO=F`×42−`CL=F` closed
**101.96**, the **100th percentile of its own trailing 252 sessions** (mean 53.87, sd 19.87, p95 90.11).
Multivariate, 120 sessions: refiner excess = β_crack·Δcrack($) + β_Brent·Brent% →
`MPC` **+0.2144 / +0.256** (R² 0.414) · `PSX` +0.2016 / +0.231 · `VLO` +0.2771 / +0.248.
⇒ **A $6 crack decline costs `MPC` as much as a 5% Brent decline. A Hormuz reopening does not falsify
`MPC`/`PSX` unless the crack rolls with it.** Invalidation: **crack ≥95 at the 08-21 settle**.

### 3c · ★★ The correlated-cluster finding that binds every bracket this desk owns

| pair | **excess** vs `SPY` corr (60d) | **raw** return corr (60d) |
|---|---|---|
| `XLE`–`XLU` | **+0.467** | **−0.003** |
| `XLE`–`XLP` | **+0.496** | **+0.050** |

**The coupling is entirely the `SPY` subtraction.** `XLK`-excess correlates **−0.401** with `XLE` and
**−0.606** with `XLU` ⇒ **the dominant factor on this desk's own measurement axis is IT-vs-rest, and
every non-IT tilt loads it with the same sign.** Every S-row is denominated in excess vs `SPY`, so
**the whole board shares one common factor it has never measured** — including `S84`, whose
`XLE` − EW{`XLU`,`XLRE`} spread is partly measuring that factor rather than Hormuz.

**Correlated-UW, real on both axes** (so `S80`'s framing survives): raw 60d `XLRE`–`XLP` **+0.654** ·
`XLU`–`XLRE` **+0.588** · `XLU`–`XLP` **+0.395**, all **tighter than when `S80` was registered on
08-13**. ★ But `XLV` is **not** the fourth leg the excess axis implies: raw `XLV`–`XLU` is only
**+0.297** (excess +0.633) while `XLV`–`XLRE` is +0.592 and `XLV`–`XLP` +0.547 — **Health Care joins
through REITs and Staples, not through Utilities.** `S82` branch B may not be read as "one duration
bet" without the raw column.

**Correlated-OW, real**: **`MPC`–`PSX` raw 60d +0.897** — the book holds both; **that is one position
with two tickers.** And **`ETN`–`ANET` raw 60d +0.722**, *higher than* `AVGO`–`ANET` (+0.596) and
`NVDA`–`AVGO` (+0.495), while `ETN`–`RTX` raw is **−0.040** ⇒ **`ETN` sits in the Industrials OW− as
the diversifying leg and trades as AI-compute.** Registered as `S99`.
**Genuinely independent**: `XLE`–`XLI` excess −0.139 · `RTX`–`ETN` excess −0.203.

---

## 4 · LENS 3 — MOMENTUM-CONTINUATION

### 4a · The avoidance is not argued in words — it is enforced mechanically, which is worse

`corr(rs60, vol_surge) = **−0.124**` across 299 names. The 9 names clearing `vol_surge ≥ 1.2` average
**rs60 −12.7 vs `SPY`** against a universe mean of **+2.9**. **Of the top-20 by `flow_score`, 18 are 🟡
and only 2 are 🟢.** Exactly **1 of the top-15 runners (`RTX`) is green.**

**Countable consequence — independently re-verified against the sweep JSON:**

| ticker | flow | tag | `vol_surge` | RS60 vs `SPY` | OBV |
|---|---|---|---|---|---|
| `DELL` | +0.094 | 🟡 | 0.82 | **+82.1** | 중립 |
| `AXON` | +0.511 | 🟡 | 0.72 | **+55.7** | 매집 +0.323 |
| `PANW` | +0.023 | 🟡 | 0.73 | +44.6 | 중립 |
| `SHOP` | +0.506 | 🟡 | 0.71 | +36.5 | 매집 +0.211 |
| `ABNB` | **+0.717** *(#2 flow on the board)* | 🟡 | 1.09 | +33.2 | 매집 +0.169 |
| `PYPL` | +0.345 | 🟡 | 0.62 | +33.1 | 매집 +0.155 |
| `DASH` | +0.594 | 🟡 | 0.87 | +32.5 | 매집 +0.389 |
| `CRWD` | −0.108 | 🟡 | 0.69 | +28.1 | 분산 |
| `TMO` | +0.461 | 🟡 | 0.63 | +27.8 | 매집 +0.266 |
| `BKNG` | +0.455 | 🟡 | 0.63 | +27.1 | 매집 +0.157 |

**Nine of these ten have ZERO mentions across all five of this run's documents; `AXON` has one.**
**Ten of the fifteen strongest names vs `SPY` were never discussed.**

★ **The desk half-caught it and failed to generalise.** `SWEEP_READ §3` and `SECTOR_ROTATION §2e`
both correctly ruled that Energy's `0🟢/16` *"is NOT evidence against it — a pure volume-gate
exclusion."* **That ruling was applied to two sectors and never to the ten individually-erased names.**
And `EVENT_ALPHA §247` flags `LITE`/`COHR`'s negative 60d excess as a caution **while admitting `LITE`
(rs60 −12.8 vs `SPY`) to the shortlist and never naming `DELL` (+82.1)** — the bias in one comparison.

### 4b · Re-tags (measured; z = 5-day move ÷ σ20·√5)

**EXTENDED-BUT-LIVE** *(RULE D6 — OBV is grade C and never carries a tag alone here; every row below pairs it with an excess-vs-`SPY` figure and a `module_chart --read` structure verdict, and the 5-name instrument conflict is named at the end of this section)*: `DELL` (ex60 **+82.1 vs `SPY`**, RSI 65.4, CONFIRMED-TURN, OBV +45% 누적) · `HPE` **(HELD**, +60.8, 매집
+0.303) · **`AXON`** (+55.4, OBV **+87% 누적**, −3.2% from a high set 42 days ago, RSI cooled to 64.7) ·
`PSX` **(HELD**, +37.0, OBV +50% 누적, RSI 78.0) · `ABNB` (OBV **+194% 누적**) · `DASH` · `BKNG` ·
`MPC` **(HELD**, fragile — bearish divergence).
**EXHAUSTED**: **`AVGO` (HELD)** — ex60 **−11.8 vs `SPY`**, **−21.0% from a 60-day high last touched
77 days ago**, **0 of 4 MAs above**, RSI 41.5, OBV **−88% 분배**, and **the worst two-session Δ in the
entire 299-name universe (−0.488)** · `ANET` **(HELD**, ex60 +26.4 but bearish divergence, OBV −21%
분배, **−8.2% off its high — deepest drawdown of the fifteen leaders**) · `NVDA` **(HELD**, OBV −95%
분배) · `ETN`/`MET`/`NDAQ` (HELD) · `PANW` · `VLO` · `SHOP` · `PYPL`.

⚠ **n=1 discipline applied**: `MPC`/`VLO`/`PSX`'s +8~9% five-day moves are **one observation** (the
crack, `P66`), not three. Normalised z = **1.45 / 1.68 / 1.96 — none reaches 2σ.**
⚠ **Instrument conflict, material**: the sweep's `obv_state` and `module_chart --read`'s 20d OBV slope
**flip sign on 5 of 22 names** (`ANET`, `NUE`, `RTX`, `PYPL`, `CRWD`). **Any tag resting on OBV alone
for those five is unsafe today** (rule D6: OBV is grade C).

---

## 5 · LENS 4 — CYCLE EXPOSURE

**The green tick means *"no gap among 3 registry cycles covering 62.4% of invested capital, one of
which has its floor set to 0.0."*** Registry `updated 2026-07-17` — **33 days stale**.

**The registry failed in the direction it cannot fail in.** It can only flag *too little*; today the
finding is **an unaudited 16.28%-of-invested optical position that no registry entry, no floor and no
gate touches** — larger than the registered rank-2 Energy epicenter (`PSX`+`MPC` = 15.45%) and larger
than defense (`RTX` 9.60%).

| unregistered cycle | epicenter measurable in `us_top300`? | the measurable layer, 08-18 | book |
|---|---|---|---|
| **Optical / interconnect** | **YES** — `LITE COHR CIEN GLW` | **2 of 4 clear `vol_surge` 1.2 — `COHR` 1.70 = rank 1 of 299, `LITE` 1.25 = rank 7**, the only node clearing the gate that blocked all 16 Energy and all 32 Health Care names | **16.28% of invested** |
| **Advanced packaging / ABF** | **NO** — Ajinomoto/Ibiden/Shinko non-US; `AMKR ONTO KLIC COHU` outside `us_top300` | WFE proxy (`AMAT LRCX KLAC ASML TER`) `eqflow` **−0.460**, **0/5 accumulating, 3/5 dispersing** | **0%, and unbuyable here** |
| **China humanoid robotics** | **NO** — Shanghai-listed | only US-named humanoid is `TSLA`: **−0.789 🔴분산**, RS20 **−13.7 vs `SPY`** | **0%** |

⚠ **Rule stated where it applies**: the 0% on cycles 2 and 3 is licensed **only** by
`N.유니버스부재`. The dispersing tape would **not** on its own license 0% core in a multi-year cycle —
a crowded tape gates ADD **timing**, never core. Those are different arguments and only the first holds.
★ **The book is long the victims of the ABF constraint (`NVDA`+`AVGO`), not the constraint.**
★ **`T` (9.59% of invested) maps to no cycle, no card and no thread — and sits in Communication
Services, this run's Rotating-2 DEEP.** Its own flow: **+0.383 🟡, OBV 매집 +0.291, RS20 +9.3 vs `SPY`.**
🚨 **The rank-3 defense floor is `min_epicenter_pct: 0.0` — a guard that is off** — for a second
consecutive audit, while defense posts the board's broadest accumulation (6/6 names accumulating,
6/6 positive RS20 vs `SPY`).

---

## 6 · Brackets registered this run — all both-sided, all frozen, all implied-move-checked

**ID allocation (D76 collision class)**: `grep` for `S99`–`S103` across `handoff/*.md`,
`llm_outputs/` and `REPORT/` returned **0 hits**; highest existing **S98 (US) / S64-KR (KR)**.

### `S99` — Is `ETN` an Industrials position, or the book's 5th AI-compute unit? *(Lens 2)*
- **Observable**, settled closes, `auto_adjust=False`, window **08-14 close → 08-21 close**:
  `M` = EW{`ANET`,`AVGO`,`HPE`} 5-session excess vs **`SPY`**; `E` = `ETN` 5-session excess vs **`SPY`**.
- **A (AGAINST US)** |M| ≥ **3.0pp** ∧ sign(E)=sign(M) ∧ |E| ≥ **1.5pp** ⇒ the Industrials OW− holds a
  5th AI-compute risk unit; the book's AI concentration is **5 of 12, not 4 of 12**.
- **B (FOR US)** |M| ≥ 3.0pp ∧ (sign(E)≠sign(M) ∨ |E| ≤ **0.5pp**) ⇒ `ETN` is a genuine electrical leg.
- **C** |M| < 3.0pp — the basket never moved enough for the test to have a null. Declared now.
- **D93 before freezing** (252 overlapping 5-session windows to 08-18): **A 29.8% · B 17.1% · C 46.8%**;
  conditional on |M|≥3.0 (53.2% of windows) **A 56.0% / B 32.1%**. 2-year check n=496: 27.8/15.9/50.2.
  sd(M) 5.095 · sd(E) 3.988 · corr(M,E) **+0.467**.
- **State at registration** (2 of 5 sessions elapsed): **M −2.617pp · E −3.324pp** — same sign,
  |M| below the gate. **Leaning A, gate not cleared** — disclosed now, not discovered at scoring.
- **Implied move**: no single-name straddle governs a 4-name sign test; `ETN`'s own leg is a **1.5pp**
  threshold against `RTX`-class ±1.9% D2 implied for comparable industrials ⇒ **inside the implied
  move, and therefore declared LOW-RESOLUTION on the `E` leg at registration.**
- 🚨 **Anti-signal**: M&A, guidance withdrawal or index deletion at `ETN` or any of the three; an
  `ETN`/`HPE` earnings print in-window; a market-wide halt ⇒ **VOID**. `NVDA`'s 08-26 print is outside
  by design — the row tests **carry**, not an event.
- **B4 grading**: **A falsifies** the desk's concentration count and the Industrials OW−'s status as a
  diversifier — a live sizing input. **B falsifies this lens**, and at 17.1% is genuinely reachable.
  **C confirms nothing and is modal at 46.8%** — disclosed. **Settle 2026-08-21.**

### `S100` — Does the Staples promotion survive its own two earnings prints? *(Lens 1)*
- **Observable**: EW{`TGT`,`WMT`} cumulative return, **08-18 settled close → 08-21 settled close**,
  minus **`SPY`** over the identical window. **Both prints land inside it** (`TGT` 08-19, `WMT` 08-20).
- ★ **Implied-move check, and it moved the thresholds**: `TGT` **±7.8%** and `WMT` **±5.3%**
  (both expiry 2026-08-21, **D2**) ⇒ EW implied ≈ **±6.55%**. Lens 1 proposed ±3.0pp; **that is inside
  the implied move and would carry no information.** **Thresholds are set AT the implied move instead.**
- **A (AGAINST US)** excess **≥ +6.5pp vs `SPY`** ⇒ the Staples improvement is issuer-level demand,
  β-free (basket β vs `SPY` = **0.09**) ⇒ **the one-notch promotion was too small and the big-box node
  needed separate treatment.**
- **B (FOR US)** excess **≤ −6.5pp vs `SPY`** ⇒ the +0.325 flow Δ was defensive-rotation beta, as
  `XLP`'s β-adjusted **−0.592** said.
- **C** between — **and C is the unconditional favourite: measured 2-session |excess| sd 2.27pp,
  P(≥+3.0) 11.1%, P(≤−3.0) 7.9%.** ⚠ **But C is NOT the conditional favourite**: in the identical
  calendar week last year `TGT` printed **−6.33% (2025-08-20)** and `WMT` **−4.49% (2025-08-21)**,
  an EW one-day move of ≈**−5.4%**. **Both figures are stated so neither can be used as an escape
  hatch later.**
- 🚨 **Anti-signal**: `SPY` 08-18→08-21 move beyond **±2.5%** (index shock, not issuer), or a
  guidance/M&A event at either name that is **not** the scheduled print ⇒ **VOID**.
- **B4 grading**: **A falsifies** ROTATION §2b's explicit "mega-cap-narrow" reasoning on a β-0.09
  basket — the very defence used to discount the raw print. **B falsifies the delta rule itself**, the
  same rule that produced the other promotion today (HLTH on Δ +0.228). **Neither branch merely
  confirms. Settle 2026-08-21.**

### `S101` — Does the `vol_surge` gate cost the desk return? *(Lens 3, thresholds measured here)*
- **Observable**: `spread` = EW{`DELL`,`PANW`,`SHOP`,`ABNB`,`CRWD`,`DASH`,`PYPL`,`TMO`,`BKNG`,`AXON`}
  5-session excess vs **`SPY`** **MINUS** EW{`KKR`,`RTX`,`LITE`,`ORCL`,`CSCO`,`MA`,`BAC`,`WMT`}
  5-session excess vs **`SPY`**, at the **2026-08-26** settle. Both baskets are frozen **as printed by
  today's run** — the 10 erased runners and the 8 admitted shortlist names.
- **A (the gate is costing us)** spread **≥ +4.45pp** ⇒ the names the `vol_surge` gate excluded beat
  the names it admitted by more than the measured p85 ⇒ **the gate is an alpha filter with the wrong
  sign, not a quality filter.**
- **B (the gate is earning its keep)** spread **≤ −3.88pp** ⇒ the excluded runners gave it back and
  the gate did its job.
- **C** between.
- **D93 executed BEFORE freezing**, 252 trailing settled 5-session windows to 08-18: **mean −0.035 ·
  sd 4.527 · p15 −3.877 · p85 +4.452** ⇒ **P(A) 20.6% · P(B) 20.6% · P(C) 58.7%.**
  ★ **This estimator is centred at −0.035 — the first near-zero-centred estimator this desk has
  measured**, against nine consecutive rows where the centre was materially non-zero.
- **State at registration**: **−0.677** (erased EW exc5 −0.406 vs green EW exc5 +0.271) — **mid-band,
  near-neutral.** Context, not the observable: **erased EW 60-day excess vs `SPY` +40.04 against green
  EW +1.18 — a 38.9pp gap.**
- 🚨 **Anti-signal**: an earnings print at ≥3 of the 10 erased names inside the window (the basket
  would then be measuring an earnings cluster, not the gate) ⇒ **VOID**.
- **B4 grading**: **A falsifies the desk's own shortlist construction** and would make the 🟢 gate a
  measured liability. **B falsifies this lens** and vindicates the gate. **Both branches bite.
  Settle 2026-08-26.**

### `S102` — replaces the self-voided `P73`: which tenor carries the FOMC minutes?
- **Why**: `P73` voided at registration on its own Warsh anti-signal (§1.2). The binary is real and
  **unbracketed**, which is the condition this stage exists to prevent.
- **Observable**: `DGS2` **and** `30y−10y`, at the **first `[FRED]` close covering 2026-08-21**.
- **A (front-end repricing)** `DGS2` **≥ 4.30** ⇒ bear flattener; **every duration-UW argument on this
  desk changes sign again**, and per Lens 2's regression the ENRG OW is *helped*.
- **B (long-end-only)** `DGS2` **≤ 4.08** ∧ `30y−10y` **≥ 0.59** ⇒ term premium owns the move.
- **C** between — **favourite, disclosed**: `DGS2`'s realised 9-observation range is **4.15–4.25**,
  entirely inside C.
- 🚨 **Anti-signal, reachability-checked AND not already fired**: an **intermeeting Fed policy
  action** or a **Treasury refunding announcement** inside the window ⇒ **VOID**. ⚠ **Warsh testimony
  is deliberately NOT an anti-signal here** — it is same-day with the minutes and inseparable from
  them, which is exactly the defect that killed `P73`.
- **Implied move**: `SPY` **±0.5% (expiry 08-19, D0)** — a same-day index straddle prices **no**
  meaningful move, so a ≥15bp `DGS2` threshold is **outside** anything the options market has priced.
- **B4 grading**: **A falsifies** the desk's rate framing for the third time in six days
  (`P56`→`R73`→`P65`→§A-1). **B falsifies** `R73`'s successor mechanism. **Settle 2026-08-21.**

### Recorded, NOT re-thresholded — three existing rows whose bands sit inside their own implied moves
**Thresholds are frozen; these are construction notes for the settling desk, per the rule that a
bracket firing inside the implied move carries no information.**

| row | band | implied move (D±n) | verdict |
|---|---|---|---|
| **`S96`** (08-21) | ±5.0pp on EW{`COHR`,`LITE`} | `LITE` **±6.7%** · `COHR` **±6.6%** (both D2) | 🚨 **bands INSIDE the basket's own implied move ⇒ NO-INFORMATION.** Pre-settle +0.247 |
| **`P70`/`P75`** (08-21) | ±2.0pp on EW{`MPC`,`PSX`,`VLO`} | `MPC` **±3.8%** · `PSX` **±7.3%** (D2) | 🚨 **inside.** `P75`'s crack leg (≥95 / ≤88) is **outside** and is the informative half |
| **`S98`** (08-20) | sign agreement | no straddle | 🚨 **branch A ≡ its own VOID trigger** (§0.1) |

★ **New instrument finding on `LITE`**: `US_LIVE_SHORTLIST` labels it **`✅저숏/숏커버 (청정 상승)`** on
a FINRA short-**volume** z of −1.07, while `module_flow --positioning` reads **12.2% of float short,
DTC 2.0, 🔥크라우디드**. **Both are true and they measure different objects** — a large short *stock*
being *covered*. **The shortlist's "low-short" wording is wrong on the stock measure**, and `S96`'s
name may be moving on a squeeze rather than on accumulation. Logged as dig **`D289`**.

## 7 · Hand-offs

- **→ DEEP**: **`STPL` promoted as the 5th slot** (Lens 1 LEG A; two dated prints inside 24h).
  Continuous `ENRG`, `INDU`; rotating `MATR`, `COMM`. **`COMM`'s mandate gains a name it did not have:
  `T` is 9.59% of the real book and OBV-accumulating inside the desk's UW sector.**
- **→ ALPHA action bracket**: `S99` · `S100` · `S101` · `S102`, plus the three no-information notes.
- **→ BET**: the **`MPC`–`PSX` raw 60d +0.897** finding — one position with two tickers — and the
  `ETN`–`ANET` +0.722 coupling. **BET owns sizing; this stage states neither size nor direction.**
- **→ human (P5)**: arm the rank-3 defense floor (`min_epicenter_pct: 0.0` is a disarmed guard) and
  add the **optical/interconnect** registry entry with a new `epicenter_unmeasurable` field so
  `cycle_exposure.py` prints `N.유니버스부재` instead of silently scoring 0%.

## ✅ EXIT CHECK
- [x] **4 lenses fanned out in parallel in one message**; each returned named tickers + dated
      catalysts. **None rubber-stamped** — three broke this run's own work.
- [x] **Every new bracket (`S99` `S100` `S101` `S102`) names its observable, frozen threshold and
      settle date, and carries BOTH branches.** No one-way brackets.
- [x] **Every magnitude threshold stated against the implied move** with its `D±n` — `S100`'s
      thresholds were **moved to the implied move** because Lens 1's ±3.0pp sat inside it; `S99`'s `E`
      leg and three existing rows are **pre-declared no-information** rather than presented as triggers.
- [x] **D93 executed before freezing** on `S99` (A 29.8/B 17.1/C 46.8), `S100` (sd 2.27, 11.1/7.9/81)
      and `S101` (mean −0.035, sd 4.527, p15/p85, 20.6/20.6/58.7). State at registration disclosed on
      all three.
- [x] Under-computed legs: **1 PROMOTED to a 5th DEEP**, 2 logged WITHIN-RUN-WATCH — none dropped.
- [x] Both-sides brackets per binary produced; **the catalyst instrument's missed FOMC binary is
      bracketed by `S102`** after `P73` voided.
- [x] Momentum re-tags done with flip conditions, n=1 normalisation, and the OBV instrument conflict named.
- [x] Cycle-GAP read **with the registry-coverage caveat quantified (62.4% of invested)**; the
      correlated-UW pattern was hunted and found, and the correlated-**OW** pattern too.
- [x] §4c section written: **three claims this run made and then refuted are left standing with what
      broke them appended.**
