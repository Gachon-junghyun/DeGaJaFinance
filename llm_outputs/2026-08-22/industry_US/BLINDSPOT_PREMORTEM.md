# BLINDSPOT_PREMORTEM — industry_US · 2026-08-22 (Sat) · Stage 7 / L1·PREMORTEM ★US-only

> Four adversarial lenses argue **AGAINST our own tilt** before the deep budget is committed.
> Draft tilt: `HLTH OW · ENRG OW · MATR N+ · STPL/DISC/COMM N · FIN N− · RE UW · INDU UW− · UTIL UW ·
> IT UW`. DEEP set: **continuous [ENRG, HLTH] · rotating [FIN, INDU]**.
> Flow `asof` **2026-08-21** settled; `exc5`/`exc3` vs **`SPY`** named inline (`C1`).
> **No sizing, no buy/sell language (P4).**

## §0 · Execution constraint, declared

**The four lenses ran IN-CONTEXT, not as a parallel adversarial agent fan-out** — a standing session
constraint, declared here as on 08-21. **What is lost is the *diversity* of independent agents; what is
not lost is the adversarial construction** — each lens below is run against the draft tilt with its own
mandate and returns named tickers and dated catalysts. The loss is real and is not glossed.

---

## §1 · The binary inventory — and the one the calendar cannot see

`catalyst_calendar --days 10` (run this stage, saved to `llm_outputs/2026-08-22/CATALYST_WATCH.json`)
returns **4 binaries**: `NVDA` earnings **08-26** · July PCE **08-28** · `FRO` earnings **08-28** ·
Hormuz "open" statement **undated** (+ MSCI review 08-31, structural).

🚨 **It carries no row for the Jackson Hole symposium, 2026-08-27 → 08-29, at which a new Fed chair
makes his debut.** The date is measured from a body, not a headline: `economictimes` **08-22** —
*"the **August 27 to August 29 event** in Jackson Hole"*. (`HANDOVER §7` / `R93` retracts yesterday's
claim that this event was 08-21; `D18` remains real, and this run hand-injects the row.)

**Three binaries therefore land inside seven days, and two of them share one window.** A named market
view ranks them: `bloomberg` 08-21 — *"Allspring's Miletti Sees **Jackson Hole as Bigger Risk Than
Nvidia**."*

---

## §2 · LENS 1 — UNDER-COMPUTED LEGS (the bull case for what we did NOT deep-dive)

**Mandate**: strongest bull case for a sector/sub-leg with a catalyst ≤ ~5 trading days that did not
get a DEEP slot. Verdict per leg: **PROMOTE-TO-DEEP** or **WITHIN-RUN-WATCH**. Nothing silently dropped.

| Leg | The bull case against our tilt | Catalyst | Verdict |
|---|---|---|---|
| **IT / semis (UW)** — `SMH`, `NVDA`, `MU`, `AMD` | **The UW is priced, not building.** `SMH` exc5 **−3.293 = 12th %ile of 252** and `NVDA` **−3.269 = 21st %ile** *before* the print. `Nasdaq-100` spec is at the **4th %ile with +30,838 WoW covering** `[COT 08-18]`. `eqflow` **−0.102 rank 8** is materially better than the price rank (11th) — **mega-cap weakness, not breadth collapse**. `MU` delta **+0.300** off a low base. And the tape's own attribution, 08-21: *"Chip Stocks Didn't Fall on Chip News This Week"* | **`NVDA` 08-26 (D-4)** | 🚨 **PROMOTE-TO-DEEP as the 5th slot** — see §7. A UW carried into the board's largest binary, from the 12th percentile, with the desk holding **four** names in it, is the definition of an under-computed leg |
| **MATR / gold (N+)** — `NEM` | `NEM` `obv_norm` **+0.487 — the highest of all 299 names**, `S89` fired **branch B**, and **gold spec is only at the 48th %ile** ⇒ the move is not a futures crowd. `GLD` 5d **+5.450 = 94th %ile** | July PCE **08-28** | **WITHIN-RUN-WATCH.** ⚠ The counter is decisive enough to block promotion: **`M779` puts 246.4% of `NEM`'s rs60 in the last 20 sessions** and its option **P/C is 0.17 with skew +0.1 — nobody is hedged.** Complacency plus exhaustion geometry is not a promotion case |
| **DISC (N)** — `AMZN`, `TSLA`, `ABNB` | `eqflow` **+0.103 rank 2 of 11**; `TSLA` delta **+0.519**, `ABNB` rs60 **+39.8**, `CVNA` +0.722 | none dated ≤5d | **WITHIN-RUN-WATCH.** breadth **0.00 of 28** — not one name clears the 3-axis gate. ⚠ **And its testing row `S91` VOIDed today**, so the leg is now un-bracketed; logged in ROTATION §3b as the board's largest un-tested contradiction |
| **COMM (N)** — the 9 non-Alphabet names | ex-Alphabet the bucket read **+0.217** on 08-21; `GOOGL` has left the flipper set | none | **WITHIN-RUN-WATCH.** `D297` unfixed ⇒ the complex is 76.6% of the bucket and the guard reports half. **A promotion here would rest on an instrument that cannot see its own concentration** |
| **Tanker / freight** — `FRO`, `STNG`, `INSW` | Panama Canal transit cuts (11 outlets, 08-21) **+** Hormuz near-zero traffic **+** Red Sea re-routing. `FRO` prints **08-28** | **`FRO` 08-28 (D-6)** | **WITHIN-RUN-WATCH — structurally.** 🚨 **Not one of these names is in `us_top300`**, so the leg has **no flow reading on this desk at all**. `S109` already brackets `FRO`. Registered as dig `D314` (EVENT_ALPHA §4) — the fix is the universe file, not a verdict |

---

## §3 · LENS 2 — REGIME-FLIP / BOTH-SIDES (the against-us branch for every known binary)

> **Rule: a one-way tilt into a known binary is a protocol violation.** Every row below carries the
> branch that hurts us, with a starter list, a trigger and an invalidation.

### 3-a · Jackson Hole 08-27→29 + July PCE 08-28 — **one window, and it is bracketed as one**

**Against us.** Our tilt is **long duration-insensitive cyclicals (ENRG OW, HLTH OW) and short the
duration complex (UTIL UW, RE UW, STPL N)**. **If Warsh delivers the "inflation credibility"
reassurance the market is asking for — and July PCE confirms the run-rate rather than the YoY — the
front end stays anchored, the long end retraces, and the three underweights rip together.** The wedge
that makes this live is measured and quoted in both halves (`C2`): **core CPI (`CPILFESL`, 2026-07)
+0.215% MoM · +2.47% YoY · +1.64% 3-month annualized**, and headline (`CPIAUCSL`) **+0.074% MoM ·
+3.30% YoY · +0.49% 3-month annualized** (`MACRO §A-3`). **The YoY half and the run-rate half point
opposite ways, and that is the whole of the against-us case.** ★ **`P78`'s dispersion range is already 7.807, far past its own branch A** — the four
"duration" legs are not one leg, so a rip would not be uniform, and that is the second thing to watch.

- **What rips**: `XLU` (`eqflow` −0.621, **0🟢/13🔴 of 15** — the most compressed spring on the board),
  `XLRE`, `XLP`. Starter list, epicenter first: **`NEE` · `EXC` · `D` · `ETR` · `AEP`** (all 🔴 today,
  flow −0.62 to −0.87) and **`PLD` · `SPG` · `WELL`**.
- **Which of our OWs gets hit**: **ENRG** — a credibility rally weakens the dollar less and lets the
  war premium bleed; **MATR/`NEM`** — a dovish credibility read is *good* for gold, so `NEM` is the one
  position that hedges this branch rather than suffering it.
- **Trigger / invalidation**: **`S112`** below.

### 3-b · `NVDA` 08-26 — and the honest reason we cannot use the options market

**Against us.** IT is **UW while the desk holds four names in it** and the sector enters its own binary
from the **12th percentile** (`SMH` exc5 −3.293). **A beat that re-rates the AI-capex complex hits our
UW on the sector and does not help the four positions enough to compensate, because the positions are
the mega-caps that led the weakness.**

🚨 **`D295`, UNMET for a THIRD run — but this run measured WHY, and it is a tool defect, not neglect.**
`module_flow NVDA --positioning` returns **예상변동 ±1.6%, 만기 2026-08-24, D2.** **The event is
2026-08-26.** The straddle it selected **expires two days before the print**. Meanwhile `NEM` and
`MSTR` both returned **만기 2026-08-28** — so an 08-28 chain exists; `NVDA` simply has an 08-24 weekly
and the tool takes the **nearest** expiry rather than **the first expiry that covers the event.**
⇒ **Registered as `D315`** (§8). **Positive-form remedy: give `--positioning` an `--after DATE`
argument and select the first expiry ≥ that date.**

**Consequence, applied rather than noted:** `S103`'s hand-set ±5.0pp bands **are NOT re-frozen** (`D242`
— a threshold that moves after registration is a description wearing a forecast's clothes). Instead a
**companion bracket on the chain rather than the name** is registered with properly derived bands —
**`S113`** below — which is additive and leaves `S103` intact.

- **What rips on the against-us branch**: `SMH` complex — `NVDA`, `AVGO`, `MRVL`, `AMD`, `MU`.
- **Which of our OWs gets hit**: none directly; the cost is the **UW**, i.e. an opportunity cost, and
  `L3` grading says that is still worth bracketing because the UW is a live verdict.

### 3-c · Hormuz "open" statement — **undated, 20th consecutive run**

**Against us.** ENRG is our **#1 tilt by price** and a de-escalation statement is its cleanest killer.
`S8` remains `[blank]`-dated and unscoreable (`HANDOVER §2(f)`). **`S92` (settles 08-31) and `S95`
(fired A this morning) bracket what `S8` cannot**, and `P89` (registered in MACRO) carries the joint
physical test.
⚠ **The against-us branch is already partly measured and it is uncomfortable**: `theme-age Hormuz` is
**0.80× — decelerating** — on 141.9 articles/day, and WTI spec sits at the **36th %ile**. **Neither
narrative nor positioning is carrying the barrel; only the physical is.** ⇒ **no new bracket is
registered here** — `S92` + `P89` already cover both sides, and a third row would be duplication.

### 3-d · The correlated-UW check (the 2026-07-15 field note)

🚨 **RE + UTIL + STPL are one bet in disguise, and this run made it worse, not better.** All three are
UW/N on the same underlying claim — *the duration complex stays repressed*. **Today's verdict change
(RE N− → UW) increased the correlation of the tilt rather than diversifying it.**
⚠ **The counter-measurement, and it is why the tilt is not withdrawn**: `P78`'s dispersion range is
**7.807** — `XLV` +5.700 vs `XLU` −2.108 — i.e. **the four "duration" legs are demonstrably not one
leg.** So the *label* is correlated and the *behaviour* is not. **`S112` tests exactly this**, and it
is the reason its observable is the EW of the three rather than any one of them.

### 3-e · Binaries DROPPED, with the reason (rather than bracketed to look thorough)

- **`FRO` 08-28** — already owned by `S109` (settles 09-02). **A second row would be duplication.**
- **MSCI quarterly review 08-31** — `catalyst_calendar` labels it `[derived≈]`, the constituent
  changes are announced ~2 weeks ahead and **this desk has not read the announcement**. ⇒ **neither
  branch could change a conclusion** on evidence the desk possesses. **Dropped, and the reason is that
  we have not done the reading — not that the event is unimportant.**

---

## §4 · LENS 3 — MOMENTUM-CONTINUATION ("already ran" ≠ avoid)

**Mandate**: re-tag each runner **EXTENDED-BUT-LIVE** (cycle KPI still accelerating ⇒ dips are
addable) vs **EXHAUSTED**, each with its flip condition.
⚠ **Carried correction from 08-21**: this lens tagged `EW{DELL,HPE}` **EXTENDED-BUT-LIVE** and `S87`
fired **branch A (EXHAUSTED)** the next settle — a **20.4pp** mean reversion. **The lens was wrong, in
writing, and it is re-run today with that on the record.**

| Runner | Run | Re-tag | Flip condition |
|---|---|---|---|
| **`NEM`** (`S89` fired **B**) | exc5 **+13.104 = 97th %ile of 252**; `obv_norm` **+0.487**, the board's highest | 🚨 **EXHAUSTED-GEOMETRY, LIVE-TAPE — and the two halves must be reported together** | **`M779`: 246.4% of rs60 is the last 20 sessions**, days 21–60 negative ⇒ no base. **P/C 0.17, skew +0.1, implied ±5.2% (D6)** ⇒ nobody hedged. **BUT** gold spec is only **48th %ile** ⇒ not a futures crowd. **Flip to EXHAUSTED outright if `GLD` 5d turns negative while `NEM` exc5 stays > p85 (+6.976)** — that divergence would say the name has decoupled from its own commodity |
| **`MSTR` · `COIN`** (both 🟢, flow +1.000) | `MSTR` rs20 +26.5 with **rs60 −24.7**; `COIN` rs20 +14.2 with **rs60 +5.3** | **EXTENDED-BUT-LIVE for `COIN`, EXHAUSTED-BASE for `MSTR`** — and the split is the finding | **They are ONE risk unit in two GICS labels** (the 08-21 finding, reproduced). `MSTR` short **8.9% float covering, DTC 1.8, implied ±9.0% (D6)** ⇒ squeeze fuel, **not a buy signal on its own (`D6`)**. **Flip `COIN` to EXHAUSTED if its rs60 turns negative** — that is the only thing separating the two today |
| **`TGT`** (🟢, +0.950) | exc5 **+8.463**, rs60 +26.9 | **EXTENDED-BUT-LIVE** | Its Q2 beat contained **$1.65 of $4.11 EPS = 40.1% tariff refund** (`P84`, carried). **Flip to EXHAUSTED on the next print if the refund line is absent** — the run has a one-off inside it |
| **`MPC` · `PSX` · `VLO`** | rs60 **+44.0 / +37.0 / +43.1** | **EXTENDED-BUT-LIVE** | All three OBV-accumulating; `M779` measured the refiners as **broad-based** (21–35% of rs60 in days 21–60), the opposite of `NEM`'s shape. **Flip on `P89` branch B** (`BZ=F` ≤ 88.00 **or** distillate crack ≤ 90.0 at 08-28) |
| **`HPE`** *(held)* | `S87` fired **A — EXHAUSTED**, `EW{DELL,HPE}` **−8.076** | **EXHAUSTED — confirmed by settle, and the 08-21 tag is retracted** | Today: flow +0.489, rs60 **+41.6**, `vol_surge` 0.68. **The price leg is still high and the run leg is scored dead** — DEEP must not re-argue the run |
| **`RTX`** *(held)* | rs60 +16.8 | 🚨 **ROLLING OVER** | `obv_norm` **+0.023 — it has LEFT 매집** and delta **−0.304** is the book's largest for a second run. `M779` had already put it at **−10.6%** decomposition. **`S97` FIRED-A on `LHX` ⇒ `M704`'s defense EW must be re-run ex-`LHX` before any defense claim** — handed to INDU's DEEP as its #1 question |

★ **Lens-3 verdict against the draft tilt**: the tilt's two strongest runners sit on **opposite**
geometries — the refiners are broad-based and `NEM` is not — and **the desk's own risk-unit tool cannot
separate them at a stable window** (`G4`: 250d and 500d both print 11 units with different membership).

---

## §5 · LENS 4 — CYCLE-EXPOSURE (registry vs coverage vs the REAL book)

`cycle_exposure.py`, run this stage: **AI-compute rank 1 — epicenter 16.52% vs a 12.0% floor ✅**
(`NVDA`, `ANET`) · **Energy/refining rank 2 — 9.84% vs 8.0% ✅** (`MPC`, `PSX`) · **Missile-defense
rank 3 — 3.79%, no floor set ⚪**. **Verdict: no top-rank cycle GAP.**

**But the lens exists to attack that ✅, and there are three ways in:**

1. 🚨 **An unset threshold is not a pass.** Rank 3 (missile-defense) prints `⚪ n/a (기준 미설정)`, and
   **the one held name there is `RTX`, which lens 3 just tagged ROLLING OVER** and whose sector took a
   DEEP slot today because `S97` fired A. **A cycle whose floor was never set cannot fail** — that is a
   silent pass, and it is named here rather than counted as a ✅.
2. 🚨 **The registry cannot see the cycle with the loudest price-vs-fundamental split.**
   `cycle_registry.json` has **no optical/interconnect row** for a **10th** run (`D250`/`M731`), while
   `S86` **and** `S96` both settled **branch B** this morning and `Fabrinet` guided *"AI optical demand
   fueling years of growth"* (5 outlets, 08-21). ⇒ **Exposure there is unmeasurable, not zero.**
3. ★★ **Epicenter vs one-layer-off, and this is the run's sharpest exposure finding.** The AI-compute
   ✅ is carried by `NVDA` and `ANET` — the **compute** layer. The week's largest dated commitment was
   in the **power** layer: **`NVDA` guaranteeing up to $105bn of OpenAI's Ohio leases for an 8-GW
   campus**, plus talks to invest in a data-centre **power** developer (`EVENT_ALPHA` CARD 2). **Every
   power-layer name is 🔴 or deeply negative** — `GEV` −0.741, `CEG` −0.625, `VRT` −0.539 — while the
   book's one electrical name, `ETN`, is measured by `M778` to load on **`XLI` (β +1.402, t +6.12)**,
   **not** on this chain. ⇒ **The book's AI-power exposure is a label, not a measured position**, and
   the registry's ✅ does not test the layer where the money was committed.
   ⚠ **Rule respected**: a 🔴 tape gates ADD *timing*; it does not justify zero core. **The finding is
   that the desk cannot currently tell whether it has core exposure to the power layer at all** — which
   is a measurement gap, not a sizing conclusion (P4).

---

## §6 · Brackets registered this run — **both-sided, `D93`-banded, information-graded**

> ⚠ **ID 3-grep at WRITE time** across `handoff/*.md`, `llm_outputs/`, `REPORT/`: `S112` `S113` `S114`
> returned **0 files**. Highest existing **`S111`** (US) / **`S67-KR`** (KR) — numbers taken beyond
> both series (the `D76` collision class). All observables and thresholds **frozen at registration**;
> bands are 252-obs distributions **of the observable itself**, with the state's own percentile
> disclosed. Benchmark **`SPY`** named inline. **SETTLEMENT MODE = TERMINAL** on all branches (`D242`).

### `S112` — ★★★ The MANDATORY Jackson Hole bracket, and it owns the PCE print inside its own window

| | |
|---|---|
| **Why it exists** | The window's biggest binary is **invisible to `catalyst_calendar`** (`D18`), and the desk's tilt is **short the duration complex in three buckets at once** (§3-d). A one-way tilt into a known binary is a protocol violation |
| **Frozen observable** | `EW{XLU, XLRE, XLP}` **3-session excess return vs `SPY`**, settled closes, window **2026-08-26 close → 2026-08-31 close** |
| **Branch A (AGAINST US)** | **≥ +1.653pp** (252-obs **p85**) — the three underweights rip **together**; the correlated-UW pattern is real and it cost us |
| **Branch B (with us)** | **≤ −1.757pp** (252-obs **p15**) — the credibility story does not land and the repression holds |
| **Branch C** | between — the disclosed favourite |
| **`D93` before freezing** | trailing 252: mean **−0.156** · sd **1.754** · p05 −2.893 · **p15 −1.757** · p50 −0.168 · **p85 +1.653** · p95 +2.719 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **−0.225 = the 49th percentile — dead centre.** ★ **Both branches are equally reachable**, which is the most informative starting state this desk has registered in weeks (compare `S88`, which started at the 100th percentile and could only produce its favourite) |
| **⚠ Window honesty** | **July PCE (08-28) falls INSIDE this window and is NOT treated as an anti-signal.** Making it one would be a designed-to-void clause — the `D300-KR` defect that VOIDed `S91` and `S93` today. **Jackson Hole and PCE are one macro event-cluster for the duration complex, and the bracket owns both. Stated at registration, not discovered at scoring** |
| **Anti-signal (VOID)** | an **intermeeting FOMC action** or an **emergency Treasury operation** (not a scheduled buyback) inside 08-26 → 08-31. ⚠ **Base rate checked**: no FOMC meeting falls inside; the buyback expansion already executed on **08-19**; a *discretionary emergency* operation inside four sessions is not the base case ⇒ **the clause is not near-certain to fire** |
| **Implied move** | **not pulled** — these are sector ETFs and `--positioning` was run only on single names. **The gap is stated rather than glossed**; the threshold is distribution-based |
| **Information (L3)** | **HIGH and symmetric.** A falsifies three sector verdicts at once; B confirms them. **Warsh's speech is deliberately NOT an anti-signal — it is the event** |
| **Owner** | `industry_US` |

### `S113` — ★★ The semis chain into `NVDA` 08-26, registered because the straddle path is broken

| | |
|---|---|
| **Why it exists** | `S103` brackets `NVDA` **the name** with **hand-set ±5.0pp bands**, and `D295` obliged a straddle re-derivation for a third run. **This run measured why that is impossible**: `--positioning` returns the **08-24, D2** expiry for an **08-26** event (`D315`). ⇒ **`S103` is left frozen (`D242`)** and this row is registered on **the chain** with derived bands, which is additive rather than a re-freeze |
| **Frozen observable** | **`SMH` 5-session excess return vs `SPY`**, settled closes, window **2026-08-25 close → 2026-09-01 close** (the 08-26 print sits inside it) |
| **Branch A (AGAINST our IT UW)** | **≥ +4.461pp** (252-obs **p85**) — the complex re-rates on the print and the UW was the wrong verdict at the 12th percentile |
| **Branch B (with us)** | **≤ −2.627pp** (252-obs **p15**) — the de-rate continues through the catalyst |
| **Branch C** | between |
| **`D93` before freezing** | trailing 252: mean **+1.063** · sd **3.740** · p05 −5.413 · **p15 −2.627** · p50 +1.573 · **p85 +4.461** · p95 +7.172 |
| **State at registration** | **−3.293 = the 12th percentile** — *already below p15*. ⚠ **Branch B is therefore only 0.67pp away and is declared LOW-INFORMATION at registration**; **branch A carries the row** and needs **+7.75pp**, a genuine reversal |
| **Implied move** | 🚫 **UNUSABLE and the reason is recorded**: `NVDA` **±1.6%, expiry 2026-08-24, D2 — before the event.** No event-covering straddle was obtainable through this tool (`D315`). **The threshold is distribution-based and is labelled as such** |
| **Anti-signal (VOID)** | a **US export-control announcement on advanced semiconductors** inside the window ⇒ a policy read, not an earnings read. ⚠ **Base rate**: moderate but not near-certain; keyed to a **specific dated action**, not to "an event occurring" |
| **Information (L3)** | **Asymmetric and disclosed**: A can falsify the IT UW; B can only confirm what the state already shows |
| **Owner** | `industry_US` |

### `S114` — ★★ The steel/tariff bracket, registered because it points at a HELD name

| | |
|---|---|
| **Why it exists** | `EVENT_ALPHA` CARD 4: the US–Canada tariff thread (peak **23 outlets 08-19**) resolved toward a **CUT on metals and autos**, and **`NUE` is held**. The desk should not carry a position whose live thread runs against it without a dated test |
| **Frozen observable** | `EW{NUE, STLD}` **5-session excess return vs `SPY`**, settled closes, window **2026-08-26 close → 2026-09-03 close** |
| **Branch A (the tariff bid is being removed)** | **≤ −6.219pp** (252-obs **p05**) — the pair stays at its extreme and the domestic-steel thesis needs re-argument |
| **Branch B (already discounted)** | **≥ +0.826pp** (252-obs **p50**) — full mean reversion; the market had priced it and the card was wrong |
| **Branch C** | between |
| **`D93` before freezing** | trailing 252: mean **+0.936** · sd **4.376** · **p05 −6.219** · p15 −2.978 · **p50 +0.826** · p85 +4.906 · p95 +8.488 |
| **State at registration** | **−8.628 = the 3rd percentile of 252** — already **below p05**. ⚠ **Branch A is declared LOW-INFORMATION at registration** (it merely requires the extreme to persist); **branch B carries the row**, needing **+9.45pp** |
| **Implied move** | **`NUE` ±8.1%, expiry 2026-09-18, D27**, `P/C` 0.79, **IV skew +36.1 — the highest downside-fear skew this stage has pulled.** Branch B needs ~**9.45pp on the pair ≈ 1.2× `NUE`'s implied move** ⇒ **marginally outside ⇒ WEAK-INFORMATION, labelled now** rather than presented as a trigger |
| **Anti-signal (VOID)** | a **Section 232 steel action or a new steel-specific tariff** inside the window ⇒ the opposite policy, not a discount test. ⚠ **Base rate**: low — the week's measured direction is de-escalation across food, metals and autos |
| **Information (L3)** | **B carries it.** ★ Registered anyway because A-persisting is exactly the outcome that convicts a live position, and this desk's rule is that a bracket which can convict us is worth the slot |
| **Owner** | `industry_US` |

### Registered and then DROPPED, with the reason
- **Hormuz de-escalation** — `S92` (08-31) and `P89` (08-28) already carry both sides. **Duplication.**
- **MSCI review 08-31** — constituent changes are announced ~2 weeks ahead and **this desk has not
  read the announcement**; neither branch could change a conclusion on evidence we hold. **Dropped for
  our own unreadiness, stated as such.**
- **`FRO` 08-28** — owned by `S109`. **Duplication.**

---

## §7 · The DEEP set after the pre-mortem

**Lens 1 promoted one leg.** `IT / semis` enters as a **5th DEEP slot**: the desk carries a **UW** into
the board's largest binary (`NVDA` 08-26), from the **12th percentile**, holding **four names** in the
sector, with `eqflow` materially better than price, `Nasdaq-100` spec at the **4th %ile**, and a
newly-registered bracket (`S113`) whose informative branch is the one that would convict the UW.

**Final DEEP set — N = 5:**
`ENRG` (continuous) · `HLTH` (continuous) · `FIN` (rotating) · `INDU` (rotating) ·
**`IT` (PREMORTEM-promoted 5th)**

**Hand-offs:** brackets `S112` · `S113` · `S114` → **ALPHA** (action brackets) · the three cycle-exposure
attacks in §5 → **BET** (the epicenter-starter question, explicitly as a *measurement gap*, not a size)
· the lens-3 re-tags → **DEEP** and **BET §B**.

---

## §8 · Dig registered by this stage

| ID | Dig | Evidence (measured this run) |
|---|---|---|
| **`D315`** | 🚨 **`module_flow --positioning` selects the NEAREST option expiry, not the first expiry that covers the event — so the desk's own straddle rule is unreachable exactly when it matters.** Measured: `NVDA --positioning` → **예상변동 ±1.6%, 만기 2026-08-24, D2**, for an **08-26** earnings print; the same call on `NEM` and `MSTR` returned **만기 2026-08-28**, so an 08-28 chain exists and `NVDA` simply has an 08-24 weekly. **This is the mechanism behind `D295` being unmet for three consecutive runs and behind `M47`/`M785`'s "all nine straddles expire D0".** **Positive-form remedy: add `--after DATE` to `--positioning` and select the first expiry ≥ that date; print the chosen expiry and its D±n on the same line as the implied move** |

## ✅ EXIT CHECK
- [x] **4 lenses run** (§0 declares they ran **in-context, not as a parallel agent fan-out** — a
      standing session constraint, and the loss of agent diversity is stated rather than hidden).
      Each returned **named tickers and dated catalysts**.
- [x] **Every bracket names its observable + frozen threshold + date and carries BOTH branches**
      (`S112` · `S113` · `S114`). Registration into `handoff/SCENARIOS_US.md` + the `SCENARIOS.md`
      MASTER INDEX happens in this run's handoff write-back.
- [x] **Every magnitude threshold stated against the implied move** — `S114` against `NUE` **±8.1%
      (D27)** and pre-declared **WEAK-INFORMATION**; `S113`'s straddle declared **UNUSABLE with the
      measured reason** (`D315`); `S112`'s absence of a straddle stated plainly.
- [x] **Each branch graded by information content before the event** — `S113` branch B and `S114`
      branch A are both pre-declared **LOW-INFORMATION**; `S112` is graded **HIGH and symmetric** on a
      49th-percentile state.
- [x] **Binaries where no branch would change a conclusion are DROPPED with the reason stated** —
      MSCI (our own unreadiness), `FRO` and Hormuz (duplication).
- [x] `BLINDSPOT_PREMORTEM.md` written: **legs (§2) · brackets (§3, §6) · momentum re-tags (§4) ·
      cycle-exposure attack (§5)**.
- [x] **Every catalyst-bearing leg promoted or logged** — 1 promoted (IT), 4 logged as
      WITHIN-RUN-WATCH, **none silently dropped**.
- [x] **DEEP set updated and stated: N = 5** — `ENRG` · `HLTH` · `FIN` · `INDU` · **`IT`**.
- [x] ★ **The lens-3 self-correction is on the record**: 08-21's `EXTENDED-BUT-LIVE` tag on
      `EW{DELL,HPE}` was **wrong** and `S87` fired A the next settle (−20.4pp reversion).
