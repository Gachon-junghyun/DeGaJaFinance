# BLINDSPOT_PREMORTEM — industry_US · 2026-08-21 (Fri) · Stage 7 / L1·PREMORTEM ★US-only

> Four adversarial lenses argue **AGAINST this run's own tilt** before the DEEP budget is committed.
> **No sizing, no buy/sell language (P4).** Settled bar **2026-08-20**.

## §0 · Execution mode — declared deviation

⚠ **The four lenses were executed IN-CONTEXT, not as a parallel adversarial agent fan-out.** The
stage specifies an Agent fan-out; **this session operates under a standing instruction not to invoke
the Agent tool**, so the fan-out could not be run. **Documented practice on this desk when that
happens is to run the lenses in-context and log it as the run's honest weakness** (2026-08-03 and
2026-08-04 both did exactly this and said so). **This is that log.**
★ **What the in-context mode costs, stated rather than glossed**: four independent contexts cannot
disagree with each other here, so the *diversity* of the fan-out is lost. **What is preserved is the
adversarial construction** — each lens below is built to attack a specific verdict this run made, and
**three of the four succeed.**

---

## LENS 1 · UNDER-COMPUTED LEGS — the strongest bull case for a leg we did NOT deep-dive

**DEEP set as ROTATION left it: continuous [ENRG, HLTH] · rotating [MATR, RE].**
**Un-covered with a dated catalyst ≤ ~5 trading days: INDU, IT, STPL, FIN.**

| Leg | Dated catalyst | The bull case, in its strongest form | Verdict |
|---|---|---|---|
| **INDU (defense sub-node)** | **`S97` and `S99` both settle on TONIGHT'S close (D-0)**; `LHX` succession 08-17 | **`S99`'s pre-settle already meets ALL THREE of branch A's conditions** — `M` = EW{`ANET`,`AVGO`,`HPE`} exc5 **−6.517**, `E` = `ETN` exc5 **−6.252**, same sign, both far outside their gates (3.0pp / 1.5pp). ⇒ **`ETN` trades as AI-compute, and the book's concentration reads 5-of-12, not 4-of-12.** ★ **And `CYCLE_EXPOSURE.json` already half-agrees**: it lists `ETN` as the AI-compute cycle's **adjacent** layer (`adj_usd` **$420.4**). If `S99` fires A, the *epicenter* number moves **19.8% → 23.57% of total assets = 32.8% → 39.0% of invested capital.** **That is a live sizing input on a book of $6,734.87 invested.** Plus the defense short-axis group turn (`NOC` **+2.43** · `LMT` **+1.76** · `LHX` **+1.35**, all against OBV 매집) and **`S97` already inside branch A at −5.557** | ★★★ **PROMOTE-TO-DEEP (5th slot)** |
| **IT / AI-compute** | `NVDA` **08-26 (D-5)** · `P79` settles **08-25** | `EW{AVGO,ANET,HPE,COHR,LITE}` exc5 has climbed **−12.425 → −7.178**, i.e. from the 0.8th to ≈the 5th percentile of two years, **0.54pp above `P79`'s branch-B line**. `Nasdaq-100` spec net is at the **0th percentile short** `[COT 08-18]` — the most extreme reading this desk has recorded — which is rebound ammunition. ⚠ **But `D6` puts COT contrarianism in the rejected ledger and it may not be cited as a signal**, and IT was deep-dived **08-20, one run ago** | **WITHIN-RUN-WATCH** — `P79` and `S85`/`S86` already own it; a DEEP slot would duplicate three live rows |
| **STPL** | **`S100` settles tonight**; both prints (`TGT` 08-19, `WMT` 08-20) are behind us | `TGT` carries the sector's best flow (**+0.889**), rs60 **+24.6**, OBV 매집 and `vol_surge` **1.40** — one of only four greens clearing the volume bar on its own. **The bull case is that the sector is not one object**: `TGT` +3.784% against `WMT` −9.861% over the same two sessions | **WITHIN-RUN-WATCH** — and the bull case is **materially weakened by a fact this run found**: **$1.65 of `TGT`'s $4.11 quarterly profit was a tariff refund (40.1%)**. `P84` carries it to 09-03 |
| **FIN** | none dated | **Least-recently deep-dived on the board (08-14, ~6 runs)**, and it holds three book names (`KKR` +0.651 · `NDAQ` −0.021 · `MET` −0.175). The bull case would have to rest on `COIN`'s new-🟢 — **and that is crypto beta, not a Financials signal** (`W5`, ROTATION §2c) | **WITHIN-RUN-WATCH.** 🚨 **The recency cost is now the board's largest and is logged as a cost, not a pass** |

⇒ **DEEP set after Lens 1: continuous [ENRG, HLTH] · rotating [MATR, RE] · PROMOTED [INDU] = N=5.**
**Nothing was silently dropped**; the three non-promoted legs are logged above with their reasons.

---

## LENS 2 · REGIME-FLIP / BOTH-SIDES — the against-us branch for every known binary

**Binaries in window** (`CATALYST_WATCH.json` **4 rows** + **1 hand-injected by MACRO**):

| Binary | Date | Bracketed? | Against-us branch |
|---|---|---|---|
| **Warsh at Jackson Hole** | **2026-08-21 — TODAY, D-0** | 🚨 **the calendar carried ZERO rows; MACRO hand-injected `P82`** (rate leg) | **`P82` covers the rate leg only. The EQUITY leg is unbracketed ⇒ `S108` registered below** |
| `NVDA` earnings | 2026-08-26, D-5 | `S79` · `S81` · `S103` | ⚠ **`S103`'s bands are still hand-set (`D295`, 2nd run).** The 08-21 chain has now rolled and the re-derivation is possible today — **ALPHA's obligation, named again** |
| July PCE | 2026-08-28, D-7 | `S104` (registered 08-20) | covered |
| **`FRO` earnings** | **2026-08-31, D-10** | 🚨 **NOTHING** — new on the calendar this run, and it sits on the **same Hormuz axis as four live rows** | **`S109` registered below** |
| Iran "Hormuz open" statement | **undated, 20th run** | `S74` · `S84` · `S92` · `S95` | A human must `VOID` or date `S8` (P5) |
| MSCI quarterly review | 2026-08-31 | none | **DROPPED with the reason**: neither branch would change a sector conclusion on this desk's board — a passive-flow date with no named constituent change in the corpus. **Not bracketed to look thorough** (`L3`) |

### The against-us construction, stated plainly

**If Warsh is DOVISH today** — the branch this desk is least positioned for. Per `S102`'s own
pre-registered regression (120 settled sessions, Brent-controlled), the four duration legs have
**≈ zero measurable rate beta** (`XLU` +0.0049 · `XLRE` −0.0072 · `XLP` +0.0224 · `XLV` +0.0185 pp/bp)
while **`XLE` is +0.1534 pp/bp**. ⇒ **a dovish surprise that rallies the long end helps the sectors the
desk is underweight three times AND removes the tailwind from the sector it is overweight** — the
single cleanest "everything we own is on the wrong side" configuration available this week.
⚠ **And the desk's own evidence says the market has already stopped listening to the hawkish read**:
the FOMC-minutes thread (*"'Many' Fed officials think higher rates will be needed"*) is **ENDED at a
peak of 17 outlets**, while *"Dollar Falls Sharply as Prospects Dim for Fed Rate Rise"* is FADING at a
peak of 8. **Attention rotated off the hawkish side before the speech.**

⚠ **Correlated-UW check (the 2026-07-15 field note), run explicitly.** `UTIL`, `RE` and `STPL` are
three UW/N− labels — **are they one bet?** Measured this run: the cross-sectional **range** of their
5-session excess (with `XLV`) is **3.172 = the 50.8th percentile** of its own 252, i.e. **mid-pack
dispersion, not a single factor** — and `P78` brackets exactly that to 08-26. **`S108` below tests the
three-leg version on a 3-session window around today's speech**, which is the shorter, event-specific
form `P78` does not cover.

---

## LENS 3 · MOMENTUM-CONTINUATION — "already ran" ≠ avoid. Re-tag every runner

**Estimator (`M149`): share of the 60-day excess vs `SPY` earned in the LAST 20 sessions.** A share
far above 100% with **negative** days 21–60 is the `R9`/`AXON` exhaustion geometry — the move is a
20-day event with nothing under it. Settled **08-20**, benchmark **`SPY`** named inline (`C1`).

| Name | exc60 | exc20 | days 21–60 | share in last 20 | **Re-tag** |
|---|---:|---:|---:|---:|---|
| **`NEM`** | **+12.76** | **+31.45** | **−18.68** | **+246.4%** | 🚨 **EXHAUSTED-GEOMETRY** |
| **`PLTR`** | +25.75 | +37.70 | **−11.95** | **+146.4%** | 🚨 **EXHAUSTED-GEOMETRY** |
| `MRVL` | +18.93 | +16.61 | +2.32 | +87.8% | **EXTENDED-BUT-LIVE (thin base)** |
| `ABNB` | +37.83 | +31.17 | +6.66 | +82.4% | **EXTENDED-BUT-LIVE (thin base)** |
| `TRI` | +24.87 | +20.25 | +4.62 | +81.4% | **EXTENDED-BUT-LIVE (thin base)** |
| `TGT` | +24.57 | +14.37 | +10.20 | +58.5% | **EXTENDED-BUT-LIVE** |
| `AMGN` | +27.51 | +13.44 | +14.07 | +48.9% | **EXTENDED-BUT-LIVE** |
| `MRK` | +22.85 | +10.88 | +11.97 | +47.6% | **EXTENDED-BUT-LIVE** |
| **`PSX`** | +36.43 | +12.65 | **+23.78** | +34.7% | ✅ **EXTENDED-BUT-LIVE (broad base)** |
| **`MPC`** | **+42.82** | +11.41 | **+31.41** | +26.6% | ✅ **EXTENDED-BUT-LIVE (broad base)** |
| **`VLO`** | +39.84 | +8.57 | **+31.27** | +21.5% | ✅ **EXTENDED-BUT-LIVE (broad base)** |
| `HPE` | +37.36 | +7.71 | +29.65 | +20.6% | ✅ **EXTENDED-BUT-LIVE** |
| `ANET` | +14.69 | +0.73 | +13.96 | +5.0% | **STALLED — the 20-day leg is gone, the 60-day stock remains** |
| **`DELL`** | +40.91 | **−4.35** | +45.26 | **−10.6%** | 🚨 **ROLLING OVER** — the last 20 sessions SUBTRACTED |
| **`RTX`** | +17.02 | **−1.81** | +18.83 | **−10.6%** | 🚨 **ROLLING OVER** — same shape, and it is held |
| `MSTR` | **−31.33** | +16.73 | −48.05 | −53.4% | **REVERSAL OFF A LOSING BASE — not a runner** |

### ★★★ Three findings, and two of them attack this run's own decisions

1. **`NEM` is the exhaustion geometry, and `S89` settles TONIGHT already inside branch B ("the run is
   live").** **246.4% of its 60-day excess is in the last 20 sessions and days 21–60 are NEGATIVE.**
   `S89`'s pre-settle is **+10.160 against a +9.658 branch-B line.** ⇒ **the bracket is about to
   report "live" on a name whose 60-day base is negative**, and **MATR's DEEP mandate rests partly on
   `NEM` being the sector's non-steel driver.** **This is registered before the settle so it cannot be
   raised afterwards** (`§4c`/`D48`). ⚠ Its anti-signal (gold ±8% over the window) is at **+3.10%
   settled / +5.95% live** — **2.1pp from firing.**
2. 🚨 **`PLTR` was REVIVED on the rejection ledger by this run's own HANDOVER**, on the condition
   *"rs60 vs `SPY` turns positive"* — measured **+25.75** ✅. **The condition is met by a decaying
   stock**: 146.4% of that +25.75 is the last 20 sessions and days 21–60 are **−11.95**. **The
   revival stands** (the ledger scores the frozen condition, not a re-reading — `L3`), **but the
   condition itself is now known to be satisfiable by exhaustion geometry**, and that is a
   condition-writing defect this run created and is naming (§5, `D299`).
3. **`S87`'s 08-13 tag on {`DELL`,`HPE`} is refuted by BOTH instruments, and the pair does not agree.**
   `S87`'s pre-settle is **−8.895, already inside branch A (EXHAUSTED)**; and the decomposition says
   **`HPE` is live (+20.6% share, days 21–60 **+29.65**) while `DELL` is rolling over (−10.6%
   share)**. **An EW pair averaged two opposite states.** ⇒ **`S110` registered below.**

⚠ **`RTX` carries the same rolling-over shape as `DELL` and it is held in both books** — and its
carried thesis line calls it *"the defense group's beta, not its leader."* **A diversifier whose last
20 sessions subtracted is handed to the promoted INDU DEEP, not resolved here.**

---

## LENS 4 · CYCLE-EXPOSURE — the ✅ is real and it is measured on a registry that cannot see one cycle

`CYCLE_EXPOSURE.json`, real book **total $11,142.58 · invested $6,734.87 · cash ₩4,827,207**:

| Cycle | rank | epicenter % of total | floor | any-layer % | held | GAP |
|---|---:|---:|---:|---:|---|---|
| AI-compute / semiconductors | 1 | **19.8%** | 12.0% | **23.57%** | `AVGO` `NVDA` `ANET` (+`ETN` adjacent, $420.4) | ✅ |
| Energy / oil-refining | 2 | **9.78%** | 8.0% | 9.78% | `MPC` `PSX` | ✅ |
| Missile-defense / rearmament | 3 | 5.73% | **none set** | 5.73% | `RTX` | ⚪ |

**No top-rank cycle GAP.** Three things the ✅ does not say, in order of how much they matter:

1. 🚨 **`D250`/`M731`: the registry has NO row for the optical/interconnect cycle**, which the real
   book holds (`LITE`, `COHR`) and which `S86`/`S96` bracket. **The GAP check cannot fire on a cycle it
   cannot see**, so **23.57% "any-layer" is a floor, not a measurement.** ⚠ **And this run could not
   even read that cycle's narrative**: its only BUILDING thread is a **paywall stub** (`EVENT_ALPHA`
   §K). **A held cycle with no registry row and no readable thread is the definition of a blind spot.**
2. ★ **The rank-3 cycle has no floor set at all** (`min_epicenter_pct` = 0.0), so `RTX`'s 5.73% is
   reported as ⚪ rather than judged. **A floor of zero cannot fail** — it is a `D122` zero-information
   gate in the exposure instrument. **Setting it is a human item (P5)**; naming it is not.
3. ★★ **The ✅ on rank 1 is about to become the opposite problem.** If `S99` fires branch A tonight,
   **`ETN` is not adjacent, it is epicenter**: **19.8% → 23.57% of total = 32.8% → 39.0% of invested
   capital in one cycle.** The floor asks *"is there enough?"*; **nothing on this desk asks *"is there
   too much?"* except `MAX_THEME_PCT`, and `G4` has failed 12 consecutive runs on exactly this
   grouping** (250d splits `ANET`↔`ETN`; 500d/750d merge them, `--days` stated). ⇒ **the concentration
   guard cannot be quoted as a single number today, and the DEEP promoted in Lens 1 is where that gets
   resolved.**

**Cleanest epicenter expressions already held**: `AVGO` `NVDA` `ANET` (AI-compute) · `MPC` `PSX`
(refining) · `RTX` (defense). **No epicenter is at zero.** ⇒ **no cycle-GAP flag to hand to BET.**

---

## §5 · Brackets registered — 4, all both-sided, all with the observable and threshold frozen

★ **ID 3-grep at WRITE time** across `handoff/RESEARCH.md`, `handoff/STANDING_VIEW*.md`,
`handoff/SCENARIOS*.md`, `llm_outputs/` and `REPORT/`: **`S108` `S109` `S110` `S111` → 0 files.**
Highest existing **`S107`** (US, 08-20) / **`S64-KR`** (KR).

🚨 **Implied-move check — performed, and its result is that it CANNOT be used on these rows.**
`module_flow --positioning` was run (2026-08-21, intraday) on `HPE` **±2.7%** · `DELL` **±1.8%** ·
`FRO` **±3.1%** · `NEM` **±2.0%** — **and every one of those straddles expires 2026-08-21 = D0.**
They price today's session and **cover none of the events being bracketed** (`FRO`'s print is 08-31;
`S110` settles 09-03). ⇒ **taking a threshold from them would be fabricating one — the `M47` defect
by name.** **All four thresholds below are therefore distribution-based (`D93`), and the gap is
stated rather than glossed.** ★ The positioning pull was not wasted: it produced the load-bearing
input to `S109` (below).

### `S108` — ★★★ The MANDATORY D-0 equity bracket: if Warsh is dovish, the three underweights rip together · ARMED · → settle **2026-08-25**

**Why it exists.** Today's Jackson Hole speech is the window's only D-0 binary and
`catalyst_calendar` carried **zero** rows for it. `P82` brackets the **rate** leg. **Nothing brackets
the equity leg**, and the equity leg is where this desk's three underweights live.

| | |
|---|---|
| **Frozen observable** | `EW{XLU, XLRE, XLP}` **3-session excess return vs `SPY`** (benchmark named inline, `C1`), settled closes, `yfinance auto_adjust=False`, window **2026-08-20 close → 2026-08-25 close** |
| **Branch A (AGAINST US)** | **≥ +1.68pp** (the estimator's own 252-obs **p85**) ⇒ the three sectors this desk is UW/N− rip **together** on a dovish read; the correlated-UW pattern the 2026-07-15 field note warns about is real and is live |
| **Branch B (with us)** | **≤ −1.76pp** (252-obs **p15**) ⇒ a hawkish read, and the underweights are on the right side of it |
| **Branch C** | between = **the favourite, disclosed** |
| ★ **`D93` before freezing** | trailing 252 obs: **mean −0.148 · sd 1.759 · p15 −1.757 · p50 −0.118 · p85 +1.675 · p95 +2.719** ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **+1.432 = the 78.6th percentile** ⇒ **inside C but already leaning A**, and that is disclosed now rather than discovered at scoring |
| 🚨 **Anti-signal (VOID)** | a **CPI/PPI/PCE print**, an **intermeeting FOMC action**, or a **Treasury refunding/buyback-schedule change** inside 08-20 → 08-25. ⚠ **Base rate checked at registration** (`D300-KR`, the defect that made `S98` void almost-surely): **the next scheduled macro print is July PCE on 08-28 — OUTSIDE the window**; no FOMC meeting falls inside it; Treasury's buyback schedule is published in advance and no change is scheduled before 08-25. ⇒ **this clause is NOT near-certain to fire**, which is the whole point of writing it this way. ⚠ **Warsh's speech is deliberately NOT an anti-signal — it is the event** |
| **Information content (`L3`)** | **HIGH and it is the against-us branch that carries it.** A **falsifies three sector calls at once** and revives the correlated-UW hypothesis; B confirms. Registered because A is precisely the outcome this desk is least positioned for |
| **Owner** | `industry_US` |

### `S109` — ★★★ `FRO` 2026-08-31: the Hormuz axis's one un-bracketed dated binary, on a crowded short · ARMED · → settle **2026-09-02**

**Why it exists.** `FRO` (Frontline, crude tankers) appeared on `catalyst_calendar` for the first time
this run, at **D-10**, and **no row on the board is keyed to it** — while `S74`/`S84`/`S92`/`S95` all
bracket the *same* Hormuz axis through refiners and ETFs. **A tanker is the axis's freight leg, and it
is the leg `M45` measured the desk had never owned.**

| | |
|---|---|
| **Frozen observable** | `FRO` **5-session excess return vs `SPY`**, settled closes, window **2026-08-26 close → 2026-09-02 close** (the print is 08-31; the window brackets it) |
| **Branch A (escalation is paid to the freight leg)** | **≥ +8.04pp** (252-obs **p85**) ⇒ the Red Sea re-routing and Bab el-Mandeb avoidance (`EVENT_ALPHA` §2, primary-sourced) show up in ton-mile earnings, and the desk's Energy exposure has been in the wrong leg of its own thesis |
| **Branch B (the freight leg is not paid)** | **≤ −4.62pp** (252-obs **p15**) ⇒ `M45` replicates a second time: the blockade's literal beneficiaries are still not bought, and the axis's only tradeable expression remains refining margin |
| **Branch C** | between |
| ★ **`D93` before freezing** | trailing 252 obs of `FRO` exc5: **mean +1.483 · sd 6.523 · p05 −9.299 · p15 −4.620 · p50 +1.649 · p85 +8.043 · p95 +11.390** ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **+9.467 = the 89.3rd percentile**; close **43.56**, 20-day return **+12.12%**. ⚠ **Disclosed: the state is ALREADY between p85 and p95**, so **branch A is the weaker-information branch and branch B is the informative one** — a reversal from a 89th-percentile state is the outcome that would change a conclusion |
| ★ **Positioning, and it is the reason this row is worth a bracket** | `module_flow --positioning` (2026-08-21): **short interest 6.1% of float and BUILDING, DTC 4.1**, put/call **0.56**, IV skew **+18.5**, tag 🟢가속 with OBV 매집 and news velocity **1.62×** (**RULE D6 exemption: the C-grade OBV reading carries nothing here — the row's positioning evidence is the B-grade short-interest/DTC/skew triple, and OBV is listed beside it as corroboration, not as the claim**). ⚠ **Crowded-short is turn-conditional squeeze fuel and NEVER a buy signal alone (`D6`)** — it is cited here as the reason both branches are reachable, not as a direction |
| 🚨 **Anti-signal (VOID)** | an **`FRO`-specific corporate action** (M&A, equity raise, special dividend, fleet sale) or a **general tanker-rate collapse driven by an OPEC+ supply decision** inside the window ⇒ the move is not the Hormuz axis |
| **Information content (`L3`)** | **B is the informative branch and it is stated as such.** ⚠ `FRO` is **not in `us_top300`** — the desk can price it but cannot flow-tag it through the sweep; **this row is a measurement, not a candidate hand-off** |
| **Owner** | `industry_US` |

### `S110` — ★★ `S87` averaged two opposite states; this is the pair test it could not perform · ARMED · → settle **2026-09-03**

**Why it exists.** `S87` (EW{`DELL`,`HPE`} exc5, settles tonight) reads **−8.895 = already inside
branch A (EXHAUSTED)**. **Lens 3 says the pair does not agree**: `HPE` earned **+29.65** in days 21–60
and **+7.71** in the last 20 (**20.6% share = live**), while `DELL` earned **+45.26** in days 21–60 and
**−4.35** in the last 20 (**−10.6% share = rolling over**). **An EW basket cannot report that**, and
`HPE` is held while `DELL` is not.

| | |
|---|---|
| **Frozen observable** | [`HPE` 5-session excess vs `SPY`] **−** [`DELL` 5-session excess vs `SPY`], settled closes, at the **2026-09-03** close |
| **Branch A (the held name is the better half)** | **≥ +6.02pp** (252-obs **p85**) ⇒ the pair genuinely diverges and `S87`'s EXHAUSTED verdict was an averaging artifact on the leg the desk owns |
| **Branch B (the held name is the worse half)** | **≤ −6.14pp** (252-obs **p15**) ⇒ `HPE` is the drag and the exhaustion verdict lands on the held name |
| **Branch C** | between |
| ★ **`D93` before freezing** | trailing 252 obs: **mean −0.698 · sd 7.109 · p15 −6.141 · p50 −0.665 · p85 +6.022** ⇒ **A ≈15% · B ≈15% · C ≈70%.** ⚠ **sd 7.109 is wide** — this is a genuinely noisy pair spread and C is the honest favourite |
| **State at registration** | **+0.494 = the 55.2nd percentile** ⇒ **mid-pack; both branches equally reachable**, which is the cleanest starting state of the four rows registered today |
| 🚨 **Anti-signal (VOID)** | an earnings print at **either** name inside the window. ⚠ **Reachability-checked**: `module_fundamentals_us` puts `HPE`'s next print at **2026-09-04**, i.e. **one day after the settle** — outside by one session, and **that margin is stated because it is thin** |
| **Information content (`L3`)** | **A and B both change a conclusion**; C is uninformative and likely. The row exists because `S87` **cannot** distinguish them, not because a new direction is claimed |
| **Owner** | `industry_US` |

### `S111` — ★★★ `P67`'s credit observable is pointed at HIGH YIELD while the measured move is in IG TECH · ARMED · → settle at the first `[FRED]` close covering **2026-08-28**

**Why it exists.** `P67` tracks *"the AI capex bill is migrating to debt and credit does not see it"*
on **`HY OAS` ≥ 2.85%**. Measured today: **`HY OAS` 2.73%, 10bp off its 365-day low — no move.**
But `EVENT_ALPHA` §1's body-read gives the number that does move: **AI hyperscaler debt issuance
$220bn in 2026 vs $12.5bn in the comparable period last year (17.6×)** `[BNP Paribas via Reuters]`,
and **tech IG spreads at 89bp — 9bp WIDER than the overall investment-grade market**, inverting a
sector that historically traded through it. ⇒ **`P67` may be about to score "credit does not see it"
on an instrument that cannot see it.** This row puts the observable where the issuers are.

| | |
|---|---|
| **Frozen observable** | **`BAMLC0A0CM` (`ig_oas`)**, `[FRED]`, level at the first close covering **2026-08-28** |
| **Branch A (the channel is real and credit IS pricing it)** | **≥ 0.85%** ⇒ above the series' own 252-obs **p85 (0.83)**; the IG index widens with the tech-issuance wave, and `P67`'s conclusion is right while its instrument was wrong |
| **Branch B (it is idiosyncratic to tech, not a credit event)** | **≤ 0.77%** ⇒ below the 252-obs **p50 (0.79)**; the index tightens while tech stays wide ⇒ **the widening is a sector-relative-value story, not a credit-cycle story**, and the AI-debt card is downgraded to a rotation observation |
| **Branch C** | between = **the favourite, disclosed** |
| ★ **`D93` before freezing** | `ig_oas` 252-obs **level**: min 0.730 · p15 0.750 · p50 0.790 · p85 0.830 · max 0.940; **current 0.810 = the 68.7th percentile**. 5-obs **change**: mean −0.01bp · sd **2.76bp** · p10 −3.0 · p90 +3.0 ⇒ **A needs +4bp over ~7 observations (outside p90), B needs −4bp (outside p10)** ⇒ **A ≈12% · B ≈10% · C ≈78%** |
| **State at registration** | **`IG OAS` 0.81% (08-19)** · `HY OAS` 2.73% · `NFCI` −0.559 (08-14) · tech IG **89bp** `[news, Capital Group via Reuters]` |
| 🚨 **Anti-signal (VOID)** | a **credit event unrelated to technology issuance** (a large non-tech default, a bank failure, or a sovereign-rating action) inside the window ⇒ the index moves for a reason the row is not about. ⚠ **Base-rate checked**: the term `bankruptcy` returns **52** hits over two days — **the lowest count on this desk's bucket board** — and `NFCI` is at its loosest of the window. **The clause is far from near-certain** |
| **Information content (`L3`)** | **HIGH, and it is an instrument test rather than a direction bet.** A says `P67`'s conclusion survives on a better axis; **B says the desk has been watching the wrong credit series for six runs.** ⚠ **`P67` is NOT re-frozen and still settles on `HY OAS` at 08-28 as registered** — this row runs beside it, and **if the two disagree, the disagreement is the finding** (the `S14-ANNEX`/`S35-ANNEX` precedent) |
| **Owner** | `industry_US` |

### Registered and then DROPPED, with the reason
- **MSCI quarterly review (08-31)** — **neither branch would change a conclusion.** No constituent
  change is named anywhere in the window's 4,991-article corpus, and a passive-flow date with no named
  name cannot move a sector verdict on this board. **Not bracketed to look thorough** (`L3`).
- **A `PLTR` bracket** — considered, because Lens 3 shows this run revived it on a condition satisfied
  by exhaustion geometry. **Dropped**: `PLTR` is **not held**, has no DEEP slot, and a bracket on it
  would be measuring the desk's ledger hygiene rather than the market. **The defect is filed as a dig
  (§6) instead, which is where it belongs.**

---

## §6 · Hand-offs

**To DEEP** — final set **N = 5**: **continuous [ENRG, HLTH] · rotating [MATR, RE] · PROMOTED [INDU]**.
INDU's mandate: `S99`/`S97` settle tonight; **if `S99` fires A the book's AI-compute epicenter is
39.0% of invested capital, not 32.8%**, and `G4` has failed 12 runs on that grouping.

**To ALPHA (action brackets)**: `S108` (D-0, settles 08-25) · `S109` (08-31 print, settles 09-02) ·
`S110` (09-03) · `S111` (08-28). ⚠ **And the standing obligation, named for a second run: `D295` —
`S103`'s `NVDA` bands are hand-set and the 08-21 chain has now rolled, so the re-derivation is
possible today.**

**To BET**: **no cycle-GAP flag** — every rank≤2 cycle holds a core. ⚠ Carry the two caveats: the
optical cycle is **unmeasurable, not zero**, and the rank-3 floor is **unset, so it cannot fail**.

**To the D+1 scorer**, in priority order: ① **`S89`'s anti-signal** (gold ±8%: **+3.10% settled /
+5.95% live — 2.1pp from firing**) must be checked **before** the branch; ② `S95`'s anti-signal was
**probed and not fired**, not *"not fired"*; ③ `S99` branch A is pre-met on all three conditions;
④ `S87` is pre-set in branch A while its two legs disagree (`S110` succeeds it).

**New dig candidate (`D299`-class, from Lens 3):** **a ledger revival condition written on a
cumulative excess (`rs60 > 0`) can be satisfied by exhaustion geometry.** `PLTR` was revived this run
on rs60 **+25.75** of which **146.4% is the last 20 sessions and days 21–60 are −11.95**. **The
revival stands as scored** (the ledger settles the frozen condition), **but the condition class is
defective**: a cumulative-excess bar should carry a **decomposition floor** (e.g. days 21–60 also
positive), the same way `D93` requires a baseline on a threshold.

---

## ✅ EXIT CHECK — self-audit

- [ ] ⚠ **4 lenses fanned out in parallel** — **NOT MET, and declared in §0**: run in-context under a
      standing instruction against Agent invocation. **Documented practice (2026-08-03/04) when the
      fan-out cannot run.** All four lenses executed and each returned **named tickers + dated
      catalysts**.
- [x] **Every bracket names its observable + frozen threshold + date, and is registered in
      `handoff/SCENARIOS_US.md` with BOTH branches and indexed in the `SCENARIOS.md` spine.** Zero
      one-way brackets.
- [x] **Every magnitude threshold is stated against the implied move** — and the implied-move check's
      **result** is that all four available straddles **expire D0 and cover none of the bracketed
      events**, so the thresholds are distribution-based and **the gap is declared, not hidden**
      (`M47`).
- [x] **Each branch graded by information content**, including three rows where the *state at
      registration* makes one branch the weak one (`S108` leaning A · `S109` already p89 ⇒ B is the
      informative branch · `S111` C ≈78%). **One binary dropped with its reason** (MSCI).
- [x] `BLINDSPOT_PREMORTEM.md` written — legs · brackets · re-tags · cycle GAP.
- [x] **Every catalyst-bearing leg promoted or logged**: INDU **promoted**; IT · STPL · FIN logged as
      WITHIN-RUN-WATCH with their reasons. **Nothing silently dropped.**
- [x] **DEEP set updated and stated: N = 5** — [ENRG, HLTH] + [MATR, RE] + **[INDU]**.
