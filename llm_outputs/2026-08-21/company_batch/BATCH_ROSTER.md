# BATCH_ROSTER — company_batch · 2026-08-21 (금) · Stage 1/3 (L1·BATCH_SELECT)

> 명단 **5** · 걸러냄 **0** · 요청 5. **N 을 채우려고 부풀리지 않았고, 줄이지도 않았다.**
> 전원 KR ⇒ 자식은 `--market kr` 델타로 실행한다(6자리 KIS/DART · `.KS` chart/flow · 한글명 news).

## 명단 (source 우선순위 = batch_select 표의 6출처)

| # | 티커 | 이름 | source | 이 슬롯이 답하는 질문 (mandate 요약) | 이전 company 평결 |
|---|---|---|---|---|---|
| 1 | **000660** | SK하이닉스 | ②reject_ledger due (+⑥사용자) | **08-14 「파생상품거래손실발생」 금액 `unknown`·뉴스흔적 0** + 자사주 40조 소각 + 마진 이력최고인데 증가폭 2.7배 감속 + 수주 계약방벽 부재 | **없음** |
| 2 | **028050** | 삼성E&A | ①보유·리포트 없음 | **보유 중인데 테제 문서 0** (dig #1, 6런 이월). 오늘 기회비용 원장 `entered` 로 닫힘 | **없음** |
| 3 | **316140** | 우리금융지주 | ①보유·리포트 없음 | **보유 중인데 테제 행 2런 연속 없음** (dig #2). 08-20 벤치 +6.3% 날 **−5.8%** 를 설명하는 문서 부재 | **없음** |
| 4 | **011200** | HMM | ④BET_SHEET 🟢 + ⑤LIVE 숏리스트 | **커버리지 0건.** 카드1(호르무즈)·카드5(북극항로) 양쪽 등장 ⇒ **두 베팅인가 한 베팅인가**(구동 기준) | **없음** |
| 5 | **036460** | 한국가스공사 | ④BET_SHEET 🟢 | **커버리지 0건.** `per_fwd 3.0` 이 **의심(unverified)** — EPS 8.4배 점프를 마진 7.2%가 설명 못 함 | **없음** |

전체 mandate 원문은 `roster.json` 의 `mandate` 필드에 있다 — 자식 브리프에는 **요약이 아니라 원문**이 들어간다.

## ★ 걸러낸 이름: 0건 — 그리고 이것은 조용한 0이 아니다

신선도 게이트는 **4조건 AND** 다. 조건①(«`company_research` 평결이 ≤10 정산세션 이내로 존재»)이
**5종 전부에서 거짓**이라 나머지 3조건(관측점 도래 · 실적/8-K · 드라이버 밴드)은 평가할 필요조차 없다.

- `REPORT/` 에 존재하는 **company 리포트는 `035760`·`AVGO` 둘뿐**이다.
- `module_report_tags ticker` 개별 확인 — 000660 **8건** · 028050 **8건** · 316140 **7건** · 011200 **2건** · 036460 **3건**.
  ⚠ **전부 `industry_KR`/`industry_US` 탑다운 산출물이지 company 리포트가 아니다.** 커버리지 숫자가 큰 것과
  «이 회사를 판 적이 있다»는 다른 객체다 — 000660 은 8건에 등장하면서도 **자기 이름으로 된 평결이 0건**이다.

⇒ **5종 전부 명단 유지.** 걸러낼 것이 없어서가 아니라 **감사했더니 0** 이다.

## 원장 교차 (①~③ 출처의 근거)

- `reject_ledger due` — 도래/경과 **10건**, 그중 명단에 든 것은 **000660 1건**(2026-08-08 · `C.차트붕괴`).
  나머지 9건(T·PLTR·SLB·BKR·TRI·073240·047040·000720·CF)은 **이번 배치가 처리하지 않는다** — 이 배치는
  사용자 지정 5종이고, 그 9건은 다음 HANDOVER 의 `resolve` 몫으로 **여기 명시해 남긴다**(조용히 넘기지 않는다).
- `missed_ledger due` — 도래/경과 **5건**(FSLR·TMUS·APH·LYB·XLY), **전부 US 이고 이번 명단과 교집합 0**.
  단 **028050 은 오늘 `entered` 로 이미 닫힌 행**이라 `due` 목록엔 없고, 그 사실 자체가 이 슬롯의 mandate 다.
- 보유 확인 — BET_SHEET §5 의 집중도 가드가 **`--days 250` 에서 `U0 = 028050 + 316140`** 이라고 적었다.
  ⇒ 2·3번 행은 **책의 KR 보유 2종**이고, 서로 **같은 위험단위**다. 두 자식 브리프에 이 조건을 박아 보냈다.

## ✅ EXIT CHECK
- [x] `roster.json` 5행 — 요청과 동수, 패딩 0, 축소 0.
- [x] 모든 행에 `source`·`mandate`·`market`·이전 평결 나이(전부 `null`) 기재.
- [x] 스킵 목록 **개수와 함께** 기재 — 0건이고, 어느 조건이 성립해서가 아니라 **조건①이 깨져서** 0이라고 적었다.
- [x] ②출처 행(000660)은 저장된 부활조건을 **verbatim** 으로 mandate 에 옮겼다.
- [x] 중복 0 · 전원 KR 6자리(`.KS` 변환은 자식이 chart/flow 호출에서 수행).
