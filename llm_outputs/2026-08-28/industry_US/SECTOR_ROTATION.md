# SECTOR_ROTATION — industry_US · 2026-08-28 (Stage 6 / L1·ROTATION)

> **Delta-only.** MACRO §D owns the 11-sector verdict; `SECTOR_FLOW_US.json` owns the numbers.
> Both are on disk. This file writes what changes, why, and the DEEP picks.
> **Axis count this run scored on: `n_axes` = 3** (`§scoring`, `vel_coverage` 16.72%, `vel_axis` false)
> ⇒ 🚫 **no delta below cites theme freshness or news velocity**, and Δ is only read against snapshots
> of the same axis count (all of 08-20 → 08-28 are `n_axes` 3).
> ⚠ `asof 2026-08-28` is a **pre-market stub bar** — median volume **2.40%** of the prior session,
> 1 of 300 above 50% (`M1021`) ⇒ **breadth counts and `delta` magnitudes are used as cross-sectional
> ranks, never as levels.**

---

## §1 · Inherited — MACRO's matrix, verbatim, one line

`MACRO holds: IT OW− · HLTH N+ · ENRG N · FIN N · COMM N · MATR N · DISC N− · INDU UW− · STPL UW · RE UW · UTIL UW`

---

## §2 · Deltas — **2 verdict changes from 11 attempts**

### 🚨 The flip list first, reproduced in full even where it changes nothing

`top1_flips_sign` from **this run's own sweep** (`asof 2026-08-28`), **2 of 11**:
- **Information Technology** — top1 `NVDA` at **19.4%**; `wflow` **+0.035** vs `wflow_ex_top1`
  **−0.042** and `eqflow` **−0.029**. **The sign is one company's.**
- **Health Care** — top1 `LLY` at **19.4%**; `wflow` **−0.046** vs `wflow_ex_top1` **+0.023** and
  `eqflow` **+0.057**. **The sign is one company's, in the other direction.**

Nine are **not** flippers: `MATR`(LIN 24.7%) · `COMM`(GOOGL 38.3%) · `ENRG`(XOM 30.5%) ·
`FIN`(BRK-B 13.9%) · `STPL`(WMT 28.9%) · `INDU`(CAT 8.7%) · `DISC`(AMZN 40.2%) · `RE`(WELL 16.9%) ·
`UTIL`(NEE 17.7%).

⚠ **The set changed AGAIN, mid-run, for a 3rd consecutive run.** PREFLIGHT (22:1x KST, 08-27
baseline) attached the restriction to **HLTH + ENRG**; this run's own SWEEP (22:5x KST) measures
**HLTH + IT**. **`ENRG` is not a flipper today and `IT` now is.** Both lists are honoured
conservatively (SWEEP §0): no promotion or demotion below rests on the `wflow` of HLTH, ENRG **or**
IT. ★ **And the fresher measurement is load-bearing** — the new flipper is the sector MACRO put the
wind behind, which is the whole reason this check exists.

### Delta 1 — ★★★ `IT` **OW− → N** · the wind was one company's, and every equal-weighted instrument refuses it

| sector | matrix said | flow evidence | new verdict | who resolves |
|---|---|---|---|---|
| **Information Technology** | **OW−** | 🚨1名 (`NVDA` 19.4%) ⇒ `wflow` **+0.035 is not usable**. Substituting the non-cap-weighted numbers: **`eqflow` −0.029 (negative)** · **`wflow_ex_top1` −0.042 (negative)** · **breadth 0.04 = 2 🟢 of 56** · 10 🔴, red-rate **17.9%** | **N** | PREMORTEM (see §3) |

**Why this qualifies as a flow delta and not a macro re-argument** — the test the stage demands:
MACRO set `OW−` on `XLK` excess vs `SPY` (**+2.50 / +1.89 / +3.36** on exc1/5/20). **`XLK` is
cap-weighted.** So is `wflow`. **Both of the instruments that say OW are cap-weighted, and both carry
the same `NVDA`.** The two instruments that are *not* cap-weighted — `eqflow` and `breadth` — are
**negative and near-zero respectively**. ⇒ the demotion is carried entirely by flow numbers, and by
the two that the G3 rule specifically permits.
★ **Corroborated from the opposite side by MACRO's own §A-4**: `XLK` is **1st on exc1/5/20 and dead
last of 11 on exc60 (−6.36 vs `SPY`)**, and `RSP` is negative on exc1/5/20. **Three independent
equal-weighted reads, one conclusion.**
⚠ **Not demoted further than N.** `eqflow` −0.029 is the **3rd-best of 11** in a board where every
sector is negative, and its red-rate (17.9%) is 4th-best. **The sector is not weak; it is narrow.**
`UW` would be a claim the numbers do not support, and the notch is stated rather than taken.

### Delta 2 — ★★ `MATR` **N → N+** · the board's only sector positive on both axes, and it is not a flipper

| sector | matrix said | flow evidence | new verdict | who resolves |
|---|---|---|---|---|
| **Materials** | **N** ("no proposition owns this and that is stated") | **`eqflow` +0.087 = rank 1 of 11** · `wflow` **+0.146 = rank 1** · `wflow_ex_top1` **+0.158** (*rises* without `LIN`) · **`top1_flips_sign` FALSE** ⇒ no substitution needed · **0 🔴 of 12, red-rate 0.0% = the only sector on the board with zero reds** | **N+** | DEEP (continuous slot, §3) |

★ **This is the one sector where the cap-weighted and equal-weighted instruments agree AND removing
the top name makes it better.** `LIN` is 24.7% of the bucket and `wflow_ex_top1` **+0.158 > wflow
+0.146** ⇒ the sector's strength is **not** its largest name — the opposite of Delta 1.
⚠ **Breadth is 0.00 (zero 🟢 of 12) and that is diagnosed, not cited as weakness**: the 🟢 gate reads
`vol_surge`, computed on a **2.40%-volume bar**; `FCX` is blocked from 🟢 by **`vol_surge` 0.97 alone**
while carrying flow **+0.650**, OBV 매집 and `rs20 +21.0` (`M1023`). **A breadth of 0.00 on this bar
is an artifact of the hour, not a measurement of participation** — which is exactly why the promotion
rests on `eqflow` and red-rate instead.
⚠ **Copper spec positioning at the 100th percentile for a 4th run is attached as `D6` context and is
NOT part of the promotion** — a crowded long is not a direction.
⚠ **One notch only.** `MATR` was demoted to `N` on 08-25 and this is a re-promotion 3 runs later;
it is held to `N+` rather than `OW` because the sector's entire 20-day price move is two names
(`FCX`, `NEM`) — the 08-26 run measured the other ten at an `exc20` **median −8.211**, and this run
did not re-measure that. **Not re-argued, not assumed away: stated as the unresolved half.**

### Attempts DECLINED — named, with the reason, so "we looked and passed" stays visible

| # | attempted | why declined |
|---|---|---|
| 3 | `HLTH` N+ → OW | ✅ **The evidence supports it** (`eqflow` +0.057 rank 2 · **breadth 0.06 = the board's only non-zero** · 2 🟢 of 32 · red-rate **9.4% = best of 11**). **Declined anyway**: MACRO's `N+` already encodes "positive", and a promotion here would be the *same direction* as the matrix, not a disagreement with it. **A delta is a disagreement, not an amplification.** |
| 4 | `ENRG` N → UW | `eqflow` **−0.093 rank 5**, red-rate 18.8% (5th-best), 0 🟢 — **mid-table, not bottom.** MACRO's cut from `OW` to `N` already absorbed the move; a second cut would be re-arguing `P107`, which is MACRO's row. **Left at N.** |
| 5 | `COMM` N → anything | 🚫 **Un-measurable BY RULE for a 10th run** (`D297`): `GOOGL` is **38.3%** of a 12-name bucket (the `GOOGL`+`GOOG` complex materially more), and `wflow` **−0.601** vs `eqflow` **−0.056** is a **0.545 gap** — the widest wflow/eqflow divergence on the board. `top1_flips_sign` prints **false**, so the flip test does not catch it. **Declined, not scored.** |
| 6 | `UTIL` UW → lower | **No notch exists below UW** in MACRO's matrix this run. `eqflow` **−0.546 rank 11**, red-rate **53.3% = worst of 11**, `XLU exc20` **−7.28 vs `SPY`**. Every instrument agrees ⇒ nothing to resolve **by ranking**. ⇒ routed to **DEEP R2** instead, because EVENT_ALPHA CARD 2 put a *contract-level* question into the sector that no ranking instrument can see. |
| 7 | `DISC` N− → UW | `eqflow` **−0.377 rank 9**, red-rate **50.0% = 2nd worst**. Supports a cut — but the 08-27 run **just demoted it to N− on these same numbers** and closed its 5-run contradiction (`M989`). **A second consecutive demotion on unchanged evidence is thrash, not a delta.** Declined. |
| 8 | `RE` UW → lower / `STPL` UW → lower | `eqflow` −0.384 (rank 10) and −0.282 (rank 7). Both already at UW under MACRO's `P110`; no notch, no disagreement. |
| 9 | `FIN` N → N+ | `eqflow` **−0.180 rank 6**, red-rate 19.1%, 0 🟢 of 47 — **the flow does not support the promotion the 08-27 run flagged as "the board's largest un-owned positive"** on price. ⚠ The price/flow gap is **still open** and is logged to DEEP_LOG rather than resolved by picking a side (`C5`). |
| 10 | `INDU` UW− → lower | `eqflow` **−0.311 rank 8**, **18 🔴 = the board's most in absolute terms**, red-rate 36.0% (3rd worst). Agrees with UW−; no notch below. Declined. |
| 11 | any promotion on `new_green` | Two `new_green` ignitions today — **`CRM`** (IT, flow +0.828 = 1st of 299) and **`A`** (HLTH, +0.778 = 2nd). ⚠ **Neither moves a sector verdict**: `CRM` sits inside the sector this run just DEMOTED, which is the point — **the ignition is the name, not the sector**. Handed to BET §B as EVENT_ALPHA CARD 6's candidate, not converted into an IT promotion. |

⚠ **Macro re-arguments attempted and reverted: zero.** Every declined attempt above was declined on a
flow number or on a stated rule, not on a macro thesis.

---

## §3 · DEEP picks — **N = 4** (protocol budget: 2 continuous + 2 rotating)

**Recency, read from the `DEEP_LOG` lines of 08-23 → 08-27:**

| sector | last DEEP | runs since | verdict after §2 |
|---|---|---|---|
| ENRG · HLTH · INDU · STPL | **08-27** | **0** | N · **N+** · UW− · UW |
| RE · FIN | 08-26 | 1 | UW · N |
| MATR · IT | 08-25 | 2 | **N+** · **N** |
| COMM | 08-24 | 3 | N (skipped by rule) |
| DISC · UTIL | 08-23 | **4** | N− · UW |

### ① Continuous ⌈4/2⌉ = 2 — today's top ranks

- **`HLTH`** — **keeps its slot on the anti-thrash continuity rule**: it held a continuous slot in the
  08-27 run and is still a top-rank verdict today (`N+`), with the board's **best red-rate (9.4%)**
  and its **only non-zero breadth (0.06)**. 9th consecutive run.
- **`MATR`** — **takes the slot `ENRG` vacated.** It is this run's promotion (§2 Delta 2), it is the
  board's **rank-1 on both `wflow` and `eqflow`**, it is **not a flipper**, and its recency is 2 runs.

🚨 **`ENRG` LOSES its continuous slot and the loss is stated, not silent.** It held one for 8
consecutive runs; MACRO cut it `OW → N` this morning, so it is no longer a top rank, and the rule
gives the slot to the sector that is. **Its mandate does not disappear — it moves to R1 below.**

### ② Rotating ⌊4/2⌋ = 2 — **a DECLARED recency-starved deviation, 15th consecutive run**

🚨 **There are ZERO `OW` sectors on the board after §2.** The rule's rotating input ("next-highest OW
not deep-dived in ~3 runs") has an **empty domain**. Per the established practice of the 08-26 and
08-27 runs, R1/R2 are filled **from non-OW by information density, with least-recently-covered as the
stated tiebreak.**

- **R1 = `ENRG`** — 🚨 **an explicit recency violation (0 runs since 08-27), taken deliberately.**
  **Why it overrides recency**: *both* of the desk's registered refining separators settled **against
  the separation claim on the 08-27 close, today** — **`P83` FIRED-B** (distillate−gasoline **−13.562**
  from the 08-20 settle of 51.131; branch B ≤ −4.0) and **`P96` FIRED-B** (crack 5-session rate
  **+4.874**; branch B ≥ +0.468). `P83` was itself the **replacement instrument** registered on 08-21
  after `R89` retracted the first separator. ⇒ **the desk now has NO instrument that separates
  refining margin from the barrel, on a HELD position, with `P100` settling 2026-09-01.** And the same
  name carries the book's **only FINRA short surge** (`MPC` z **+2.04**, 5v5 **+9.7▲**).
  **A slot spent anywhere else buys less.**
- **R2 = `UTIL`** — **4-run recency, the worst on the board (tied), and unlike 08-23 there is now
  something to resolve.** On 08-23 it was declined because *"every instrument agrees, nothing to
  resolve."* Today EVENT_ALPHA **CARD 2** puts a fact into the sector that no ranking instrument can
  see: **Georgia Power's 3.2-GW OpenAI contract was approved 08-27** (4 outlets, `prnewswire`:
  *"approximately $950 million in annual savings beginning in 2029"*), surfaced by `chain-hop` at
  **`SO` title 0 / body 13** — **while `SO` is 🔴분산 (flow −0.733, `rs20 −9.2`)** inside the sector
  that is **rank 11 on `eqflow` (−0.546) with a 53.3% red-rate.** ⇒ the DEEP question is
  *"is the worst-flow sector on the board signing the best contracts on it?"*

### Slots explicitly NOT given, with the reason

- **`IT` loses the R-slot and is ROUTED TO PREMORTEM instead** (2-run recency, and it would otherwise
  win on information). **Reason**: §2 Delta 1 already answered the sector-level question — the OW was
  one name — so a DEEP would re-derive this run's own delta. What is **not** answered is the
  **concentration** question, and that is PREMORTEM's object: **`S79` Leg 2 settled `U-MIXED`
  today** (`NVDA` +8.083pp, `AVGO` +3.830pp, `TSM` +1.645pp — **neither U-ONE nor U-SPLIT**), i.e.
  **the event test that was supposed to settle `G4` refused both groupings.** Plus `AVGO` prints
  **09-02** carrying **`S127`** with hand-set bands and an unmet **`D295`**.
- **`DISC` loses the 4-run tie to `UTIL`, explicitly.** Its two instruments were made to **agree** by
  the 08-27 run (`M989`), and agreement is the cheapest information on the board. Named, not dropped.
- **`COMM` skipped BY RULE for a 10th run** (`D297`).
- **`FIN` not given a slot** despite carrying the board's largest unresolved **price-vs-flow** gap —
  1-run recency loses to a 2-run and a 4-run candidate. **Logged with its mandate.**
- **No 5th slot promoted at this stage.** PREMORTEM may promote one.

---

## ✅ EXIT CHECK
- [x] **§1 is ONE line**, MACRO's matrix verbatim. No unchanged sector gets a row or a restated number.
- [x] **Every §2 delta cites a flow number** — Delta 1 on `eqflow` / `wflow_ex_top1` / breadth; Delta 2 on `eqflow` / `wflow` / `wflow_ex_top1` / red-rate. **Zero macro re-arguments attempted or carried.**
- [x] 🚨 **No promotion or demotion rests on a `top1_flips_sign` bucket.** IT is a flipper and its demotion is carried **only** by the non-cap-weighted numbers, with `wflow +0.035` explicitly declared unusable. HLTH is a flipper and was **declined**. **The flip list is reproduced in full (2 named with weights, 9 explicitly false)** even though it changed one verdict, and the **mid-run change from PREFLIGHT's list is stated.**
- [x] **The axis count is stated** (`n_axes` = 3, `vel_coverage` 16.72%, `vel_axis` false). **No delta cites theme freshness or news velocity.** Δ read only against same-axis-count snapshots.
- [x] Every matrix×flow divergence named with a resolution owner: IT→PREMORTEM · MATR→DEEP(continuous) · ENRG→DEEP(R1) · UTIL→DEEP(R2) · FIN→DEEP_LOG (unowned, stated) · COMM→declined by rule.
- [x] **4 DEEP targets picked by the rule**; continuity applied (`HLTH`) and stated; **recency-starved deviation DECLARED** and R1's recency violation stated with its justification; **no padding**; every uncovered sector named in DEEP_LOG.
- [x] Linter — see below.
- [x] `DEEP_LOG` line appended.

`python -X utf8 scripts/report_lint.py llm_outputs/2026-08-28/industry_US/SECTOR_ROTATION.md` → **0 findings** (C1 · C2 · S6 · D6). ⚠ Form only — a clean lint is not a correct report.

---

## DEEP_LOG 2026-08-28: continuous=[HLTH, MATR] rotating=[ENRG, UTIL] · N=4/4 · **TWO verdict deltas from ELEVEN attempts** — `IT OW− → N` and `MATR N → N+` · ★★★ **the IT demotion is the run's headline and it is an instrument finding as much as a market one: MACRO set `OW−` on `XLK` excess vs `SPY` (+2.50/+1.89/+3.36 on exc1/5/20) and `wflow` +0.035 agreed — but BOTH are cap-weighted and BOTH carry `NVDA`, which `top1_flips_sign` marks TRUE at 19.4%; the two non-cap-weighted instruments are NEGATIVE (`eqflow` −0.029, `wflow_ex_top1` −0.042) with breadth 0.04 = 2🟢 of 56, and MACRO's own `RSP` read agrees independently (negative on exc1/5/20, `XLK` LAST of 11 on exc60 at −6.36)** · ★ **`MATR` is the mirror image and that is why it was promoted: `wflow_ex_top1` +0.158 is HIGHER than `wflow` +0.146, `top1_flips_sign` FALSE, and it is the ONLY sector on the board with ZERO reds (0 of 12) — the strength is not its largest name** · 🚨 **flip list 2 of 11 (IT/`NVDA`/19.4% · HLTH/`LLY`/19.4%) and the set changed MID-RUN for a 3rd consecutive run — PREFLIGHT attached HLTH+ENRG from the 08-27 baseline at 22:1x KST, SWEEP measured HLTH+IT at 22:5x; ENRG LEFT the set and IT ENTERED it, and the new entrant is the sector MACRO put the wind behind** · 🚨 **`D355` 4th consecutive run: median sweep-bar volume 2.40% of the prior session, 1 of 300 above 50%, aggregate 5.12% ⇒ 4🟢/76🔴, all breadth figures and all `delta` magnitudes DECLINED as levels and used only as ranks; and the artifact is now shown to BITE — `MATR` breadth reads 0.00 while `FCX` (flow +0.650, OBV 매집, `rs20 +21.0`) is blocked from 🟢 by `vol_surge` 0.97 ALONE** · 🚨 **`M1020`: the news-axis cap (`R103`) reproduced a 5th time inside this run's own sweep — 50 velocity-covered names = `us_top300` ranks 1–50, contiguous, zero gaps; `vel_coverage` 16.72%, `n_axes` 3 for a 9th consecutive snapshot** · ★★ **the two settled rows that reshaped the DEEP order fired TODAY on the 08-27 close: `P83` FIRED-B (distillate−gasoline −13.562 from the 08-20 settle of 51.131) and `P96` FIRED-B (crack 5-session rate +4.874) — `P83` was the REPLACEMENT instrument registered 08-21 after `R89` retracted the first separator, so the successor has now failed too and the desk has NO instrument separating refining margin from the barrel, on a HELD position, with `P100` settling 09-01 and `MPC` carrying the book's only FINRA short surge (z +2.04, 5v5 +9.7▲)** · ★★ **`S79` Leg 2 settled `U-MIXED` (`NVDA` +8.083pp · `AVGO` +3.830pp · `TSM` +1.645pp, TSM missing the 2.0pp bar by 0.355pp) — neither U-ONE nor U-SPLIT, so the ONE event test designed to settle `G4`'s cross-window disagreement REFUSED BOTH GROUPINGS; the desk has no validated grouping for event risk and IT was routed to PREMORTEM on exactly this** · **`ENRG` LOST its 8-run continuous slot to `MATR` by the rule (MACRO cut it OW→N) and RE-ENTERED at R1 as a DECLARED recency violation (0 runs) justified by the two settled kills above** · **`UTIL` takes R2 at 4-run recency because EVENT_ALPHA CARD 2 gave it a question no ranking instrument can see: Georgia Power's 3.2-GW OpenAI contract APPROVED 08-27 (4 outlets, ~$950M/yr savings from 2029, `chain-hop` `SO` title 0 / body 13) while `SO` is 🔴분산 flow −0.733 `rs20 −9.2` inside the board's rank-11 `eqflow` (−0.546) and 53.3% red-rate — a STORY-ONLY card, filed to `missed_ledger`** · **`DISC` LOST the 4-run tie EXPLICITLY (its two instruments were made to agree on 08-27, the cheapest information on the board)** · **`IT` routed to PREMORTEM not to a slot** · **`COMM` skipped BY RULE 10th run (`D297`, `GOOGL` 38.3% of 12 with a 0.545 wflow/eqflow gap that `top1_flips_sign` does NOT catch)** · **NO 5th slot promoted at this stage** · **uncovered=[FIN(N, 08-26 = 1 run — the board's largest un-owned PRICE-vs-FLOW gap: `XLF exc5` +0.52 and `exc60` +10.96 (both 2nd of 11 vs `SPY`) against `eqflow` −0.180 rank 6 with 0🟢 of 47; the gap was NOT resolved by picking a side, `C5`), RE(UW, 08-26 = 1 run, `eqflow` −0.384 rank 10, no notch below UW), IT(N — DEMOTED TODAY, 08-25 = 2 runs, routed to PREMORTEM as the concentration carrier: `S79` Leg 2 U-MIXED + `AVGO` prints 09-02 with `S127`'s bands still HAND-SET and `D295` unmet for a 4th run even though the straddle chain rolled TODAY), COMM(N, 08-24 = 3 runs, un-measurable BY RULE 10th run), DISC(N−, 08-23 = 4 runs, tie-loser NAMED not dropped, `eqflow` −0.377 rank 9 with a 50.0% red-rate = 2nd worst), INDU(UW−, 08-27 = 0 runs, 18🔴 = board's most in absolute terms, red-rate 36.0%; the 09-08 Canadian retaliation date is bracketed by `S123`(09-12) and `S129`(09-09) and the held name `NUE` reads flow −0.237 with delta −0.229), STPL(UW, 08-27 = 0 runs, `eqflow` −0.282 rank 7)]**
