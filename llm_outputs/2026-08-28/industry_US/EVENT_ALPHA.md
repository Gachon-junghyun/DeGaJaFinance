# EVENT_ALPHA — industry_US · 2026-08-28 (Stage 5 / L1·EVENT_ALPHA)

> Bottom-up complement to MACRO's top-down matrix: **which stories ARE building, and is money
> already following.** `--scope foreign` on every call. Flow tags are `SECTOR_FLOW_US.json`
> `asof 2026-08-28` — ⚠ a **pre-market stub bar** (median volume **2.40%** of the prior session,
> `M1021`), so **`OBV`/`delta`/`rs` orderings are used and the 🟢 tag is not trusted as a gate**.
> P4 — no sizing anywhere. BET owns that.

## §0 · Selection log — no silent truncation

`thread --date 2026-08-27 --days 7 --scope foreign`: **4,659 daily events → 3,543 threads → 601
multi-day → 268 ALIVE.** Of those, **25 carry a BUILDING tag.**
**8 selected** (below). **Not selected: 17 BUILDING + 243 other alive = 260.**

**Named reasons for the notable non-selections** (so the cut is visible, not silent):
- **Iran–Oman Hormuz deal** — the window's largest *state change*, but tagged **FADING (9→13→3)**,
  not BUILDING. **Already owned by `P107`** (MACRO §C), registered both-sided this run. Not double-carded.
- **Microsoft AI push** (BUILDING 6→…→8), **IPO fever OpenAI/Anthropic** (6→3→2→6),
  **China–EU subsidies** (6→6→3→5→7), **Dollar** (3→5→4→3→8) — read, no exposure name passed the
  money axis; **not carded, and not dropped either** (see §2 ledger).
- **Nepal/Tibet floods** (14→14), **UEFA/FIFA** (3→…→8), **Mladic**, **Lake Ontario renaming** —
  BUILDING and genuinely large, **non-market**. Counted, not carded.

---

## §1 · Forward cards

### CARD 1 — ★★★ The AI-compute re-rate: the print landed, and the money axis says LATE

- **Thread**: *"Nvidia Results, Semiconductor Rally Lift US Equity Futures"* · **BUILDING** ·
  **6→4→3→13→8→14→14**, **406 articles / 7 days** · window denominator 4,659 events / 3,543 threads.
  A second thread (*"Stock Futures Jump as Nvidia Earnings Power AI Rebound"*, 6→4→5→9→13) is the
  same object; both counted once.
- **Direction (body-read, and it is NOT the headline)**: the beat is real — *"Nvidia rises after
  signaling **longer AI spending runway**"*, *"Nvidia Adds **$442 Billion** in Second Biggest Ever
  Stock Surge"*, *"Nvidia projects **70% revenue growth in 2028**"* — **but the same 08-27 cluster
  carries the funding-quality question**: 🚨 *"**Nvidia Pauses Revenue-Sharing Deals With AI Cloud
  Companies**"* and *"Nvidia Beats Again — But **Cash Flow Must Keep Up** With Blackwell"* and
  *"The Glaring Flaw in Nvidia's Operating Results That Wall St…"*. ⇒ **the bull tape and the
  circular-financing objection arrived together**, which is the mechanism `S79` branch B named on
  08-12 and which no card has owned since.

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `NVDA` (held) | **epicenter — headline layer, crowded by construction** | 🟡중립 · flow **+0.358** · OBV **중립** · `rs20 +9.9` / `rs60 +3.5` · surge **1.15** · delta **+0.218** | FINRA z **−1.04** (shorts leaving, 08-27) |
| `AVGO` (held) | epicenter peer, prints **09-02** | 🚨 **🔴분산** · flow **−0.656** · OBV **분산** · `rs20 −6.9` / **`rs60 −24.0` = still the board's worst** | FINRA z **−1.22** |
| `ANET` (held) | datacenter networking | 🟡중립 · flow +0.357 · OBV 매집 · `rs20 +6.3` / `rs60 +11.1` · delta **−0.126** | FINRA z **+0.63**, 5v5 **+12.3▲** — the book's rising short |
| `MRVL` | custom silicon; printed 08-27 | **🟢가속** flow +0.483, OBV 매집, `rs20 +15.1` / **`rs60 −28.7`** | ⚡ FINRA z **+2.70 = crowded-short** |
| `SNPS` | EDA — Elliott stake headline 08-27 | 🟡 flow **+0.735**, OBV 매집, `vol_surge` **1.18**, delta **+0.212** | — |

- **Future — IF the thread keeps building AND flow stays accumulating** → the re-rate broadens past
  the epicenter and `SNPS`/`MRVL` lead the second leg. **Track KPI**: `RSP` 5-session excess vs `SPY`
  (**this is `P106`'s registered observable** — branch B ≥ +1.20pp). **Horizon: 2026-09-04.**
- **ELSE / kill condition** → **`P106` branch A (`RSP` exc5 ≤ −1.60pp)**: the index holds while
  breadth keeps falling ⇒ the card is a one-name card wearing a sector label, and it is retired.
- **Cell: LATE-MONEY.** ⚠ **Stated as a discipline, not a judgement**: the thread is at **day 7 of
  its peak** (`14` outlets, twice), which the L2 defines as *chasing*; and `MRVL`'s `rs60 −28.7`
  against `rs20 +15.1` is a reversal, not a trend. ⇒ **valuation gate note handed to BET**, no
  candidate promoted from this card.

### CARD 2 — ★★★ The power layer: a dated, regulator-approved AI contract, and the money is LEAVING

- **Thread**: *"Georgia Power Wins Approval for **3.2-GW** OpenAI Power Deal"* · **new, 08-27** ·
  **4 distinct outlets** (`seekingalpha` · `nasdaq` · `prnewswire` · `yahoo_finance`) · surfaced by
  **chain-hop, not by any bucket term**: `SO` scores **title 0 / body 13, proximity 10** on
  `AI capex` + `data center` + `power`.
- **Direction (body-read)**: `prnewswire` 08-27 — *"Georgia Power's contract with OpenAI **approved**;
  latest approval part of a portfolio of large-load contracts delivering approximately **$950 million
  in annual savings beginning in 2029**."* This is **not a letter of intent** — it is a regulator
  approval with a size, a counterparty and a start year.

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `SO` | **the contracted utility itself** | 🔴**분산** · flow **−0.733** · `rs20 −9.2` · OBV 분산 (three axes, `D6`) | — |
| `AEP` | same layer, chain-hop 6/6 | 🟡 flow −0.371, OBV 중립, delta **−0.210** | — |
| `NEE` | Utilities top1, chain-hop 4/4 | 🟡 flow −0.661, OBV 중립, `rs20 −9.0` | — |
| `VST` | merchant power | 🔴 **분산** · flow **−0.817** | — |

- ★★★ **This is the card that justifies the 2×2.** The story is **as concrete as a story gets** —
  approved, sized, dated — and **every exposure name fails the money axis on THREE instruments at
  once, not on OBV alone** (`D6` — OBV is C-grade and may not carry a proposition by itself):
  `SO` **flow −0.733** *and* **`rs20 −9.2`** *and* OBV 분산; `AEP` delta **−0.210**; `NEE` `rs20 −9.0`;
  `VST` flow **−0.817**. All of it inside the sector that is **last of 11 on `eqflow` (−0.546)** and
  **last on `XLU exc20` (−7.28 vs `SPY`)**. A headline-only desk buys this. **The money axis says no.**
- **Future — IF the thread keeps building AND flow turns** → the Utilities underweight is wrong for a
  *contract* reason rather than a rate reason. **Track KPI**: `SO` OBV state + flow delta on
  **settled** sweeps. **Horizon: 2026-09-11.**
- **ELSE / kill** → `SO` stays OBV 분산 through 09-11 ⇒ the AI-power narrative is not being bought at
  the regulated utility, and the exposure question moves to the unregulated layer.
- **Cell: STORY-ONLY.** → **watchlist with a dated re-check. Filed to `missed_ledger`** (`SO`,
  `S.테마회피`, enters if **OBV 매집 ∧ flow delta > 0 on two consecutive SETTLED sweeps**, recheck
  **2026-09-11**). **Not handed to BET.**

### CARD 3 — ★★ `ASML`: the chain-hop name the tape refused to put in a headline

- **Thread**: *"AI Can't Thrive Without This Stock. Hint: **It's Not Nvidia.**"* · BUILDING **6→7** ·
  3 articles / 3 outlets (`nasdaq` · `yahoo_finance` · `fool`, 08-26).
  ★ **The article never names the stock in its title. `chain-hop` did**: `ASML` **title 0 / body 17**,
  proximity 10 — the highest body-to-title ratio in the semis layer.
- **Direction (body-read via the chain-hop evidence line)**: the AI-capex/data-center/power complex
  discusses `ASML` in-body **17 times with zero title mentions in 3 days** ⇒ it is being reasoned
  *through*, not traded *about*. Corroborating same-window items: *"Micron Technology Doubles Capex
  as AI Memory Shortage Deepens"*, *"Intel Unveils Next-Gen AI Processors"*.

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `ASML` | **litho — the chain's hard constraint, one hop behind the headline** | 🟡 flow +0.150, **OBV 매집**, `rs20 +2.8`, `rs60 −2.2`, delta **+0.112** | not in the FINRA probe set |
| `MPWR` | power-management semis | 🟡 flow −0.771, delta **−0.389** ⇒ **fails the money axis** | — |

- ★ **`OBV 매집` with `rs20` barely positive is precisely the shape the tool defines as chain-hop
  alpha** (*"accumulating while RS has not yet risen"*), and it is why the card exists.
- ⚠ **Its 🟡 tag is not evidence against it today**: the 🟢 gate reads `vol_surge`, and `vol_surge`
  is being computed on a **2.40%-volume bar** (`M1024`). `ASML` reads surge **0.46** — mechanically,
  not informatively.
- 🚨 **`W1`/universe caveat, stated**: `ASML` is a foreign domicile and is exactly the class the
  SWEEP invariant warns about (*"index membership is not a universe"*). It **is** in `us_top300` this
  run, so it is taggable — but it is not held, and this desk has no `--positioning` read on it.
- **Future — IF** → tag turns 🟢 on a **settled** bar with `rs20 > +8` ⇒ CONFIRMED-EARLY, BET candidate.
  **Horizon: 2026-09-11.** **ELSE / kill** → OBV turns 분산 ⇒ the body mentions were commentary, not accumulation.
- **Cell: CONFIRMED-EARLY on the money axis, held back on the tag.** **Filed to `missed_ledger`**
  (`Q.확신부족`, enters if 🟢 on a settled bar ∧ `rs20 > +8`, recheck **2026-09-11**) rather than
  promoted — **because the desk's own gate cannot be evaluated on today's bar, and inventing a
  substitute gate mid-run is the `C5` error.**

### CARD 4 — ★★★ Memory capacity is being COMMITTED — on the desk's own regime axis

- **Thread**: Kioxia + SanDisk **>$31bn Japan investment** · **new, 08-27/28** · **3 outlets**
  (`cna` · `digitimes` · `seekingalpha`, + `japantimes` body-error). Follow-on 08-28:
  *"**SK Hynix weighs closer ties with Kioxia in NAND**"*.
  ★ **Surfaced by the blind-spot pass (`burst`), not by any bucket** — `KIOXIA` 5 articles / 5 outlets,
  and **none of the seven buckets contains a memory term** (`M1015`).
- **Direction (body-read)**: *"Kioxia weighs **third Kitakami fab** as **NAND demand rises**"*
  [`digitimes`]. Supply response, not demand news. Same-window corroboration:
  *"Memory crunch: Cloud operators may be pushed to splurge **68% of capex on DRAM and NAND**"*
  [`theregister` 08-26, **1 outlet**] — 🚫 quoted as `[news]`-grade at **one** outlet, and **not**
  used to update `M9`'s ~30%→48% chain.

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `SNDK` | **the JV partner — named in the commitment** | 🟡 flow +0.246, OBV 중립, **`rs20 +17.4` / `rs60 −22.3`**, delta +0.089 | — |
| `MU` | DRAM/HBM, the regime call's own name | 🟡 flow +0.367, **OBV 매집**, `rs20 +8.7` / `rs60 −17.0`, delta **+0.223** | — |
| `STX` | storage, chain-hop 3/9 | 🟡 flow −0.219, OBV 매집, `rs60 −12.3` | — |

- 🚨 **The threat leg is in the same window and it is not suppressed**: *"Nvidia custom '**NVHBM**'
  promises 30% higher bandwidth… available to NVLink Fusion partners"* [`tomshardware` 08-26]
  ⇒ **the epicenter is internalising part of the memory stack** while memory commits capacity.
  Both legs are on the card; neither is scored.
- **Future — IF** → **`P109` branch A**: a **second, separately-sourced ≥$10bn** memory-capacity
  commitment from a **different producer**, ≥3 outlets, by **2026-09-30**.
  ⚠ **Pre-settle reading, stated honestly**: a candidate exists and **fails the bar** —
  *"Micron Technology Doubles Capex as AI Memory Shortage Deepens"* is **1 outlet** (`yahoo_finance`
  08-27) with **no dollar figure in the title**. ⇒ **`P109` is live, not pre-settled.**
- **ELSE / kill** → no second commitment by 09-30 ⇒ Kioxia is idiosyncratic; the regime call is unchanged.
- **Cell: CONFIRMED-EARLY (money axis: `MU` OBV 매집 + delta +0.223; `SNDK` 20-day reversal).**
  **`SNDK` filed to `missed_ledger`** (`M.숏리스트탈락`, enters if OBV 매집 ∧ `rs60 > −10`, recheck
  **2026-09-11**). ⚠ **`MU` is not handed to BET** — it is the name `S117` just settled **`FIRED-B`**
  on (−9.06pp vs `NVDA`), with the row's own **construction defect recorded**; a candidate cannot be
  built on the same session that refuted its framing without a fresh measurement.

### CARD 5 — ★★★ The refining thesis lost BOTH of its separators today

- **Thread**: *"Russia attacks Kyiv with drones…"* / *"As Ukraine hits Russia's **refineries**"* ·
  **BUILDING** · **14→6→8→12→15→13→15**, 146 articles. Same-window body:
  *"**Russia's NORSI refinery halts oil processing after drone attack**"* [`reuters` 08-26] and
  *"**Wheat** Rallying to New Highs as Russia Looks to Escalation"*.
- **Direction (body-read)**: refining **capacity destruction continues** and is now **dated and
  named** at a specific refinery. That mechanism is intact.
  🚨 **What is NOT intact is the desk's ability to measure it.** Both registered separators settled
  against the thesis on the **08-27 settled close** (MACRO §E-2, HANDOVER §2c/§2d):
  **`P83` FIRED-B** — distillate−gasoline **−13.562** from the 08-20 settle of 51.131 (branch B is
  ≤ −4.0); **`P96` FIRED-B** — crack 5-session rate **+4.874** (branch B is ≥ +0.468).
  ⇒ ★ **`R89` retracted the first separator on 08-21; `P83` was its replacement instrument; the
  replacement has now failed too.** The desk has **no surviving instrument that separates refining
  margin from the barrel.**

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `MPC` (held) | refining — Energy's best flow | 🟡 flow **+0.567 = 1st in Energy**, OBV 매집, `rs60 +34.6` | 🚨 FINRA **z +2.04, 5v5 +9.7▲ = the book's only short surge** |
| `PSX` (held) | same node | 🟡 flow +0.258, OBV **중립**, delta **−0.212** | z +0.05 |
| `VLO` | same node | 🟡 flow +0.209, OBV 중립, delta **−0.194** | — |
| `ADM` | the **agricultural** leg (wheat) | not in the shortlist | `S129` armed, settles **09-09** |

- **Future — IF the thread keeps building AND flow stays accumulating** → capacity destruction
  eventually shows up in the crack **again**, and `P83`/`P96` are re-registered on a **third**
  instrument. **Track KPI**: `EW{MPC,PSX,VLO}` `exc5` vs `SPY`; **`P100` settles 2026-09-01.**
- **ELSE / kill** → 🚨 **already partly fired.** The standing claim of *separation* is withdrawn
  (see §3). What is not withdrawn is the thesis; what is withdrawn is the desk's right to assert it
  on an instrument.
- **Cell: the thesis needs REWRITING, not dropping** — the L2's explicit non-DEAD case: the flow is
  still accumulating on the lead name (`MPC` OBV 매집, `rs60 +34.6`) while the measurement failed.
  ⇒ **re-filed with a new thesis line and a dated re-check (09-01, `P100`), not sent to the reject
  ledger.** ⚠ `MPC`'s short surge and its instrument failure land on the **same name** — handed to
  DEEP as its first question.

### CARD 6 — ★★ `SaaSpocalypse`: the software–hardware split now has a name, and it is contested

- **Thread**: *"Salesforce, Anthropic expand partnership as Benioff responds to '**SaaSpocalypse**'
  concerns"* · `burst` new-word, **7 articles / 4 outlets / 100% market relevance** · realised in the
  08-27 tape as *"**Salesforce Surges 23% on Anthropic**"*.
- **Direction (body-read)**: `cnbc` 08-26 — *"Salesforce CEO Marc Benioff says the 'SaaSpocalypse' is
  **nonsense**"*, and the same day the company **expands** the Anthropic partnership. ⇒ the incumbent
  is answering the disruption thesis **with a partnership**, not with a denial only. The counter-item
  is in the same window: *"Atlassian: Is The SaaSpocalypse Victim Narrative **Cracking**?"*

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `CRM` | **the named incumbent** | 🟢가속 · flow **+0.828 = 1st of 299** · **`new_green`** · OBV 매집 · `rs20 +36.5` / `rs60 +32.6` · surge **1.29** · delta **+0.458** | ✅ FINRA z **−0.89** — clean rise |
| `ORCL` | application software peer | 🟡 flow +0.378, OBV 매집, `rs20 +14.7` / **`rs60 −35.8`** | carried; `missed_ledger` recheck **09-16** |
| `ADSK` · `INTU` | same layer, chain-hop 7/22 and 4/… | 🟡 flow +0.535 / +0.612, both OBV 매집, surge 0.77 / **1.18** | — |

- ★★ **Two independent instruments found this name and the term-based one was FIRST**: the blind-spot
  pass named `SAASPOCALYPSE` from a 4-outlet token before the sweep ranked `CRM` #1 of 299 (`M1025`).
  **`CRM` is the only name on the board that is 1st on flow, `new_green`, positive on both `rs20`
  and `rs60`, and clean on shorts.**
- **Future — IF** → `CRM` holds 🟢 with `rs60 > 0` on **settled** bars ⇒ the disruption discount on
  application software was wrong and this is a re-rating, not a bounce.
  **Track KPI**: `CRM` `rs60`. **Horizon: 2026-09-11.**
- **ELSE / kill** → `rs20` falls below +10 or OBV turns 분산 ⇒ a 23% one-day partnership pop, retired.
- **Cell: CONFIRMED-EARLY.** ⇒ **handed to ROTATION and BET §B as this run's cleanest candidate.**
  ⚠ **Handed with its qualification attached**: the 🟢 that qualifies it was computed on the stub bar
  (`M1024`), and its `vol_surge` **1.29** is the strongest of the four survivors — i.e. it clears the
  gate **despite** the suppression, not because of a favourable bar.

### CARD 7 — ★★ Jackson Hole D-0: the macro card, and it is registered rather than argued

- **Thread**: *"Fed's Warsh faces high stakes ahead of Jackson Hole"* · **BUILDING** ·
  **6→3→5→4→7→13** · 93 articles — **peaking directly into today's speech.**
- **Direction (body-read)**: the committee is **on record as split the day before** —
  *"Fed's Collins says latest US inflation readings are **'mixed'**"*, *"Fed Officials Remain
  **Divided** on Inflation Ahead of Warsh's Speech"* (7 outlets). Against a **hot July PCE printed
  08-26**. ⇒ a Chair who does not resolve the split *is itself* the outcome (`M1017`).
- **Exposure — the duration complex, and it is an exposure the book already has by absence**:
  `XLU` `exc20` **−7.28** · `XLRE` **−5.38** · `XLP` **−4.42** vs `SPY`; `eqflow` −0.546 / −0.384 /
  −0.282, all three bottom-4 of 11. **No individual name is promoted from this card** (P4).
- **Future — both branches are `P110`'s, registered in MACRO §C**: A = `DGS2` ≥ 4.30 (hawkish ⇒ the
  underweights are right for the stated reason) · B = `DGS2` ≤ 4.08 (dovish ⇒ **the three rip
  together**). **Horizon: first `[FRED]` close covering 2026-08-28.**
- 🚨 **Kill note carried from the desk's own scoreboard**: `S108` settled with its three legs
  **2.46pp apart, one negative** ⇒ *"the underweights move together"* is a premise this desk has
  **already measured as false once.** Branch B is a **re-test**, not a fresh idea.
- **Cell: MACRO-LINKED, no name hand-off.** Handed to PREMORTEM as a ≤48h binary already bracketed.

### CARD 8 — ★★ Trade-war implementation: the date is 09-08 and the held name is `NUE`

- **Thread**: *"Trump's Canada, beef trade actions add to GOP midterm worries"* · **BUILDING** ·
  24 articles / 11 outlets, 08-27. Sub-events: *"Canada **excludes US seafood** from retaliatory
  tariffs"*, *"Trump administration won't 'just sit down and take' further…"*, *"Wilbur Ross led
  trade talks with Canada…"*.
- **Direction (body-read)**: the retaliation is being **narrowed by category** (seafood carved out),
  not withdrawn. ⇒ implementation is proceeding with **exemptions**, which changes *which* names are
  hit, not *whether*. Effective date **2026-09-08** (`M941`, carried).

| ticker | chain position | flow tag (asof 2026-08-28) | crowding |
|---|---|---|---|
| `NUE` (held) | **steel — named on Canada's list** | 🟡중립 · flow **−0.237** · `rs20 −4.2` / `rs60 −3.4` · delta **−0.229** (OBV 매집, cited **with** the three price axes per `D6`) | FINRA z **−1.30** (shorts leaving) |
| `ADM` | agricultural leg | 🟡중립 · flow **−0.388** · `rs20 −0.7` / `rs60 −5.6` · delta **+0.255** (OBV 분산, cited with the price axes per `D6`) | **`S129` armed, settles 09-09** |

- **Future — IF** → tariffs take effect 09-08 unmodified ⇒ `S123` (implementation, settles 09-12) and
  `S129` (agricultural, 09-09) both resolve on the same event. **ELSE / kill** → a further carve-out
  covering steel before 09-08 ⇒ the `NUE` leg is void and the card reduces to `ADM`.
- **Cell: STORY-ONLY** — the story is dated and building; **neither exposure name passes the money
  axis** (`NUE` is OBV 분배 on its own chart read). No BET hand-off.

---

## §2 · Ledger hand-offs — the funnel's widest stage now has a scoreboard

**Filed to `missed_ledger` this run (4 rows, all `prospective`, all with `--enters-if` and a
`--recheck-date` of 2026-09-11):**

| ticker | class | why | enters if |
|---|---|---|---|
| `SO` | `S.테마회피` | CARD 2 — approved 3.2-GW OpenAI contract, 4 outlets, **and flow −0.733 / OBV 분산** | OBV 매집 ∧ delta > 0 on **two consecutive settled** sweeps |
| `ASML` | `Q.확신부족` | CARD 3 — chain-hop title 0 / body 17, OBV 매집, RS not yet risen | 🟢 on a **settled** bar ∧ `rs20 > +8` |
| `SNDK` | `M.숏리스트탈락` | CARD 4 — named in the $31bn commitment; `rs20 +17.4` / `rs60 −22.3` | OBV 매집 ∧ `rs60 > −10` on a settled sweep |
| `FCX` | `M.숏리스트탈락` | SWEEP §3 — Materials leads the board on `eqflow` and **FCX is blocked from 🟢 by `vol_surge` 0.97 alone** on a 2.40% bar | `vol_surge ≥ 1.0` on a **settled** bar with flow > +0.5 |

**Filed to `reject_ledger` this run: none.** ⚠ **Stated rather than omitted** — no thread this run
reached the 2×2's DEAD cell (**FADING/ENDED × 🔴 dispersing, both axes**). The nearest candidate,
CARD 2's `SO`, is 🔴 dispersing on a **BUILDING** thread, which is **STORY-ONLY by definition, not
DEAD** — and mis-filing it as a rejection is precisely the error the L2's warning exists to prevent.

---

## §3 · Book cross-check — ENDED threads under open positions

- **Zero ENDED threads sit under an open book thesis.** The 333 ENDED threads in the window were
  scanned against the 11 US holdings' theme labels; none matched.
- 🚨 **But one book thesis lost its instrument rather than its thread** (CARD 5): the refining
  position (`MPC`, `PSX`, held) rides a thread that is **BUILDING at 15 outlets**, while **both**
  registered separators settled against the separation claim on 08-27. ⇒ **flagged to the book desk**:
  the position does not need re-justifying on *attention* — attention is there — it needs
  re-justifying on a **measurement**, and the desk currently has none.
- ⚠ **`RTX` (held)**: its rank-3 cycle still has **no exposure threshold set** (`cycle_exposure`
  ⚪ n/a), so 3.81% neither passes nor fails, and **`W4` — "name the customers" — is unpaid for 209
  days.** Carried, not resolved.
- ⚠ **Optical (`LITE`/`COHR`)**: `cycle_registry.json` still carries **no optical row, 13th run**
  (`D250`) ⇒ that exposure is **unmeasurable, not zero.**

---

## §4 · In-run self-correction (`§4c` / `D48`) — written down, not edited away

**This stage drafted CARD 1's exposure table from memory of the carried 08-26 registry and the
numbers were WRONG.** The draft read `NVDA` *"flow +0.043, OBV 매집, rs20 +14.3"* and `AVGO`
*"🟡, rs60 still the book's worst"*. Re-reading `SECTOR_FLOW_US.json` refuted both:

| name | drafted | **measured, `asof 2026-08-28`** |
|---|---|---|
| `NVDA` | flow +0.043 · OBV 매집 · `rs20 +14.3` | **flow +0.358 · OBV 중립 · `rs20 +9.9`** |
| `AVGO` | 🟡 | **🔴분산 · flow −0.656 · OBV 분산** |

⇒ **`AVGO`'s tag is materially worse than drafted**, and it is the name that **prints 09-02** with
`S127` armed. The corrected row is the one in CARD 1; this block records that the first version was
wrong, per the rule that a run showing only its surviving claims has hidden its own error rate.
★ **The mechanism is the one HANDOVER §1a named this morning**: the 08-27 run never wrote back its
§3a registry, so the freshest carried per-name state is **08-26** — and drafting from a 2-day-old
carry instead of from today's artifact is exactly how `D394` costs a run. **It cost this one, in the
same session it was registered.**

⚠ **Caught by the linter too, separately**: a `D6` violation (OBV cited alone as CARD 2's verdict)
was flagged by `report_lint.py` and fixed by adding the three price axes that independently agree —
`SO` flow −0.733 **and** `rs20 −9.2`. The conclusion did not change; its evidentiary basis did.

---

## ✅ EXIT CHECK
- [x] **Scope market-correct** — `--scope foreign` on every `brief`, `thread`, `search`, `burst` and `chain-hop` call. No card cites the KR feed.
- [x] **Selection logged**: 268 alive → 25 BUILDING → **8 selected**, **260 not selected** and the notable non-selections named with reasons. No silent cap.
- [x] **Every card has a direction body-read**, not a headline read — and two cards' body-reads **contradict their headlines**: CARD 1 (the beat cluster carries the funding objection) and CARD 2 (an approved contract on a dispersing tape).
- [x] **Every exposure name carries a flow tag with its `asof`** (2026-08-28), plus the stub-bar caveat that governs how those tags may be read.
- [x] **STORY-ONLY names did not leak into the hand-off**: `SO`, `NUE`, `ADM` are watchlist/ledger only. Only **`CRM` (CARD 6)** is handed to ROTATION/BET.
- [x] **Every card has both branches + a kill condition + a dated horizon.**
- [x] `EVENT_ALPHA.md` written; **CONFIRMED-EARLY (`CRM`) handed forward**; ENDED-thread book flags emitted (§3, zero found, plus the instrument-loss flag that is not an ENDED-thread flag and is labelled as such); ledger updated with 4 rows.
- [x] **No sizing anywhere** (P4).
