# BLINDSPOT_PREMORTEM — industry_US · 2026-08-26 (Stage 5 / L1·PREMORTEM ★US-only)

> Four adversarial lenses argue **AGAINST this run's own tilt** before the DEEP budget is committed.
> **Zero sizing, zero buy/sell (P4).**
> ⚠⚠ **DECLARED SESSION CONSTRAINT, 9th consecutive run**: the four lenses ran **in-context and
> serially**, not as a parallel adversarial Agent fan-out. **They therefore share this run's blind
> spots**, and each lens states below what it could **not** independently check. This is a
> known weakness of the run, declared rather than papered over.

**The draft tilt being attacked** (`SECTOR_ROTATION §1` + its one delta):
`ENRG OW · HLTH OW · STPL N− · MATR N · COMM N · DISC N · FIN N · RE N− · UTIL UW− · INDU UW− · IT UW`
**DEEP set: continuous [ENRG, HLTH] · rotating [RE, FIN].**

---

## Lens 1 — UNDER-COMPUTED LEGS

*Strongest bull case for a sector/sub-leg NOT deep-dived that carries a catalyst ≤ ~5 trading days.*

| Leg | Catalyst | The strongest case AGAINST our not covering it | Verdict |
|---|---|---|---|
| **IT / AI-semis** | 🚨 **`NVDA` D-0 TONIGHT** · `MRVL` **D-1** · `AVGO` D-5 | `eqflow` **rank 2 of 11** and the board's **2nd-lowest red-rate (10.7%)**, holding **4 of the board's 6 greens**, into three prints in six sessions — against a `UW` set on price breadth. **This is the largest live disagreement on the board** | **WITHIN-RUN-WATCH** — and the reason is stated: it was **routed here by ROTATION** precisely because its question is *"which instrument is right"* (`C16`), which a sector deep-dive cannot answer. **`P101` (09-04) settles it.** A 5th DEEP slot would re-measure, not resolve |
| **INDU** | Canada tariffs effective **09-08 (D-9)** — outside the ~5-day bar | The board's **#1 thread** (`BUILDING` 23→9→11→24→20→21→29, 370 articles, peak 29 outlets) sits on a sector nobody has deep-dived in **4 runs**, and it **lost ROTATION's tie-break** | **WITHIN-RUN-WATCH.** Catalyst is D-9, outside the bar. `S123` (09-12) owns the implementation date. ⚠ **`P93` settles 08-28, eleven days early (`D342`)** — the announcement is bracketed, the implementation is not, except by `S123` |
| **STPL** (demoted today) | Canada's retaliation list names **dairy and agricultural goods**, effective **09-08 (D-9)** | The sector was demoted to `N−` on flow, and its **named tariff exposure (`ADM`) has NO bracket** — `D342`'s unbracketed half, **2nd consecutive run** | **WITHIN-RUN-WATCH**, logged. ⚠ **Not promoted**: D-9 is outside the bar and adding a 10th row to the 08-28/09-08 cluster is `D343` |
| **MATR** | No dated catalyst ≤5d | `FCX` `exc5 +20.69` / `NEM` `+16.72` with the other ten at an `exc20` **median −8.211**, Copper COT at the **100th percentile for a 4th run**, and a **Reuters 08-25 headline** — *"US tariff threat upends copper surplus as prices test all-time peak"* — that EVENT_ALPHA surfaced only after killing the fake thread around it | **WITHIN-RUN-WATCH** — **already bracketed by `P102` (09-09)**, registered this run at the **98.8th percentile** |
| **FIN / crypto leg** | — | `COIN` carries the board's **highest flow score (+0.978)** and **highest `vol_surge` (1.56)** | ✅ **ALREADY COVERED** — `FIN` is this run's **R2 DEEP pick**, selected partly for exactly this |

⚠ **What Lens 1 could NOT independently check**: it read the same `SECTOR_FLOW_US.json` and the same
`_thread7` dump the rest of the run read. **It has no independent evidence** and therefore cannot
have caught a leg that is invisible to both. **A parallel agent with its own search would have been
the control this lens is not.**

**Verdict: no leg PROMOTED to a 5th DEEP slot.** Four legs logged as WITHIN-RUN-WATCH, each with the
reason and the row that already owns it. **Nothing silently dropped.**

---

## Lens 2 — REGIME-FLIP / BOTH-SIDES: every known binary, and the against-us branch

**Binaries in window** (`CATALYST_WATCH.json`, `--days 10`): **6 flagged** — `NVDA` **D-0**,
July PCE **08-28 D-2**, `FRO` **08-28 D-2**, `AVGO` **09-02 D-7**, Aug NFP **09-04 D-9**, and the
**undated** *"Iran 'Strait of Hormuz open' statement"*.
🚨 **Two more that the calendar does NOT carry and this stage adds by hand**: **`MRVL` 08-27 (D-1)**
and **Jackson Hole 08-27~29** — the `D26`/`D320` class, and `MRVL` is **one of only 6 board greens**.

### 2a · Coverage audit — the mandate is "no one-way tilt into a known binary", and it is met

| Binary | Rows already ARMED | Against-us branch (what rips if we are wrong) | New row? |
|---|---|---|---|
| **`NVDA` D-0** | **SEVEN**: `P90` (08-26) · `S79` `S115` `S117` (08-27) · `S118` (08-28) · `S103` (08-29) · `S113` (09-01) | **A beat re-rates the sector this desk just underweighted.** `NVDA` implied **±6.0% (08-28, D2 — correctly spans the print)**, P/C **0.5**, skew **0.0** ⇒ **complacent, little hedging fuel.** Our `IT UW` and our `HLTH`/`ENRG` OWs would all be on the wrong side of a broad AI re-rate | 🚫 **DECLINED** — septuply bracketed. **An eighth row on one print is `D343` in its purest form.** The mandate is satisfied; **this is not a one-way tilt** |
| **`MRVL` 08-27** | **`S116`** (settle 08-28), spread `MRVL − AVGO` ≥ +12.0 / ≤ −12.0 | Registered implied `MRVL` **±11.31%**; today the same 08-28 chain reads **±10.3%** ⇒ **the ±12.0 thresholds still sit OUTSIDE the implied move.** ✅ **`S116` is not re-banded** (`D242`) and remains information-carrying | 🚫 Declined — covered |
| **July PCE 08-28** | `S104` (08-31) · `S111` (08-28) · `S112` (08-31) | A hot print with `DGS2` already at the 92nd percentile hits `RE`/`UTIL`, which we are short-of-neutral | 🚫 **DECLINED — 08-28 carries NINE rows** (`D343`) |
| **Jackson Hole 08-27~29** | `S120` (08-28) · `S112` (08-31) | A dovish Warsh rips the three underweights **together** — the exact object `S108` was built for and **failed to measure** (its window contained no event; see `HANDOVER §2b`) | 🚫 Declined — covered, and `D365` filed on the construction defect |
| **`FRO` 08-28** | `S109` (09-02) | 🚨 **`FRO` is OUTSIDE `us_top300`** (`D341`) — **the desk has a row armed on a name it cannot flow-tag on any axis** | 🚫 Declined — the gap is an instrument gap, not a bracket gap |
| **Aug NFP 09-04** | `S126` (09-04) | A weak print converts `INDU UW−` from a tariff call into a cycle call — `S126`'s own registered question | 🚫 Declined — covered |
| **Hormuz statement** | 🚨 **`S8` — UNDATED, unscoreable for a 27th run** | Its object appeared in the 08-25 head layer (*"Iran, Oman Push Talks for 'Interim' Reopening"*, 9 art / 8 outlets) **and** in `CATALYST_WATCH` as an explicitly undated binary | 🚫 **A human must `VOID` or re-date it (P5).** This stage cannot |
| 🚨 **`AVGO` 09-02** | **NONE.** `S116` measures `MRVL − AVGO` on **08-28**, i.e. **before `AVGO`'s own print**, and it is a *relative* franchise question | **THE OPEN OBLIGATION.** `HANDOVER §2c` carried it forward from 08-25 verbatim: *"🚨 `AVGO` print — HELD, `rs60 −20.1`, NO ROW ON THE BOARD; registration obligation"* | ✅ **REGISTERED — `S127`** |

### 2b · 🚨 An instrument failure found while doing this audit — `D353` REPRODUCES, 7 days later, on a held name

`module_flow AVGO --positioning` returns **`예상변동 ±1.3% (만기 2026-08-26, D0)`**.
**`AVGO` prints 2026-09-02.** ⇒ **the quoted straddle expires SEVEN DAYS BEFORE the event and the
±1.3% is not the event's implied move at all.**

This is the identical defect `D353` filed on 08-25 for `NVDA` (±1.3% at a D0 expiry, two days before
its print), where the 08-25 desk wrote: *"a defect that heals itself on a calendar roll will
re-appear on the next same-day expiry — and the desk will by then have logged a 'fix'."*
**It has re-appeared, on the next same-day expiry, on a different name, seven days later.**
⇒ **`D353`'s prediction is confirmed; its remedy (refuse to quote an expiry with `D ≤ 1` when a
scheduled earnings date sits beyond it) is still unimplemented.** Registered as **`M948`**.
⇒ **Consequence for `S127` below, stated at registration**: its thresholds **cannot** be taken from
the options market as the protocol prefers. **They are taken from the trailing-252 distribution and
the provenance is labelled**, rather than a broken ±1.3% being dressed up as a trigger.

### 2c · ✅ NEW REGISTRATION — `S127` (both-sided, `D93` baseline computed BEFORE freezing)

> **ID 3-grep at WRITE time** across `handoff/*.md` · `llm_outputs/**` · `REPORT/**`: `S127` `S128`
> → **0 hits**. Highest existing: `S126` (2026-08-25 `industry_US`).

**`S127` — ★★★ `AVGO` 2026-09-02: the held name with the worst `rs60` on the book and, until now, no row**

| Field | Value |
|---|---|
| **Event** | `AVGO` FQ3 results, **2026-09-02**. **HELD** (`AI-compute-EPICENTER` label). The registration obligation was recorded on 08-25 and carried into `HANDOVER §2c` today |
| **Frozen observable** | **`AVGO` 5-session excess return vs `SPY`**, settled closes, `auto_adjust=False`, window **2026-09-01 close → 2026-09-08 close** (the print falls inside) |
| **Branch A (the de-rate was a discount)** | excess **≥ +5.928pp** (trailing-252 **p85**) |
| **Branch B (the de-rate was a franchise loss)** | excess **≤ −4.922pp** (trailing-252 **p15**) |
| **Branch C** | between = **the favourite, disclosed** |
| **`D93` executed BEFORE freezing** | `AVGO` exc5 vs `SPY`, trailing 252: mean **+0.281** · sd **6.023** · p05 −8.036 · **p15 −4.922** · p50 −0.341 · **p85 +5.928** · p95 +10.033 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **−5.920 = the 9.9th percentile of 252** — i.e. it enters its own print **already inside branch B's zone**, disclosed now rather than discovered at scoring |
| 🚨 **Threshold provenance, and why it is NOT from the options market** | The protocol prefers the implied move. **`module_flow` returns ±1.3% at an 08-26 (D0) expiry for a 09-02 event** (`D353`/`M948`), so **no implied move for this event is readable today.** Thresholds are the measured 252-day tails, labelled as such. ⚠ **Obligation: re-derive from the 09-04-or-later straddle at the 08-28 or 08-31 run, before the print.** Until then this row is **MEDIUM-RESOLUTION** and is labelled so rather than quietly scored as if measured |
| **Information content (`B4`)** | **Both branches change a conclusion, asymmetrically and it is graded here**: **A** falsifies the `IT UW` on the desk's own held epicenter name and would say the −24.1 `rs60` was a discount; **B** falsifies the `AI-compute-EPICENTER` label on a name the book holds and turns `S116`'s Alphabet dual-sourcing story from a relative-share question into an absolute one. ⚠ **`B4` note**: `C` at ~70% is genuinely likely and is **not** informative — this row is worth the slot only because A and B are both thesis-breaking |
| **Positioning at registration** | Short **1.2% float, covering**, DTC 3.0 · **P/C 2.75 (put-heavy = hedged/fearful)** · skew **−5.3** · FINRA **z −2.17, 5v5 −10.4▼ = the board's LARGEST short exit**. ⚠ **The option book and the daily short tape disagree in tone** (puts bid vs shorts leaving) and that is stated, not averaged |
| **Anti-signal (VOID)** | an **announced acquisition of or by `AVGO`**, or a **trading halt ≥1 full session**, inside 2026-09-01 → 2026-09-08. ⚠ **Base rate checked (`D300-KR`)**: no such action is scheduled; **the print itself is deliberately NOT an anti-signal — it is the event** |
| **Non-redundancy (`D343`)** | 09-08 carries the Canada tariff effective date, `MSTR` revive and six ledger rechecks — **no other price bracket settles there.** `S116` (08-28) is a **relative** `MRVL − AVGO` spread settling **before** this print; `S127` is `AVGO` **absolute**, **after** it. They can disagree and the disagreement is the point |
| **Owner** | `industry_US` |

### 2d · ✅ NEW REGISTRATION — `S128`: the against-us bracket on this desk's own strongest measured signal

**`S128` — ★★★ The `rs60` reversal cell says the board's decayers should lose. 39% of the board is a decayer, and so is a third of the book.**

| Field | Value |
|---|---|
| **Why it exists** | `HANDOVER §5`: **`rs60` h=5 is this desk's strongest US IC cell — `t(NW) −5.46`, `n_eff 5.4`, mean IC −0.1310, positive in only 7% of 27 run-dates, Bonferroni-passing NEGATIVE.** The desk has **never tested it out of sample**, and it points directly at the book: sorting today's 299 names gives **35 "reversal" names (`rs20 > 0 ∧ rs60 < 0`) against 117 "decay" names (`rs20 < 0 ∧ rs60 > 0`) — 39% of the entire US board is a decaying stock of past excess** (`M949`), and **4 of the 11 holdings sit in the decay basket** (`MET`, `NDAQ`, `RTX`, `XOM`) against **1 in reversal** (`NVDA`) |
| **Frozen observable** | `EW{35 reversal names}` **minus** `EW{117 decay names}`, **5-session returns**, settled closes, **both baskets frozen exactly as printed by this run's sweep**, at the **2026-09-09** close. *(Baskets listed in full in `SECTOR_FLOW_US.json`; membership rule: `rs20 > 0 ∧ rs60 < 0` vs `rs20 < 0 ∧ rs60 > 0`, asof the 2026-08-26 sweep)* |
| **Branch A (the ledger's sign is real out of sample)** | spread **≥ +5.433pp** (trailing-252 **p85**) — reversal beats decay past the measured tail |
| **Branch B (the ledger's sign is a regime artifact)** | spread **≤ −3.158pp** (trailing-252 **p15**) — decay wins, and the `rs60` cell's negative sign does not survive its own window |
| **Branch C** | between |
| **`D93` executed BEFORE freezing** | trailing 252 of the same spread: mean **+1.268** · sd **4.552** · p05 −6.833 · **p15 −3.158** · p50 +2.131 · **p85 +5.433** · p95 +7.154 ⇒ **A ≈15% · B ≈15% · C ≈70%.** ⚠ **The estimator's centre is +1.268, NOT zero** — the reversal basket has beaten the decay basket on average over the window, so branch A is **not** the "surprising" side and the row says so before the fact |
| **State at registration** | **−0.556 = the 31.7th percentile** — currently mildly favouring decay, i.e. **against** the ledger's sign. Disclosed now |
| **⚠ `S6` / `D4` — the honest caveat, written INTO the row** | the IC ledger's window contains the 08-24 rotation, i.e. **a reversal regime**, so a reversal IC measured inside it may not generalise. **This row is the out-of-sample test, and it can fail.** That is the point |
| **Information content (`L3`/`B4`)** | **A** says the desk's own measured axis holds prospectively and that the book's 4 decay names are on the wrong side of it. **B** kills a Bonferroni-passing cell before it becomes doctrine — which is the **more valuable** outcome, because it prevents a false signal from being wired into selection. **Neither branch merely confirms** |
| **Anti-signal (VOID)** | an index-level shock (`SPY` 09-01 → 09-09 beyond **±5%**), or a **GICS/index reconstitution removing ≥5 names from either basket** inside the window. ⚠ **MSCI's 08-31 quarterly review is checked and is explicitly NOT a voider** — it falls before the window opens and does not change GICS assignment |
| **Non-redundancy (`D343`)** | 09-09 carries `P102` (a two-name Materials spread) and `S58-KR`. **`S128` is the board's only cross-sectional factor row** and is orthogonal to `P101`'s sector-internal software−hardware cut |
| **Owner** | `industry_US` |

⚠ **What Lens 2 could NOT independently check**: it could not verify the `CATALYST_WATCH` rows it did
not add by hand. It found **two** missing binaries (`MRVL`, Jackson Hole) **by reading the desk's own
`SCENARIOS_US.md`**, not by an independent calendar pull. **There may be a third it cannot see.**

---

## Lens 3 — MOMENTUM-CONTINUATION: *"already ran"* ≠ avoid. Re-tag every runner.

**All flow figures asof the 2026-08-26 sweep. ⚠ `vol_surge` is pre-market-depressed on every row (`D355`).**

| Name | `rs20` / `rs60` | OBV · `vol_surge` | Re-tag | Flip condition |
|---|---:|---|---|---|
| `MPC` (held) | **+10.4 / +36.8** | 매집 · 0.83 | **EXTENDED-BUT-LIVE** — refining `exc20 +11.60` at **100% participation**, `M879`'s ordering replicated a 5th time | **`P100` branch B** (`exc20` ≤ −4.731 at the 09-01 close), or OBV → 분산 on two consecutive **settled** sweeps |
| `PSX` (held) | +9.9 / +30.9 | 매집 · 0.75 | **EXTENDED-BUT-LIVE** | same |
| `VLO` | +7.7 / **+33.6** | 매집 · 0.57 | **EXTENDED-BUT-LIVE** — the purest refiner and the only one the book does not own | same |
| `NEM` | **+40.1** / +21.5 | 매집 · 0.87 | ⚠ **EXTENDED — approaching EXHAUSTED.** `exc20 +44.28`, at/above consensus target within 3.5% of its 52-week high, current-year revisions **1↑/11↓ on a −9.6%/90d cut** (carried `[measured]` from 08-25) | **`P102` branch B** (spread ≤ +2.505 at the 09-09 close) |
| `FCX` | **+27.6** / +17.6 | 매집 · **1.16** | **EXTENDED-BUT-LIVE** — the only one of the two with a surge above 1.0 on this bar | **Copper COT falling out of the 100th percentile**, or `P102` branch B |
| `MRVL` | **+41.7 / +8.2** | 매집 · 0.60 | **EXTENDED-BUT-LIVE** — 🟢, and the cycle KPI (the Alphabet dual-source) is **still accelerating**; **prints in 1 session** | **`S116` branch B** (`MRVL − AVGO` ≤ −12.0pp on 08-28) |
| `DE` | −1.0 / **+16.2** | 중립 · **1.37** | **EXTENDED-BUT-LIVE**, and one of only **four** names on the entire board clearing `vol_surge` ≥ 1.2 today | Canada tariff suspension; on the missed ledger, recheck 09-08 |
| 🚨 `MSTR` | **+28.9 / −17.5** | 매집 · 1.49 | 🚨 **RE-TAG: NOT A RUNNER — a violent 20-day reversal off a NEGATIVE 60-day.** The shortlist called it *"✅ clean rise"* on FINRA `z −0.60`; **that reads the short book, not the trend** | `rs20` crossing back below 0 while `rs60` stays negative |
| 🚨 `ORCL` | **+22.6 / −40.5** | 매집 · 0.41 | 🚨 **RE-TAG: NOT A RUNNER, and the most extreme case on the board** — `rs60 −40.5` is the worst 60-day reading of any 🟢 name. Its `vol_surge` **0.41** is also the lowest of the six greens | same |
| `MU` | +21.4 / −10.8 | 매집 · 0.45 | **REVERSAL, not continuation** — same shape, memory complex | — |
| `COIN` | +9.9 / **−0.2** | 매집 · **1.56** | **EARLY, not extended** — `rs60` ≈ 0 means there is no stock of past excess to decay | `rs20` < 0, or the debasement thread's kill (`EVENT_ALPHA` Card 4) |
| `RTX` (held) | **−7.1 / +19.8** | 중립 · 0.76 | 🚨 **EXHAUSTED-SHAPE (`C6-bis`): a decaying stock of past excess** — positive 60-day, negative 20-day | `rs20` crossing back above 0 |
| `MET` · `NDAQ` (held) | −5.7 / +16.7 · −1.4 / +5.7 | 중립 · 0.75 / 0.63 | **EXHAUSTED-SHAPE**, same class | same |
| `AVGO` (held) | −9.4 / **−24.1** | 중립 · 0.69 | **EXHAUSTED / DE-RATING** — negative on **both** windows, the worst of the 11 holdings | **`S127` branch A** (registered above) |
| `AAPL` | −13.0 / +0.7 | 🔴 분산 · 0.54 | **EXHAUSTED** — flow −0.914; filed to `missed_ledger` this run | — |

### ★★ The lens's own headline, and it is about the book, not about any one name
**Sorting all 299 names by the two windows: 35 names are `rs20 > 0 ∧ rs60 < 0` (reversal) and
117 are `rs20 < 0 ∧ rs60 > 0` (decay). 39.1% of the entire US board is a decaying stock of past
excess** (`M949`) — `C6-bis` at board scale, which the desk has only ever measured name-by-name.
🚨 **And the book is skewed to the losing side of its own measured signal**: **4 of 11 holdings sit in
the decay basket** (`MET`, `NDAQ`, `RTX`, `XOM`) against **1 in reversal** (`NVDA`), while
`HANDOVER §5` records `rs60` h=5 at **`t(NW) −5.46`, Bonferroni-passing NEGATIVE**.
⇒ **`S128` (registered above) is the out-of-sample test of exactly this**, and it is deliberately
written so that it can **kill** the desk's own strongest cell.

⚠ **What Lens 3 could NOT independently check**: every `vol_surge` above is depressed by the
pre-market bar (`D355`), so **no re-tag rests on a surge value** — they rest on `rs20`/`rs60`/OBV.
And it could not verify the revision books (`NEM` 1↑/11↓) against a fresh pull; those are **carried
`[measured]` from 08-25** and are tagged as inherited.

---

## Lens 4 — CYCLE-EXPOSURE: registry vs coverage vs the REAL book

`cycle_exposure --json` (this run): **AI-compute / semiconductors rank 1 — epicenter 16.69%** (need
≥12.0) via `NVDA`, `ANET`; **Energy / oil-refining rank 2 — epicenter 9.7%** (need ≥8.0) via `MPC`,
`PSX`; **Missile-defense rank 3 — 3.83%** via `RTX`, no threshold set. Book total ≈ **$11,022**,
invested **$5,135**. Tool verdict: **✅ no GAP.**

### 🚨 The ✅ is downgraded to **⚠ UNDER-DETERMINED**, and the reason is 12 runs old
`cycle_registry.json` **still has no optical / interconnect row** (`D250`/`M731`, **12th run**) — on a
run where `COHR` and `LITE` were the **largest single contributors** to `P79` entering branch B, and
where both sit in this run's **reversal basket**. **An epicenter percentage computed against a
registry that omits a live chain layer cannot say "no gap."** Handed to ALPHA's action bracket as an
open flag, not a pass.

### 🚨 The correlated-UW check — and this run it fires the OTHER WAY
The 2026-07-15 field note tells this lens to watch for **two UW sectors that are one bet in disguise**.
**This run has the inverse defect: three underweights the desk treats as one bet that are measurably
three.** `UTIL UW−` + `RE N−` + `STPL N−`, plus `HLTH OW`, are the four legs of `P78`, and
**`P78`'s pre-settle range is 4.889pp — ABOVE its own branch-A line of 4.71** (`XLV +3.476` ·
`XLRE +1.836` · `XLP +1.299` · `XLU −1.412`). `S108` settled the same way: **three legs 2.46pp apart
with one negative**, so its premise (*"the three underweights rip together"*) **was not observed even
in the branch that fired.**
⇒ **Two independent registered observables now say the duration complex is dispersing** (`M950`).
**This is why `RE` took the R1 DEEP slot**, and it is the single strongest argument in this file
against the desk's own tilt: **a UW built on a complex that no longer moves as a complex is four
separate bets wearing one label, and only one of them (`UTIL`) is supported on both instruments.**

### Cleanest epicenter expressions (named, not sized — P4)
- **AI-compute**: held `NVDA` (epicenter, prints tonight) and `ANET` (networking). The **un-labelled
  chain layer** the registry misses is `COHR`/`LITE`/`HPE`; the 08-25 desk put true any-layer
  exposure at **≈38.4%** on that basis, against the registry's printed 20.46%.
- **Energy/refining**: held `MPC`, `PSX`; the un-held pure-play is `VLO` (`rs60 +33.6`, OBV 매집).
- **Missile-defense**: held `RTX` — but it is an **EXHAUSTED-SHAPE** name (Lens 3) and the registry
  sets **no minimum**, so "3.83%" is neither a pass nor a fail. ⚠ Blind-spot `burst` put
  **`NORTHROP` at z 4.0 (5 articles, 2 outlets)** this run with no desk object attached.

⚠ **What Lens 4 could NOT independently check**: it read `cycle_registry.json` as given. **It cannot
know which cycles are missing** — only that one specific omission (optical/interconnect) has been
logged 12 times. **A registry's blind spot is invisible from inside the registry**, and that is the
strongest argument for the parallel-agent fan-out this run did not have.

---

## Synthesis — what this stage changes

| Output | Result |
|---|---|
| **Under-computed legs** | **0 promoted to a 5th DEEP.** 4 logged as WITHIN-RUN-WATCH (`IT`, `INDU`, `STPL`, `MATR`), each naming the row that already owns it. **Nothing silently dropped** |
| **Both-sides brackets** | **8 binaries audited. 6 already bracketed (one of them 7×). 1 unbracketable by this stage (`S8`, undated — human call). 1 REGISTERED: `S127` (`AVGO` 09-02).** Plus **`S128`**, the against-us test of the desk's own strongest measured cell. **Both new rows are two-sided with `D93` baselines computed before freezing** |
| **Momentum re-tags** | **2 names re-tagged AGAINST the shortlist's own reading** — `MSTR` and `ORCL` are **reversals, not runners** (`rs60` −17.5 and −40.5). 4 held names tagged **EXHAUSTED-SHAPE**. Board-level finding `M949`: **39.1% of the universe is a decayer** |
| **Cycle GAP** | **✅ → ⚠ UNDER-DETERMINED** (`D250`, 12th run — no optical/interconnect row). Handed to ALPHA |
| **Correlated-UW** | 🚨 **Fires in the inverse direction**: three underweights treated as one bet are measurably dispersing (`P78` above branch A, `S108`'s legs 2.46pp apart) — `M950`. **This is the file's strongest argument against the run's own tilt** |
| **DEEP set** | **UNCHANGED: continuous [ENRG, HLTH] · rotating [RE, FIN]. No 5th slot promoted**, with the reason stated (the `IT` question is *"which instrument"*, which `P101` settles, not a deep dive) |

### Did all four lenses agree with the draft? **No — and the disagreements are named**
- **Lens 2** found an **open registration obligation on a held name** (`AVGO`) that had survived two
  runs, and an **instrument failure while checking it** (`D353` reproducing on `AVGO`, `M948`).
- **Lens 3** **contradicted this run's own shortlist** on two of its six names (`MSTR`, `ORCL`
  re-tagged from *"clean rise"* to *"reversal"*), and found the book skewed to the losing side of the
  desk's own Bonferroni-passing cell.
- **Lens 4** found the **correlated-UW pattern firing inverted** and downgraded the cycle-exposure ✅.
- **Lens 1** alone agreed with the draft, and it is also the lens with **no independent evidence**.

### New measured rows — `M948` – `M950`
| id | Fact | Tag |
|---|---|---|
| **`M948`** | 🚨 **`D353` reproduces on a HELD name 7 days later, exactly as `D353` predicted.** `module_flow AVGO --positioning` returns **`예상변동 ±1.3% (만기 2026-08-26, D0)`** for a company that **prints 2026-09-02** — the straddle expires **seven days before the event**. `D353`'s own text: *"a defect that heals itself on a calendar roll will re-appear on the next same-day expiry."* **Remedy still unimplemented** | `[measured]` |
| **`M949`** | **39.1% of the US board is a decaying stock of past excess.** Sorting all 299 scored names on the 2026-08-26 sweep: **35 reversal** (`rs20 > 0 ∧ rs60 < 0`) vs **117 decay** (`rs20 < 0 ∧ rs60 > 0`). **4 of the 11 book holdings sit in decay** (`MET`, `NDAQ`, `RTX`, `XOM`) against **1 in reversal** (`NVDA`), while the desk's `rs60` h=5 IC cell reads `t(NW) −5.46`, Bonferroni-passing NEGATIVE. 252-day spread distribution: mean **+1.268**, sd 4.552, p15 −3.158, p85 +5.433; current **−0.556 = 31.7th %ile** | `[measured]` |
| **`M950`** | **Two independent registered observables now say the "duration complex" disperses.** `P78` pre-settle range **4.889pp — ABOVE its branch-A line of 4.71** (`XLV +3.476` · `XLRE +1.836` · `XLP +1.299` · `XLU −1.412`); `S108` settled with its three legs **2.46pp apart and one negative**, so its own premise was not observed even in the branch that fired. ⇒ the correlated-UW field note fires **inverted**: three underweights treated as one bet are measurably three | `[measured]` |

---

## ✅ EXIT CHECK
- [x] 4 lenses run, each returning **named tickers and dated catalysts**. ⚠ **NOT a parallel agent
      fan-out** — declared session constraint, 9th run, with each lens stating what it could not
      independently check.
- [x] **Every bracket names its observable + frozen threshold + date, with BOTH branches.** Two new
      rows (`S127`, `S128`), both two-sided, both with `D93` baselines computed **before** freezing.
      **No one-way bracket.** To be written into `handoff/SCENARIOS_US.md` and indexed in the
      `SCENARIOS.md` spine at run end (§handoff).
- [x] **Every magnitude threshold stated against the implied move.** `NVDA` ±6.0% (D2, spans the
      event) · `MRVL` ±10.3% vs `S116`'s ±12.0 thresholds ⇒ **outside, information-carrying** ·
      🚨 **`AVGO` ±1.3% at a D0 expiry is UNREADABLE for its 09-02 print and `S127` says so at
      registration** rather than presenting it as a trigger.
- [x] **Each branch graded by information content.** The `NVDA` binary was **declined** as a new
      registration with the reason stated (seven rows already own it, `D343`); July PCE declined
      (nine rows on 08-28); `S127`'s branch C explicitly graded as **not** informative.
- [x] `BLINDSPOT_PREMORTEM.md` written — legs · brackets · re-tags · cycle GAP.
- [x] Every catalyst-bearing leg **promoted or logged** (4 WITHIN-RUN-WATCH, 0 promoted, 0 dropped);
      brackets handed to ALPHA; the **⚠ UNDER-DETERMINED** cycle flag handed to BET.
- [x] **DEEP set stated and unchanged**: continuous `[ENRG, HLTH]` · rotating `[RE, FIN]`, N = 4/4,
      no 5th slot, reason given.
