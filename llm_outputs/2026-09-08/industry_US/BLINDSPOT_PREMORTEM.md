# BLINDSPOT_PREMORTEM — industry_US · 2026-09-08 (Tue) · Stage 7 / L1·PREMORTEM ★US-only

> Four adversarial lenses argue **against this run's own tilt** before the deep budget is committed.
> **P4 — no sizing, no buy/sell language. Brackets are pre-registrations, not recommendations.**

⚠ **Declared runtime constraint, 9th consecutive run.** The four lenses ran **serially and
in-context**, not as a parallel adversarial agent fan-out. This session's harness forbids spawning
subagents unless the operator asks, and the operator is absent (scheduled run). Resolved the way the
09-01 → 09-07 runs resolved it — **stated, not hidden**, because a serial in-context lens shares the
author's priors in a way a fan-out does not, and that is a real weakening of this stage.

## 0 · The binaries this stage must bracket (from `CATALYST_WATCH.json`, `--days 10`)

| when | event | inside 48h? | status entering this stage |
|---|---|:--:|---|
| **2026-09-10 08:30 ET** | **US Aug PPI** | 🚨 **YES** (T+2 sessions, ~47h) | **DROPPED on information grade (`B4`) for FOUR consecutive runs, BY DECISION** |
| 2026-09-10 | **ECB rate decision** | no (T+2) | **absent from `catalyst_calendar`** (`D562`, 2nd run) |
| 2026-09-10 16:00 ET | **`ORCL` + `ADBE` prints** | no | calendar prints *"(none in window)"* (`D560`) |
| 2026-09-11 | US Aug CPI | no (T+3) | bracketed |
| **2026-09-16** | **FOMC decision + SEP / dot plot** | no (T+6) | 🚨 **`D564` LIVE COMMITMENT — must be registered by the 2026-09-11 run at the latest** |
| 2026-09-18 | S&P quarterly rebalance = quadruple witching | no (T+8) | **unbracketed** (`D533`, 2nd run) |
| undated | Iran "Strait of Hormuz open" statement | — | it is `P145`'s VOID clause and `P122`'s KPI |

### 🚨 The PPI collision, resolved explicitly rather than inherited

The US runtime delta says: *any binary ≤48h ⇒ PREMORTEM MUST produce a both-sides bracket; a one-way
tilt into a known binary is a protocol violation.* This desk has **dropped PPI on `B4` four runs
running**, and that decision was taken **while PPI sat outside the 48-hour window**. **This is the
first run in which the two collide, and the collision is resolved in favour of the mandatory rule.**

The `B4` argument was *"PPI is a nowcast update to the CPI print one day later; neither branch would
change a verdict `P116`/`P120` do not already carry."* **That argument is weaker today than when it
was written**, for a reason this run measured rather than assumed: §A-2 shows **two-thirds of the
five-session 10-year move is REAL, not breakeven** — i.e. the desk's own instrument says the current
repricing is **not** an inflation event. **A PPI surprise is therefore the single cleanest available
test of that claim**, and both branches change it. ⇒ **bracket registered: `P147`.**

---

## Lens 1 — UNDER-COMPUTED LEGS

**Rule applied: a leg qualifies if it has a dated catalyst ≤ ~5 trading days AND was not deep-dived.**

| leg | catalyst ≤5 sessions | why it is under-computed | verdict |
|---|---|---|---|
| **Financials — the curve leg** | **PPI 09-10 · CPI 09-11 · FOMC+SEP 09-16**, three dated binaries in eight sessions | **Last DEEP 2026-08-30 = 6 runs, the 2nd-longest gap on the board.** Its own bracket **`P128` settles today and is BLOCKED by `D427` for a 6th run**, so nothing else will resolve it. `eqflow` **−0.023 = the flattest reading on the board**, `exc20` **+1.92 / median +1.04** positive, and the strongest fact standing for it (`hy_oas` **2.65** near a 1-year low with `nfci` easing six straight weeks) is a **macro** number ROTATION is forbidden to use — **which is exactly what a DEEP is for** | ★ **PROMOTE-TO-DEEP** (rotating slot ①) |
| **Industrials — the defense node** | **`S150` settles 09-14**; German/Japanese defense re-rating dated 09-07 | **Last DEEP 2026-08-31 = 5 runs.** Carries the board's **worst `exc20` (−5.04 mean / −5.90 median)** *and* the board's best-but-4×-restated Δ. 🚨 The book holds **`RTX`**, which `M1370` measured as the **worst name** in a node sitting at a **7.1st-percentile 5-session extreme with zero narrative carriage**. ★ **And this run's tape moved the story OUTSIDE the bracketed names**: *"Volkswagen finds defense role for German factory"*, *"Japanese banks in a bind over defense investments and loans"* — a re-rating happening in names `S150` cannot reach | ★ **PROMOTE-TO-DEEP** (rotating slot ②) |
| **Energy — the GAS/LNG sub-node** | QatarEnergy force majeure **into November**; Oct/Nov tender season now; **`P145` settles 09-14** | The physical dislocation is measured (Asia spot LNG **$24.614/MMBtu** 5-month high; TTF at a **3.5-year high**) and **the desk owns no instrument that can see it** — `LNG`/`GLNG`/`FLNG` are outside `us_top300`, so no sweep score, no shortlist, no `chain-hop` reach (`D563`) | **FOLDED INTO DEEP-ENRG's mandate as a named sub-node** — a sub-node cannot take a sector slot, and ENRG already holds one |
| **IT** | **`S140` settles on tonight's close**; `S148` 09-14 | Routed to PREMORTEM for an **8th** consecutive run — **but it was deep-dived yesterday (0 runs ago)** and `D566` measures its dispersion/move at **4.6×**, i.e. *there is no sector-level fact to attach a verdict to*. A second consecutive DEEP would re-derive the same non-answer | **WITHIN-RUN-WATCH** — `S140` settles it on its own tonight, without a DEEP |
| **UTIL / the AI-power lane** | **`P123` settles today**; `P127` 09-10; `S146` 09-14 | Also deep-dived yesterday (0 runs ago). ★ New evidence **strengthens the case but points one way**: **Thailand halted ALL data-centre builds** (dated 09-04, National Economic and Social Development Council) — a **second** jurisdiction after Texas. That is a *supply-side stop*, i.e. it argues **for** the existing N−, not against it | **WITHIN-RUN-WATCH** with a **BOOK FLAG on held `ETN`** (`[FINRA]` z **+2.16 🔴**) |

**Final DEEP set: `ENRG` · `HLTH` (continuous) + `FIN` · `INDU` (PREMORTEM-promoted rotating) = 4 of 4.**
★ **This is the first run in five in which all four slots are filled**, and the mechanism is the
promotion lens rather than the OW ranking — which is what the unfilled-slot offer exists to do.
⚠ **Stated plainly**: `FIN` is `N` and `INDU` is `UW−`. Neither is an OW promotion; both are
**question assignments**, and ROTATION's verdicts on them are unchanged.

---

## Lens 2 — REGIME-FLIP / BOTH-SIDES

### 2a · The against-us branch of this run's own tilt

**Our tilt**: `ENRG OW` (the board's only unambiguous OW, and the run's two strongest cards are both
energy-chain cards) + three duration underweights (`RE`, `STPL`, `UTIL`) + `FIN N`.

**What rips against it — the single event, and it is on the calendar as an undated 🔀binary:**
a **confirmed Iranian "Strait of Hormuz open" statement (the TACO trigger)**. Mechanism, all four legs
at once: Asia spot LNG and TTF unwind from 5-month / 3.5-year highs → `P145` fires **B** → the
refining scarcity premium compresses even though the 9.6 mb/d of destroyed capacity is unchanged →
**breakevens fall** → `T10YIE` 2.35 drops, the *real* leg of the 10-year move loses its supply
narrative, `DGS10` rallies → **all three duration UWs get relief simultaneously** (`D512`: they are
four legs of one bet, and `FIN` is the offset on the other side).
⇒ **A single de-escalation headline is a correlated hit across the entire board.** That is the
correlated-UW pattern this lens exists to catch, and it is **larger** today than the 09-07 run found
it, because the Energy OW's evidence base grew this run.

**Trigger**: ≥3 outlets carrying an Iranian statement of reopening, or a confirmed transit resumption.
**Invalidation of the invalidation**: the QatarEnergy force majeure is dated **into November** — a
reopening statement that does not lift the force majeure does **not** clear the physical dislocation,
and the desk should not treat the headline as the event (this is the `M1363` OPEC+ precedent).
**Starter list, named**: `LNG` `GLNG` `FLNG` `MPC` `PSX` `VLO` `SLB` `WMB` on the losing side;
`RE`/`STPL`/`UTIL` baskets on the relief side.

### 2b · Bracket coverage of every dated binary — no one-way tilt

| binary | bracket | both branches? |
|---|---|:--:|
| **US Aug PPI 09-10** | ★ **`P147` — REGISTERED THIS STAGE** | ✅ |
| **FOMC + SEP 09-16** | ★ **`P148` — REGISTERED THIS STAGE** (discharges `D564`'s live commitment three days early) | ✅ |
| US Aug CPI 09-11 | the 09-07 `TLT` row (window **09-09 → 09-14** deliberately contains it) + `P142` (DGS2, 09-11) | ✅ |
| ECB 09-10 | **`P146`** (MACRO, `EWG` − `SPY`, window 09-04 → 09-14 deliberately contains it) | ✅ |
| `ORCL` + `ADBE` 09-10 | `S148` (09-14) | ✅ |
| Hormuz reopening (undated) | it **is** `P145`'s VOID clause and `P122`'s KPI; §2a is its narrative bracket | ✅ |
| **S&P quad witching 09-18** | 🚨 **NONE — unbracketed for a 2nd consecutive run** (`D533`) | ❌ |

★ **The quad-witching gap is named, not silently carried.** It is **not** registered here because a
mechanical index-rebalance flow has no branch this desk's verdicts depend on (`B4`: neither outcome
would change a sector call) — **so it is dropped BY DECISION with the reason written**, which is the
distinction `D533` asks for. It is 8 sessions out and outside the mandatory 48-hour rule.

---

## Lens 3 — MOMENTUM-CONTINUATION: *"already ran"* is measured, not assumed

Method per `B3`/`R142`: normalise each 20-session return by the name's **own 20-day daily sigma**
(z = ret20 / (σ·√20)); a raw ranking is a volatility ranking in disguise. Settled closes to
**2026-09-04** (`D577` filter applied).

| name | raw ret20 | exc20 vs `SPY` | own σ(20d) | **z20** | z60 | re-tag |
|---|--:|--:|--:|--:|--:|---|
| **MPC** *(held)* | +30.42% | +30.81pp | 2.19% | ★ **+3.11** | +2.82 | **EXTENDED-BUT-LIVE** |
| **VLO** | +24.27% | +24.67pp | 1.82% | ★ **+2.99** | **+3.11** | **EXTENDED-BUT-LIVE** |
| **PSX** *(held)* | +25.10% | +25.50pp | 1.90% | ★ **+2.95** | +2.74 | **EXTENDED-BUT-LIVE** |
| **CTVA** | +14.36% | +14.75pp | **1.45%** | **+2.21** | +1.60 | ★ **QUIETLY EXTREME — on no watchlist** |
| **LNG** | +14.00% | +14.40pp | **1.55%** | **+2.03** | +1.73 | ★ **QUIETLY EXTREME — outside the universe** |
| **MSTR** | **+42.79%** *(board's largest raw)* | +43.18pp | **6.68%** | **+1.43** | +0.46 | **NOT extended** — inside its own regime |
| SLB | +13.81% | +14.21pp | 2.48% | +1.25 | +0.19 | live, not extended |
| HOOD | +30.89% | +31.29pp | 5.81% | +1.19 | +0.92 | **NOT extended** |
| DE | +11.71% | +12.11pp | 2.56% | +1.02 | +1.20 | live |
| DELL | +15.51% | +15.90pp | 5.27% | +0.66 | +1.02 | live |
| **NVDA** *(held)* | +2.86% | +3.25pp | 2.87% | **+0.22** | +0.67 | **flat — not extended, not exhausted** |

★★ **The raw and normalised rankings disagree on four names, and the disagreement is systematic.**
`MSTR` and `HOOD` are the board's #1 and #3 raw runners and are **inside their own volatility regime**
(z 1.43 / 1.19) — `R142` reproduced independently. `CTVA` and `LNG` look ordinary on raw and are
**2σ+ moves**, precisely because their sigmas are the board's smallest (1.45% / 1.55%). ⇒ **an
"already ran, avoid" screen built on raw returns is systematically wrong in both directions**, and it
is wrong *against* the two names with live catalysts (`LNG` — Card 1; `CTVA` — 🟢 in a `N−` sector).

**Flip conditions, stated now:**
- `MPC`/`PSX`/`VLO` **EXTENDED-BUT-LIVE → EXHAUSTED** if the cycle KPI stops accelerating: the
  `refinery` term Δ turns negative **two runs running** (today **+12.50%**, the board's top riser), **or**
  a confirmed restart of **≥3 mb/d** of Middle East refining capacity, **or** Russia lifts its diesel
  export ban. Until then a z≈3 with a physically-destroyed 9.6 mb/d behind it is **not** an exhaustion
  signal.
- `NVDA` is the one held name with **no momentum in either direction** — and its most recent catalyst
  thread (Hugging Face) is **ENDED** on both instruments (EVENT_ALPHA Card 8). **Book flag stands.**

---

## Lens 4 — CYCLE-EXPOSURE: the registry says no GAP, and the registry is the gap

`cycle_exposure` (REAL KIS book, read-only, this run): **AI-compute 17.57% epicenter vs need ≥12.0%
✅ · Energy/oil-refining 10.73% vs ≥8.0% ✅ · Missile-defense 3.64% ⚪ (no threshold set)** ⇒
**"No top-rank cycle GAP."**

🚨 **That verdict is true of the three cycles the registry knows and says nothing about the board.**

| cycle the tape carried this week | in `cycle_registry.json`? | measurable by any desk instrument? |
|---|:--:|---|
| **Gas / LNG dislocation** (Asia spot LNG 5-month high, TTF 3.5-year high, QatarEnergy force majeure into November, Oct/Nov tenders) | ❌ **no row** | ❌ **no** — `LNG`/`GLNG`/`FLNG` outside `us_top300`; `chain-hop`'s universe *is* `us_top300` (`D563`) |
| **AI-power / interconnect permitting** (Texas, now Thailand halting **all** builds) | ❌ **no row** (`D416`, carried) | partially — the lane spans **three GICS labels**, and `TLN`/`NRG` (inside `P127`'s **live** basket, settling 09-10) are **outside the universe** |
| Refining-capacity destruction (IEA 9.6 mb/d) | ✅ as "Energy / oil-refining" | ✅ |
| AI-compute | ✅ | ✅ |
| Missile-defense | ✅ but **no min% threshold set** | ✅ |

⇒ **Two of the week's three loudest cycles have no registry row, and for one of them the desk cannot
even measure its own exposure.** The `✅ no GAP` line is therefore **not** evidence the book is
positioned; it is evidence the registry is a three-row list. **Handed to BET as a stated limit on the
epicenter-starter module, and to the human as a registry-build item (`P5`).**
⚠ `C23` **12th reproduction** — this tool reads the book at **₩15,149,158 / $10,978** while
`module_paper_book status` reads **₩17,791,092** and `exposure_rule` stores **85.2%** invested with its
current-% **blank for a 3rd run**. Four legs, four numbers, none self-identifying.

---

## Registrations — both indexed in `handoff/SCENARIOS.md`, detail in `handoff/SCENARIOS_US.md`

### `P147` — ★★★ US Aug PPI 2026-09-10: is the repricing INFLATION, or is it real? · settle **2026-09-10**

| field | value |
|---|---|
| **Why it exists** | 🚨 **MANDATORY** — a 🔀binary inside 48h. And it is not a formality: §A-2 measured the five-session 10-year move as **+0.08 real vs +0.04 breakeven ⇒ two-thirds REAL**, i.e. the desk's own instrument says this repricing is *not* inflation. A PPI surprise is the cleanest available test of that claim, and **both branches change it** |
| **Frozen observable** | **`TIP` minus `IEF`, 1-session excess return**, settled closes, `auto_adjust=False`, **2026-09-09 close → 2026-09-10 close** (the PPI print falls inside) |
| **A (it IS an inflation event)** | excess **≥ +0.1514%** (trailing-252 **p85**) ⇒ the real-vs-breakeven decomposition in §A-2 is wrong and `P142`/the 09-07 `TLT` row are measuring the wrong driver |
| **B (it is REAL — supply / term premium)** | excess **≤ −0.1492%** (trailing-252 **p15**) ⇒ §A-2 confirmed; inflation-linked underperforms nominal into an inflation print, which is what a real-yield-led selloff looks like |
| **C** | between — disclosed favourite |
| **`D93` before freezing** | trailing 252 settled sessions to **2026-09-04** (`D577` filter): mean **+0.0023** · sd **0.1760** · p05 −0.2712 · **p15 −0.1492** · p50 +0.0054 · **p85 +0.1514** · p95 +0.2612 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **+0.0045 = the 49.2nd percentile — dead centre.** ★ The most balanced starting state this desk has registered; **both branches carry equal information**, which is exactly what `B4` asks for and is the reason the four-run `B4` drop no longer holds |
| **Implied move** | 🚫 **UNCHECKED, and the reason is recorded rather than papered over.** The observable is a **spread**, and no straddle prices a spread. The leg straddles are `TIP` **±0.7% (expiry 2026-09-18, D10)** and `IEF` **±0.6% (expiry 2026-09-11, D3)** — both bound each leg's **total path to expiry**, not the spread's isolated 1-session move. Borrowing them would be the scope error `M1305` made and `M1326` corrected (`C3`) |
| **Anti-signal (VOID)** | a **BLS delay or methodology restatement** of the August PPI release; an **unscheduled Federal Reserve action**; or a **market-wide trading halt** on 09-10. ⚠ Base rate pre-stated: the ECB decision and the `ORCL`+`ADBE` prints also land on 09-10 — **neither is a voider**, they are same-day noise the 1-session window deliberately accepts, and this is written now so it is not improvised at scoring (`D450`) |
| **Track KPI** | under **B**, `T10YIE` (the one unblocked `[FRED]` series) does **not** rise more than 3 bp on the first close covering 09-10 |
| **Information content (`B4`)** | **A falsifies §A-2's central measurement and the driver behind three live rows; B confirms it against the single strongest counter-hypothesis.** Both change a conclusion ⇒ the bracket is worth its slot, which was not true when PPI was dropped four runs running |
| **Non-redundancy (`D343`)** | `P142` measures the **DGS2 level** and settles 09-11 (after CPI, so PPI is not isolable in it) · the 09-07 `TLT` row opens **09-09** and spans PPI **and** CPI **and** the ECB together · `P128` is credit · `P124` the dollar. **No row isolates the PPI print, and none measures the real-vs-breakeven split at all** |
| **Owner** | `industry_US` |

### `P148` — ★★★ FOMC + SEP 2026-09-16: does the dot plot move the CURVE, or only the level? · settle **2026-09-17**

| field | value |
|---|---|
| **Why it exists** | 🚨 **`D564`'s live commitment, discharged three days before its deadline.** Deferred on 09-06 and 09-07 with valid reasons; the deadline is the 2026-09-11 run, and **CPI prints 09-11**, after which the pre-print information is in the base and the row loses what it exists to capture. Registering today is the last clean pre-CPI opportunity |
| **Frozen observable** | **`IEF` minus `SHY`, 2-session excess return** (belly minus front end = the **curve-slope** proxy), settled closes, `auto_adjust=False`, **2026-09-15 close → 2026-09-17 close** (the decision and the SEP fall inside; the start is 09-15 so the pre-meeting positioning day is in the base) |
| **A (hawkish dots — the curve FLATTENS)** | excess **≤ −0.3469%** (trailing-252 **p15**) ⇒ the front end sells off harder than the belly; the three duration underweights (`RE`/`STPL`/`UTIL`) are **vindicated on the Fed's own path**, and `P142`'s front-end read extends |
| **B (dovish dots — the curve STEEPENS)** | excess **≥ +0.2856%** (trailing-252 **p85**) ⇒ the front end rallies; `RE`+`STPL`+`UTIL` lose their driver **together** (`D512` — they are four legs of one bet) and `FIN N` is the offset |
| **C** | between — disclosed favourite |
| **`D93` before freezing** | trailing 252 settled 2-session windows to **2026-09-04**: p05 −0.5119 · **p15 −0.3469** · p50 −0.0199 · **p85 +0.2856** · p95 +0.4615 ⇒ **A ≈15% · B ≈15% · C ≈70%** |
| **State at registration** | **+0.0147 = the 54.0th percentile — near dead centre**, marginally nearer B. Disclosed now, not discovered at scoring |
| **Implied move** | 🚫 **UNCHECKED, with the reason.** `SHY` reads **±0.5% (expiry 2026-09-18, D10)** and `IEF` **±0.6% (expiry 2026-09-11, D3)** — **`IEF`'s straddle EXPIRES FIVE DAYS BEFORE THE EVENT**, and neither prices a spread. Labelled unchecked rather than "outside the priced move" (`C3`, the `S141` precedent) |
| **Anti-signal (VOID)** | an **unscheduled Fed action or emergency facility** before 09-16; a **change to the FOMC release date**; a **US bank failure**; or **`hy_oas` ≥ 3.10% on a close** inside 09-15 → 09-17 (then it is a credit event and the rate attribution is void — the clause `S19`/`S41`/`S46` all used) |
| **Track KPI** | under **A**, `DGS2` (if `D427` ever unblocks) rises more than `DGS10`; under **B**, `bond selloff` `theme-age` falls back inside 2× from today's **4.18×** |
| **Information content (`B4`)** | **B falsifies three live sector verdicts at once; A defends them on the single strongest external cause.** Both change a conclusion. ★ **And B is the informative branch** because the registration state already sits on B's side of the median |
| **Non-redundancy (`D343`)** | `P142` = the **DGS2 level** (front end, settles 09-11, **before** the meeting) · the 09-07 `TLT` row = the **long end** (settles 09-14, **before** the meeting) · `P143` = the **yen** (09-14) · `P124` = the **dollar** (09-18) · `P147` = the **real-vs-breakeven split** (09-10). 🚨 **Nothing on this board measures the SLOPE, and nothing at all spans the FOMC date** — both gaps close here |
| ⚠ **Dependency note** | this observable is deliberately **ETF-based, not `[FRED]`-based**, because `D427` has blocked `DGS2`/`DGS10`/`DFII10` for **six consecutive runs** and `R147` retracted the desk's model of when they unblock. A bracket that cannot be settled is not a bracket |
| **Owner** | `industry_US` |

---

## Hand-offs

- **→ DEEP (Stage 8)**: final set **`ENRG` · `HLTH` · `FIN` · `INDU`**, 4 of 4 filled for the first
  time in five runs. Mandates: ENRG = *"which cut of the crack complex is the driver, and does the
  gas/LNG sub-node the universe cannot see belong to it?"* · HLTH = *"breadth, or absence of a
  driver?"* · FIN = *"the curve leg, with `P128` blocked — resolve it on something that settles"* ·
  INDU = *"the defense node is at a 7.1st-percentile extreme with zero narrative and the book holds
  its worst name, while the re-rating shows up in German and Japanese names `S150` cannot reach."*
- **→ ALPHA (Stage 10)**: both-sides brackets `P147` (09-10) and `P148` (09-17); the §2a correlated-hit
  trigger (a confirmed Hormuz reopening ⇒ four legs move together).
- **→ BET (Stage 9)**: the cycle-registry limit — **`✅ no GAP` covers three cycles, and two of the
  week's loudest have no row**; the epicenter-starter module must say so rather than read the ✅.
- **→ the human (`P5`)**: (1) build registry rows for the gas/LNG and AI-power cycles; (2) the US DEEP
  budget question (`M1371`, n=5); (3) `C23`'s four disagreeing book legs; (4) `D533`'s quad-witching
  gap, dropped by decision here but still a gap.

---

## ✅ EXIT CHECK — PREMORTEM

- [x] **4 lenses run, each returning named tickers + dated catalysts** — ⚠ **serially and in-context,
      not as a parallel fan-out**, declared at the top with the reason (9th consecutive run).
- [x] **Every bracket names its observable + frozen threshold + date and carries BOTH branches**;
      `P147` and `P148` are registered in `handoff/SCENARIOS_US.md` and indexed in the
      `handoff/SCENARIOS.md` spine. **No one-way bracket.**
- [x] **Every magnitude threshold stated against the implied move** — both are labelled
      **`implied-move UNCHECKED`** with the specific reason (spread observables; `IEF`'s straddle
      expires before `P148`'s event), not silently omitted and not falsely claimed as "outside".
- [x] **Each branch graded by information content**; the one binary where **no branch would change a
      conclusion (quad witching 09-18) is DROPPED with that reason written**.
- [x] `BLINDSPOT_PREMORTEM.md` written — legs · brackets · re-tags · cycle GAP.
- [x] **Every catalyst-bearing leg promoted or logged**: 2 promoted (`FIN`, `INDU`), 1 folded into an
      existing mandate (gas/LNG → DEEP-ENRG), 2 logged as WITHIN-RUN-WATCH with reasons (`IT`, `UTIL`).
      **Nothing silently dropped.**
- [x] **DEEP set updated and stated: `ENRG` · `HLTH` · `FIN` · `INDU` — 4 of 4.**
- [x] 🚨 **The ≤48h binary (PPI 09-10) is bracketed both ways, and its collision with the desk's own
      four-run standing drop is resolved explicitly in the text rather than inherited.**
