# SECTOR_ROTATION — industry_US · 2026-09-08 (Tue) · Stage 6 / L1·ROTATION

> **Delta-only.** MACRO §E owns the 11-sector verdict; `SECTOR_FLOW_US.json` owns the flow numbers.
> Both are on disk. This file writes **only what changes and why it does not**.

## 🚨 Instrument state governing every number below

`SECTOR_FLOW_US.json §scoring` = `n_axes` **3** (`nonews`) · `vel_coverage` **17.39%** ·
`dropped_missing_axis` **0** · `scored` **299**.
⇒ **ZERO deltas below cite news velocity or theme freshness**, and no sector is called "quiet" —
**Utilities, Materials and Real Estate have zero velocity-measured names for a 5th run**. The cause was
**probed, not assumed** (8/8 alive pre-sweep with counts moving · 0/4 post-sweep · 2/2 after ~5 min of
probe-free idleness — `D553`'s own prescription followed).
Δ is legal on scale (prior same-mode snapshot 09-04, `3/nonews/299`) but is **the 09-03 → 09-04 change
for the FOURTH statement**. Cap weights are **55 days stale**, so **`eqflow` is the citable cut on
every line** (G5).

## §1 · Inherited — one line, verbatim from MACRO §E

**MACRO holds: `ENRG OW` · `HLTH N+` · `IT N` · `FIN N` · `UTIL N−` · `MATR N−` · `STPL UW` ·
`RE UW−` · `INDU UW−` · `DISC UW` · `COMM no-verdict`.**

## §2 · Deltas — **0 of 11**, and the reason is structural, not judgemental

`asof` **2026-09-04 for a FOURTH consecutive run**; all 11 sectors reproduce identically to the 09-07
file on **all 11 checked fields**. Every flow number available today was already spent producing the
09-05 run's three deltas (`ENRG OW→OW+`, `FIN UW→N`, `STPL N→UW`). Re-issuing them is double-counting;
issuing different ones is a claim the prior run misread; issuing on today's genuinely new evidence is a
**macro re-argument**, which this stage is forbidden to make.

⚠ **And today that is no longer a calendar result.** The 09-06 and 09-07 runs could say "the market was
shut". **Today the market is open** and the desk simply runs pre-open (`D572`). The zero is now a
property of **when this stage fires**, not of the week.

### 11 attempts, each declined with its number

| sector | attempted | flow number that decides it | why declined |
|---|---|---|---|
| **ENRG** | `OW → OW+` | `eqflow` **+0.562** rank 1 · breadth **0.19** rank 1 · **0 🔴 of 16** — the board's only zero-red sector · non-flipper (`XOM` 30.5% **holds it down**: +0.440 → ex-top1 +0.621) | **All three ranks are already 1.** The promoting argument available today is EVENT_ALPHA Card 2 (IEA: **9.6 mb/d = a fifth of Middle East refining capacity destroyed**; record-high margins; Russia's diesel export ban) — that is a **macro/news re-argument, declined and logged** |
| **HLTH** | `N+ → OW` | `eqflow` +0.190 rank 2 · Δ **−0.043**, wrong-signed and unchanged · `exc20` median +3.74 ≥ mean +3.63 | Δ is negative and has not moved; a promotion on an unchanged wrong-signed Δ is padding |
| **IT** | `N → UW` | `eqflow` −0.195 · `exc5` **mean −0.27 vs median +0.27** with **30 of 56 positive** · Δ −0.010 · non-flipper | **8th consecutive decline, 8th distinct reason.** Today's is `D566`: **dispersion / sector-move = 4.6×** ⇒ there is **no sector-level fact** to attach a verdict to, and `W5` bars an IT-wide call at that spread. Routed to PREMORTEM for an 8th run; **`S140` settles on tonight's close** |
| **UTIL** | `UW → N` *(MACRO holds N−)* | `eqflow` **+0.050** vs Δ **−0.037** — the two admissible axes disagree in sign · breadth 0.00 | **8th decline.** The breadth-0.00 reason is **dead** and was re-measured today: `CEG` flow **+0.644** / surge 0.96 / OBV 매집 is **🟡**, while `PG` flow **+0.231** / surge 0.89 is **🟢** — two gates by cap rank (`D561`, **2nd independent reproduction with the identical pair**). The surviving reason is the axis sign-disagreement alone. **RULE D6 exempt** — the OBV term here is part of a measurement *of the tag gate itself* (it shows the two names are alike on the non-news axes), not a verdict resting on OBV; no delta is issued either way |
| **MATR** | `N− → UW` | `eqflow` −0.107 · `exc20` median **−2.99** vs mean −0.85 · **3 of 12** positive on `exc5` | **Numbers DO support it — declined on `D343` for a 4th run: `P102` settles 09-09** asking whether MATR is two names. Moving one day early makes that row unscoreable as designed. `C24` carried a **9th** run (copper COT **100th %ile**, on 09-01 data) |
| **FIN** | `N → OW−` | `eqflow` **−0.023 = flat**, unchanged · Δ −0.004 · `exc20` +1.92 / median +1.04 | The promoting argument is `hy_oas` **2.65 near its 1-year low with `nfci` easing six straight weeks** — a **macro number**. **Declined as a re-argument for a 3rd consecutive run.** `P128` settles it 09-08, `D427` permitting |
| **STPL** | `UW → UW−` | `eqflow` −0.098 · breadth 0.05 · `exc5` median −2.29 | 🚨 **Declined on instrument grounds, measured again today**: the bucket's only 🟢 is `PG`, and `PG` is green **because rank 34 grants it a velocity** on an axis `scoring.vel_axis` declares **false**. A bucket whose green count is set by a dead axis may not carry a delta in either direction (`D561`) |
| **RE** | `UW− → lower` | **1 of 12** positive on `exc5` — the board's most internally consistent bucket · Δ **−0.020**, the 4th-smallest on the board | Consistent but **not newly worse**. ⚠ Its breadth 0.00 **is evidence, unlike UTIL's**: RE's four best names are all 매집 but top out at flow **+0.360**, below the gate on the **non-news** axes |
| **INDU** | `UW− → N` | board's best Δ **+0.082** · `exc20` **−5.04 / −5.90 = worst on the board** · 17 of 50 positive | The Δ is the **09-03 → 09-04 change for the fourth statement** and is not evidence of a turn (G2). ★ New tape argues the other way for the US names specifically: *"Volkswagen finds **defense role** for German factory"* and *"Japanese banks in a bind over **defense investments**"* — the defense re-rating is happening **outside** the names `S150` brackets |
| **DISC** | `UW → UW−` | 🚨**1名** `AMZN` **40.2%**, `top1_flips_sign` **True** (−0.255 → +0.068) ⇒ `wflow` **barred**. Admissible `eqflow` **−0.280 AGREES** and 21 of 28 are negative on `exc5` | Declined for **absence of change** — Δ moved **0.000** — not for absence of support |
| **COMM** | any verdict | 🚫 **barred, 18th run.** `D459` **14th** reproduction: Alphabet **76.6%** across `GOOGL`+`GOOG`, invisible to the per-ticker flag which prints `False`; ex-issuer `wflow` −0.389 → **+0.272**, swing **0.661**. `eqflow` **−0.035 = flat**; `exc5` **2 of 13** contradicts `exc20` median **+3.70** | A sector this desk cannot aggregate is a sector it does not rank |

**Macro re-arguments attempted and declined: 3** — ENRG on the refining-capacity destruction, FIN on
`hy_oas`, INDU on the stale Δ. Logged rather than dropped.

### Flip list — reproduced in full even though it changes nothing

| by | sector | owner | share | `wflow` → ex- | swing | bars a verdict? |
|---|---|---|--:|---|--:|---|
| **the flag** | Consumer Discretionary | `AMZN` | 40.2% | −0.255 → **+0.068** | 0.323 | **YES** — `wflow` barred |
| **by ISSUER (flag blind)** | Communication Services | Alphabet (`GOOGL`+`GOOG`) | **76.6%** | −0.389 → **+0.272** | **0.661** | **YES** — `wflow` barred |
| top-heavy but **NOT** a flipper | Energy | `XOM` | 30.5% | +0.440 → +0.621 | 0.181 | no — the sector is **more** positive without it |

⚠ Both `top1_w` figures rest on a **55-day-old** cap vector (G5). Where weighted and equal-weighted
disagree, the **equal-weighted** cut governs.

## §3 · DEEP picks + DEEP_LOG

**N = 4 (protocol: 2 continuous + 2 rotating). Filled: 2. Unfilled: 2.**

| slot | pick | rule applied |
|---|---|---|
| Continuous ① | **ENRG** | held the continuous slot in the previous run **and** is still the board's top OW today (`eqflow` rank 1, breadth rank 1, 0🔴) ⇒ **anti-thrash continuity keeps the slot** |
| Continuous ② | **HLTH** | same continuity rule; the only other sector with a positive `eqflow` (+0.190) and the only other positive `exc5`/`exc20` pair |
| Rotating ① | **— unfilled** | the board has **two** OW-or-better sectors and both hold continuous slots. **Padding with N/UW is forbidden** |
| Rotating ② | **— unfilled** | same |

★ **Both unfilled slots are offered to PREMORTEM promotion, with named candidates and reasons:**
- **IT** — routed for an **8th consecutive run**; the book's largest concentration; `exc5` mean and
  median **contradict** (−0.27 vs +0.27, 30/56 positive); **`S140` settles on tonight's close** and
  `S148` on 09-14; `D566` measures the sector's dispersion/move at **4.6×**.
- **UTIL / the AI-power lane** — its breadth-0.00 is a **measured filter artifact** (`D561`, second
  reproduction today with the identical `CEG`/`PG` pair); the lane spans **three GICS labels** so no
  single bucket can hold it (`D416`); **BOOK FLAG on held `ETN`** (`[FINRA]` z **+2.16 🔴**);
  **`P123` settles today**, `P127` 09-10, `S146` 09-14; and EVENT_ALPHA Card 3 adds **Thailand as the
  second jurisdiction to halt data-centre builds**.

🚨 **`N=2/4` for a FIFTH consecutive run — `M1371`'s n is now 5, and the protocol's own precondition is
met.** `pipeline/protocols/industry_us.md` says the US DEEP budget stays at N=4 unless *"it needs its
own measurement first"*. **Five consecutive runs in which the board could not fill more than two slots
IS that measurement.** ⇒ **Escalated to the human (`P5`); this stage does not change N.**

★ **DEEP mandates carried into Stage 8** (one question each, so DEEP does not start from a blank page):
- **ENRG** — *"Card 2 says the scarce asset is refining CAPACITY (9.6 mb/d destroyed, record margins,
  record-low inventories) while carried `M1361` says the distillate crack's 3-session rate is at the
  **6.0th percentile**. `D565` shows the sign flips with the window. Which cut is the sector's actual
  driver, and does `P140`'s 5-session leg measure it?"*
- **HLTH** — *"positive on every cut with median ≥ mean on both windows and 20 of 32 positive on
  `exc5` — is that breadth, or is it the absence of a driver? Name the driver or say there is none."*

## DEEP_LOG 2026-09-08: continuous=[ENRG, HLTH] rotating=[] · **N=2/4 filled for a FIFTH consecutive run — `M1371` n=5, the protocol's stated precondition for revisiting the US DEEP budget is now MET and is escalated to the human (P5); both empty slots offered to PREMORTEM with named candidates (IT, UTIL/AI-power lane)** · **verdict deltas 0 of 11 attempts — `asof` 2026-09-04 for a FOURTH run, all 11 sectors identical on all 11 fields, every flow number already spent by the 09-05 run; ★ and today the zero is NOT a calendar result — the market is OPEN and the desk runs pre-open (`D572`)** · **11 declines each with its number: `ENRG OW→OW+` (all three flow ranks already 1, 0🔴/16; the promoting argument — IEA 9.6 mb/d of Middle East refining capacity destroyed, record-high margins, Russia diesel export ban — is a macro/news re-argument, declined and logged), `HLTH N+→OW` (Δ −0.043 wrong-signed and unchanged), `IT N→UW` (**8th decline, 8th distinct reason**: `D566` dispersion/move **4.6×** ⇒ no sector-level fact exists to attach a verdict to; `exc5` mean −0.27 vs median +0.27, 30/56 positive; routed to PREMORTEM 8th run, `S140` settles tonight), `UTIL UW→N` (**8th decline, now on ONE reason** — breadth-0.00 is dead under `D561`, **re-measured today with the identical pair**: `CEG` flow +0.644/surge 0.96/OBV 매집 = 🟡 vs `PG` flow +0.231/surge 0.89 = 🟢, two gates by cap rank (**RULE D6 exempt** — this measures the tag gate, it does not rest a verdict on OBV; the decline stands on the axis sign-disagreement); surviving reason is `eqflow` +0.050 vs Δ −0.037 sign-disagreement), `MATR N−→UW` (numbers DO support it — `eqflow` −0.107, `exc20` median −2.99 vs mean −0.85, 3/12 — declined on `D343` **4th run** because **`P102` settles 09-09**; `C24` 9th run, copper COT 100th %ile on 09-01 data), `FIN N→OW−` (the `hy_oas` 2.65 near-1yr-low + `nfci` easing six straight weeks is a MACRO number — re-argument declined **3rd consecutive run**; `eqflow` −0.023 flat and unchanged; `P128` settles 09-08 if `D427` permits, and it did NOT today), `STPL UW→UW−` (🚨 declined on instrument grounds: the bucket's only 🟢 is `PG`, green because **rank 34 grants it a velocity on an axis `scoring.vel_axis` declares false** — `D561`), `RE UW−→lower` (1/12 positive = board's most consistent, but Δ −0.020 is 4th-smallest ⇒ not newly worse; ⚠ its breadth 0.00 IS evidence unlike UTIL's — RE's four best names are 매집 but top out at flow +0.360, below the gate on the NON-news axes), `DISC UW→UW−` (🚨1名 `AMZN` 40.2% flips −0.255→+0.068; admissible `eqflow` −0.280 AGREES and 21/28 negative on `exc5` — declined for **absence of change**, Δ 0.000), `INDU UW−→N` (board's best Δ +0.082 is the **09-03→09-04 change for the FOURTH statement**; `exc20` −5.04/−5.90 worst on the board; 🚨 new tape argues the other way for the US names — the defense re-rating is showing up in **Volkswagen's German factory** and **Japanese bank defense lending**, outside the names `S150` brackets), `COMM` any (**barred, 18th run**; `D459` **14th** reproduction, Alphabet 76.6% across two share classes invisible to the per-ticker flag, swing 0.661, `eqflow` −0.035 flat, `exc5` 2/13 contradicts `exc20` median +3.70)** · **macro re-arguments attempted and declined: 3 (ENRG on refining-capacity destruction, FIN on `hy_oas`, INDU on the stale Δ)** · **flip list: by the flag 1 of 11 (DISC/`AMZN`/40.2%); by ISSUER 2 of 11 (adds COMM/Alphabet/76.6%, swing 0.661 — 14th run); `XOM` at 30.5% of ENRG is top-heavy but NOT a flipper and is not a bar** · 🚨 **instrument state: `n_axes` 3 (`nonews`), `vel_coverage` **17.39%** ⇒ ZERO deltas cite velocity or freshness; cause PROBED (8/8 pre-sweep with counts moving, 0/4 post, **2/2 after ~5 min of PROBE-FREE idleness — `D553`'s own prescription followed, and the reading is consistent with the idle-timer hypothesis without settling it**); the wall sits at universe ranks **1–52** (49→50→52→52) and is visible only in universe-rank order (`D554`); cap weights **55 days stale** so `eqflow` is the citable cut on every line; ★★ **NEW THIS RUN — the price feed CHANGED STATE MID-RUN**: PREFLIGHT found no 09-08 bar at 09:10 ET and MACRO found a partial one at 09:5x ET, so tools called after the open silently take an incomplete bar as their last row (`D577`); every price number in this file is filtered to `index <= 2026-09-04`** · ★★ **the structural line: this is the THIRD consecutive zero-delta run and the FOURTH reading of session 2026-09-04, but the first in which the cause is the desk's own clock rather than a closed market. The run's new information is entirely non-price — MACRO §A (`D427`'s stated cause REFUTED, `R147`; the FRED split proven FRED-side by bypassing the module, `M1455`; `P141` falsified by its own pre-registered test, `M1456`) and EVENT_ALPHA (Asia spot LNG $24.614/MMBtu 5-month high, QatarEnergy force majeure into November, TTF at a 3.5-year high, IEA 9.6 mb/d destroyed, bunker shortfall 218 kb/d vs 6 kb/d in 2025, Thailand halting ALL data-centre builds)** · **uncovered=[IT(N, last DEEP 09-07 = 0 runs, routed to PREMORTEM 8th consecutive run, book's largest concentration, `D566` dispersion/move 4.6×, `S140` settles tonight · `S148` 09-14), UTIL(N−, last DEEP 09-07 = 0 runs, offered to PREMORTEM with the strongest case yet — `D561` reproduced today with the identical `CEG`/`PG` pair — AI-power lane spans 3 GICS labels `D416`, BOOK FLAG on held `ETN` FINRA z +2.16🔴, `P123` settles today · `P127` 09-10 · `S146` 09-14, and EVENT_ALPHA Card 3 adds THAILAND as the 2nd jurisdiction halting data-centre builds), MATR(N−, last DEEP 09-01 = 4 runs, `P102` settles 09-09, `C24` 9th run), COMM(no-verdict, last DEEP 09-01 = 4 runs, un-rankable by rule 18th run), INDU(UW−, last DEEP 08-31 = 5 runs, board's worst `exc20` AND board's best but 4×-restated Δ, `S150` 09-14, 🚨 the defense re-rating is appearing outside the bracketed US names), FIN(N, last DEEP 08-30 = 6 runs, `P128` 09-08 BLOCKED by `D427` for a 6th run · `S142` 09-11), STPL(UW, last DEEP 08-27 = 7 runs, 🚨 its green count is set by a dead axis — `D561`), DISC(UW, last DEEP 08-27 = 7 runs, 🚨1名 `AMZN` 40.2%, 21/28 negative on `exc5`), RE(UW−, last DEEP 08-26 = **8 runs, the longest recency gap on the board for a 5th consecutive run**, 1/12 positive on `exc5`, breadth 0.00 confirmed as EVIDENCE not artifact; NOT slotted because UW sectors do not take DEEP slots)]** · **MANDATORY brackets handed to PREMORTEM: (1) US Aug PPI 2026-09-10 08:30 ET is now INSIDE 48h of this run — the protocol's both-sides rule fires, and it COLLIDES with the desk's own 4-run standing `B4` drop of PPI, which PREMORTEM must resolve explicitly rather than inherit; (2) the ECB decision 2026-09-10, still ABSENT from `catalyst_calendar` (`D562`, 2nd run); (3) `D564`'s live commitment — the FOMC+SEP 2026-09-16 bracket must be registered by the 2026-09-11 run at the latest, and today is the third of four remaining opportunities**

---

## ✅ EXIT CHECK — ROTATION

- [x] **§1 is ONE line**, inherited verbatim from MACRO §E. No unchanged sector gets a paragraph.
- [x] **Delta count 0** — and per this stage's own rule that is a valid, informative output, not a
      reason to manufacture differences. Every one of the 11 attempts is logged **with its flow number**.
- [x] **Every macro-only argument was reverted and logged** — 3 attempts (ENRG, FIN, INDU).
- [x] 🚨 **No promotion or demotion rests on a `top1_flips_sign` bucket.** The flip list is reproduced
      in full (2 flippers: `AMZN` by the flag, Alphabet by issuer) even though it changes nothing, and
      `XOM`'s top-heaviness is explicitly distinguished from a flip.
- [x] **Axis count stated** (`n_axes` 3, `vel_coverage` 17.39%); no delta cites velocity or freshness;
      Δ is read only against a same-axis-count snapshot and labelled **09-03 → 09-04**.
- [x] Every matrix × flow divergence named with a resolution owner (DEEP mandates in §3; IT and UTIL
      routed to PREMORTEM).
- [x] **N DEEP targets by the rule** — continuity stated for both continuous slots, recency printed
      from files on disk, **no padding**, unfilled slots offered to PREMORTEM with named candidates.
      **Every uncovered sector is named in DEEP_LOG with its recency.**
- [x] **Linter run on this file** — `python -X utf8 scripts/report_lint.py llm_outputs/2026-09-08/industry_US/SECTOR_ROTATION.md` ⇒ first pass **2 findings**, both `D6` on the same `CEG`/`PG` instrument comparison; **fixed by carrying the rule ID and the exemption reason on both lines** (they measure the 🟢 gate's own behaviour, they do not rest a verdict on OBV — and the sector delta is **declined**, so no verdict rests on anything). Re-run ⇒ **0 findings**. ⚠ Form only; a clean run is not a correct report.
- [x] `DEEP_LOG` line appended for the next run.
